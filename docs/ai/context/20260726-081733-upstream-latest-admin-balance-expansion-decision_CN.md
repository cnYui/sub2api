# 内层 latest 管理员余额扩容执行决策

时间：2026-07-26 08:17（Asia/Tokyo）

## 新证据

- 内层 `18086` 的正式管理员界面在用户管理页仅暴露“充值（add）”与“扣款（subtract）”操作；当前 `UserBalanceModal` 不支持 `set`。
- 该界面调用 `POST /api/v1/admin/users/1/balance`，会通过应用服务刷新相关缓存并留下余额调整记录。
- 用户原始要求是“把这里的余额添加更大，比如 10000000”，与充值语义一致。

## 执行决定

- 对 `xiaobianfuai@gmail.com`（`users.id=1`）通过正式管理界面执行一次 `add 10000000.00`。
- 不通过数据库直接设值，也不重置管理员密码或更改 JWT/管理 API Key。
- 最终余额为提交瞬间的原余额加 `10000000.00`；内层模型请求持续发生时，余额会有正常的小额变化，因此不承诺恰好等于 `10000000.00`。

## 验证

- 读取管理页更新后的余额。
- 打开余额调整记录，确认最新记录金额、类型和备注。
- 检查 `http://127.0.0.1:18086/health`。
