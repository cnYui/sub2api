# Sub2API Material Relay 前端重设计结果

- **状态**：完成
- **分支**：`codex/material-relay-frontend-redesign`
- **范围**：公开页、认证页、用户端、管理端、通用组件与相关测试
- **约束**：不改后端 API、计费、支付、权限和路由语义

## 完成内容

- 将前端统一到 Material Relay 视觉基线：更克制的实色表面、系统字体、紧凑信息密度、弱化玻璃感叠层。
- 收敛页面与组件动效：只保留 transform / opacity，去掉高频 hover lift、transition-all 和不必要的装饰性运动。
- 重做公开页、登录页、用户仪表盘、管理员仪表盘、支付页与支付结果页的层级、间距和移动端适配。
- 修复并保留兼容：
  - 历史图片计费记录的展示契约。
  - 图表缺失字段与空值兜底。
  - 旧分页状态缓存的清理逻辑。
  - 相关单测断言与视觉主题守卫。

## 验证

已完成并通过：

- `pnpm typecheck`
- `pnpm lint:check`
- `pnpm test:run`
- `pnpm build`

浏览器验收：

- 测试用户 `2799523972@qq.com / 123123` 登录后，`390×844` 的用户仪表盘无横向溢出，主内容可正常下滚到图表与快捷操作区。
- 管理员 `xiaobianfuai@gmail.com / Wu15951875192` 登录后，`1440×900` 与 `390×844` 的管理员仪表盘均无横向溢出，桌面与移动断点布局正常。
- 管理员首次引导遮罩已关闭后复核页面主体，未见布局断裂。

## 结果

- 视觉统一已完成。
- 动效约束已落地。
- 相关测试与本地构建已通过。
- 未部署、未推送、未改运行态。
