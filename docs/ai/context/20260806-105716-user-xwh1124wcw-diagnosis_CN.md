# xwh1124wcw@163.com 无法使用排查

## 结论

用户 `xwh1124wcw@163.com`（`users.id=453`）无法使用的直接原因不是账号、API Key、余额、配额、限流或 IP 限制，而是其唯一 API Key 绑定的 OpenAI 分组 `groups.id=9` 在调用 `gpt-5.6-luna` 时，上游唯一账号 `accounts.id=1128` 间歇性返回 `502`/`503`。路由尝试切换后没有第二个候选账号，因此返回 `no available accounts`。

该问题为分组上游容量与模型可用性问题，不是该用户的专属账户问题。账号 `1128` 在故障间隙仍成功处理其它同分组调用，说明不是持续完全下线；但 `luna` 的失败更集中，不能向用户承诺该模型当前可稳定使用。

## 当前用户状态

- 用户状态为 `active`，普通余额为 `$6.40172771`，冻结余额为 `$0`。
- 余额套餐 `user_balance_packages.id=113` 为 `active`，当前周剩余额度为 `$6.40172771`，到期时间为 `2026-08-28 14:43:10 +08`。
- OpenAI 流量卡仍有 `$0.511534625` 可用额度。
- 唯一 API Key（`api_keys.id=15`）为 `active`，未过期，无 IP 白名单/黑名单，无配额和 5 小时、1 天、7 天限额。
- 用户并发为 `20`，用户 RPM 限制为 `0`，Key 限额也均为 `0`，不存在本地限流。

## 失败证据

- `2026-08-06 09:52:58 +08`：用户调用 `/v1/chat/completions` 的 `gpt-5.6-luna` 已通过网关安全与资格检查；账号 `1128` 返回 `503`，随后记录 `openai_chat_completions.account_select_failed`，错误为 `no available accounts`。
- `2026-08-06 09:51:47 +08`：同一路径与模型，账号 `1128` 返回 `502`，随后同样没有可替换账号。
- 近 48 小时用户共记录 `977` 次 Responses 路径和 `16` 次 Chat Completions 路径的账号选择失败；有明确上游切换记录的尝试全部指向账号 `1128`。
- 分组 `9` 当前只关联一个上游账号：`1128`，平台为 OpenAI，状态 `active`、可调度、并发 `50`。
- 近两小时分组 `9` 仍有 `538` 次成功调用，其中 `gpt-5.6-terra` 214 次、`gpt-5.6-sol` 195 次、`gpt-5.5` 103 次、`codex-auto-review` 33 次；同期 `gpt-5.6-luna` 有 26 次上游切换失败，最新一次为 `09:53:55 +08`。

## 处理建议

1. 修复或替换账号 `1128` 对 `gpt-5.6-luna` 的上游可用性，并在恢复后用最小请求验证。
2. 为分组 `9` 增加至少一个已验证健康的同模型 OpenAI 账号，避免单账号失败直接使整个分组无可用候选。
3. 如需立即临时恢复该用户，需要管理员明确授权后将其 Key 改绑到兼容且健康的分组；这会影响可用模型和计费倍率，应先确认目标分组并留存审计。
4. 不应重置用户余额、套餐权益或 API Key，这些操作不能解决当前故障。

## 操作范围

本次仅读取生产 PostgreSQL、应用容器日志与运行状态；未修改用户、API Key、余额、套餐、分组、账号或任何生产配置。
