# 2026-06-27 SMTP 测试端口 18085 与文档缺失排查计划

## 目标

1. 在本机另起一个隔离的 Sub2API 实例，用于测试 SMTP，不影响当前公网链路 `8080 -> 18084`。
2. 调查 `docs/ai/context/` 下 SMTP 相关文档是否存在，以及当前 main 为什么看起来没有对应修改。

## 约束

- 不停止、不重建、不覆盖 `sub2api-candidate`、`sub2api-candidate-postgres`、`sub2api-candidate-redis`。
- 不占用 `8080` 或 `18084`。
- 不在文档、命令输出和最终回复中记录 SMTP 密码、API Key、内部 token 或 secret。
- 当前工作树有用户未提交改动，不回滚、不覆盖。

## 设计

- 新测试后端端口：`127.0.0.1:18085 -> container:8080`。
- 新测试数据库：独立容器 `sub2api-smtp-test-postgres`，只在 Docker 网络内暴露 `5432`。
- 新测试 Redis：独立容器 `sub2api-smtp-test-redis`，只在 Docker 网络内暴露 `6379`。
- 新测试应用容器：`sub2api-smtp-test`。
- Docker 网络：`sub2api-smtp-test-net`。
- 镜像来源：优先从当前工作树构建 `sub2api-smtp-test:YYYYMMDD-HHMMSS`，用于贴近当前 main 本地状态；不会替换 `weishaw/sub2api:latest`。

## 操作步骤

1. 确认 `18085` 当前未被占用。
2. 构建本地测试镜像，标签为 `sub2api-smtp-test:20260627-111200`。
3. 创建独立 Docker 网络。
4. 启动独立 Postgres 和 Redis。
5. 启动 `sub2api-smtp-test`，映射到 `127.0.0.1:18085`。
6. 等待 `/health` 返回 200。
7. 检查公开配置与 settings 表，确认注册/邮箱验证/SMTP 的初始状态。
8. 搜索 `docs/ai/context/`、git 分支、git reflog/log 中的 SMTP/ZPay 运行态配置文档线索。
9. 写结果文档并更新 AGENTS.md 运行态记忆。

## 预期结论格式

- 明确本地测试 URL。
- 明确 18085 的数据库/Redis 隔离状态。
- 明确 SMTP 当前是“未配置”还是“已有可用配置”。
- 明确 SMTP 相关文档在哪些文件/分支出现，哪些修改没有进入当前 main。
