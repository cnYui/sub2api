# 2262423876@qq.com 持续重连诊断

## 结论

截至 2026-07-16 21:40:59（Asia/Shanghai），没有证据显示 Sub2API 对该用户持续断开或重连。该用户唯一一组 API 调用均成功完成；所谓“持续重连”发生在请求抵达服务端之前，或来自用户客户端对成功响应后的本地处理。

## 运行态事实

- 只读检查公网链路的 `sub2api-candidate`、`sub2api-candidate-postgres`、Nginx、Cloudflare Tunnel 和 CLIProxyAPI；未改数据库、Redis、容器、Nginx、用户、Key 或代码。
- 用户为 `users.id=114`，状态 `active`，唯一 Key 为 `api_keys.id=164`，状态 `active`，无 Key 配额、有效期或 5 小时/1 天/7 天频率限制。
- 有效订阅为 `user_subscriptions.id=122`，分组 `codex-pool-29-usd`，有效至 2026-08-15 20:40:35+08；当日已用仅 `0.008602 USD`。该分组绑定的唯一上游账号 `accounts.id=1/cliproxy-local-openai` 状态为 `active`、`schedulable=true`、并发 100。
- 北京时间 21:24:52 至 21:25:12，来源 `59.146.58.235` 使用 `curl/8.5.0` 先请求 `/v1/models`，随后对 `gpt-5.5`、`gpt-5.6-terra`、`gpt-5.6-luna` 各发起一次 `/v1/responses` 与一次 `/v1/chat/completions`。
- 6 次模型请求均落入 `usage_logs`，均为 HTTP 200 的成功请求，耗时 1,395 至 5,199 ms；均是非流式请求。没有后续 API 请求。
- `ops_error_logs` 对该用户为 0；Nginx 同一时间窗中的 6 次 API 请求也均为 200。内网 `127.0.0.1:18084/health` 和公网 `https://api.aaccx.pw/health` 均返回 `{"status":"ok"}`。

## 判断与后续证据

这组访问形态是连通性验证，不是一个被服务端反复中断的流式会话。用户客户端使用的 `curl` 请求没有携带 `stream=true`，服务器也没有收到后续重试或重连请求。

若用户界面仍显示“重连中”，需要用户侧提供该界面的完整错误文字、客户端名称和版本、发生时间及客户端日志；并确认实际配置使用 `https://api.aaccx.pw/v1`、当前完整 Key 和 `Authorization: Bearer <Key>`。在取得这些证据前，不应修改套餐、Key、Nginx 超时或上游账号。
