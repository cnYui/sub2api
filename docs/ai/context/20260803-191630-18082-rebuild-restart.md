# 18082 Docker 重建与重启

## 范围

- 仅重建并重启 `sub2api` 应用容器。
- 保留 `sub2api-official-18082-postgres`、`sub2api-official-18082-redis` 和已有数据卷。

## 执行结果

- Compose 项目：`sub2api-official-18082`
- 配置文件：`deploy/docker-compose.dev.yml`、`deploy/docker-compose.18082.yml`
- 新应用镜像：`sha256:6d4086d4f57106998786c3b578d12acdaad8032cb447620d5faaee22499ec788`
- 应用容器：`sub2api-official-18082`，状态 `healthy`
- 端口：`127.0.0.1:18082 -> 8080`
- 验证：`GET /health` 与 `GET /purchase` 均返回 HTTP 200。

## 注意

Compose 启动时提示 `REDIS_PASSWORD` 未设置，因此按现有配置使用空值；本次未变更该配置。
