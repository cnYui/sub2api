# 普通用户创建记录

- 目标实例：`sub2api-official-18082`（`127.0.0.1:18082`）。
- 按管理员请求创建了一名本地普通用户，角色为 `user`，状态为 `active`，注册来源为 `email`。
- 实例的公开注册开关保持关闭；为避免扩大注册面，未临时变更该设置。
- 密码仅在一次性 bcrypt 哈希过程中使用，未写入仓库、文档或命令输出；未读取任何现有认证秘密。
- 已通过登录接口的 HTTP 200 响应验证账号可用。
