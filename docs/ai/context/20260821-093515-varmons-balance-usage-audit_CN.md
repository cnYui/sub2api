# varmons@proton.me 余额与使用记录只读核查

## 核查范围

- 核查时间：2026-08-21（Asia/Shanghai）
- 生产容器：`sub2api-official-18082`
- 用户：`varmons@proton.me`，用户 ID `456`
- 本次仅查询数据库、Redis 配置、后端查询代码和前端刷新逻辑，没有修改生产数据。

## 数据库事实

- `users.balance = 75.48288134 USD`，`users.updated_at = 2026-08-21 04:10:01 +08`。
- 当前有效余额套餐为 `user_balance_packages.id=141`：每周到账 `76 USD`，当前剩余 `75.48288134 USD`，状态 `active`，下一次到账 `2026-08-28 03:22:26 +08`。
- 当天 `2026-08-21 03:22:46 +08`，套餐第 2 期到账 `76 USD`，先抵扣余额欠费 `0.51711866 USD`，欠费账本 `balance_debt_ledger.id=36` 的余额变为 `75.48288134 USD`。
- 流量卡共 5 张，前 4 张已耗尽或历史过期；当前有效卡 `user_traffic_credits.id=254` 剩余 `0.000954673 USD`。流量卡欠费账本为空。
- `usage_logs` 中用户 ID `456` 共 `1,788` 条，累计 `actual_cost=307.430811791 USD`，这些记录均属于该用户自己的 API Key（`8`、`55`、`306`），没有发现 `api_key.user_id != usage_logs.user_id` 的记录。
- 2026-08-21 当天已有 12 条使用记录，累计实际扣费 `3.14439531 USD`；截图中列出的记录时间、模型、IP `5.34.218.11` 与数据库记录一致。

## 截图中负余额的原因

截图显示的 `-$0.52` 与当天到账前的欠费 `0.51711866 USD` 四舍五入完全一致。它不是错误记录，也不是把其他用户的使用记录串进来，而是页面在余额套餐自动到账前读取到的旧余额快照。到账任务随后已完成抵扣并把数据库余额恢复到 `75.48288134 USD`。

## 页面刷新与时效性

- `/auth/me` 每 30 秒、页面恢复前台、窗口聚焦时刷新用户资料；余额顶部显示来自该响应的 `users.balance`。因此在自动到账前后最多存在约 30 秒的旧余额显示窗口。
- 顶部流量卡额度单独请求 `/payment/checkout-info`，首次挂载及支付成功事件时刷新；代码没有在余额套餐周期到账事件后主动广播刷新。若用户正停留在页面，流量卡额度不会随余额套餐到账自动重拉，直到组件重新挂载或支付事件触发。
- 使用记录页 `/usage`、统计接口和趋势接口均由后端从 `usage_logs` 按认证主体 `subject.UserID` 查询，前端不会携带可任意指定的用户 ID；当前实现没有发现跨用户查询条件。
- 使用记录页只在挂载、筛选、分页、排序时刷新，不会因为每次余额变动自动刷新。当天新记录会在下一次主动刷新/重新进入页面时出现。

## 结论

1. 该用户当前真实普通余额为 **75.48288134 USD（约 75.48 USD）**，流量卡剩余 **0.000954673 USD（约 0.00 USD）**。
2. “已经欠费”的截图是余额套餐自动到账前的短暂旧快照；数据库当前已经完成欠费抵扣，不再欠费。
3. 使用记录属于用户 ID `456`，不是其他用户记录。
4. 确实存在展示时效性缺口：余额套餐自动到账后没有向已打开页面广播用户资料和流量卡汇总刷新，使用记录页也不会因余额变化自动刷新。该问题影响展示及时性，不改变服务端实际扣费和余额。

## 建议

- 增加“余额/套餐到账完成”事件，前端收到后同时调用 `authStore.refreshUser()` 与 `/payment/checkout-info`。
- 使用页在页面恢复前台或收到余额变更事件时重新加载当前页记录和统计；保留现有后端 `subject.UserID` 约束。
- 若要修复，需要单独实现和验证前端事件链；本次核查没有直接改代码或生产容器。
