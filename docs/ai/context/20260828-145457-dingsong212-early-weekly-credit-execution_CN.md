# 用户 dingsong212@gmail.com 提前刷新额度记录

## 授权与范围

按管理员要求，提前刷新用户 `dingsong212@gmail.com`（`users.id=563`）当前余额套餐的下一期额度，并顺延下一次刷新时间。

本次仅处理当前余额套餐 `user_balance_packages.id=166` 及关联订单 `payment_orders.id=705`。未修改订单金额、订单状态、退款金额、套餐有效期、历史用量、API Key 配额、流量卡或历史套餐。

## 执行前状态

生产数据库执行前核查（2026-08-28 14:49 +08:00）：

- 用户普通余额：`218.71168793 USD`。
- 当前套餐：`id=166`，状态 `active`，已到账 `1/4` 期。
- 每期额度：`781 USD`；当前套餐窗口剩余：`0 USD`。
- 原定下一次刷新：`2026-08-30 08:02:57.114416 +08:00`。
- 套餐有效期：至 `2026-09-20 08:02:57.114416 +08:00`。
- 订单 `705`：`COMPLETED`，退款金额 `0`，实付金额 `301.99 CNY`。
- 订单 `705` 尚无 `BALANCE_PACKAGE_EARLY_WEEKLY_CREDIT_2` 审计，满足幂等前置检查。

## 执行方式

执行时间为 `2026-08-28 14:54:57 +08:00`。使用 PostgreSQL `SERIALIZABLE` 事务，事务内按邮箱重新锁定用户，锁定套餐和关联订单，并校验套餐状态、到账进度、刷新间隔、有效期、订单状态及退款金额仍符合预期。

按余额套餐正常周刷新语义计算：

- 本期到账：`781 USD`。
- 执行前普通余额为正，无需抵扣欠费。
- 旧套餐窗口剩余为 `0 USD`，因此普通余额增加 `781 USD`，本期套餐窗口剩余为 `781 USD`。
- 套餐从 `1/4` 变为 `2/4`，保持 `active`。
- 下一次刷新按原计划时间加 `7` 天，从 `2026-08-30 08:02:57.114416 +08:00` 顺延至 `2026-09-06 08:02:57.114416 +08:00`。

## 执行结果

- 用户普通余额：`218.71168793 USD` -> `999.71168793 USD`。
- `users.total_recharged`：`2651 USD` -> `3432 USD`。
- 套餐 `166`：到账 `1/4` -> `2/4`，剩余 `0 USD` -> `781 USD`，状态保持 `active`。
- `next_credit_at`：`2026-08-30 08:02:57.114416 +08:00` -> `2026-09-06 08:02:57.114416 +08:00`。
- 套餐有效期保持 `2026-09-20 08:02:57.114416 +08:00` 不变。
- 订单 `705` 仍为 `COMPLETED`，退款金额保持 `0`。
- 新增支付审计 `payment_audit_logs.id=2238`：`BALANCE_PACKAGE_EARLY_WEEKLY_CREDIT_2`，操作者 `admin:authorized_manual_settlement`。
- 本次无普通余额欠费，未新增 `balance_debt_ledger` 还款流水。

## 缓存与核验

- 已删除 Redis 余额缓存 `billing:balance:563`，删除结果为 `0`。
- 用户现有 API Key 的认证缓存失效 outbox 新增 `1` 条，随后已被 worker 消费，数据库回读无待处理记录。
- Redis 回读确认 `billing:balance:563` 和该 API Key 的鉴权缓存均不存在；Redis `PING` 返回 `PONG`。
- 应用容器、PostgreSQL、Redis 均为 healthy；`http://127.0.0.1:18082/health` 返回 HTTP `200`。
