# 用户 1850226892@qq.com 提前结算第二期余额套餐额度抵扣欠费记录

## 授权与范围

管理员授权将用户 `1850226892@qq.com`（`users.id=480`）下一次余额套餐刷新额度用于抵扣当前欠费，并直接进入下一刷新周期。

本次只处理余额套餐 `user_balance_packages.id=115` 及关联订单 `payment_orders.id=580`。未修改订单金额、订单状态、退款金额、历史用量、API Key 配额、流量卡、套餐有效期或其他用户数据。

## 执行前状态

- 普通余额：`-1.93300329 USD`。
- 余额套餐：ID `115`，每周额度 `154.00000000 USD`，当前窗口剩余 `0 USD`。
- 到账进度：`1/4`；第 2 期原定到账时间为 `2026-08-11 19:35:42.239496 +08:00`。
- 套餐状态：`debt_paused`；有效期至 `2026-09-01 19:35:42.239496 +08:00`。
- 关联订单：ID `580`，状态 `COMPLETED`，退款金额 `0 CNY`。

执行时点已经超过原定到账时刻；定时刷新因负余额保护将套餐置为 `debt_paused`，未自动结算该期。

## 执行方式

在单个 `SERIALIZABLE` PostgreSQL 事务中锁定用户、套餐和关联订单，并严格校验用户余额、套餐状态、周额度、当前剩余额度、到账期数、原定刷新时间、有效期、订单状态和退款金额。任一前置条件不匹配即回滚。

按正常周额度刷新语义计算：

```text
新余额 = 旧余额 - 当前窗口剩余 + 本周额度
欠费还款 = min(max(-(旧余额 - 当前窗口剩余), 0), 本周额度)
```

## 执行结果

- 第 `2/4` 期 `154 USD` 已于 `2026-08-11 22:19:52 +08:00` 结算。
- 其中 `1.93300329 USD` 优先偿还全部欠费。
- 结算事务提交时，用户普通余额与当前套餐剩余额度均为 `152.06699671 USD`。
- 套餐从 `debt_paused` 恢复为 `active`，到账进度为 `2/4`。
- 下次刷新按原 7 天节奏进入 `2026-08-18 19:35:42.239496 +08:00`；到期时间保持不变。
- 订单继续保持 `COMPLETED`，退款金额保持 `0 CNY`。
- 新增支付审计 `payment_audit_logs.id=1569`：`BALANCE_PACKAGE_EARLY_WEEKLY_CREDIT_2`。
- 新增欠费还款账本 `balance_debt_ledger.id=18`：来源 `balance_package_early_weekly_credit`，引用 `package:115:credit:2`。

## 缓存与核验

- 已删除余额缓存 `billing:balance:480`；该键原本不存在，删除返回 `0`。
- 已为该用户 7 个 active API Key 写入鉴权缓存失效队列；工作线程已全部处理，当前无待处理事件。未记录或输出 API Key 原文。
- 数据库回读确认余额、套餐状态、套餐剩余额度、到账进度、下次刷新时间、订单状态、支付审计和欠费账本均与上述结果一致。
- 执行后用户随即以 API Key `144` 发起 3 次 `gpt-5.5` 请求，均通过余额准入并按正常计费扣除合计 `0.34349184 USD`；截至最后一笔于 `2026-08-11 22:21:52 +08:00` 记账后，实时余额与套餐剩余额度均为 `151.72350487 USD`。
- `127.0.0.1:18082`、本地 Nginx 及 `aaccx.pw`、`www.aaccx.pw`、`api.aaccx.pw` 健康检查均返回 HTTP `200`。
