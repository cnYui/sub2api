# 2466439791@qq.com request timed out 排查结果

## 结论

截至 `2026-06-22 21:20:22 +09`，`2466439791@qq.com` 这位用户的 API Key 只产生了 1 条成功落库模型请求，即此前公网连通性测试产生的 `usage_logs.id=8536`。

用户反馈的 `request timed out` 请求没有以 `user_id=38` / `api_key_id=43` 身份进入 Sub2API 的成功用量记录、错误表或系统日志；该 Key 的 `last_used_at` 也仍停留在此前测试时间 `2026-06-22 17:17:17.316886+08`。

本次未修改用户、Key、订阅、分组、上游账号、nginx 或运行配置。

## 用户与 Key 状态

- 用户：`users.id=38`
- 邮箱：`2466439791@qq.com`
- 用户状态：`active`
- API Key：`api_keys.id=43`
- Key 掩码：`sk-263e6...ec751d`
- Key 状态：`active`
- 分组：`codex-pool-19-usd`
- 订阅：`user_subscriptions.id=56`，状态 `active`
- 订阅到期：`2026-07-22 15:38:30.544797+08`

## 请求数量

`usage_logs`：

- 总数：`1`
- 第一条：`2026-06-22 17:17:30.375627+08`
- 最后一条：`2026-06-22 17:17:30.375627+08`

唯一落库请求：

- `usage_logs.id=8536`
- `request_id=client:5bc457ed-2d89-4f6f-9082-772bd1abd6c4`
- `model=gpt-5.4-mini`
- `inbound_endpoint=/v1/chat/completions`
- `upstream_endpoint=/v1/responses`
- `input_tokens=315`
- `output_tokens=5`
- `actual_cost=0.0002587500`
- `duration_ms=1495`

## 错误与日志

该用户和该 Key：

- `ops_error_logs`：`0` 条
- `ops_system_logs`：`0` 条
- Sub2API 容器日志中只看到此前成功测试的 `user_id=38/api_key_id=43` 记录，没有新增超时记录。

近期有 6 条 `/v1/responses` 的 `401 Invalid API key`，但不是该用户的 `sk-...` Key：

- 时间：`2026-06-22 19:30:01`、`19:32:21`、`19:39:33` 到 `19:39:34 +08`
- `attempted_key_prefix=cli-prox`
- `client_ip=8.219.150.152`
- `user_agent=Mozilla/5.0 ... Chrome/138`

这说明至少有客户端在用 `cli-prox...` 前缀的错误 Key 请求 Sub2API；该 Key 前缀不是 `2466439791@qq.com` 当前 active API Key。

## 判断

当前没有证据表明 `2466439791@qq.com` 的 active API Key 在 Sub2API 内部发生了超时。更可能的问题是客户端配置不正确：

1. API Key 没有使用该用户的 `sk-263e6...ec751d` 这把 Key，而是填了 `cli-prox...` 这类上游/旧 Key。
2. Base URL 或路径配置不正确，导致请求未按 `https://api.aaccx.pw/v1` 进入 Sub2API 的 OpenAI 兼容入口。
3. 客户端本地超时发生在请求真正进入 Sub2API 之前，因此服务端没有该用户身份的请求记录。

建议用户侧核对：

- Base URL：`https://api.aaccx.pw/v1`
- API Key：使用用户面板里的 `sk-...` Key，不使用 `cli-prox...` 或其他内部上游 Key。
- 如果继续超时，提供客户端截图、完整 Base URL、模型名、请求时间和客户端 request id，但不要发送完整 API Key。
