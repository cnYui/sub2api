# xinlise@gmail.com 退款天数只读审计结果

时间：2026-07-10 17:20 JST / 16:20 +08

## 结论

- `xinlise@gmail.com` 为 `users.id=69/status=active`，余额 `0`。
- 当前订阅仍 active：`user_subscriptions.id=88`，`group_id=8/codex-pool-89-usd`，每日额度 `89 USD`。
- 订阅周期：`2026-07-08 03:57:35+08` 到 `2026-08-07 03:57:35+08`，共 30 天。
- 截至 `2026-07-10 16:20:36+08`，实际时钟已过约 `2.516` 天；按当前自动退款规则，剩余完整天数为 `27` 天，等价于已消耗/不退款 `3` 天。
- 订单基础价 `amount=99.00`，支付宝实付 `pay_amount=99.99`，手续费率 `1%`；支付宝退款不退手续费，建议/系统试算退款额为 `99 * 27 / 30 = 89.10` 元。

## 今日额度

- 订阅当前 `daily_window_start=2026-07-10 00:00:00+08`。
- `daily_usage_usd=89.1284430000`，已超过 `daily_limit_usd=89.00000000`。
- 今天 usage_logs 聚合完全对应：`849` 次请求，`89.1284430000 USD`，时间范围 `2026-07-10 01:35:58+08` 到 `06:49:07+08`。

## 实际使用自然日

`usage_logs` 中该订阅有成功扣费的自然日为 3 天：

| 日期 | 请求数 | 流式请求数 | 费用 USD |
| --- | ---: | ---: | ---: |
| 2026-07-08 | 724 | 719 | 85.7832327500 |
| 2026-07-09 | 592 | 592 | 70.3355070000 |
| 2026-07-10 | 849 | 849 | 89.1284430000 |

合计 `2165` 次请求，`245.2471827500 USD`，全部为 `billing_type=1`，全部扣到 `subscription_id=88/group_id=8`；没有余额或流量卡扣费。

## 退款订单状态

- 对应订单：`payment_orders.id=140`。
- 订单创建：`2026-07-08 03:57:18+08`；支付完成：`2026-07-08 03:57:35+08`。
- `amount=99.00`，`pay_amount=99.99`，`payment_type=alipay`，`provider_instance_id=1`，`provider_key=easypay`。
- 用户已在 `2026-07-10 16:16:44+08` 发起过自动退款，原因 `换套餐`，系统试算 `refund_amount=89.10`。
- 当前订单状态为 `REFUND_FAILED`，失败原因：`easypay refund failed (HTTP 200): 卖家余额不足`。
- 订阅没有被撤销，仍然 active；退款没有成功出款。

## 操作注意

- 用户自助退款入口只允许 `COMPLETED` 订单；这笔订单已是 `REFUND_FAILED`，用户侧不能直接再次重试。
- 管理员退款接口允许 `COMPLETED`、`REFUND_REQUESTED`、`REFUND_FAILED`，因此补足 ZPay/EasyPay 商户可退款余额后，可由管理员按 `89.10` 元继续处理。
- 如果线下手工退款，需同步撤销 `user_subscriptions.id=88` 并记录订单状态，否则用户套餐仍会保留。

## 本轮影响

- 只读查询运行态 `sub2api-candidate-postgres`。
- 未执行退款，未修改订单、订阅、用户、usage、Redis。
- 未重启容器、未发布代码。
