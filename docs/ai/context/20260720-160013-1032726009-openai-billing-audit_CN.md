# 1032726009@qq.com 扣费审计

时间：2026-07-20 14:59:47 +08（数据库快照）

## 结论

- 没有发现金额错误扣费或重复结算。
- 这个账号今天的额度增长，和 `usage_logs` / `usage_facts` / `user_subscriptions` 三边数据完全对得上。
- 主要消耗来自大量 `gpt-5.6-sol` 长上下文请求，以及较大的 cache read 成本，不像是“空扣”。

## 关键事实

- 用户：`user_id=70`，邮箱 `1032726009@qq.com`，状态 `active`
- 当前订阅：`subscription_id=102`，`group_id=12`，`codex-pool-179-usd`
- 订阅组限额：日限额 `179 USD`
- 当前日用量：`110.095907 USD`
- 流量卡：`user_traffic_credits` 为空

## 今日扣费概况

- 今日 `usage_logs`：`350` 条
- 今日总扣费：`110.095907 USD`
- 全部都落在 `billing_type=1`、`subscription_id=102`、`group_id=12`
- `usage_logs` 与 `user_subscriptions.daily_usage_usd` 求和差值：`0`
- `usage_logs` 重复 `request_id`：`0`
- `usage_facts` 重复 `request_id`：`0`
- `usage_facts` 今日全部为 `settled`，`attempt_count=1`
- 今日没有 `billing_incomplete=true`

## 成本拆分

- `input_cost`：`52.799455`
- `output_cost`：`5.938500`
- `cache_read_cost`：`51.357952`

说明：今天的高消耗不只是输入 token，cache read 也占了接近一半。

## 来源特征

- 同一公网 IP 贡献了全部请求
- 主要 User-Agent：
  - `codex_vscode/0.144.0-alpha.4 ...`
  - `GitHubCopilotChat/0.57.0`
- 推断是同一台机器上的 IDE / Chat 插件在持续发请求，不像分散的异常来源

## 代表性大额请求

- `request_id=client:63b78e99-8de2-4891-b22d-774452594dc1`
- `input_tokens=328449`
- `output_tokens=2112`
- `cache_read_tokens=6912`
- `actual_cost=3.386442`

该笔的分项成本和计价规则能对上，没有异常加扣。

## 备注

- `gpt-5.6-sol` 的现行价格文件里有长上下文计价规则：`long_context_input_token_threshold=272000`，输入倍率 `2.0`，输出倍率 `1.5`。
- 今天这些大额请求属于真实高 token 使用，不是金额计算错误。
