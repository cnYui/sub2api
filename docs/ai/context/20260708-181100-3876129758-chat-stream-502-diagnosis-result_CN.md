# 3876129758 chat/completions 流式 502 诊断结果

时间：2026-07-08 18:11 JST

## 触发问题

用户截图显示本地 `127.0.0.1:57321/v1/responses` 重连，并报：

- `unexpected status 502 Bad Gateway`
- `供应商「gpt」请求上游失败`
- upstream endpoint：`https://api.aaccx.pw/v1/chat/completions`

## 当前订阅状态

- 用户：`3876129758@qq.com`
- 用户 id：`56`
- active Key：`api_keys.id=75`，自动 Key，`group_id=NULL`，掩码 `sk-c7b2c6c...5607`
- 当前未删除 active 订阅数：`1`
- 当前唯一 active 订阅：`user_subscriptions.id=90`，`group_id=4 / codex-pool-49-usd`
- 旧 29 元订阅 `user_subscriptions.id=76` 已是 `expired` 且 `deleted_at` 非空。

## 日志定位

截图对应的失败不是订阅或额度拒绝，而是一次流式 chat 请求在响应收尾阶段失败：

- 时间：`2026-07-08 17:07:56 +08`
- endpoint：`/v1/chat/completions`
- user_id：`56`
- api_key_id：`75`
- group_id：`4 / codex-pool-49-usd`
- model：`gpt-5.5`
- stream：`true`
- HTTP 状态：`502`
- Sub2API 错误：`stream usage incomplete: missing terminal event`

含义：该请求已选中 59 元套餐和 OpenAI 上游账号，但流式响应缺少终止 usage 事件，Sub2API 无法完整结算，所以把请求判为上游失败。

## 验证结果

用同一 active Key 验证截图里的 chat/completions 路径：

- `POST https://api.aaccx.pw/v1/chat/completions`
- `model=gpt-5.5`
- `stream=false`
- HTTP `200`
- 回复 `OK`
- 新增 `usage_logs.id=66675`，扣在 `subscription_id=90`

继续验证流式 chat/completions：

- `POST https://api.aaccx.pw/v1/chat/completions`
- `model=gpt-5.5`
- `stream=true`
- HTTP `200`
- SSE 包含 `usage` 事件和 `[DONE]`
- 新增 `usage_logs.id=66690`，扣在 `subscription_id=90`

此外，旧 29 元删除后用户已有多条真实成功请求都落在 `subscription_id=90` 上，例如 `usage_logs.id=66595/66607/66626/66630/66636/66675/66690`。

## 判断

- 不是旧 29 元套餐仍 active 导致。
- 不是当前 59 元套餐不可用。
- 不是该 API Key 未绑定当前套餐。
- 不是流量卡扣费路径。
- 是一次流式 `/v1/chat/completions` 上游流缺少终止 usage 事件导致的偶发 502。

## 临时建议

- 让用户直接重试；复测同 Key、同模型、同 chat/completions 流式路径已成功。
- 如果用户本地工具支持，优先使用 Responses 协议直连 `base_url=https://api.aaccx.pw/v1`，避免本地 `127.0.0.1:57321` 代理把 Responses 转成 chat/completions。
- 如果还持续复现，应单独修 Sub2API 的流式 chat 记账容错：缺少 terminal usage 时不要在已经产出正常内容后直接 502，而应有明确的 fallback 计量或不扣费成功策略。

## 本轮未执行事项

- 未修改公网数据库。
- 未构建镜像。
- 未替换或重启 18084 相关容器。
- 未提交 git。
