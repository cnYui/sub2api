# 超额订阅按 72 USD/周扣减有效期结果

## 结果

已仅在本地开发数据库 `sub2api-postgres-dev` 完成两个超额订阅的有效期扣减；未触碰公网候选环境、Nginx 或 CLIProxyAPI。

计算口径固定为 `72 / 7 = 10.2857142857 USD/天`，并按 `floor(超额 / 日均额度)` 向下取整：

| 用户 | 超额 USD | 原始计算天数 | 实际扣减天数 | 调整后到期时间（+08） | 调整后剩余 |
|---|---:|---:|---:|---|---:|
| xunskyler@gmail.com | 234.1998836 | 22.7694331278 | 22 | 2026-07-23 09:20:37 | 约 1.49 天 |
| luzhiyuan2026@163.com | 189.9496876 | 18.4673307389 | 18 | 2026-08-04 09:33:58 | 约 13.50 天 |

## 数据一致性

- 订阅 `21`：`user_subscriptions.expires_at` 已更新；两个重叠的 active 权益段 `159`、`168` 同步截断为 8 天并于 2026-07-23 到期，避免截断后仍由旧权益段覆盖 Dashboard。
- 订阅 `53`：`user_subscriptions.expires_at` 已更新；后续的 active 权益段 `209` 截断为 12 天并于 2026-08-04 到期；此前已于 2026-07-23 到期的权益段 `142` 保持不变。
- 未修改历史订单、退款金额、`usage_facts`、`usage_logs`、日/周用量或套餐配置。

## 安全与验证

- 执行前完整 PostgreSQL custom dump：`deploy/backups/sub2api-dev-before-overage-expiry-deduction-20260721-223843.dump`，已通过 `pg_restore -l` 验证可读；SHA-256 前缀为 `437B7A14E7F36`。
- 数据库更新在单一事务内完成，并对订阅更新严格要求 2 行、权益截断严格要求 3 行，否则会抛错并回滚。
- `billing:sub:19:2` 和 `billing:sub:35:2` 在本地 Redis 中均不存在；应用订阅 L1 缓存默认 TTL 为 10 秒，复核时已自然失效。

## 回滚

如需撤销本次扣减，使用上述 dump 整库恢复，或仅将订阅 `21` 恢复至 `2026-08-14 09:20:37.743599+08`、订阅 `53` 恢复至 `2026-08-22 09:33:58.218403+08`，并相应恢复被截断权益段的 `expires_at` 与 `period_days`，随后删除同一 Redis 键。
