# AGENTS.md 日常瘦身归档（2026-09-07）

- 时间：2026-09-07 06:30（+09）
- 类型：文档瘦身（不涉及生产数据、代码逻辑与运行配置）
- 判据来源：沿用 `docs/ai/context/20260905-112834-agents-md-compression_CN.md` 第 2、3 节的同一套保留/移除判据，未自创
- 结果：`AGENTS.md` 由 233 行 / 36,040 字节 瘦身为 231 行 / 35,396 字节（移除 2 条一次性 `gpt-6-astra` 开放/核查流水，可复用要点合并进 §三 的定价条目）

## 1. 本次做了什么

先跑 `scripts/prune-ai-context.ps1`（保留 15 天，截止 2026-08-23）清理了 7 个超期 context 文档；再对 `AGENTS.md` 做瘦身。

`AGENTS.md` 自 2026-09-05 压缩后已是「当前事实/规则/坑」的精简形态，正文无顶部 `- 2026-…` 日期日志。全文逐节比对判据，**唯一命中「一次性执行流水（开放/核查）且已有对应归档」移除判据的，是 §三 计费口径里的两条 `gpt-6-astra` 开放记录**。其可复用要点（上游只认精确串、别照搬给别的 Key 加）已合并进上方既有的 `gpt-6-astra` 定价条目，一次性执行细节（具体账号号、`usage_logs` 逐条验证算式、当时可用分组枚举）移除，原文完整照抄在第 2 节，对应归档文档仍在。

其余各章（§一 协作约定、§二 部署拓扑、§四 业务规则、§五 坑、§五点五 待处理计费偏差、§六 负面教训、§七 未完成）均为当前生效事实/规则/坑/未完成，按判据全部保留，无一条命中移除条件。

## 2. 被移除的条目（原文完整照抄）

### 移除条目 A（原 §三 计费口径）

> - **`gpt-6-astra` 已于 2026-09-05 开放并实测计费正确**：三个 上游B 上游账号（`#1132`/`#1164`/`#1168`）的 `model_mapping` 已加入该键。实测 `usage_logs` id `404425`——`input 864×$10/M + cache_read 7488×$1/M + output 22×$50/M = total_cost 0.017228`，`actual_cost = 0.017228 × 0.16 × 18 = 0.04961664`，余额差额一致。**上游只认精确串 `gpt-6-astra`，别名一律 404**，所以坑 11 的少收风险在这条链路上触发不了。详见 `docs/ai/context/20260905-134951-gpt6-astra-enablement-and-billing-verification_CN.md`。

**命中判据**：一次性执行流水（开放 + 单条计费核查），已有对应归档 `20260905-134951-gpt6-astra-enablement-and-billing-verification_CN.md`。

### 移除条目 B（原 §三 计费口径）

> - **2026-09-05 又给 `Gpt0.35倍率(优质)`（分组 `10`、账号 `#1129`、上游 `api.ai-genesis.app`）加了该模型**，上游实测 `200`。当前 4 个分组可用：`GPT0.16(日常2)`、`GPT0.16(日常3)`、`GPT0.28`、`Gpt0.35`。**`#1128`（0.15）和 `#1130`（0.1，已下架）没加，也不要照搬着加**——见坑 24。详见 `docs/ai/context/20260905-203005-gpt6-astra-enable-on-035-group_CN.md`。

**命中判据**：一次性执行流水（给某分组开放某模型），已有对应归档 `20260905-203005-gpt6-astra-enable-on-035-group_CN.md`。

## 3. 合并进 §三 定价条目的可复用事实

原 `gpt-6-astra` 定价条目末尾追加，合并后为：

> - `gpt-6-astra` 两处均已登记：$10/$1/$12.50/$50 每百万 token，>272K 输入转 2x 输入与缓存、1.5x 输出（复用 `openAIGPT54LongContext*` 常量）。别名 `gpt-6`、`gpt6`、带 effort/日期后缀的写法都解析到同一份价格，回归测试见 `billing_service_gpt6_test.go`。**上游只认精确串 `gpt-6-astra`，别名一律 404**，故坑 11 的少收风险在这条链路触发不了。已在多个 GPT 分组（含 `Gpt0.35倍率(优质)`）开放并实测计费正确；**但同上游不同 Key 的模型清单不一样——`#1128`(0.15) 与 `#1130`(0.1，已下架) 上游没有该模型，不要照搬着加（见坑 24）**。

保留的可复用要点：① 上游只认精确串 `gpt-6-astra`、别名一律 404（坑 11 在此链路触发不了）；② 已在多个 GPT 分组开放；③ 别照搬给 `#1128`/`#1130` 加该模型（坑 24：同上游不同 Key 模型清单不同）。

## 4. 本次刻意保留的内容

- **§三 分组倍率表与唯一计费公式**：全部保留（判据保护项）。
- **§五 坑 的全部条目**：包括读起来像「历史已解决」的（测模型要用精确 ID、`schema_migrations` 行数多于文件数是正常的、核对历史扣费须用 `usage_logs.rate_multiplier`、`schedulerSnapshot` 两个同名不同物 等）。它们是曾据此得出错误结论的陷阱，删掉会再犯。坑编号未重排。
- **§五点五 待处理的计费偏差 全章**：包括标注「已过期，别照做」的 `gpt-5.6-sol` 段落——警示价值仍在。
- **§六 负面教训（结论已撤回）全章**：金额与方法明确作废、不得使用。
- **§七 未完成 全章**。
- **文件开头 `> 本文件每个会话完整载入上下文……` 引用块**。
- §一 `gpt-6-astra` 定价登记条目本身（当前生效定价规则）、§二 部署拓扑当前状态、§四 业务规则，均为当前生效事实，全部保留。

公开仓库口径：本归档中 IP / 主机 / 桶名 / Tunnel ID / 密钥一律沿用 `${变量名}` 占位；原文已是占位符的保持不变，未还原任何真实值。
