# SMTP 与邮箱验证边界记录

时间：2026-08-03

## 已完成

- `18082` 已保存与 `18080` 一致的 SMTP 配置。
- `go test -tags=unit ./internal/service -run '^TestSMTP' -count=1` 通过，覆盖隐式 TLS、强制/机会式 STARTTLS 与认证路径。

## 未变更的行为

- 源实例开启了邮箱验证；目标实例的 `email_verify_enabled` 尚未设置。
- SMTP 配置可由邮件服务直接读取，但邮箱验证开关决定注册流程是否要求邮箱验证，并决定管理端是否显示 SMTP 设置区。
- 因开启邮箱验证会改变新用户注册流程，本次未迁移该非敏感开关，避免超出“复制 SMTP 凭证”的变更范围。

## 后续验证

- 由管理员在 `18082` 管理后台执行 SMTP 连接测试或发送测试邮件，验证真实认证和邮件投递。
