# itjiangzengwen@gmail.com 订单 536 退款失败只读诊断

## 结论

用户 `itjiangzengwen@gmail.com`（ID `463`）的订单 `536` 已走自动退款流程，失败原因是 ZPay 对原支付商户返回业务错误 `卖家余额不足`。不是退款未实现、支付实例缺失、旧代理连接失败或本地报价校验拒绝。

该订单只能使用创建订单时绑定的 ZPay 商户退款；当前代码禁止把它路由到任意其他现有支付实例，以避免退款从错误商户账户出款。

## 订单与调用证据

- 订单 `payment_orders.id=536`：余额套餐订单，支付方式 `alipay`，标价 `49.00 CNY`，实付 `49.49 CNY`，状态 `REFUND_FAILED`。
- 订单快照固定为支付实例 `1`、`provider_key=easypay`、商户标识 `2026061815273954`；实例名称 `ZPay Alipay`，`refund_enabled=true`、`allow_user_refund=true`。
- 用户于 2026-08-23 20:49:46、20:50:09、20:57:28（+08）发起三次自助退款。生产日志均记录 `easypay refund failed (HTTP 200): 卖家余额不足`，请求延迟分别约 3.9、3.3、5.3 秒，说明请求已经到达 ZPay 并收到业务响应。
- 退款页面当次计算并写入订单的报价为 `1.99`。部分退款会连同原支付手续费按比例折算网关退款金额：`49.49 * 1.99 / 49.00 = 2.01 CNY`（保留两位小数）。因此 ZPay 实际需要从原商户可退款余额中支出 `2.01 CNY`，不是仅 `1.99`。

## 边界与后续建议

1. 在 ZPay 后台确认商户标识 `2026061815273954` 的可退款卖家余额至少覆盖 `2.01 CNY`；账户总余额、冻结余额、提现中余额或其他商户账号余额均不能替代该可退款额度。
2. 资金确认后可再次点击退款。`REFUND_FAILED` 是允许重试的状态，且每次重试会重新计算报价。
3. 当前重复失败会触发 `payment_audit_logs(order_id, action)` 唯一索引冲突，导致第二次起无法追加 `REFUND_FAILED` 审计，但不会阻止请求发往 ZPay，也不影响订单失败原因更新。该审计幂等性缺陷应单独修复并覆盖回归测试。

本次仅做只读核查，未修改订单、套餐、余额、支付实例或 ZPay 数据，也未额外重试退款。
