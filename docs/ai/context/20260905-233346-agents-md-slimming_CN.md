# AGENTS.md 瘦身（移除顶部两条一次性运维流水条目）

- 时间：2026-09-05 23:33（+09）
- 类型：文档结构瘦身（不涉及生产数据、代码逻辑与运行配置）
- 瘦身本身：移除顶部两条一次性运维流水条目、并把其中缺失的可复用事实并入正文，**孤立测量为净 −3 行 / 约 −2.2 KB**
- 实际落到 `main` 的 `AGENTS.md` 为 **233 行 / 35,607 字节**：本次瘦身与**本地累积的大量未提交正文一次性合并提交**（坑 24-26、五点五章「只有少收是缺陷」口径块、长上下文计费开启记录、`service_tier flex` 实测、两段 Live 敞口分析等），且提交前有并行会话并发改动同一文件（如把「Anthropic 全 18 模型无长上下文档」扩写为「Claude 没有长上下文加价档，这是正确状态」），二者一并纳入，故行数与孤立测量不同——**瘦身部分未受影响，顶部两条确已移除、我加的倍率行与坑 20 补充确在**
- 判据沿用 `docs/ai/context/20260905-112834-agents-md-compression_CN.md` 第 2 节，未自创标准
- 执行方式：**本地手工执行并直接 fast-forward 到本地 `main`（提交 `e576b2b35` + `c4a980ff1`），未推送、未开 PR**。云端 routine（每天 06:00 JST）当前因 Claude GitHub App 对本仓库只有读权限而无法交付，已按用户决定改为本地手工瘦身，见文末

## 1. 被移除的条目（原文完整照抄）

### 条目一

命中判据：**针对单次操作的一次性执行流水，且已有对应的 `docs/ai/context/*.md`**（`20260905-120500-group-rate-and-shelf-adjustments_CN.md`）。

```
- 2026-09-05：按管理员要求调整生产分组倍率与上下架。`Claude0.4倍率(日常1)` 改名 `Claude0.5倍率(日常1)` 并将 `rate_multiplier` 由 `0.4` 改为 `0.5`；`【国产】DeepSeek（5折）` 改名 `【国产】DeepSeek（7折）` 并由 `3.5x` 改为 `4.9x`（涨价 1.4 倍，与 5 折→7 折口径一致）；`Grok0.9倍率(优质)` 与 `Gpt0.1倍率(优惠)` 状态改为**停用**下架，倍率保持原值。名称与真实扣费倍率同步修改，不留名称与实际不一致的情况；隐藏最终倍率 `18x`、上游账号凭证、账号分组绑定、用户余额、订单和历史用量均未改动。下架后已绑定这两个分组的存量 API Key 将不可用，需要时可把状态改回「正常」回滚。另发现分组编辑弹窗提交后按钮长期停在「更新中...」但 `PUT` 已返回 200、数据已落库，勿重复提交。详见 `docs/ai/context/20260905-120500-group-rate-and-shelf-adjustments_CN.md`。
```

### 条目二

命中判据：**针对单次操作的一次性执行流水，且已有对应的 `docs/ai/context/*.md`**（`20260905-114500-gpt028-group-and-account-creation_CN.md`）。

```
- 2026-09-05：新增 GPT 分组 `GPT0.28倍率`（分组 ID `74`、OpenAI、`0.28x`、公开非专属）与上游账号 `#1168`（API Key 类型，Base URL `${OPS_UPSTREAM_GPT028_HOST}`，并发数对齐为 `100`，创建时自动探测上游声明倍率为 `0.28x`）。模型白名单按上游 `/v1/models` 实际返回的 23 个模型配置为 **20 个文本模型**，排除 `gpt-image-1/1.5/2`——`Image-2生图` 分组为 `1x`，放行图像模型会让生图以约 1/3.5 价格绕过独立生图渠道。创建表单的默认预置列表与该上游不匹配，已移除上游不存在的 `gpt-5.6-luna`、`gpt-5.4-mini`，补入缺失的 `gpt-5.3-codex`、`gpt-5.2-openai-compact`、`gpt-5.3-codex-openai-compact`、`gpt-5.4-openai-compact`、`gpt-5.5-openai-compact`。**注意：分组「复制」会把源分组已绑定的账号一并绑到副本**，本次账号 `#1164` 被连带绑定后已解绑，用复制建分组时务必检查。实测 `/v1/models` 返回 20 个模型、`gpt-5.6-terra` 与 `gpt-5.6-sol` 均 HTTP `200`，计费 `0.003382 × 0.28 × 18 = 0.017043` 与既有公式一致；`gpt-5.3-codex` 的 `502` 经直连上游复现为 `429` 限流，属上游问题。账号「同步最新支持模型」按钮不发请求、疑似失效，待排查。详见 `docs/ai/context/20260905-114500-gpt028-group-and-account-creation_CN.md`。
```

## 2. 并入正文的事实

本次最值得记录的发现是：**这两条条目里的可复用事实，绝大多数正文里已经有了**——因为它们被追加到顶部之后，后续会话又把同样的结论以更准确的措辞写进了对应章节。逐条核对结果：

| 事实 | 正文是否已有 | 处置 |
| --- | --- | --- |
| DeepSeek `4.9x`、名称已同步为「7 折」 | ✅ 第三章倍率表 | 不并入 |
| `Grok0.9` / `Gpt0.1` 已停用下架、可改回「正常」回滚 | ✅ 第三章倍率表末行 | 不并入 |
| GPT `0.28` 分组（id `74`、独立中转站上游） | ✅ 第三章倍率表 | 不并入 |
| 低价 GPT 渠道必须排除图像模型 | ✅ 坑 6 | 不并入 |
| 分组「复制」会连带绑定源分组的账号 | ✅ 坑 21 | 不并入 |
| 「同步最新支持模型」按钮点了没反应 | ✅ 坑 24（且更准确：**接口本身是好的**，是前端问题） | 不并入 |
| **`Claude0.5倍率(日常1)` 由 `0.4` 调至 `0.5`** | ❌ 缺 | **并入第三章倍率表** |
| **分组编辑弹窗「更新中...」假象** | ⚠️ 坑 20 有同款症状，但只写了**账号**编辑弹窗 | **并入坑 20** |

两处实际改动：

**① 第三章倍率表**，在 `Claude Kiro` 行后新增：

```
  | Claude 日常1 | — | 0.5 | 名称 `Claude0.5倍率(日常1)`，2026-09-05 由 `0.4` 调至 `0.5` |
```

原表里 `Claude` 系只有 `Claude Max 1.5`、`Claude Kiro 0.45`、`Claude 71 0.78` 三档，没有「日常1」。若整条移除而不补这一行，**当前生效的 `0.5` 倍率就没有任何地方记录**——而该章明确写着「分组名称不能作为倍率依据，只查数据库」，倍率表是唯一的速查口径。

**② 坑 20 末尾**追加一句：

```
；**分组编辑弹窗是同款症状**——`PUT` 已返回 200、数据已落库，按钮却长期停在「更新中...」。
```

坑 20 原文描述的是**账号**编辑弹窗（`EditAccountModal.vue`）提交后卡「更新中...」但数据已落库。条目一记录的是**分组**编辑弹窗的同一症状。两者处置完全一致（不要重复提交），因此**合并进同一条坑而非新增一条**——新增会制造重复表述，也会占用下一个坑编号。

## 3. 本次刻意保留的内容

以下是考虑过删、但决定保留的：

- **第五章全部的坑**，尤其是读起来像「已经解决的历史」的坑 16（测错字符串比没测更误导）、坑 23（`schema_migrations` 行数多于文件数是正常的）、坑 25（用 `groups.rate_multiplier` 反推会造出假异常）、坑 26（`schedulerSnapshot` 两个同名不同物）。它们记录的是**曾经据此得出过错误结论**的陷阱，删掉就会再犯。
- **第五点五章「待处理的计费偏差」全章**，包括 `gpt-5.6-sol` 那条标注「⚠️ 已不构成缺陷 / 上面那句先核实官方价再改已过期，别照做」的段落。那句标注本身就是防止照做旧决定的护栏，删掉等于把旧决定重新放回台面。
- **第六章「负面教训（结论已撤回）」全章**。里面的 `$652.17178575` / `$384.62055975` 是**明确作废、不得用于补扣**的，删掉反而会让后续会话重新捡起同一套时间窗口归属方法。
- **第七章「未完成」全章**，包括「`build.yml` 从未实际触发过」「外部拨测未配置」「单点故障无冗余」。
- 文件开头 `> 本文件每个会话完整载入上下文……` 引用块（维护规则本身）。
- 条目二里「账号 `#1168` 的 Base URL、并发数 100、20 个文本模型白名单」等具体参数**未并入正文**：它们是单个账号的配置快照，属于流水，完整原文见本文档第 1 节及 `20260905-114500-*` 归档。

## 4. 关于自动化：云端 routine 当前无法交付

已配置云端 routine「AGENTS.md 每日瘦身」（`trig_019SG3pjQ6WVBSfTPNby1RXA`，cron `0 21 * * *` UTC = 每天 06:00 JST，Opus 4.8）。

**它的分析与编辑逻辑实测可用**，但**交付步骤被 GitHub 权限挡死**：Claude GitHub App 对 `cnYui/sub2api` 只有读权限，沙箱内 `git push` 与 MCP `create_branch` 均返回 `403 Resource not accessible by integration`（已排除代理因素，`recentRelayFailures: []`）。因此分支推送、开 PR、合并三步全部无法执行，产出只剩临时容器里的本地提交。

要启用自动交付，需在 https://github.com/apps/claude/installations/select_target 给 `cnYui/sub2api` 授予**写**权限。在那之前**本文件这类瘦身只能本地手工执行**。

另有一个已修正的坑：routine 的 prompt 最初按**编号**引用「不能删坑 16/23/25/26」，但已提交的 `main`（176 行）里坑只编到 23——**编号会随内容增删漂移，按编号定位不可靠**。已改为按内容识别，并要求新增坑时接**当前文件里实际存在的最大编号**往后排、绝不重排已有编号。
