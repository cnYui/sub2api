# Claude Kiro 的 Fable-5 价格复核

## 中转站明细

浏览器筛选 API Key `claude-kiro`、模型 `claude-fable-5` 后，明细中看到：

- 当前第一条：输入 `37`、输出 `1`，页面费用 `$0.000147`。
- 用户截图中的上一条：输入 `36`、输出 `1`，页面费用 `$0.000143`。
- 截图明细单价：输入 `$10 / 1M Token`、输出 `$50 / 1M Token`、倍率 `0.35x`。

## 复算

截图中的 `36 / 1`：

```text
标准成本 = 36 × 10 / 1,000,000 + 1 × 50 / 1,000,000
         = 0.000410
中转站实际 = 0.000410 × 0.35
           = 0.0001435
```

页面显示 `$0.000143` 是其小数显示截断/精度处理；原始数值为 `0.0001435`。

当前第一条 `37 / 1`：

```text
0.000420 × 0.35 = 0.000147
```

也与页面 `$0.000147` 完全一致。

## 本地余额扣费

本地 `model_pricing.json` 中 `claude-fable-5` 也是输入 `$10/M`、输出 `$50/M`；Kiro 分组倍率为 `0.35`，最终倍率为 `15`。

因此截图中的 `36 / 1` 在本地的两层结果是：

```text
15 倍之前：0.000410 × 0.35 = 0.0001435（等于中转站实际费用）
余额最终扣除：0.0001435 × 15 = 0.0021525
```

结论：本地标准价格和 Kiro 中转倍率计算正确；本地余额页面若显示最终扣费，应显示约 `$0.002153`，它比中转站 `$0.000143` 多 15 倍是预设的本地利润倍率，不是模型价格算错。
