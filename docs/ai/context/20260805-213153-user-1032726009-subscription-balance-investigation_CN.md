# 用户 1032726009@qq.com 订阅与补偿余额核查

## 核查范围

- 核查时间：2026-08-05 21:31（Asia/Tokyo）
- 实例：`sub2api-official-18082`
- 用户 ID：`505`
- 本次仅执行 PostgreSQL 只读查询，未修改用户、套餐、订单或用量数据。

## 当前用户与套餐

- `users.balance`：`122.67729564 USD`，这是普通余额，不等同于套餐本周剩余额度。
- 当前有效套餐记录：`user_balance_packages.id=21`，关联订单 `payment_orders.id=163`。
- 套餐：余额套餐 `¥199`（`balance-199`），每周额度 `520 USD`，4 次刷新，共承诺 `2080 USD`。
- 套餐状态：`completed`，`credited_count=4/4`，开始时间 `2026-07-11 10:05:27+08`，到期时间 `2026-08-08 10:05:27+08`。
- 当前套餐窗口：`2026-08-01 10:05:27+08` 至 `2026-08-08 10:05:27+08`，`remaining_usd=0`。
- 旧的 `¥99` 套餐记录 `user_balance_packages.id=89` 已 `expired`；其订单 `142` 为 `REFUND_FAILED`，不属于当前有效套餐。
- 用户没有 `user_subscriptions` 模型订阅记录；截图中的“订阅”是余额套餐，不是模型分组订阅。

## 为什么本周剩余为 0

当前窗口内共有 `832` 条 `usage_logs`，累计 `actual_cost=544.1321814000 USD`，超过本周套餐额度 `520 USD`。服务端扣费会把套餐窗口剩余额度减到 0；套餐额度耗尽后，仍可使用普通余额继续扣费。因此页面显示 `$0.00` 是当前窗口已用完的结果，不表示订单已退款或套餐被取消。

## 刚才的补偿是否在这里

2026-08-05 19:37:00+08，管理员针对该用户 `36` 条超过 `3 USD` 的历史普通余额用量执行了 `BALANCE_MANUAL_REFUND`，合计退回普通余额 `149.3559957750 USD`。这些审计记录的 `refundDestination` 均为 `balance`，且对应记录全部位于当前套餐窗口。

补偿逻辑只增加 `users.balance`，不会增加 `user_balance_packages.remaining_usd`。因此补偿已经进入用户普通余额，但不会让 `/subscriptions` 卡片中的“本周剩余额度”从 0 变回正数。

补偿后该用户又产生 `11` 条普通余额请求，累计扣费 `54.4566015000 USD`。按当前余额和这段时间的扣费回推，补偿后余额约为 `177.13389714 USD`，当前剩余 `122.67729564 USD`；差额中还包含补偿前已有的普通余额，不能把当前 `122.67729564 USD` 全部视为刚才补偿金额。

## 结论

该用户当前是已完成 4/4 次到账、将在 2026-08-08 到期的 `¥199` 余额套餐，本周套餐窗口额度已用完。刚才的 `149.3559957750 USD` 确实已补偿到普通余额；当前 `122.67729564 USD` 是补偿后继续使用后的普通余额，不是套餐本周剩余额度。
