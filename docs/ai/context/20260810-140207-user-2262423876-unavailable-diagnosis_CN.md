# 用户 2262423876@qq.com 无法使用诊断

## 结论

用户账户本身正常，无法使用的直接原因是其 API Key 固定绑定 GPT `0.15x` 分组（`groups.id=9`）。该分组当前只有一个下游账号（`accounts.id=1128`），该账号最近持续收到上游 `502/503`，网关排除该账号后返回 `503 Service temporarily unavailable`。

## 证据

- 用户 ID `549`，状态 `active`，余额 `102.00000000 USD`，并发 `20`，冻结余额 `0`。
- 余额套餐 `id=40` 状态 `completed`，剩余 `102 USD`，有效期至 `2026-08-13 20:40:35 +08`。
- 用户有两把有效 API Key：`id=164`、`id=274`；两把均绑定 `group_id=9`，没有过期、配额或限流设置。
- `account_groups` 显示 `group_id=9` 仅关联 `account_id=1128`；账号数据库状态仍为 `active`、`schedulable=true`。
- 账号 `1128` 当前错误记录为上游暂时不可用；最近请求多次收到上游 HTTP `502/503`，随后产生 `no available accounts` 和网关 HTTP `503`。
- 近 6 小时 `group_id=9` 累计 `44` 次 upstream 错误、`253` 次 routing 错误；用户两把 Key 分别产生 `58`、`150` 条错误记录。
- 账号错误信息还保留 `2026-08-08` 的上游 `403 Your request was blocked`，说明该凭证/上游线路此前已有被阻断记录；当前 503 是持续不可用表现。

## 影响范围与处理建议

这不是用户余额、套餐、账号状态或 Key 失效问题，主要影响绑定 `group_id=9` 的请求。建议先恢复或更换账号 `1128` 的上游凭证/线路，并确认该账号对请求模型可用；若需要临时恢复，可在确认价格和权限后为用户提供健康的 GPT 分组 Key（例如 `group_id=10`），不要直接修改现有 Key 的分组倍率。

本次仅执行数据库和容器日志读取，未修改用户、Key、账号或计费数据。
