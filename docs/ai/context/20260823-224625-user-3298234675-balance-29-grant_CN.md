# 用户 3298234675@qq.com 29 元余额套餐发放

- 时间：2026-08-23 21:45:48（数据库记录时区 +08）。
- 管理员授权为用户 `3298234675@qq.com`（`users.id=540`）发放购买页在售的 29 元余额套餐。

## 发放前核验

- 用户状态为 `active`，普通余额和累计充值均为 `0 USD`。
- 仅有一条已过期的 59 元套餐记录（`user_balance_packages.id=37`），不存在未过期的 `active`、`completed` 或 `debt_paused` 余额套餐。
- 目标计划为 `balance_package_plans.id=21`：`balance-29`，标价 29 CNY，每期 76 USD，28 天有效，每 7 天到账一次，共 4 期，仍在售。

## 执行结果

- 在单个事务内创建零金额管理员赠送订单 `payment_orders.id=710`，编号 `ADMIN-GRANT-710`，类型为 `admin_grant` / `balance_subscription`，状态 `COMPLETED`；不创建支付服务商交易，也不产生退款资格。
- 创建余额套餐 `user_balance_packages.id=169`：首期 `76 USD` 已到账，当前剩余 `76 USD`，状态 `active`；下次到账时间为 `2026-08-30 21:45:48 +08`，到期时间为 `2026-09-20 21:45:48 +08`。
- 用户普通余额和累计充值均更新为 `76 USD`；原有过期套餐、历史订单、流量卡、API Key、用量及欠费账本未修改。
- 新增支付审计：`payment_audit_logs.id=2069`（`BALANCE_PACKAGE_INITIAL_CREDIT`）和 `id=2070`（`ADMIN_BALANCE_PACKAGE_GRANTED`，操作人 `admin:448`）。

## 核验

- Redis 键 `billing:balance:540` 不存在，后续请求会从数据库读取最新余额。
- `http://127.0.0.1:18082/health` 返回 HTTP 200；应用、PostgreSQL 和 Redis 容器均正常。
