# AGENTS.md 瘦身（2026-09-08）

- 时间：2026-09-08 10:49（+09）
- 类型：文档瘦身（不涉及生产数据、代码逻辑与运行配置）
- 判据来源：沿用 `docs/ai/context/20260905-112834-agents-md-compression_CN.md` 第 2、3 节，未自创
- 结果：`AGENTS.md` 由 **231 行 / 36,701 字节** 瘦身为 **230 行 / 36,149 字节**（移除 2 条一次性执行流水，可复用结论折叠进相邻既有条目）

## 1. 移除的条目（原文完整照抄）

### 移除条目 1（原「三、计费口径」中 `gpt-6-astra` 开放+实测核查流水）

> - **`gpt-6-astra` 已于 2026-09-05 开放并实测计费正确**：三个 上游B 上游账号（`#1132`/`#1164`/`#1168`）的 `model_mapping` 已加入该键。实测 `usage_logs` id `404425`——`input 864×$10/M + cache_read 7488×$1/M + output 22×$50/M = total_cost 0.017228`，`actual_cost = 0.017228 × 0.16 × 18 = 0.04961664`，余额差额一致。**上游只认精确串 `gpt-6-astra`，别名一律 404**，所以坑 11 的少收风险在这条链路上触发不了。详见 `docs/ai/context/20260905-134951-gpt6-astra-enablement-and-billing-verification_CN.md`。

**命中判据**：针对特定账号的一次性执行流水（开放 + 实测核查），且已有对应独立归档 `20260905-134951-gpt6-astra-enablement-and-billing-verification_CN.md`。逐条实测（`usage_logs` id、算式、逐账号 `model_mapping` 变更）无复用价值，属「一次性执行流水（发放/刷新/取消/退款/核查）」。

### 移除条目 2（原「三、计费口径」中 `Gpt0.35倍率(优质)` 加模型流水）

> - **2026-09-05 又给 `Gpt0.35倍率(优质)`（分组 `10`、账号 `#1129`、上游 `api.ai-genesis.app`）加了该模型**，上游实测 `200`。当前 4 个分组可用：`GPT0.16(日常2)`、`GPT0.16(日常3)`、`GPT0.28`、`Gpt0.35`。**`#1128`（0.15）和 `#1130`（0.1，已下架）没加，也不要照搬着加**——见坑 24。详见 `docs/ai/context/20260905-203005-gpt6-astra-enable-on-035-group_CN.md`。

**命中判据**：一次性的「给某分组/账号加模型」执行流水，已有对应独立归档 `20260905-203005-gpt6-astra-enable-on-035-group_CN.md`。「上游实测 200」「账号 #1129」等执行细节属一次性流水。

## 2. 折叠进既有条目的可复用事实

均并入「三、计费口径」中原有的 `gpt-6-astra` 定价登记条目及其后一条，措辞与该章风格一致、不制造重复：

- **上游别名 404 的复用结论**：并入定价登记条目末尾——
  「**但上游只认精确串 `gpt-6-astra`，别名一律 404**，所以坑 11 的别名少收在这条链路触发不了。」
  （保留了「坑 11 少收在此链路触发不了」这一与坑 11 交叉引用的判断。）
- **当前可用分组这一系统状态**：折叠为一条现状条目——
  「`gpt-6-astra` 已开放并实测计费正确，当前在 4 个 GPT 分组可用：`GPT0.16(日常2)`、`GPT0.16(日常3)`、`GPT0.28`、`Gpt0.35`（分组 `10`）。**`#1128`（0.15）与 `#1130`（0.1，已下架）未加，也不要照搬着加**——同一 `base_url` 不同 key 的模型清单不同，见坑 24。」
  （保留了「当前 4 个分组可用」这一当前状态，以及「别照搬 #1128/#1130 → 坑 24」这一前瞻规则与交叉引用。）

## 3. 本次刻意保留的内容

以下项符合保留判据或落在「有疑问就保留」范围，本次未动：

- **「五、坑」全章**（含读起来像「已解决历史」的每一条：精确 ID 测模型、`schema_migrations` 行数多于文件数、对账用 `usage_logs.rate_multiplier`、`schedulerSnapshot` 两个同名不同物 等）。
- **「六、负面教训（结论已撤回）」全章**。
- **「五点五、待处理的计费偏差」全章**（含标注「已过期，别照做」的 `gpt-5.6-sol` 段落）。
- **「三、计费口径」的唯一计费公式与分组倍率表**。
- **「七、未完成」全章**。
- 文件开头 `> 本文件每个会话完整载入上下文……` 引用块。
- **Grok/GLM/GPT 0.1 已硬删除** 那一条：虽含一次性删除动作，但它确立「当前系统已无这些组/账号」这一当前系统状态（读错会误以为仍存在），并带前瞻规则「日后重新上架 glm/grok 必须先补回定价再开放（坑 12）」，故整条保留。
- `gpt-6-astra` **定价登记**条目（$10/$1/$12.50/$50、>272K 档、回归测试）：当前生效的定价事实，保留。

## 4. prune 情况（同一 PR）

同日先跑 `scripts/prune-ai-context.ps1`（保留 15 天、截止 2026-08-24），删除 5 个超期文档：

- `20260823-211000-itjiangzengwen-order-536-refund-diagnosis_CN.md`
- `20260823-214500-zpay-net-partial-refund-adaptation_CN.md`
- `20260823-222000-zpay-refund-logic-simplification_CN.md`
- `20260823-223919-zpay-refund-quote-main-sync-production-deploy_CN.md`
- `20260823-224625-user-3298234675-balance-29-grant_CN.md`
