# 18082 实例数据清空记录

时间：2026-08-03 23:10:26（Asia/Tokyo）

## 清理范围

- 停止并重新启动 `sub2api-official-18082` 应用容器。
- 清空该实例 PostgreSQL 的全部业务表数据，包含用户、管理员、设置、余额套餐、分组、渠道、订单、用量、审计和关联数据。
- 保留 `schema_migrations`、`atlas_schema_revisions` 迁移元数据和数据库表结构。
- 清空该实例 Redis 全部键，并清空应用日志文件。
- 保留 `deploy/data-18082` 中的数据库连接配置、安装锁和模型定价缓存，防止启动时自动创建管理员。

## 验证

- 用户数：`0`
- 管理员数：`0`
- 设置数：`0`
- 余额套餐数：`0`
- 分组数：`0`
- 支付渠道数：`0`
- Redis 键数：`0`
- `GET http://127.0.0.1:18082/health` 返回 HTTP `200`。

当前实例没有管理员账号；后续需要通过受控的管理员初始化流程重新建立。
