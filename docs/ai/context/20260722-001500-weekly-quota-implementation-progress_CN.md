# 2026-07-22 周额度实施进度

## 本轮补齐

- 购买卡片按后端套餐字段展示周额度、每 7 天刷新和有效期；公共套餐不再拼接“日限额 / 24 点刷新”。
- 用户订阅页、顶部订阅进度、Key 用量与 Dashboard 消费有效周额度和服务端重置时间；展示统一为整数 USD。
- API Key 用量响应新增有效周额度与周窗口重置时间；Dashboard 新增通用窗口字段并在公共周额度订阅中返回当前窗口事实。
- 用户订单页新增只读退款 quote；普通用户退款和管理员普通退款都在服务端重新计算 quote，手续费不计入可退款本金，无法唯一归属的历史用量转人工审核。
- 退款撤销只针对目标 `payment_order` 权益段，保留后续续费权益；普通管理员退款忽略手工金额，强制退款仍可走人工路径。
- 管理员新建公共 Codex 套餐默认 28 天，选择周额度 Codex 分组时自动回填 28 天。

## 待继续

- 把 entitlement period ID 写入每个新的成功 usage fact，并为兑换/默认赠送/管理员调整统一显式快照。
- 用本地附件数据库执行 dry-run、备份、cutover、缓存失效和两条超额审计门禁；未执行前不得切换本地运行服务。
- 补齐 Quote、后续续费退款、存量尾窗和 Dashboard 的集成测试及浏览器验收。
