# 用户 yuheng-wu@foxmail.com 提前结算最后一期周额度抵欠费记录

## 授权与范围

管理员授权将用户 `yuheng-wu@foxmail.com`（`users.id=518`）尚未到账的下一周余额套餐额度提前结算，并以该额度优先抵扣此前余额击穿形成的普通余额欠费，恢复用户使用资格。

本次只处理余额套餐 `user_balance_packages.id=26` 及其关联订单 `payment_orders.id=357`。未修改历史用量、API Key 配额、流量卡、订单金额、订单状态、退款金额、套餐有效期或其他用户数据。

## 执行前状态

- 普通余额：`-0.08877124 USD`。
- 余额套餐：ID `26`，每周额度 `520.00000000 USD`，当前窗口剩余 `0 USD`。
- 到账进度：`3/4`；原定第 4 期到账时间为 `2026-08-12 15:19:58.032294 +08:00`。
- 套餐状态：`debt_paused`；有效期至 `2026-08-19 15:19:58.032294 +08:00`。
- 关联订单：ID `357`，状态 `COMPLETED`，退款金额 `0 CNY`。

## 执行方式

在单个 PostgreSQL 事务中对用户、套餐和订单加行锁，并将余额、套餐状态、每周额度、到账进度、刷新时间、到期时间与订单状态作为前置条件。任一前置条件不匹配即抛错回滚。

按周额度刷新语义计算：

```text
新余额 = 旧余额 - 当前窗口剩余 + 本周额度
欠费还款 = min(max(-(旧余额 - 当前窗口剩余), 0), 本周额度)
```

## 执行结果

- 第 `4/4` 期 `520 USD` 额度已于 `2026-08-11 15:48:29 +08` 提前结算。
- 其中 `0.08877124 USD` 优先偿还全部欠费。
- 普通余额与本周套餐额度均为 `519.91122876 USD`，用户不再处于欠费状态。
- 套餐状态更新为 `completed`，到账进度为 `4/4`，`next_credit_at` 已清空；到期时间保持 `2026-08-19 15:19:58.032294 +08:00`，未额外延长权益。
- 订单继续保持 `COMPLETED`，退款金额保持 `0 CNY`。
- 新增支付审计 `payment_audit_logs.id=1559`：`BALANCE_PACKAGE_EARLY_WEEKLY_CREDIT_4`。
- 新增欠费还款账本 `balance_debt_ledger.id=17`：来源 `balance_package_early_weekly_credit`，引用 `package:26:credit:4`。

## 缓存与核验

- 已失效余额缓存 `billing:balance:518`；缓存键原本不存在，删除返回 `0`。
- 已删除该用户 API Key 的 L2 鉴权缓存，并向 `auth:cache:invalidate` 发布哈希键失效消息；一个应用订阅者已接收。未记录或输出 API Key 原文。
- 数据库回读确认余额、套餐剩余额度、到账进度、套餐状态、订单状态、支付审计和欠费账本均与上述结果一致。
- `http://127.0.0.1:18082/health` 返回 HTTP `200`。
