# 单笔超过 1 美元用量退款审计

## 快照

数据快照时间：`2026-08-05 19:06:55.903243 +08`。公网服务保持运行，后续新请求不在本快照范围内。

逐条明细见：

- `20260805-190655-over-1-usd-refund-audit-detail.csv`：3098 条记录，每条包含用户邮箱、用量日志 ID、实际扣费、计费类型、流量卡流水映射和建议退款去向。
- `20260805-190655-over-1-usd-refund-audit-summary.csv`：75 个用户的汇总表。

## 分类汇总

| 计费类型 | 含义 | 日志数 | 用户数 | `actual_cost` 合计 USD | 处理建议 |
| ---: | --- | ---: | ---: | ---: | --- |
| 0 | 普通余额 | 451 | 31 | 971.4324801 | 如确认错误，退回普通余额 |
| 1 | 订阅套餐 | 2352 | 73 | 4038.0100496 | 历史日志无 `subscription_id`，无法原路恢复套餐，退普通余额 |
| 2 | 流量卡 | 186 | 41 | 345.5252406375 | 127 条可原路回流原流量卡 `$235.5207093375`；59 条来源不完整/已过期，退普通余额 |
| 3 | Shadow 仅观测 | 109 | 16 | 195.475722 | 未实际扣费，不退款 |

实际发生扣费的候选金额为 `$5354.9677703375`，不包含 Shadow 观测金额 `$195.475722`。以上是“单笔超过 1 美元”的筛选结果，不代表每一笔都必然错误；K3 的历史 3.5 倍率错误已单独审计。

## 原路退款限制

- `billing_usage_entries` 当前没有历史流水，普通余额扣款只能依据 `usage_logs.billing_type=0` 审计，退款应新增可审计余额流水，不能直接改余额字段。
- 流量卡只有能够通过 `traffic_credit_ledger.request_id` 和 `credit_id` 精确映射、且原额度仍有效的记录才适合原路恢复。
- 订阅类历史记录的 `subscription_id` 全为空，不能安全恢复到某个套餐额度；按用户要求应退普通余额。
- 本审计只读查询，没有修改用户余额、流量卡、套餐或退款订单。
