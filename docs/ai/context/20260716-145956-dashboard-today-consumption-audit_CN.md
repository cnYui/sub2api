# Dashboard 今日消费核对

## 结论

截图中的今日消费计算正确。

截图累计显示的 `$0.0322 / $0.0322` 唯一对应运行库中的 `users.id=31`：精确累计标准价与实际扣费均为 `$0.032164`，前端以 `toFixed(4)` 显示为 `$0.0322`。截至 `2026-07-16 13:58:31 +08`，该用户当天没有任何 `usage_logs`，因此今日标准价和实际扣费均为 0，显示 `$0.0000 / $0.0000`。

## 核对口径

- 页面请求 `/api/v1/usage/dashboard/stats`，后端按当前认证用户的 `user_id` 聚合 `usage_logs`。
- 今日边界为服务端 `Asia/Shanghai` 的 `2026-07-16 00:00:00 +08`；查询条件为 `created_at >= today`。
- 左侧金额是 `actual_cost`，右侧金额是 `total_cost`；该用户 9 条历史成功记录中两者相等。
- 最近一条使用记录为 `2026-07-13 07:17:52 +08`，7 月 15 日和 7 月 16 日均无用量记录。

## 排除项

- `ops_error_logs` 中该用户 7 月 16 日没有失败请求。
- 当前候选运行库尚未包含 `usage_facts` 表，因此不存在“已持久化但尚未结算”的 usage fact 可供核对；同时今天 `usage_logs` 也为 0，截图不存在漏计迹象。

## 范围

本轮只读查询源代码、运行态 PostgreSQL 与容器日志，未修改业务代码、数据库、Redis、容器、配置或用户数据。
