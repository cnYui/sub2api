# xunskyler@gmail.com 今日 token 用量只读核验

## 结论

- 统计窗口为北京时间 `2026-07-14 00:00:00+08` 至查询时。
- `usage_logs` 中成功落库 196 条请求，账面 token 合计 `18,932,487`。
- 该数字只能作为今日实际消耗的下限，不能代表完整实际用量。
- 套餐到期且流量卡接近耗尽后，大量模型请求已经返回 HTTP 200，但 `RecordUsage` 因 `INSUFFICIENT_BALANCE` 失败，没有写入 `usage_logs`；这些请求的 token 明细没有被持久化。
- CLIProxyAPI 当前 `usage-statistics-enabled=false`、`logging-to-file=false`，现有上游日志也没有保存可按 Sub2API 用户关联的成功响应 token，因此无法可靠补算实际总 token。

## 账面明细

- 非缓存输入 token：`1,125,281`
- 缓存读取输入 token：`17,722,496`
- 输出 token：`84,710`
- 缓存创建 token：`0`
- 总 token：`1,125,281 + 17,722,496 + 84,710 = 18,932,487`
- 账面费用：`15.8650683000 USD`
- 首条成功落库时间：`2026-07-14 08:57:51+08`
- 末条成功落库时间：`2026-07-14 14:23:09+08`

## 模型明细

- `gpt-5.6-sol`：161 条；非缓存输入 875,573，缓存读取 16,490,496，输出 74,429。
- `gpt-5.4`：27 条；非缓存输入 211,848，缓存读取 1,210,496，输出 9,578。
- `gpt-5.4-mini`：8 条；非缓存输入 37,860，缓存读取 21,504，输出 703。

## 未落库范围

- 今日容器日志中存在 1,958 条该用户 `openai.record_usage_failed / INSUFFICIENT_BALANCE` 记录；该日志只证明成功转发后的记账失败，不包含 token 数。
- 已知来源 `64.118.128.112` 今日有 397 条 `/v1/responses` HTTP 200，但该来源并不覆盖用户的所有请求来源。
- 因缺少不可变 usage fact，不能用请求体字节数、费用或平均值反推精确 token；任何估算都会存在较大误差。

## 本次操作

- 只查询 PostgreSQL、Sub2API 容器日志、CLIProxyAPI 配置和本地日志。
- 未修改数据库、Redis、容器、用户、API Key、额度或上游配置。
