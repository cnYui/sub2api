# 内层 latest 管理员余额扩容结果

时间：2026-07-26 08:19（Asia/Tokyo）

## 完成结果

- 已在本机内层 latest Sub2API（`127.0.0.1:18086`）通过正式管理员界面，为 `xiaobianfuai@gmail.com`（`users.id=1`）增加 `10000000.00000000` 余额。
- 外层 `sub2api-dev:18080` 的用户计费、模型价格和上游账号池均未修改。
- 最终数据库核对余额为 `10006870.46572295`；与提交前余额及充值金额之间的微小差异来自内层仍在执行的模型请求。

## 审计与验证

- 最新 `redeem_codes` 记录：
  - `type=admin_balance`
  - `value=10000000.00000000`
  - `status=used`
  - 备注：`内层 18086 上游转发余额扩容，外层 18080 继续独立计费`
  - 写入时间：`2026-07-26 07:18:19 +08`（容器时区）。
- `GET http://127.0.0.1:18086/health` 返回 `200`。
- 变更前备份：`deploy/backups/20260726-080646-upstream-latest-admin-balance-prechange.dump`，大小 `12685040` bytes，已用 `pg_restore -l` 验证可读取。

## 发现的后续风险

- 内层部署文件中的初始化管理员密码已无法登录现有管理员账号，且未配置管理员 API Key；本次使用用户现有 Chrome 管理会话完成正式操作，未重置密码、未写入或输出任何凭据。
- 后续需要单独轮换并验证内层管理员登录凭据，避免本地自动化运维再次因认证配置漂移受阻。
