# xwh1124wcw@163.com 两张 20 USD 流量卡管理员发放

## 授权与范围

- 管理员要求为用户 `xwh1124wcw@163.com`（`users.id=453`）添加两张“20 刀”OpenAI 流量卡。
- 本次仅发放两笔独立的 `20 USD` 流量额度，共 `40 USD`；不修改普通余额、既有流量卡批次、API Key、订单历史或模型配置。

## 前置核验

- 用户状态为 `active`。
- 在售套餐为 `traffic_packs.id=3`、`gpt_traffic_20usd_5cny`，额度 `20 USD`、有效期 `28` 天、平台 `openai`。
- 发放前普通余额为 `-4.72882879 USD`，现有可用 OpenAI 流量卡额度为 `0.0594241250 USD`。

## 执行与审计

- 使用零金额 `admin_grant` 订单发放，不伪造支付服务商交易。
- 首次事务中，尝试在同一数据修改 CTE 内更新刚插入的订单；PostgreSQL 的同语句快照使更新看不到新行，因此创建了订单 `592`、`593`，但未创建额度、流量流水或支付审计。
- 立即锁定并核验这两笔订单，确认它们恰为本次目标订单且不存在对应额度、流水和审计后，在单一事务中补齐发放数据。修复前置断言避免重复发放。
- 最终两笔订单均为 `COMPLETED`、`payment_type=admin_grant`，订单编号分别为 `ADMIN-TRAFFIC-GRANT-592` 和 `ADMIN-TRAFFIC-GRANT-593`。
- 两笔分别对应 `user_traffic_credits.id=387`、`388`，各有一条 `purchase` 流水和 `ADMIN_TRAFFIC_PACK_GRANTED` 支付审计；审计详情以 `provisioning_repair=true` 保留此次补齐过程。

## 结果核验

- 每张卡额度均为 `20 USD`，初始和当前剩余额度均为 `20 USD`。
- 生效时间为 `2026-08-06 12:30:29 +08`，到期时间为 `2026-09-03 12:30:29 +08`。
- 用户当前可用 OpenAI 流量卡总额度为 `40.0594241250 USD`，包含原有未用 `0.0594241250 USD`。
- 普通余额保持 `-4.72882879 USD`；`http://127.0.0.1:18082/health` 返回 `200`。

## 后续

- 当前管理端仅提供余额套餐的手动发放接口，未提供流量卡的等价入口。若此类操作需要常态化，应新增复用现有订单、额度批次和审计模型的管理员流量卡发放接口，避免再由运维 SQL 执行。
