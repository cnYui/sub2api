# xwh1124wcw@163.com 无法正常使用排查

## 结论

用户 `xwh1124wcw@163.com` 对应 `users.id=453`，当前不是账号权限、API Key、余额或限额问题。失败发生在 OpenAI 上游账号选择和转发阶段，用户唯一 API Key（`api_keys.id=15`）绑定分组 `groups.id=9`，该分组当前只有一个上游账号 `accounts.id=1128`。该账号在本次时间窗内出现过 OpenAI 403 临时冷却，随后针对目标用户请求持续返回上游 502/503，最终路由层返回 503。

## 核查证据

- 目标用户失败请求的模型后缀为 `luna`；同分组成功样本包含 `sol` 和 `terra`。

- 用户状态为 `active`，余额约 `8.65 USD`，冻结余额为 `0`。
- 余额套餐 `user_balance_packages.id=113` 状态为 `active`，本周剩余额度约 `8.65 USD`，未过期。
- OpenAI 流量卡仍有约 `0.51 USD`，不存在“无余额可用”的事实。
- API Key `id=15` 状态为 `active`，无过期时间，配额和 5 小时/1 天/7 天限额均为 `0`（不限额），用户和 Key 的 RPM 也为 `0`（不限速）。
- 失败记录中的上游失败均指向账号 `1128`；失败期间日志明确出现 `openai.account_select_failed: no available accounts`。
- 账号 `1128` 当前数据库状态仍为 `active/schedulable=true`，但保留了 `OpenAI 403 temporary cooldown (1/3): Access forbidden (403): Your request was blocked` 的临时冷却原因。
- 同一分组的其他用户在账号恢复后仍有成功调用，说明不是分组整体永久下线；目标用户在 `09:50` 后没有新的成功用量记录，较可能是连续失败后停止重试。

## 处理建议

1. 先不要改用户余额或重置 API Key；这些操作不能解决当前故障。
2. 检查并恢复/替换分组 9 的上游账号 1128，最好为该分组增加第二个健康账号，避免单账号冷却导致全组 503。
3. 账号恢复后让用户重新发起一次小请求；如果仍失败，再单独检查该模型在上游账号的可用性和请求上下文大小。
4. 若需要临时恢复用户使用，应在获得授权后将其 API Key 绑定到已验证可用的 OpenAI 分组；该动作会改变用户可用模型和计费倍率，必须保留审计记录。

## 本次操作范围

本次仅读取生产 PostgreSQL、应用容器日志和账号状态，没有修改用户、API Key、分组、上游账号或余额数据。
