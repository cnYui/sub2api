# 用户余额与 Grok 真实调用核验

时间：2026-08-03 15:42:56（Asia/Tokyo）

## 目标

为 `2799523972@qq.com` 增加 5 美元余额，并使用该用户提供的 API Key 发起一次最小成本的真实 Grok 调用，核验：

1. 请求是否路由到 Grok 账号。
2. 是否得到有效模型响应。
3. 是否产生可关联的用量和扣费记录。
4. 实际用户扣费相对上游成本是否为 10 倍。

## 操作边界

- 余额变更仅通过已登录的管理员界面完成，数据库只用于前后只读核验。
- 不调整 API Key 所属分组、账号路由或任何倍率配置。
- 请求使用极短提示和较低输出上限控制成本。
- 记录中不保存 API Key 明文或上游凭据。

## 验证方法

调用前后读取用户余额、该用户密钥的额度字段及最新用量记录；以用量记录中的账号、分组、模型、实际成本和用户扣费字段交叉判断路由与倍率。若系统没有足够字段，明确报告无法证实的部分，不以配置名称替代实际账单证据。
