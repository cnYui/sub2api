# liyutong2883@gmail.com 追加 3 元流量卡计划

## 目标

- 按用户要求，直接修改 Sub2API 运行态数据库，为 `liyutong2883@gmail.com` 对应账户添加一张 3 元 GPT 流量卡。
- 不改源码，不改套餐配置，不触碰订阅、余额充值和支付 provider 配置。

## 必须确认

- 确认运行态数据库路径，优先使用当前 Sub2API 容器或本地运行态实际挂载的数据库。
- 确认用户唯一存在，避免同邮箱多账号误写。
- 确认流量卡在当前系统中的真实数据模型：如果存在独立流量包/credit 表，按现有履约语义插入；如果只是订单驱动履约，需要沿用现有购买完成后的最终状态。

## 风险与边界

- 不记录完整 API Key、内部 token、支付密钥等敏感信息。
- 修改前做数据库备份。
- 只为目标用户增加 3 元可用流量，不增加余额，不改变订阅分组和套餐有效期。

## 验证

- 修改后查询用户记录和对应流量卡/流量包记录。
- 若服务有缓存，确认是否需要重启或调用现有刷新机制；没有必要则不重启。
