# 3876129758@qq.com API Key 真实请求验证计划

时间：2026-07-08 17:14 JST

## 目标

- 使用截图用户 `3876129758@qq.com` 当前 active API Key 发起一次真实公网模型请求。
- 请求入口使用正式模型 API：`https://api.aaccx.pw/v1/responses`。
- 不在终端输出、文档或回复中暴露完整 API Key。

## 边界

- 不修改业务代码。
- 不改数据库中的用户、订阅、余额或 Key 状态。
- 只允许因真实请求自然产生用量记录和订阅/流量卡扣费。

## 验证步骤

1. 在公网候选库中定位用户、active Key、订阅和今日额度状态。
2. 用 Key 请求 `gpt-5.5`，发送一条短消息。
3. 核对 HTTP 响应、模型回复、`usage_logs` 和扣费口径。
4. 将结果另存为新的上下文文档。
