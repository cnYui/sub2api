# 18082 最终发布记录

## 分支

本地 `codex/fix-traffic-pack-card-layout` 和 `codex/official-upstream-migration` 均已是 `main` 的祖先，没有需要再次合并的未合并本地分支。工作区原有未提交改动未被清理，镜像按当前 `main` 工作区内容构建。

## 发布修复

- 恢复 `199_normalize_balance_package_lifecycle.sql` 为生产已执行版本，SHA-256 为 `ebf3270d791be46096d4a92c0531da7ec5520de769ab196e65b0df211b8f8f8b`。
- 新增 `201_normalize_balance_package_plan_snapshots.sql` 承载订单快照和套餐字段同步。
- 修复 `200_balance_package_weekly_remaining.sql` 中 PostgreSQL 关键字别名和 `UPDATE ... FROM` 目标表引用问题。
- 补齐前端 `UserBalancePackage.current_remaining_usd` 类型声明。

## 运行结果

- 旧应用容器已停止，随后使用最新镜像重新创建并启动 `sub2api-official-18082`。
- 新镜像：`deploy-sub2api:latest`，digest `sha256:ea441e1f043c8b8d518715918e6123656386912f05efd17c4eb0ea5ea282a7fc`。
- 应用状态：`running`，健康状态：`healthy`，重启策略：`unless-stopped`。
- `http://127.0.0.1:18082/health`、`http://127.0.0.1:8080/health`、`https://aaccx.pw/health` 均返回 HTTP 200。
- `https://aaccx.pw/` 返回新版入口 HTML；新版 `ModelDistributionChart...D-B9-D0u.js` 通过 Cloudflare 返回 HTTP 200。
- 数据库已记录并校验通过 199、200、201 三个迁移。PostgreSQL、Redis、Tunnel 和公网 Nginx 未重建。
