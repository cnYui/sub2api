# 377293029@qq.com 订阅与公网 API 可用性排查计划

## 背景

- 用户邮箱：`377293029@qq.com`。
- 用户反馈：2026-07-21 购买 29 元套餐后，2026-07-22 当前无法使用。
- 当前目标：确认该用户订单、订阅、API Key、额度、调度链路和公网 `/v1` 最小请求结果。

## 范围

- 只读查询当前公网事实源数据库、容器状态、健康检查和必要日志。
- 使用该用户真实 API Key 做一次低成本公网 `/v1/models` 和最小模型请求验证。
- 不在文档、日志、回复中记录完整 API Key、内部 token、HMAC、支付密钥或 SMTP 密码。

## 安全边界

- 本轮默认不修改生产 DB、Redis、容器、Nginx、Cloudflare 或上游账号池。
- 如果定位到需要修复运行态状态，先补充计划、备份并请求明确授权。
- 公网真实请求可能产生极小额度消耗，仅用于验证认证、套餐计费和上游可用性。

## 排查步骤

1. 确认当前公网链路健康、容器和 Nginx 指向。
2. 查询用户、订单、订阅、权益周期、API Key 状态和最近用量。
3. 检查 29 元套餐对应 `group_id=2 / codex-pool-19-usd` 是否正确发放。
4. 使用真实 Key 打公网 `/v1/models`，再发一个最小 `/v1/responses` 请求。
5. 用返回 request id 反查 `usage_facts` 与 `usage_logs`，判断失败属于 Key、订阅额度、计费来源、上游账号池或公网链路问题。
6. 写结果上下文，不暴露完整密钥。
