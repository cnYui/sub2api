# 用户 1850226892@qq.com 的 Kimi K3 扣费核查

## 核查对象

- 实例：`sub2api-official-18082`
- 用户：`1850226892@qq.com`，用户 ID `480`
- API Key：ID `211`，分组 ID `7`
- 请求：`kimi-k3`，`usage_logs.id=287142`
- 请求时间：2026-08-04 11:28:44（Asia/Tokyo）
- 请求指纹：`client:b305cf74-2a03-4966-a0d2-872cadbf6158`

## 原始计费数据

| 项目 | 值 |
| --- | ---: |
| 输入 token | 17,230 |
| 输出 token | 124 |
| 缓存读取 token | 256 |
| 输入成本 | `0.0516900000` |
| 输出成本 | `0.0018600000` |
| 缓存读取成本 | `0.0000768000` |
| `total_cost`（基础成本） | `0.0536268000` |
| `rate_multiplier`（Kimi 分组倍率） | `3.5000` |
| `actual_cost`（实际扣费） | `2.8154070000` |
| `account_rate_multiplier`（账号统计/配额倍率） | `35.0000` |

Kimi K3 fallback 单价为输入 `$3/MToken`、输出 `$15/MToken`、缓存读取 `$0.30/MToken`，因此基础成本计算正确：

`17230 × 3e-6 + 124 × 15e-6 + 256 × 0.30e-6 = 0.0536268`

## 实际扣费公式

当前容器配置 `BILLING_FINAL_MULTIPLIER=15`。服务端在 `computeTokenBreakdown` 中执行：

`actual_cost = total_cost × rate_multiplier × BILLING_FINAL_MULTIPLIER`

本条记录的实际结果为：

`0.0536268 × 3.5 × 15 = 2.815407`

因此，`2.8` 美元不是浮点误差，也不是重复插入造成的；数据库中该用户当前只有这一条正成本 Kimi K3 记录，`usage_billing_dedup` 也只有一个对应请求指纹。

## 倍率字段边界

- `rate_multiplier=3.5` 是用户实际扣费使用的 Kimi 分组倍率。
- `account_rate_multiplier=35` 是账号侧统计/上游配额倍率，不会再乘进用户 `actual_cost`。
- 产生 `2.815407` 的额外放大来自全局 `BILLING_FINAL_MULTIPLIER=15`。

## 结论

从当前代码和配置看，算术结果是一致的；但如果业务预期是 Kimi 只按分组 `3.5x` 扣费，那么这条记录确实属于过扣，应该是配置口径问题而不是 token 成本计算问题。

不启用全局 15x 时，同一请求应扣：

`0.0536268 × 3.5 = 0.1876938`

需要产品/管理员确认 `15x` 是否应对 Kimi 生效后，才能决定是调整全局环境变量，还是把最终倍率改为按模型/分组生效；本次核查未修改线上数据。
