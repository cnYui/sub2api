# 迁移完成后统一用户并发为 20

## 操作

- 目标实例：`sub2api-official-18082`。
- 迁移完成后，将所有未删除用户的 `users.concurrency` 统一设置为 `20`。
- 对 153 个有效 API Key 删除认证缓存并发布缓存失效消息，避免旧的 `200` 继续生效。

## 核验

- 活动用户：142 位；并发为 20：142 位；其他值：0 位。
- `2799523972@qq.com` 当前并发：20。
- 应用容器健康检查：通过。
- 数据库列默认值和 `/app/data/config.yaml` 的新用户默认值仍为 20。
