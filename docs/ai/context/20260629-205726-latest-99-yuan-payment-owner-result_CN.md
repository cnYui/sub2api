# 最近一笔 99 元账单付款人只读排查结果

## 结论

- 公网候选库中最近一笔 99 元附近的已完成账单是 `payment_orders.id=60`。
- 系统记录的付款用户是 `2864533153@qq.com`，对应 `users.id=54`。
- 订单类型：`subscription`。
- 支付方式：`alipay`。
- 基础金额：`99.00`。
- 实付金额：`99.99`，包含运行态 1% 手续费。
- 订单状态：`COMPLETED`。
- 创建时间：`2026-06-29 19:10:09.354703+08`。
- 支付时间：`2026-06-29 19:10:31.945045+08`。
- 完成时间：`2026-06-29 19:10:31.980083+08`。

## 关联套餐与履约

- `subscription_plans.id=6`：`99 元订阅池`，价格 `99.00`。
- 订单关联分组：`groups.id=8`，`codex-pool-89-usd`，每日额度 `89.00000000` USD。
- 订单已履约为 `user_subscriptions.id=67`：
  - `user_id=54`
  - `group_id=8`
  - `status=active`
  - `starts_at=2026-06-29 19:10:31.971592+08`
  - `expires_at=2026-07-29 19:10:31.971592+08`
  - `notes=payment order 60`

## 查询口径

- 查询 `payment_orders` 中 `status IN ('PAID','COMPLETED')` 且 `amount` 或 `pay_amount` 在 99 元附近的订单。
- 当前公网候选库在该口径下只查到这一笔已完成订单。
- 数据库只能确认 Sub2API 订单归属用户和支付宝支付完成状态；没有在结论中输出完整支付流水号或第三方付款账户细节。
