# 用户 1032726009@qq.com 临时 520 USD 撤销与欠费处理

## 操作范围

- 操作时间：2026-08-06 19:50（Asia/Tokyo）
- 实例：`sub2api-official-18082`
- 用户 ID：`505`
- 本次按管理员要求处理临时手动余额，不执行支付退款，不删除历史订单。

## 临时额度核算

- 手动额度来源：`redeem_codes.id=43`，类型 `admin_balance`，额度 `520.00000000 USD`。
- 使用时间：2026-08-06 17:21:23+08。
- 使用时间点之后的 `22` 条用量合计 `actual_cost=40.3352458125 USD`。
- 入账前账户仍有 `0.4004884125 USD`，因此撤销临时额度时保留该部分原有余额。

## 执行结果

- 在锁定用户行的事务中从当前余额扣除 `520.00000000 USD`。
- 用户余额由 `480.06524260 USD` 调整为 `-39.93475740 USD`，代表撤销临时额度后的净欠费。
- 新增 `balance_debt_ledger` 欠费记录：`39.9347574000 USD`，来源 `manual_balance_grant_revoke/redeem_code:43`。
- 当前余额套餐 `user_balance_packages.id=21`（订单 `163`）标记为 `cancelled`，剩余额度为 `0`，清空后续到账时间。
- 订单 `payment_orders.id=163` 保持 `COMPLETED`，不物理删除、不伪造退款状态，以保留支付和履约审计；取消后的套餐不再参与有效套餐校验，用户可以重新购买。
- 新增审计：
  - `payment_audit_logs.id=1388`：`BALANCE_MANUAL_GRANT_REVOKED`
  - `payment_audit_logs.id=1389`：`BALANCE_PACKAGE_MANUAL_CANCELLED`
- 为该用户 4 个有效 API Key 写入认证缓存失效队列；余额缓存 `billing:balance:505` 当前不存在。

## 后续抵消口径

用户重新购买余额套餐并首期到账时，服务端会先用本次负余额抵扣新到账额度，并记录还款账本；本次负余额不会被退款或套餐取消流程覆盖。
