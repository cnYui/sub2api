# 用户 1032726009@qq.com 提前刷新周额度记录

## 授权与范围

按管理员要求，提前结算用户 `1032726009@qq.com` 的下一周余额套餐额度，使其可继续正常使用。

本次仅处理用户 `users.id=505`、当前有效余额套餐 `user_balance_packages.id=123` 及关联订单 `payment_orders.id=600`。历史已取消套餐 `id=21` 与已过期套餐 `id=89` 未修改；本次未修改订单金额、订单状态、退款金额、历史用量、API Key 配额、流量卡或套餐有效期。

## 执行前状态与并发处理

- 初始只读快照：普通余额和本周套餐剩余均为 `36.44161360 USD`，套餐为 `active`、已到账 `2/4`，每周额度 `781 USD`，下一期原定于 `2026-08-20 19:53:21.810233 +08:00`。
- 用户在执行期间持续产生已结算用量。带固定剩余额度的首个 `SERIALIZABLE` 前置校验检测到剩余已降为 `32.90110360 USD` 后立即回滚，未写入任何余额、套餐、审计或账本记录。
- 最终执行使用同一 `SERIALIZABLE` 事务，在锁定用户、套餐和订单后读取实时余额。事务锁定时普通余额与旧窗口剩余均为 `28.91773960 USD`，说明没有普通余额或欠费部分。

## 执行方式

事务校验用户、套餐、订单的身份、状态、到账期数、周额度、刷新间隔、有效期和退款状态。按正常周刷新语义，先从普通余额移除旧周未用套餐额度，再写入新一周额度；因此不会把旧窗口余额和新周额度叠加。

## 执行结果

- 第 `3/4` 期 `781 USD` 已于 `2026-08-19 16:03:56 +08:00` 提前结算。
- 旧周剩余 `28.91773960 USD` 已按正常周窗口规则替换，不保留为额外普通余额。
- 普通余额与当前套餐剩余均为 `781.00000000 USD`。
- 套餐保持 `active`，下次刷新为 `2026-08-27 19:53:21.810233 +08:00`；有效期保持不变。
- 普通余额部分为 `0 USD`，本次欠费抵扣为 `0 USD`，未新增欠费还款账本。
- 新增支付审计 `payment_audit_logs.id=1950`：`BALANCE_PACKAGE_EARLY_WEEKLY_CREDIT_3`，操作者 `admin:authorized_manual_settlement`。

## 缓存与核验

- 已尝试清理余额缓存 `billing:balance:505`，执行时不存在（删除结果 `0`）。
- 已对该用户现有 `5` 个 API Key 删除鉴权缓存并发布 `auth:cache:invalidate` 失效通知；执行时缓存键均不存在（删除总数 `0`）。
- 数据库回读确认余额 `781 USD`、套餐 `active`、到账进度 `3/4`、剩余 `781 USD`、下次刷新时间、订单状态和支付审计均符合预期。
- 当前余额为正，用户不受负余额硬拦截。

