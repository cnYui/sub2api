# /purchase 页面主线部署说明

时间：2026-08-03 16:20:00（Asia/Tokyo）

## 目标

将本次 `/purchase` 页面替换提交到当前 `main` 分支，并重新构建、启动 `18082` 服务使其加载新的前端静态资源。

## 提交范围

仅包含本次商店页面的路由切换、新页面、商品卡片组件与测试、对应上下文记录；不包含工作区中已有的 `.gitignore`、部署文件、支付改动及其他历史上下文文件。

## 部署命令

`deploy/docker-compose.18082.yml` 是覆盖文件，不能单独启动。使用基础 Compose 文件和该覆盖文件共同执行构建与启动，以保留 `18082` 专用的容器名、端口与数据卷。

## 验证

启动完成后检查容器健康状态，并访问 `http://127.0.0.1:18082/purchase`，确认新打包资源已被服务端加载。
