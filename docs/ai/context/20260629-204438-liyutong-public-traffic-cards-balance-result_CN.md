# liyutong2883@gmail.com 公网两张 10 USD 流量卡余额只读排查结果

## 结论

- 公网候选库中 `liyutong2883@gmail.com` 对应 `users.id=12`，当前用户余额为 `0.00000000`。
- 该用户当前保留两张 OpenAI/GPT 10 USD 流量卡：
  - `user_traffic_credits.id=7`：初始 `10.0000000000`，剩余 `0.0000000000`，已用完。
  - `user_traffic_credits.id=46`：初始 `10.0000000000`，剩余 `0.0231740000`，未完全用完。
- 两张卡合计初始 `20.0000000000`，当前合计剩余 `0.0231740000`，已扣 `19.9768260000`。

## 扣费账本

- `credit_id=7`：62 条 `traffic_credit_ledger.deduction`，扣费合计 `10.0000000000`；首次扣费 `2026-06-29 11:25:18.260278+08`，末次扣费 `2026-06-29 13:17:05.794669+08`，末次后余额 `0.0000000000`。
- `credit_id=46`：70 条 `traffic_credit_ledger.deduction`，扣费合计 `9.9768260000`；首次扣费 `2026-06-29 13:17:05.794669+08`，末次扣费 `2026-06-29 14:56:46.079059+08`，末次后余额 `0.0231740000`。
- 按 `request_id` 聚合 ledger 后 join `usage_logs`：131 个请求，ledger 总额 `19.9768260000`，usage 实际金额 `19.9768260000`，差额 `0.0000000000`，说明这些扣费均对应真实用量记录。

## 当前额度状态

- 今日应用计费窗口内订阅用量：`18.9935179500 / 19.00000000`，订阅剩余约 `0.0064820500` USD。
- 今日流量卡兜底用量：131 条，金额 `19.9768260000`。
- `2026-06-29 14:56:46.079059+08` 之后该用户没有新的 `usage_logs` 或流量卡 ledger。
- 现在仍有 1 张可用流量卡余额，但只有 `0.0231740000` USD；常规请求如果费用超过这个余额，流量卡无法完整覆盖。
