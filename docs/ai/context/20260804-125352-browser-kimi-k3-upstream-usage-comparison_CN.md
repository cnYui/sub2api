# 浏览器实测：上游 Kimi K3 费用与本地扣费对比

## 页面状态

- 页面：`https://api.ai-genesis.app/usage`
- 当前页面选中的 API 密钥标签显示为 `k3`；按本次核查上下文视为用户所说的 K3K3。
- 当前模型筛选为 `kimi-k3`，分组为 `【国产】月之暗面 Kimi（5折）`。
- 页面时间范围为近 24 小时。

## 上游汇总证据

页面“模型分布”显示：

| 模型 | 请求 | Token | 实际 | 标准 |
| --- | ---: | ---: | ---: | ---: |
| `kimi-k3` | 400 | 86.18K | `$1.50` | `$0.429` |

`1.50 / 0.429 ≈ 3.5`，说明上游页面的“实际”费用已经包含 `3.5x` 倍率。

## 上游具体小请求

页面中一条记录（2026/08/04 12:48:45）显示：

- 输入 token：138
- 输出 token：52
- 页面费用：`$0.004179`

按 Kimi K3 基础单价计算：

`138 × 3e-6 + 52 × 15e-6 = 0.001194`

再乘上游 `3.5x`：

`0.001194 × 3.5 = 0.004179`

与页面费用完全一致。

## 与本地大请求对齐

本地用户 `1850226892@qq.com` 的 `usage_logs.id=287142`：

- 输入 token：17,230
- 输出 token：124
- 缓存读取 token：256
- 本地基础成本（标准价）：`0.0536268`
- 上游已包含 `3.5x` 的推算价格：`0.0536268 × 3.5 = 0.1876938`
- 本地 `15x` 后：`0.1876938 × 15 = 2.815407`

因此本地 `2.815407` 与“上游实际价格 × 15”一致，不是“上游已返回金额再额外乘一次 3.5”。浏览器里的 `$0.00x` 小记录 token 规模只有约 190 个，不能与本地这条约 17.6K token 的长上下文请求直接比较。

## 业务口径结论

- 如果 `15x` 的基准是上游页面“实际费用”，当前本地扣费公式正确：`基础标准价 × 3.5 × 15`。
- 如果 `15x` 的基准明确是官方基础标准价，才需要去掉 `3.5`，本条应扣 `0.0536268 × 15 = 0.804402`。

本次只读取浏览器页面和本地数据库，没有修改线上计费配置或余额数据。
