# 8 位用户 20 USD 流量卡发放与用户 460 残留清理

## 授权与范围

- 管理员要求为 8 位用户各发放一张 20 USD 流量卡，并确认 `1915474749@qq.com` 应发放给用户 ID `451`。
- 管理员确认同邮箱用户 ID `460` 是软删除未清理干净的残留记录，要求将其删除。
- 输入的 `bymax1107@gmial.com` 在数据库中不存在；按唯一匹配的真实注册邮箱 `bymax1107@gmail.com`（ID `629`）执行。

## 前置核验

- 用户 `460` 的 `deleted_at=2026-06-20 10:22:09 +08`，余额为 0，无 API Key、认证身份、订单、用量、套餐、流量卡、债务、邀请、属性、Passkey、分组或配额等业务关联。
- 发放目标固定为用户 ID `440、451、491、525、552、605、621、629`，均为未删除的 active 普通用户，邮箱逐一匹配。
- 流量卡套餐为 `traffic_packs.id=3`、`gpt_traffic_20usd_5cny`，额度 20 USD、有效期 28 天、平台 `all`。
- 批次幂等标记 `traffic-grant-20260820-eight-users` 在执行前无订单。

## 执行

- 使用 PostgreSQL 单事务硬删除用户 `460`；提交前再次断言其仍为软删除、余额为 0 且无业务关联。
- 为 8 位用户各创建一笔金额为 0 的 `admin_grant` 完成订单、一笔独立流量卡额度、一条 `purchase` 流水和一条 `ADMIN_TRAFFIC_PACK_GRANTED` 支付审计。
- 管理员审计身份为 `admin:448`；未伪造支付服务商交易，也未修改普通余额、历史订单、历史用量、API Key、已有流量卡或模型配置。
- 事务提交前断言订单、额度、流水、审计均恰好为 8 条，任一不符会整体回滚。

## 发放结果

| 用户 ID | 邮箱 | 订单 ID | 流量卡 ID | 审计 ID |
| ---: | --- | ---: | ---: | ---: |
| 552 | `377293029@qq.com` | 681 | 423 | 1977 |
| 621 | `249155187@qq.com` | 682 | 424 | 1978 |
| 525 | `2894823499@qq.com` | 683 | 425 | 1979 |
| 440 | `897858381@qq.com` | 684 | 426 | 1980 |
| 629 | `bymax1107@gmail.com` | 685 | 427 | 1981 |
| 605 | `1729128243@qq.com` | 686 | 428 | 1982 |
| 491 | `3876129758@qq.com` | 687 | 429 | 1983 |
| 451 | `1915474749@qq.com` | 688 | 430 | 1984 |

- 每张卡初始及提交时剩余额度均为 `20.0000000000 USD`，平台为 `all`。
- 生效时间为 `2026-08-20 19:59:34 +08`，到期时间为 `2026-09-17 19:59:34 +08`。
- 本批次合计发放 `160 USD`；提交后用户 `460` 已不存在，`1915474749@qq.com` 只剩用户 ID `451`。
- 各用户提交后的有效流量卡总额依次为：ID 440 `32.7232929600`、451 `28.1688554000`、491 `22.0026391500`、525 `20.0240155250`、552 `20.1072385800`、605 `20`、621 `20`、629 `20` USD。
- 8 位用户流量卡欠费均为 0，因此本次额度未发生欠费抵扣。

## 运行状态

- `sub2api-official-18082`、PostgreSQL、Redis 均保持 healthy，未重建容器或数据卷。
- 本地应用、本地 Nginx、`aaccx.pw`、`www.aaccx.pw`、`api.aaccx.pw` 的健康检查均返回 HTTP 200。
