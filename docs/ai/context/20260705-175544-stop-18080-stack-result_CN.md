# 停止 18080 本地 Sub2API 栈结果

## 操作结论

- 2026-07-05 17:54 JST 前只读确认：`sub2api` 正在运行并占用 `127.0.0.1:18080->8080/tcp`。
- 已仅停止 18080 对应容器：`sub2api`、`sub2api-postgres`、`sub2api-redis`。
- 未删除容器、镜像、volume 或 network。
- 未停止、未重启、未重建 `sub2api-candidate`、`sub2api-candidate-postgres`、`sub2api-candidate-redis`。
- 未修改 nginx、Cloudflare Tunnel 或公网路由。

## 验证结果

- `docker ps --filter publish=18080` 无运行容器。
- `lsof -nP -iTCP:18080 -sTCP:LISTEN` 无监听进程。
- `sub2api` 为 `Exited (0)`。
- `sub2api-postgres` 为 `Exited (0)`。
- `sub2api-redis` 为 `Exited (137)`，属于 `docker stop` 后的停止态。
- `sub2api-candidate` 仍为 `Up 3 days (healthy)`，端口仍为 `127.0.0.1:18084->8080/tcp`。
- `sub2api-candidate-postgres` 与 `sub2api-candidate-redis` 仍为 `Up 3 days (healthy)`。
- `http://127.0.0.1:18084/health` 返回 200。
- `http://127.0.0.1:8080/health` 返回 200。
- `https://api.aaccx.pw/health` 返回 200。

## 当前运行态

- 18080 本地 Sub2API 栈已停止。
- 18084 公网候选 Sub2API 栈继续运行并通过 nginx 与公网 health 验证。
- 后续不要把已停止的 `sub2api/sub2api-postgres/sub2api-redis` 误认为当前公网事实源；当前公网事实源仍是 `sub2api-candidate-postgres`。
