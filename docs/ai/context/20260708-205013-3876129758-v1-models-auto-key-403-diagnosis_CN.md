# 3876129758@qq.com 请求 /v1/models 失败诊断

## 结论

- `3876129758@qq.com` 用当前 active Key 请求 `https://api.aaccx.pw/v1/models` 失败，根因在 Sub2API 的自动 Key endpoint policy。
- 这不是 CLIProxyAPI 上游模型选择/账号池问题，也不是订阅、额度、Key 无效问题。
- 当前自动 Key（`api_keys.group_id=NULL`）只放行部分正式 OpenAI API 路径，漏放行了 `/v1/models`，所以请求在 Sub2API 中间件阶段直接返回 403。

## 复现结果

- 使用用户当前 active Key：`api_keys.id=106/Codex++`，脱敏 `sk-6abb393...0cbd`。
- 请求：`GET https://api.aaccx.pw/v1/models`。
- 返回：HTTP `403`。
- 错误体：
  - `AUTO_KEY_UNSUPPORTED_ENDPOINT: Automatic API Key is not supported for this endpoint.`
  - `type=permission_error`
- 同一 Key 请求 `/v1/responses` + `gpt-5.5` 已验证 HTTP 200，说明 Key、订阅、计费链路可用。

## 运行日志证据

- `sub2api-candidate` 日志：
  - 时间：`2026-07-08 19:48:52+08`
  - path：`/v1/models`
  - method：`GET`
  - status_code：`403`
- 请求后 `api_keys.id=106` 的 `last_used_at` 更新为 `2026-07-08 19:48:52.831788+08`，说明认证识别到了该 Key，只是后续 endpoint policy 拒绝。

## 代码位置

- 中间件：`backend/internal/server/middleware/effective_group.go`
- 关键函数：
  - `DefaultAutomaticKeyEndpointPolicy`
  - `automaticKeySupportsOpenAIEndpoint`
- 当前已放行：
  - `/v1/usage`
  - `/v1/responses`
  - `/v1/responses/*`
  - `/v1/messages`
  - `/v1/messages/*`
  - `/v1/chat/completions`
  - `/v1/embeddings`
  - `/v1/images/generations`
  - `/v1/images/edits`
- 当前未放行：
  - `/v1/models`

## 判断

- `/v1/models` 是 OpenAI 兼容客户端常见的轻量探测接口，应允许自动 Key 使用。
- 自动 Key 需要先解析到有效 OpenAI group，再进入 `GatewayHandler.Models` 返回对应分组模型列表。
- 该接口不产生模型用量，不会带来额外扣费风险。

## 建议修复

1. 在 `automaticKeySupportsOpenAIEndpoint()` 中新增 `/v1/models` 放行。
2. 在 `TestDefaultAutomaticKeyEndpointPolicyAllowsOnlyFormalV1OpenAIEndpoints` 的 allow 列表中加入 `/v1/models`。
3. 运行目标测试：
   - `go test -count=1 -tags=unit ./internal/server/middleware`
   - 视影响追加 `go test -count=1 -tags=unit ./internal/server`。
4. 修复验证后再决定是否构建并替换公网 18084 应用容器。

## 本轮影响

- 本轮只读复现请求、查日志、读代码。
- 未修改代码。
- 未手工修改数据库。
- 未构建镜像、未替换容器、未重启服务、未改 nginx/Redis。
