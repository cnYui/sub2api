# 最近一小时扣费与 Claude Kiro 使用审计

## 范围

- 审计时间：2026-08-06 17:00:14 +08。
- 数据源：生产 PostgreSQL `usage_logs`、当前 `groups` 配置、运行中应用容器环境与本地 `/api/v1/model-plaza`。
- 操作：只读查询；未修改用量、余额、分组、账号或最终倍率。

## 最近一小时扣费结果

- 审计窗口内有 673 条用量记录，均有 `total_cost` 与 `actual_cost`；无负费用，且不存在基础成本大于零但实际费用为零的记录。
- 运行中容器的 `BILLING_FINAL_MULTIPLIER=15`。
- 673 条记录全部满足 `actual_cost = total_cost × rate_multiplier × 15`，差异阈值为 `0.000000001 USD`，不匹配数为 0。
- 模型广场的“实付价”前端口径是“基础单价 × 生效分组/用户专属倍率”，故意不展示也不叠加服务端最终 `15x`；服务端才在结算阶段追加该倍率。当前窗口均为 token 计费，未见图片、视频、长上下文阶梯或模型映射路径，服务等级字段不改变上述最终倍率公式。

## 与模型广场的对应关系

- 658 条记录的 `rate_multiplier` 与当前模型广场分组倍率一致：Codex 日常 `0.15x`、Codex 生产 `0.35x`、DeepSeek `4.9x`，以及 Claude Kiro 调价后的 `0.45x`。
- 另有 15 条 Claude Kiro 的 `claude-opus-5` 记录保留了调价前的 `0.35x` 快照，时间为 16:14:25 至 16:20:55 +08；当前模型广场显示的 Kiro 倍率为 `0.45x`，后续 3 条（16:29:15 至 16:33:19 +08）已按 `0.45x` 结算。
- 因此，这 15 条若拿当前页面的 `0.45x` 直接比较会显得较低，但它们按请求时公开价格和最终 `15x` 均正确扣费，不能按当前配置重算为异常或补扣。

## Claude Kiro 上游使用者

- Claude Kiro 分组所有 128 条历史用量均路由到账号 `3`（`Claude Kiro反代官方0.45倍价格`），不存在第二个承接账号。
- 持续使用者为用户 `1032726009@qq.com`（ID 505）：历史共 38 次，全部发生在最近 24 小时，其中最近一小时 18 次；最近一次为 2026-08-06 16:33:19 +08。
- 其余使用者均为零星或已停止：`xiaobianfuai@gmail.com` 81 次（最近一小时 0 次）、`465892377@qq.com` 5 次、`2761180023@qq.com` 2 次、`xunskyler@gmail.com` 与 `yannisy0225@163.com` 各 1 次。
