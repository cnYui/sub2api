# 用户 1850226892@qq.com 的余额套餐人工取消

## 背景

- 生产实例：`sub2api-official-18082`。
- 用户 ID：`480`。
- 订单 `537`（余额套餐 ¥49）的退款报价为 ¥36.07，但退款请求未到达支付网关：容器配置的代理 `172.29.0.254:7897` 拒绝连接，订单状态为 `REFUND_FAILED`。
- 管理员要求取消该用户的订阅，并将普通余额恢复为零。

## 决策

- 不删除支付订单、退款失败记录或历史套餐，避免破坏支付和退款审计。
- 订单 `537` 保持 `REFUND_FAILED`，不得标记为已退款；后续在支付链路恢复后仍可按原订单重试网关退款。
- 仅取消该订单对应的当前余额套餐。流量卡不是订阅，本次不改动：用户仍有两笔可用 OpenAI 流量额度，合计 `2.3038992375 USD`。

## 执行

在 PostgreSQL 事务中锁定并校验用户 `480`、套餐 `13` 与订单 `537` 的预期状态后：

- 将 `users.balance` 从 `-1.86031492` 更新为 `0`。
- 将 `user_balance_packages.id=13` 从 `active` 更新为 `cancelled`，同时将 `remaining_usd` 置零并清空 `next_credit_at`，因此不会再有后续周期到账。
- 向 `payment_audit_logs` 写入 `BALANCE_PACKAGE_MANUAL_CANCELLATION`，记录操作前后余额、套餐状态及退款尚未在网关执行的事实。
- 显式失效 Redis 键 `billing:balance:480`；执行前后该键均不存在，后续鉴权会从数据库加载余额。

## 核验

- 用户普通余额为 `0.00000000`。
- 当前有效余额套餐数为 `0`；当前有效模型订阅数为 `0`。
- 订单 `537` 仍为 `REFUND_FAILED`，保留原始失败原因和退款金额 ¥36.07。
- 套餐取消审计与退款失败审计均已存在。
