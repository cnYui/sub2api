# GPT 0.35 倍稳定渠道真实计费核验

## 上游页面记录

截图对应上游 `gpt-5.6-sol` 请求：

- 输入 token：`853`
- 输出 token：`5`
- 缓存读取 token：`3584`
- 输入单价：`$5 / 1M Token`
- 输出单价：`$30 / 1M Token`
- 缓存读取单价：`$0.50 / 1M Token`
- 上游有效倍率：`0.35x`

复算结果：

```text
输入费用 = 853 × 5 / 1,000,000 = 0.004265
输出费用 = 5 × 30 / 1,000,000 = 0.000150
缓存读取费用 = 3584 × 0.50 / 1,000,000 = 0.001792
标准成本 = 0.006207
上游用户扣费 = 0.006207 × 0.35 = 0.00217245
```

上游页面将该金额显示为 `$0.002172`，与复算一致。

## 本地真实结算

18082 生产数据库中对应记录为 `usage_logs.id=288990`：

| 字段 | 值 |
| --- | ---: |
| 模型 | `gpt-5.6-sol` |
| 输入 / 输出 / 缓存读取 | `853 / 5 / 3584` |
| `total_cost` | `0.0062070000` |
| `rate_multiplier` | `0.3500` |
| `actual_cost` | `0.0325867500` |

本地结算公式为：

```text
0.006207 × 0.35 × 15 = 0.03258675
```

分组 `GPT模型官方0.35倍稳定`（ID `10`）当前为公开、活跃的 OpenAI 标准分组，倍率为 `0.3500`。绑定账号处于可调度状态；账号倍率 `3.5` 仅用于账号统计，不参与用户余额扣费。应用容器健康状态为 `healthy`，最终倍率为 `15`。

结论：新渠道计费正确。上游费用先按 `0.35x` 得出，再由本地统一乘 `15x`；没有遗漏或重复叠加 `0.35x`、`3.5x`。
