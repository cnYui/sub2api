# 用户 2197057077@qq.com 149 元余额套餐核算与取消记录

## 取消前核算

- 用户 ID：`565`；订单：`559`；套餐：`user_balance_packages.id=47`。
- 套餐为 ¥149 余额套餐，周期 28 天，按 7 天刷新 4 次；每周额度 `$389`，周期总额度 `$1,556`。
- 取消前仅第 1 个窗口已到账，当前窗口剩余额度为 `$0`，下一次到账原定为 `2026-08-11 00:14:25 +08`。
- 服务端退款公式为 `¥149 × (1 - Max(时间比例, 已用额度比例))`。在 `2026-08-05 22:19 +08` 快照中，周期内 `actual_cost` 为 `$463.19669745`，时间比例约 `6.8573%`、用量比例约 `29.7684%`，按现有代码报价为 `¥104.64504632`。
- 其中 15 笔用量合计 `$73.59265125` 已于当日 `19:37 +08` 通过 `BALANCE_MANUAL_REFUND` 全额退回普通余额；现有报价代码仍会计入这些用量。若后续退款口径需要排除已退用量，净用量为 `$389.60404620`，对应比例约 `25.0388%`，参考金额为 `¥111.69215753`。本次未执行退款。

## 执行结果

- 按管理员要求取消订阅权益，不发起退款。
- 在同一数据库事务中将余额套餐 `47` 标记为 `cancelled`，`remaining_usd` 归零，并清除 `next_credit_at`。
- 订单 `559` 保持 `COMPLETED`，`refund_amount` 保持 `¥0`，以保留原支付与履约审计。
- 普通余额保持 `-$0.60404620`，流量卡额度、历史用量均未改动；该用户不存在 `user_subscriptions` 记录。
- 新增审计：`payment_audit_logs.id=1344`，`action=BALANCE_PACKAGE_MANUAL_CANCELLED`，记录了无退款、普通余额与流量卡不变的原因。

## 取消后核验

- 当前有效余额套餐：`0`。
- 当前有效流量卡额度：`$0`。
- 当前模型订阅：`0`。
