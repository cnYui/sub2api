# xinlise@gmail.com 无法使用诊断

## 结论

用户无法通过其旧 Codex Key 调用的根因是分组 9 `Codex（日常）` 的唯一上游账号不可调度，不是用户账号、余额、套餐或 API Key 状态问题。

## 只读核验

- 用户 ID `504`，状态为 `active`，普通余额为 `258 USD`，未冻结，已于 2026-08-08 登录并保持活跃。
- 用户现有 3 个未删除 API Key 均为 `active`：Key `99`（`codex`）与 Key `143`（`古月`）绑定分组 9；Key `102`（`佳一老师`）绑定分组 13 `GPT模型官方0.15倍新`。三者均未过期，且未设置 IP 白名单或黑名单。
- 分组 9 唯一绑定账号 `1128` 当前为 `error`、`schedulable=false`；错误为上游 `401 API key is disabled`。该账号同时保留了上游 `403` 冷却记录，因此此分组无法选出可用账号。
- 分组 9 与分组 13 均配置了 `gpt-5.5` 和 `gpt-5.6`，模型目录不是本次阻塞原因。
- 分组 13 的账号 `1132` 当前为 `active`、`schedulable=true`，没有错误或限流状态。该用户没有任何 `ops_ingress_reject_aggregates` 或 `ops_error_logs` 记录，最后一笔成功用量停留在 2026-07-23；这说明本服务未收到该用户近期失败请求，或其近期调用使用的是不记录为入站拒绝的失效分组链路。

## 建议

- 若要立即恢复该用户旧 Key 的调用，应将 Key `99` 和 Key `143` 迁移到当前可调度的分组 13，并失效相关认证缓存后进行最小请求验证。
- 若分组 9 需要继续保留，则应先替换或恢复账号 `1128` 的上游凭证，再将账号恢复为可调度。仅将分组显示状态保留为 `active` 不能使请求成功。

本次仅完成只读诊断，未修改任何生产数据、用户 Key 或账号凭证。
