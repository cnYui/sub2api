# 用户 kangzhao6618@gmail.com 提前刷新周额度记录

## 授权与范围

按管理员要求，提前刷新用户 `kangzhao6618@gmail.com`（`users.id=484`）当前余额套餐的下一周额度。

本次仅处理当前余额套餐 `user_balance_packages.id=175` 及关联订单 `payment_orders.id=724`。未修改历史套餐、订单金额、订单状态、退款金额、套餐有效期、历史用量、API Key 配额或流量卡。

## 执行前状态

生产数据库执行前核查：

- 用户普通余额：`2.80356880 USD`。
- 当前套餐：`id=175`，状态 `active`，已到账 `1/4` 期。
- 每期额度：`128 USD`；当前套餐窗口剩余：`2.80356880 USD`。
- 原定下一次刷新：`2026-09-02 09:29:47.032283 +08:00`。
- 套餐有效期：至 `2026-09-23 09:29:47.032283 +08:00`。
- 订单 `724`：`COMPLETED`，退款金额 `0`，实付金额 `49.49 CNY`。
- 订单 `724` 尚无 `BALANCE_PACKAGE_EARLY_WEEKLY_CREDIT_2` 审计，满足幂等前置检查。

## 执行方式

执行时间为 `2026-08-29 20:21:55 +08:00`。使用 PostgreSQL `SERIALIZABLE` 事务，事务内按邮箱重新锁定用户，锁定当前有效套餐和关联订单，并校验套餐状态、到账进度、刷新间隔、有效期、订单状态、退款金额及订单套餐快照仍符合预期。

按余额套餐正常周刷新语义计算：

- 本期到账：`128 USD`。
- 执行前普通余额为正，无需抵扣欠费。
- 先移除旧套餐窗口剩余 `2.80356880 USD`，再写入新一周额度；普通余额净增加 `125.19643120 USD`。
- 套餐从 `1/4` 变为 `2/4`，保持 `active`。
- 下一次刷新按原计划时间加 `7` 天，从 `2026-09-02 09:29:47.032283 +08:00` 顺延至 `2026-09-09 09:29:47.032283 +08:00`。

## 执行结果

- 用户普通余额：`2.80356880 USD` -> `128.00000000 USD`。
- `users.total_recharged`：`358.32000000 USD` -> `486.32000000 USD`。
- 套餐 `175`：到账 `1/4` -> `2/4`，剩余 `2.80356880 USD` -> `128.00000000 USD`，状态保持 `active`。
- `next_credit_at`：`2026-09-02 09:29:47.032283 +08:00` -> `2026-09-09 09:29:47.032283 +08:00`。
- 套餐有效期保持 `2026-09-23 09:29:47.032283 +08:00` 不变。
- 订单 `724` 仍为 `COMPLETED`，退款金额保持 `0`。
- 新增支付审计 `payment_audit_logs.id=2268`：`BALANCE_PACKAGE_EARLY_WEEKLY_CREDIT_2`，操作者 `admin:authorized_manual_settlement`。
- 本次普通余额非负，欠费抵扣为 `0`，未新增 `balance_debt_ledger` 还款流水。

## 缓存与核验

- 已为该用户 `2` 把未删除 API Key 写入认证缓存失效 outbox；worker 已处理完成，回读待处理数为 `0`、最大重试次数为 `0`。
- Redis 余额缓存 `billing:balance:484` 当前不存在。
- 两把 API Key 的 L2 认证缓存已核验：一把不存在；另一把在失效后被重新生成，认证快照余额为 `128 USD`，不是旧余额。
- 用户余额、套餐进度、套餐剩余、下次刷新时间、订单状态和支付审计均已从生产数据库回读确认。
- 应用容器、PostgreSQL、Redis 均为 `healthy`；`http://127.0.0.1:18082/health` 返回 HTTP `200`。
