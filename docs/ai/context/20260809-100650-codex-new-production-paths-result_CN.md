# Codex 新生产路径配置结果

## 完成情况

- 已将现有账号 `#1132` 更名为“Codex新（生产路径1）”。
- 已新增账号 `#1164`，名称为“Codex新（生产路径2）”。
- 两个账号均为 `openai/apikey` 渠道，使用相同的已配置上游端点。
- 两个账号均加入分组 `#13 Codex新（生产）`，没有新建分组，因此不改变既有用户路由和计费口径。
- 路径 2 使用独立 API Key，凭证已由服务端加密保存；本文档不记录凭证内容。

## 配置核验

| 账号 | 名称 | 状态 | 可调度 | 并发 | 优先级 | 计费倍率 | 模型映射 | Responses 能力 |
| --- | --- | --- | --- | ---: | ---: | ---: | ---: | --- |
| `#1132` | Codex新（生产路径1） | active | 是 | 50 | 1 | 1.0 | 20 个 | 支持 |
| `#1164` | Codex新（生产路径2） | active | 是 | 50 | 1 | 1.0 | 20 个 | 支持 |

## 验证说明

- 已通过生产 PostgreSQL 只读核验账号名称、分组、启用与调度状态、凭证已配置标记、模型映射数量和 OpenAI Responses 能力探测结果。
- 本次未执行“测试连接”，避免在没有明确必要性时向上游发送实际请求并消耗额度。
