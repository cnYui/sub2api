# 超过 3 美元异常候选过滤表

## 过滤口径

基于 `2026-08-05 19:22:21.342603 +08` 快照，从 367 条单笔超过 `$3` 的记录中去除明确正常或已处理的项目：

- 去除 237 条订阅套餐用量，合计 `$922.738048575`。
- 去除 20 条 Shadow 仅观测记录，合计 `$64.665624`，它们没有实际扣费。
- 去除 14 条能完整匹配 `traffic_credit_ledger` 的流量卡扣费，合计 `$56.351321975`。
- 去除 4 条 K3 历史 3.5 倍率错误记录，合计 `$19.1601585`；这些已包含在 `$17.665965` K3 退款中。

## 保留结果

剩余 92 条、13 个用户，实际扣费合计 `$425.751717325`：

- 85 条普通余额大额请求，合计 `$389.018784525`，保留人工复核。
- 7 条流量卡记录无法完整匹配原扣费流水，合计 `$36.7329328`，保留来源核对。

逐条表：`20260805-192221-over-3-usd-review-detail.csv`。

用户汇总表：`20260805-192221-over-3-usd-review-summary.csv`。

本次仅调整审计展示范围，没有执行新的退款。
