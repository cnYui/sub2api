# /monitor 渠道模型补全执行记录

## 执行范围

按已确认的模型清单更新生产 `18082` 实例的 `channel_monitors` 配置。未修改前端、后端代码、调度器或图像监控协议。

## 最终配置

| 监控 ID | 主模型 | 附加模型 | 总数 |
| ---: | --- | --- | ---: |
| 8 | `gpt-5.6-sol` | `gpt-5.5`、`codex-auto-review`、`gpt-5.6-luna`、`gpt-5.6-terra`、`gpt-image-1.5`、`gpt-image-2` | 7 |
| 9 | `gpt-5.5` | `codex-auto-review`、`gpt-5.6-luna`、`gpt-5.6-sol`、`gpt-5.6-terra` | 5 |
| 10 | `gpt-image-2` | 无 | 1 |

已有的 1 至 7 号监控未改动；最终 10 条启用监控均有完整配置，按模型名不区分大小写检查无重复。

## 验证结果

- 数据库事务更新 8、9 号成功，回读总模型数为 `7/5/1`。
- `http://127.0.0.1:18082/health` 返回 HTTP `200`，内容为 `{"status":"ok"}`。
- 应用、PostgreSQL、Redis 容器均保持 `healthy`，应用未因本次更新重启。
- 最近调度历史显示：8 号 `7` 个不同模型、9 号 `5` 个不同模型、10 号 `1` 个不同模型均已产生检测记录；10 号未新增文本模型。
- 未认证访问 `/api/v1/channel-monitors/8/status` 返回 `401 UNAUTHORIZED`，认证边界正常。浏览器当前没有登录会话，因此未执行弹窗点击级验证；详情接口会直接读取上述数据库配置。

## 审计说明

第一次事务回读时命令草稿出现重复 `gpt-5.6-terra`，随后立即在第二个事务中覆盖为最终的 `gpt-5.6-luna` 与 `gpt-5.6-terra` 列表，并通过数据库去重查询确认最终状态正确。执行过程中未读取、输出或修改任何 API Key。
