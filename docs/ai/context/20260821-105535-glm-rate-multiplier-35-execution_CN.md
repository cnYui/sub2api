# GLM 分组倍率调整为 3.5x

## 变更内容

- 执行时间：2026-08-21 10:55（Asia/Tokyo）。
- 目标生产分组：`groups.id=6`，当前名称为 `GLM0.5倍率`。
- 将 `rate_multiplier` 从 `0.5000` 更新为 `3.5000`。
- GLM 官方校准基础价保持不变，模型广场实付价按 `3.5x` 计算：
  - `glm-5.1` 短上下文：`$3.00 / $12.00 / $0.65`；长上下文：`$4.00 / $14.00 / $1.00`。
  - `glm-5.2`：`$4.00 / $14.00 / $1.00`。
- 分组名称暂未改动，因此名称中的 `0.5` 与实际倍率 `3.5x` 不一致；本次只按用户明确要求修改数值倍率。
- 未修改历史用量、订单、余额、用户专属倍率、账号统计倍率或最终计费倍率。

## 验证结果

- PostgreSQL 事务更新成功，既有 `trg_groups_auth_cache_invalidation` 触发器正常触发。
- `user_group_rate_multipliers` 中 `group_id=6` 的用户专属覆盖数为 `0`。
- 后端 GLM 定向测试通过。
- 前端模型广场测试通过：`PlazaModelPricingTable.spec.ts`，14 项全部通过。
- 本地与公网模型广场接口均返回倍率 `3.5` 及上述实付价格。
