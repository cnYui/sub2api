# AGENTS 记忆压缩计划

时间：2026-07-28 15:12:55（Asia/Tokyo）

## 目标

- 将根目录 `AGENTS.md` 的长期流水压缩为可执行的短入口。
- 在 `docs/ai/context/` 新建一份完整压缩记忆，不覆盖任何历史文档。
- 保留当前架构、运行态、业务红线、未完成风险和可追溯索引。

## 压缩原则

- 按最新时间和只读运行态核验结果解决冲突，不并列保留已过期“当前状态”。
- 账号导入流水按批次结果聚合，只保留最新总量、操作规范和异常类型；单批明细继续引用原结果文档。
- 计费、订阅、支付、部署等内容保留不变量和待办，删除重复过程描述。
- 2026-07-17 以前的长期背景引用上一版压缩记忆，不重复复制全部旧内容。
- 不写入完整 API Key、OAuth token、支付密钥、SMTP 密码或其他秘密。

## 已核对事实

- `sub2api-public-nginx-local:8080` 当前转发到宿主机 `18080`。
- `http://127.0.0.1:18080/health`、`18086/health`、`8080/health` 均返回 200。
- 外层 `sub2api-dev:18080` 与内层 `sub2api-upstream-latest:18086` 均为 healthy。
- 当前 Git `origin` 为 `https://github.com/cnYui/sub2api.git`。

## 变更

1. 新建 `20260728-151255-agents-memory-condensed_CN.md`。
2. 重写根 `AGENTS.md`，只保留协作规则、当前运行态、最高优先级定论、红线和维护入口。
3. 检查文件大小、引用路径、Git diff 和未跟踪上下文文档，确认未误改其他文件。
