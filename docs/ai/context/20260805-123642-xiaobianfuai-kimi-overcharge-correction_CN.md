# xiaobianfuai Kimi 近期多扣核验与修正

## 用户与请求范围

- 用户：`xiaobianfuai@gmail.com`，生产库 `users.id=448`。
- 模型：`kimi-k3`，分组：`groups.id=7`。
- 最近期请求时间：`2026-08-05 11:25:23` 至 `11:27:16`（数据库时区 `+08`）。
- 用户确认口径：`本地最终扣费 = 标准成本 × 15`，Kimi 不再额外乘 `3.5x`。

## 截图对应请求

截图明细对应 `usage_logs.id=291899`：

| 项目 | 值 |
| --- | ---: |
| 输入 / 输出 / 缓存读取 token | `17 / 130 / 21504` |
| 输入费用 | `$0.000051` |
| 输出费用 | `$0.001950` |
| 缓存读取费用 | `$0.0064512` |
| 标准成本 `total_cost` | `$0.0084522` |
| 错误倍率快照 | `3.5x` |
| 实际扣费 `actual_cost` | `$0.4437405` |
| 正确扣费 `0.0084522 × 15` | `$0.1267830` |
| 本笔多扣 | `$0.3169575` |

截图中的“原始 `$0.008452`”与数据库精确值 `$0.0084522` 一致；按数据库精确值计算，确实是多乘了 `3.5x`。

## 该用户最近 5 笔错误记录

历史 `usage_logs` 保留原始事实，正确扣费按 `total_cost × 15` 重算：

| 用量日志 | 标准成本 | 错误实际扣费 | 正确扣费 | 多扣 |
| ---: | ---: | ---: | ---: | ---: |
| `291899` | `$0.0084522` | `$0.4437405` | `$0.1267830` | `$0.3169575` |
| `291898` | `$0.0582378` | `$3.0574845` | `$0.8735670` | `$2.1839175` |
| `291891` | `$0.0106524` | `$0.5592510` | `$0.1597860` | `$0.3994650` |
| `291886` | `$0.0576348` | `$3.0258270` | `$0.8645220` | `$2.1613050` |
| `291883` | `$0.0326070` | `$1.7118675` | `$0.4891050` | `$1.2227625` |
| **合计** | **`$0.1675842`** | **`$8.7981705`** | **`$2.5137630`** | **`$6.2844075`** |

该用户更早的 `usage_logs.id=291466` 已记录为 `rate_multiplier=1.0`，实际扣费 `$0.033714` 与 `total_cost × 15` 一致，没有多扣。

## 已执行修正

- 生产 PostgreSQL 已将 `groups.id=7` 的 `rate_multiplier` 从 `3.5` 改为 `1.0`。
- `BILLING_FINAL_MULTIPLIER=15` 保持不变，后续 Kimi 请求公式为 `标准成本 × 15`。
- 分组更新触发的 API Key 认证缓存失效已完成；`auth_cache_invalidation_outbox` 当前为 `0`。
- 未改写历史 `usage_logs`，未直接修改用户余额；历史 `$6.2844075` 多扣需要单独授权后通过可审计余额流水补偿。
