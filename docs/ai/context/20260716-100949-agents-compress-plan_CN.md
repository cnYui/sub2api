# AGENTS.md 压缩计划

> 时间：2026-07-16 10:09 JST
> 来源：项目根目录 `AGENTS.md`

## 目标

- 将当前约 159 KB 的 `AGENTS.md` 压缩为可快速读取的长期上下文入口。
- 保留会影响后续设计、实现、计费、支付、数据库和公网运维的有效定论。
- 将逐日操作流水折叠为“当前状态、风险约束、追溯索引”。
- 只在 `docs/ai/context/` 新建文档，不修改、覆盖、重命名或删除根目录 `AGENTS.md` 与历史上下文。

## 保留范围

- 中文协作偏好、代码风格、design/plan/result 落盘规则和敏感信息约束。
- Sub2API、CLIProxyAPI、Nginx、Cloudflare Tunnel、yui.web 的职责边界和真实公网链路。
- 当前运行容器、端口、数据库迁移状态、已停止测试栈和只替换应用容器的发布原则。
- 支付、余额、返利、订阅续费、退款、组合支付的现行口径及尚未上线的设计依赖。
- API Key 自动分组、正式 `/v1/*` 路由、并发层级、计费事实缺失、流量卡预授权和慢首包问题。
- 数据库备份、迁移 checksum、Docker Compose project、Redis 持久化、凭据和关键账号保护规则。
- 300 API 日活容量评估与扩容前必须先处理的架构问题。
- Git 远端和上下文未跟踪文件检查规则。

## 折叠范围

- 单用户逐次排障、重复 HTTP 200 健康检查、历史镜像 digest 和重复端口说明。
- 已完成且可由结果文档追溯的测试命令、逐条数据库行和缓存清理过程。
- 已被后续修复覆盖的旧设计、旧运行栈状态和中间诊断。

## 执行步骤

1. 完整读取 `AGENTS.md`、现有压缩记忆和校验脚本。
2. 只读核对当前容器、分支、远端和数据库迁移状态，解决历史记录冲突。
3. 新建 `20260716-100949-agents-memory-condensed_CN.md`。
4. 运行压缩上下文校验脚本、`git diff --check` 和未跟踪上下文检查。

## 验收标准

- 压缩文档不包含完整 API Key、内部 token、SMTP 密码、支付密钥或敏感 dump 内容。
- 当前状态与历史索引明确分离，不把未部署设计写成已上线事实。
- 关键高风险操作都有前置约束和可追溯文档。
- 校验脚本和 Git whitespace 检查通过。
