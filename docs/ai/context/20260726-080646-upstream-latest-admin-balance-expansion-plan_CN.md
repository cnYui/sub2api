# 内层 latest 管理员余额扩容计划

时间：2026-07-26 08:06（Asia/Tokyo）

## 目标与范围

- 将本机内层 latest Sub2API（`http://127.0.0.1:18086`）管理员 `xiaobianfuai@gmail.com` 的站内可用余额设为 `10000000.00`。
- 外层 `sub2api-dev:18080` 继续负责终端用户计费；本次不修改其用户余额、计费倍率、模型价格、上游账号池或公网链路。

## 决策

- 用户提出“添加更大，比如 10000000”，按目标余额理解，使用管理接口的 `set` 操作将余额精确设为 `10000000.00`，而非在现有余额基础上再累加一千万。
- 必须通过内层正式管理接口写入，以保留余额调整记录并刷新认证/计费缓存；不直接修改数据库。

## 执行与验证

1. 对 `sub2api-upstream-postgres` 创建只读可恢复备份，并用 `pg_restore -l` 验证文件可读取。
2. 登录内层管理接口，只读核对目标邮箱、用户 ID、角色、状态和变更前余额。
3. 调用 `POST /api/v1/admin/users/:id/balance`，请求使用 `operation=set`、金额 `10000000.00`、稳定幂等键与不含敏感信息的审计备注。
4. 回读用户和余额变更历史，确认余额、调整差额与审计备注；同时检查 `18086/health`。

## 回滚边界

- 余额是有审计意义的运行态数据；如需回滚，应通过同一正式管理接口设回本次读取的旧余额，而不是恢复整库。是否回滚需另行授权。
