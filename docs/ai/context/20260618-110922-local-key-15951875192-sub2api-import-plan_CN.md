# 本地 Key 绑定 15951875192 并分配 59 元订阅执行计划

## 背景

手机号 `15951875192` 是本机自用数据，不属于 yui.web `orders` 已发放 Key 范围。该手机号在 yui.web 中有历史用量记录，但没有订单记录；对应本地 Key 存在于 CLIProxyAPI / 本机 Codex 配置中。

已验证：

- Sub2API 用户：`15951875192@phone.com` 存在。
- 当前 Sub2API 中该用户没有 API Key。
- 当前 Sub2API 中该用户没有订阅。
- 本地 Key 预览：`sk-LOCAL-454...e28804`。
- 该 Key 直连 CLIProxyAPI `127.0.0.1:8317/v1/models` 可用。
- 该 Key 直连 Sub2API `127.0.0.1:18080/v1/models` 当前返回 401。

## 目标

- 将本地 Key 写入 Sub2API `api_keys` 表。
- 绑定用户 `15951875192@phone.com`。
- 绑定 59 元档位订阅 group：`codex-pool-49-usd`。
- 给用户分配 59 元订阅权益：
  - `status='active'`
  - `starts_at=now()`
  - `expires_at=now()+30 days`
  - `daily_usage_usd=0`
  - `weekly_usage_usd=0`
  - `monthly_usage_usd=0`

## 后续变更

用户补充说明该 Key 为管理员自用，不应按普通 59 元套餐限制每日 49 USD。最终执行已改为创建专用 `codex-pool-local-unlimited` 分组并设置无上限，不修改公共 `codex-pool-49-usd` 档位。最终结果见 `docs/ai/context/20260618-111935-local-key-15951875192-unlimited-result_CN.md`。

## 口径

- 本次不迁移 yui.web 历史用量到新订阅额度。
- 原因：这是管理员手工给自用本地 Key 开通 Sub2API 权益，不是用户订单迁移；如果把历史 `98 USD` 用量计入 59 元档位，会立即超过每日 `49 USD` 额度，无法达到“先接入可用”的目标。
- 不记录完整 API Key、内部 token、HMAC secret 或密码。

## 执行步骤

1. 备份 Sub2API PostgreSQL。
2. 在事务中：
   - 查找用户 `15951875192@phone.com`。
   - 查找 group `codex-pool-49-usd`。
   - upsert `user_subscriptions`。
   - insert/upsert `api_keys`。
3. 清理 Redis：
   - `apikey:auth:<key>`
   - `billing:sub:<user_id>:<group_id>`
   - `apikey:rate:<api_key_id>`
4. 验证：
   - 数据库中该用户有 1 个 Key。
   - 数据库中该用户有 active `codex-pool-49-usd` 订阅。
   - 本地 `http://127.0.0.1:18080/v1/models` 返回 200。
   - 公网 `https://api.aaccx.pw/v1/models` 返回 200。
