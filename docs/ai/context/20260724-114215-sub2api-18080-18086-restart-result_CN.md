# `18080` 与 `18086` 本地重启结果

时间：2026-07-24 11:42:15

## 执行内容

- 重启 `sub2api-upstream-latest`（`18086`）
- 重启 `sub2api-dev`（`18080`）

## 结果

两层容器均已恢复健康：

- `http://127.0.0.1:18080/health` 返回 `200`
- `http://127.0.0.1:18086/health` 返回 `200`

## 当前状态

- `sub2api-dev`：`Up ... (healthy)`
- `sub2api-upstream-latest`：`Up ... (healthy)`

## 说明

- 重启按先内层、后外层的顺序执行，避免两层同时不可用。
- 当前没有修改配置，只做了运行态重启。
