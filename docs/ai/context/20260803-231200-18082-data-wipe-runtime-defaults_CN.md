# 18082 数据清空后的运行时默认项说明

时间：2026-08-03 23:12:00（Asia/Tokyo）

## 最终状态

- 用户：`0`
- 管理员：`0`
- 余额套餐：`0`
- 分组：`0`
- 支付渠道：`0`
- 支付订单：`0`
- 用量记录：`0`
- PostgreSQL 迁移元数据保留。

## 运行时默认项

应用启动后的运维组件会自动写入少量运行时设置，并可能在 Redis 中产生健康检查或内部缓存键。最终核验时设置表有 `2` 条运维默认设置，Redis 有 `1` 个运行时键；这些不是用户、管理员、套餐、订单或用量数据。

应用状态为 `running/healthy`，健康检查返回 HTTP `200`。
