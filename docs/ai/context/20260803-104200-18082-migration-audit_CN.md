# 18082 启动与来源数据审计

## 启动结果

- 应用地址：`http://127.0.0.1:18082`
- `/health` 返回 HTTP 200，响应状态为 `ok`。
- 管理员记录已初始化为 `xiaobianfuai@gmail.com`，登录接口返回 HTTP 200。
- `18086`、`18087` 未被停止、重启或替换。
- 当前三个端口均绑定在 `127.0.0.1`，不是直接公网监听。

## 来源数据事实

- `18086` 的 `accounts` 表包含 1084 个 OpenAI OAuth 账号，其中 635 个 active/schedulable，449 个 error/不可调度。
- `18087` 的 `accounts` 表包含 1004 个 OpenAI OAuth 账号，其中 572 个 active/schedulable，432 个 error/不可调度。
- `18087` 的 `third_party_routes` 表包含 8 条路由：`gpt-codex`、`claude-max`、`claude-kiro`、`grok`、`qwen`、`glm`、`kimi`、`deepseek`。
- 其中 7 条第三方路由各自有模型映射，总计 37 条；`gpt-codex` 使用 OpenAI OAuth 路由类型，没有第三方 API Key 记录。
- `18087` 的 `third_party_upstream_credentials` 表包含 7 条记录，字段 `api_key_ciphertext` 为加密认证秘密。
- `channels`、`channel_groups` 和 `auth_identity_channels` 在这两个来源库中均没有可迁移的常规渠道记录。

## 未执行事项

- 未读取、解密、导出或复制 `accounts.credentials`、`api_key_ciphertext` 或其他认证秘密。
- 因此 18086 的 GPT OAuth 账号凭证和 18087 的 7 个第三方 API Key 尚未迁移到 18082；目标实例需要由管理员通过管理界面或密钥管理系统重新录入。
