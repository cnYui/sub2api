# 本地 Key 绑定 15951875192 并设置无上限结果

## 结果

- 已将本机自用 Key 导入 Sub2API。
- 用户：`15951875192@phone.com`。
- Key 预览：`sk-LOCAL-454...e28804`。
- API Key ID：`32`。
- 当前绑定 group：`codex-pool-local-unlimited`。
- group ID：`5`。
- 当前 active subscription ID：`45`。
- 旧的 `codex-pool-49-usd` 订阅 ID `44` 已软删除并标记 `expired`。

## 为什么不直接改 59 元公共档位

用户明确说明该 Key 是自用，不要每天 49 USD 限额，要无上限。

Sub2API 的订阅热路径按 `groups.daily_limit_usd` 判断日限额。如果直接把公共 `codex-pool-49-usd` 改为 `NULL`，未来所有 59 元档位用户都会无上限，不符合套餐隔离。

因此本次创建专用分组：

```text
codex-pool-local-unlimited
```

该分组复制 59 元档位的基础能力，但不设置用量上限：

```text
daily_limit_usd = NULL
weekly_limit_usd = NULL
monthly_limit_usd = NULL
```

同时复制 `codex-pool` 的上游账号绑定：

```text
account_id = 1
group_id = 5
priority = 1
```

## 备份

- Sub2API PostgreSQL 备份：
  - `/Users/wujianxiang/CodeSpace/sub2api/.tmp-sub2api-before-local-key-15951875192-20260618-110957.dump`

## 执行过程

1. 初始导入时，先按用户上一条要求将 Key 绑定到 `codex-pool-49-usd`，并创建 59 元档位订阅。
2. 用户补充说明该 Key 为自用，需要无上限。
3. 检查代码确认限额来自 `groups.daily_limit_usd`。
4. 创建 `codex-pool-local-unlimited` 专用分组。
5. 将 Key `32` 从 `codex-pool-49-usd` 切到 `codex-pool-local-unlimited`。
6. 为用户创建 active unlimited subscription `45`。
7. 将旧 59 元订阅 `44` 软删除。
8. 清理 Redis 认证、订阅和 Key rate cache。

Redis 清理时仍出现 `AUTH failed` 提示，原因与前几次一致：redis-cli 自动尝试 AUTH，但当前 Redis 默认用户没有配置密码；删除命令仍执行。

## 验证

数据库验证：

- `15951875192@phone.com` 当前 Key 绑定 `codex-pool-local-unlimited`。
- `daily_limit_usd`、`weekly_limit_usd`、`monthly_limit_usd` 为空。
- `codex-pool-local-unlimited` 已绑定上游账号 `cliproxy-local-openai`。
- active subscription ID 为 `45`，到期时间为 `2036-06-15`。

模型访问验证：

- `http://127.0.0.1:18080/v1/models`
  - HTTP 200
  - 返回模型数量：10
  - 首个模型：`gpt-5.5`
- `https://api.aaccx.pw/v1/models`
  - HTTP 200
  - 返回模型数量：10
  - 首个模型：`gpt-5.5`

Responses 验证：

- `http://127.0.0.1:18080/v1/responses`
  - HTTP 200
  - response ID 前缀：`resp_0992786`
  - `usage_logs` 增量：1

## 注意

- 这条 Key 是管理员自用 Key，不纳入普通用户套餐限额。
- 不应把 `codex-pool-local-unlimited` 暴露为普通用户可购买套餐。
- 不应把公共 `codex-pool-49-usd` 改为无上限。
