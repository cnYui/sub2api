# `zhudi189@gmail.com` 多 Key 公网请求最终排查结果

时间：2026-07-24 12:09:26

## 实测结果

均通过公网入口 `POST https://api.aaccx.pw/v1/responses` 使用该用户真实有效 Key 请求 `gpt-5.5`；未保存任何完整 Key。

| API Key | 结果 | 说明 |
| --- | --- | --- |
| `api_keys.id=90`（`jun-codex`） | HTTP `200` | 响应 `completed`，输出 `OK`；成功日志 `usage_logs.id=193055`，实际扣费 `0.0096500000 USD`。 |
| `api_keys.id=128`（`sheng-codex`） | HTTP `429` | 响应 `api key 5小时限额已用完`；请求前被拦截，未产生成功用量。 |

## 根因

`api_keys.id=128` 配置了独立 Key 限额，当前已超额：

- 5 小时限额：`2 USD`，已使用 `5.317587 USD`
- 1 天限额：`3 USD`，已使用 `5.317587 USD`
- 当前 5 小时窗口起点：`2026-07-24 09:47:35 +08`，按窗口规则约在 `14:47:35 +08` 重置

用户订阅本周也已接近上限：`520 USD` 中已用 `514.53604864 USD`，仅余 `5.46395136 USD`。因此即使改用不设 Key 限额的 `jun-codex`，较大的请求也可能因套餐周额度不足被拒绝。

## 结论

公网映射、外层 `18080`、内层 `18086` 与上游链路均正常。用户的“不能用”由 `sheng-codex` 自身 Key 限额达到上限造成，不是两层服务连通问题。
