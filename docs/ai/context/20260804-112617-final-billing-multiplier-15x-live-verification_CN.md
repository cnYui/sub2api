# 18082 最终 15x 扣费真实验证

## 请求

使用当前 18082 中用户 `2799523972@qq.com` 的有效 API Key 发起最小成本的真实请求：

- 端点：`POST /v1/chat/completions`
- 模型：`gpt-5.6-terra`
- 分组：GPT，`group_id=9`
- 渠道：`channel_id=3`
- HTTP 状态：`200`
- 返回内容：`OK`

API Key 明文未写入本记录。

## 扣费证据

对应 `usage_logs.id=286781`：

| 字段 | 值 |
| --- | ---: |
| `total_cost`（基础成本） | 0.001928 |
| `rate_multiplier`（分组倍率） | 0.15 |
| `BILLING_FINAL_MULTIPLIER` | 15 |
| `actual_cost`（实际扣费） | 0.004338 |

计算结果：

`0.001928 × 0.15 × 15 = 0.004338`

用户余额从 `101.96217000` 变为 `101.95783200`，实际减少 `0.00433800`，与用量日志完全一致。

## 结论

最终 15x 已在服务端真实扣费链路生效，不是仅配置在容器环境中。前端仍只显示分组倍率，不显示或叠加该 15x。
