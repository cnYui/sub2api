# 真实 API 请求余额扣费核验

## 请求

- 实例地址：`http://127.0.0.1:18082`
- 请求地址：`POST /v1/chat/completions`
- 账户：`2799523972@qq.com`
- 模型：`gpt-5.6-terra`
- 请求内容：最小非流式文本请求，要求返回 `OK`，`max_tokens=1`
- API Key：未写入本文档、日志或验证结果

## 结果

- HTTP 状态：`200`
- 响应内容：`OK`
- 响应 usage：prompt `4687`、completion `5`、total `4692`
- 用量记录：`usage_logs.id=286780`
- 入站端点：`/v1/chat/completions`
- 上游端点：`/v1/responses`
- 用户余额：`102.00000000` → `101.96217000`
- 实际扣减：`0.03783000`

## 计费证据

该条用量记录的 `total_cost=0.0025220000`，最终 `actual_cost=0.0378300000`。当前实例配置的最终计费倍率为 `15`，计算为 `0.002522 × 15 = 0.037830`，与用户余额减少额完全一致，证明真实请求已成功落库并完成余额扣费。

## 路由

用量记录显示用户 ID `466`、API Key ID `65`、分组 ID `9`、账号 ID `1128`、渠道 ID `3`，请求模型与上游模型均为 `gpt-5.6-terra`。
