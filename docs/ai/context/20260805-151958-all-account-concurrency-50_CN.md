# 全部账号并发上限统一为 50

## 变更时间

2026-08-05 15:19:58（Asia/Tokyo）

## 变更内容

- 通过管理端账号批量编辑，选中当前实例全部 10 个账号。
- 仅修改账号并发上限（`accounts.concurrency`）为 `50`。
- 未修改当前并发数、调度开关、账号倍率、分组、模型映射或其它限额。

## 核验

- 管理端页面核验：10/10 个账号容量均显示为当前并发数 `/ 50`。
- PostgreSQL 核验：`COUNT(*)=10`、`concurrency=50` 的记录数为 `10`、非 50 记录数为 `0`。
