# 欠费用户与 961109198@qq.com 余额套餐核查

核查时间：2026-08-20（Asia/Shanghai）

## 核查范围

只读核对生产容器 `sub2api-official-18082-postgres` 中 `users`、`user_balance_packages`、`balance_debt_ledger`、`usage_logs`、`payment_audit_logs` 和 `user_affiliate_ledger`。未修改余额、套餐、订单、退款或账本。

## 当前欠费概况

- 当前普通余额为负的用户共 20 人。截图中的金额是 `users.balance`，不是单独的欠费账本总额。
- 余额套餐的周额度、刷新次数和普通余额是不同字段：套餐额度到账后会增加 `users.balance`，并把本周可用额度记录到 `user_balance_packages.remaining_usd`。
- 旧版本在普通余额为负时会把套餐置为 `debt_paused`，未到账周额度不再自动发放；2026-08-19 的连续结算规则已改为“刷新时先抵扣普通余额欠费，剩余部分才进入本周套餐额度”。历史有效 `debt_paused` 也已恢复为 `active`。
- 当前仍为负余额且有后续刷新安排的用户有 7 人；没有有效套餐的负余额用户不会凭空产生后续额度。

## 961109198@qq.com（用户 ID 506）

### 欠费形成

- 用户在 2026-08-07 处理前余额为 `-430.90361296 USD`。
- 其订单 `587` 对应套餐 `user_balance_packages.id=119`，每周额度 `128 USD`，共 4 期；当时已到账 `1/4`，剩余 3 期合计 `384 USD`。
- 2026-08-05 的历史大额用量复核中，该用户有 4 条日志退回普通余额，共 `18.72399375 USD`；相关审计动作是 `BALANCE_MANUAL_REFUND`。这笔退款已计入余额，不能再次作为未处理欠费重复计算。
- 该用户 2026-07-07 至 2026-08-07 共 5,379 条用量日志，`actual_cost` 合计 `1,493.771879385 USD`，`total_cost` 合计 `951.324909075 USD`。其中 8 月 4-7 日多条请求的 `actual_cost` 明显高于 `total_cost`，与当时大额请求/倍率复核背景一致。

### 已经发生的 384 USD 抵扣

2026-08-07 21:20:36，管理员已将未到账的 3 期 × 128 USD（`384 USD`）一次性用于抵扣欠费：

- `balance_debt_ledger.id=7`：`repayment 384 USD`，余额 `-430.90361296 -> -46.90361296`；
- 随后套餐 `id=119` 被标记为 `cancelled`，`remaining_usd=0`，`next_credit_at=NULL`，到账进度保留 `1/4`；
- 订单 `587` 仍为 `COMPLETED`，现金退款为 `0 CNY`。

所以，这个套餐的 3 期额度已经用完并取消，不能再等待该套餐刷新。

### 为什么当前是 -34.26361296 USD

抵扣后余额是 `-46.90361296 USD`。之后用户通过订单 `617`、`620` 产生邀请返利 `4.72 + 7.92 = 12.64 USD`，写入 `user_affiliate_ledger` 并增加普通余额，因此当前余额变为：

`-46.90361296 + 12.64 = -34.26361296 USD`

这 `12.64 USD` 不是套餐刷新，也不是欠费还款账本中的 repayment；该用户的 `balance_debt_ledger` 目前只有 384 USD 的历史 repayment，没有新的 repayment。

## 是否还会自动刷新

不会。当前数据库中用户 506 的余额套餐只有：

| 套餐 | 状态 | 已到账/总期数 | 下次刷新 | 到期 |
| --- | --- | ---: | --- | --- |
| `id=119` / 订单 `587` | `cancelled` | `1/4` | `NULL` | 2026-09-02 |
| `id=22` / 订单 `139` | `expired` | `4/4` | `NULL` | 2026-08-04 |

因此，961109198@qq.com 没有“之后还会刷新的额度”，下一次也不会自动抵扣这 `34.26361296 USD` 欠费。

按当前实现，只有重新购买并支付一个新的余额套餐后，首期额度才会在到账事务中按“先抵扣负余额、剩余部分形成套餐本周可用额度”的规则处理；或者管理员另行授权人工结算/充值。当前余额为负时，系统会按负余额准入规则拒绝普通请求，不能继续把欠费扩大。

## 结论

截图中的 `-34.26 USD` 不是某个仍待自动刷新的 384 USD 欠费；那 384 USD 已在 8 月 7 日抵扣并使套餐取消。当前剩余欠费约 `34.26 USD`，来源是抵扣后继续保留的负余额，后续 `12.64 USD` 邀请返利只减少了部分欠费。
