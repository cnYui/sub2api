# 用户 3415991811@qq.com 的余额归零

## 背景与套餐规则

- 用户 ID：`523`，此前已取消 29 元余额套餐（订单 `149`）。
- 归零前普通余额为 `0.45314656 USD`。
- 余额套餐新建履约时，首期套餐额度会直接增加到 `users.balance`；29 元套餐当前首期额度为 `76 USD`。
- 当前有效套餐续费只延长有效期，不重复立即增加首期额度；后续周期按套餐剩余额度补差额。

## 执行决策

- 按用户要求仅将 `users.id=523` 的普通余额原子归零。
- 保留已取消套餐、退款失败订单及原有支付审计；不修改流量卡额度。
- 写入独立余额调整审计，记录操作前后余额和原因。

## 执行与核验

- 在 PostgreSQL 事务中锁定用户 `523`，校验余额仍为 `0.45314656` 后将 `users.balance` 原子归零。
- 已写入 `payment_audit_logs`：`id=1213`，操作为 `BALANCE_MANUAL_RESET`，操作人为 `admin`。
- 普通余额核验为 `0.00000000 USD`。
- 余额套餐 `29` 仍为 `cancelled`，`remaining_usd=0`；订单 `149` 仍为 `REFUND_FAILED`，退款金额 `2.31` 未改变。
- 流量卡记录 `170`、`297` 未修改，剩余额度仍分别为 `0` 和 `3.5835821875 USD`。
- 已清理并核对 Redis `billing:balance:523`，键不存在（`EXISTS=0`）。
