# 2799523972@qq.com 购买退款流程测试资金操作计划

## 背景

- 用户希望用 `2799523972@qq.com` 测试“余额购买套餐并退款”的完整流程。
- 用户说明该账号的套餐已通过管理员页面删除，需要先确认没有 active 套餐、没有软删除 active 残留、数据库状态可再次购买。

## 只读确认

- 运行态库：`sub2api-candidate-postgres` / database `sub2api`。
- 用户：`users.id=31/email=2799523972@qq.com/status=active`。
- 当前余额：`users.balance=0.00000000`，`total_recharged=0.00000000`。
- `user_subscriptions` 共有 2 条历史订阅：
  - `id=71/group_id=9/codex-pool-69-usd/status=expired/deleted_at=2026-07-10 20:57:07+08`。
  - `id=49/group_id=2/codex-pool-19-usd/status=expired/deleted_at=2026-06-27 21:02:42+08`。
- 活跃订阅计数：
  - `active_not_deleted=0`。
  - `active_soft_deleted_residue=0`。
  - `not_deleted_rows=0`。
- `user_allowed_groups` 无记录。
- API Key：
  - `id=65/name=Codex_79/status=active/group_id=NULL`。
  - `id=85/name=codex-public-test-20260706235406/status=active/group_id=NULL/deleted_at=2026-07-07 07:56:03+08`，该行已软删。
  - `id=37/name=Codex_used/status=inactive/group_id=9/deleted_at=2026-07-03 10:36:38+08`。
- 订单最新状态无 `PENDING/RECHARGING` 订阅订单阻塞购买。
- 该用户仍有 3 张 OpenAI 流量卡余额，但它们不是订阅套餐，不影响单 active 订阅购买保护判断。
- Redis 当前只有 `billing:sub:35:2`、`billing:sub:13:5`，没有 `user_id=31` 的订阅计费缓存。

## 操作计划

1. 写库前备份 Postgres，并用 `pg_restore -l` 验证备份可读。
2. 给 `users.id=31` 增加 `200.00` 余额，用于覆盖最高 `199 元订阅池` 的余额购买测试。
3. 不修改 `total_recharged`，因为这不是用户真实充值。
4. 写入一条 `redeem_codes.type=admin_balance/status=used/used_by=31/value=200.00` 作为管理员测试资金调整记录，说明用途为购买退款流程测试。
5. 删除可能存在的 `billing:user_balance:31` 类缓存；如果实际无该 key，则保持无副作用。
6. 复核用户余额、审计记录、订阅阻塞计数、Redis 缓存状态和应用健康。

## 风险与回滚

- 这是运行态余额写入，必须先备份。
- 若需要回滚，按备份恢复或反向扣减 `200.00`，并补充一条 `admin_balance` 负向记录说明回滚原因。
- 不触碰订阅历史、订单历史、API Key、流量卡、支付实例、容器和 nginx。
