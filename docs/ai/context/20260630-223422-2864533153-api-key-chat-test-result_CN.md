# 2864533153@qq.com API Key 对话请求测试结果

时间：2026-06-30 22:34 JST

## 目标

按要求只查看运行态并使用 `2864533153@qq.com` 的 active API Key 发起一次模型对话请求；不修改源码，不打印、不记录完整 API Key。

## 运行态

- 当前公网候选容器：`sub2api-candidate`，端口 `127.0.0.1:18084->8080`，healthy。
- 公网入口 `127.0.0.1:18084/health` 与 `127.0.0.1:8080/health` 均返回 `200`。
- 使用数据库：`sub2api-candidate-postgres`。

## 用户与 Key 状态

- 用户：`2864533153@qq.com`
- `users.id=54`
- 用户状态：`active`
- 用户余额：`0.00000000`
- API Key：`api_keys.id=58`
- Key 脱敏：`sk-210a8...f8258e`
- Key 名称：`6-29`
- Key 状态：`active`
- Key 绑定分组：`groups.id=8 / codex-pool-89-usd`
- 分组平台：`openai`
- 分组类型：`subscription`
- 分组每日额度：`89.00000000`

## 订阅与扣费前状态

- 订阅：`user_subscriptions.id=67`
- 订阅状态：`active`
- 有效期：`2026-06-29 19:10:31.971592+08` 到 `2026-07-30 19:10:31.971592+08`
- 当前日用量：`0.0000000000 / 89.00000000`
- 流量卡：无。
- `usage_logs`：0 条。
- `billing_usage_entries`：0 条。

## 公网候选真实请求

请求：

- Endpoint：`http://127.0.0.1:18084/v1/chat/completions`
- Model：`gpt-5.4-mini`
- Prompt：要求只回复 `pong`
- 非流式，`max_tokens=8`
- Authorization 使用该用户 API Key，但未打印或记录完整 Key。

响应：

- HTTP：`503`
- Request ID：`189c41c5-1a85-4fda-95ab-f2a8c5740e0b`
- Body：`Service temporarily unavailable`

本次没有完成模型对话回复。

## 根因

请求认证和内容审核均通过，失败发生在上游账号选择阶段：

- 日志事件：`openai_chat_completions.account_select_failed`
- 用户：`user_id=54`
- Key：`api_key_id=58`
- Group：`group_id=8 / codex-pool-89-usd`
- Model：`gpt-5.4-mini`
- 错误：`no available accounts`

只读核对运行态绑定关系：

- `codex-pool-89-usd` 当前绑定上游账号数为 `0`。
- 其他 OpenAI 订阅分组 `codex-pool-19-usd`、`codex-pool-29-usd`、`codex-pool-49-usd`、`codex-pool-local-unlimited` 均绑定了 `cliproxy-local-openai`。
- 运行态唯一 OpenAI 上游账号为 `accounts.id=1 / cliproxy-local-openai`，状态 `active`，`schedulable=true`，并发 `10`。
- `codex-pool-89-usd` 没有 fallback group，也没有开启 model routing。

## 请求后状态

- `usage_logs` 仍为 0 条。
- `billing_usage_entries` 仍为 0 条。
- 订阅 `daily_usage_usd` 仍为 `0.0000000000`。
- 本次失败请求未扣费。

## 结论

该用户账号、订阅和 API Key 本身有效；但 Key 绑定的 `codex-pool-89-usd` 没有任何可调度上游账号，因此当前无法完成真实模型对话请求。

若要让该用户 Key 成功对话，需要先做运行态配置决策，例如把 `codex-pool-89-usd` 绑定到可用 OpenAI 上游账号，或调整该套餐/Key 的分组绑定。该操作会影响同一分组的公网请求路由，本次按“不动代码”的要求未做任何运行态写入。
