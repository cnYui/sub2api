# `zhudi189@gmail.com` 相关 API Key 的公网请求验证结果

时间：2026-07-24 12:00:37

## 结论

- `18086` 已在本轮前重启完成，当前健康。
- 公网入口 `https://api.aaccx.pw/v1` 可正常访问。
- 用户提供的这把 `API key` 可以真实请求公网接口。

## 验证结果

### `GET /v1/models`

- 返回 `200`
- 可正常拿到模型列表

### `POST /v1/responses`

- 返回 `200`
- 已产生新的 `usage_logs` 记录
- 最近一条测试落库：`usage_logs.id=192988`

## 额外发现

- 这把 key 在库里是有效的，但它对应的不是 `zhudi189@gmail.com` 那条用户记录。
- 所以如果用户本人说“用不了”，更像是：
  - 拿错了 key
  - 用的不是当前这把活 key
  - 客户端配置了别的 base_url / model / 代理

## 说明

- 本次只做只读验证，没有改配置。
- 文档里不记录完整 key。
