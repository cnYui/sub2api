# 2026-06-28 18084 DB 克隆到 18080 main-preview 结果

## 执行内容

已按确认方案执行：

1. 从 `sub2api-candidate-postgres` 只读 `pg_dump -Fc` 导出 18084 候选库。
2. 备份覆盖前的 `sub2api-main-preview-postgres`。
3. 停止 `sub2api-main-preview` 应用容器。
4. drop/recreate 18080 preview DB。
5. 将 18084 dump restore 到 18080 preview DB。
6. 清空 `sub2api-main-preview-redis`，避免旧 preview 会话/缓存污染新克隆库。
7. 启动 `sub2api-main-preview`，让当前本机 `main` 镜像自动应用新迁移。

## 备份文件

以下文件包含运行态敏感数据，权限已设为 `600`，不要提交：

- `deploy/backups/20260628-182326-18084-candidate-for-18080-preview.dump`，约 15MB
- `deploy/backups/20260628-182326-18080-preview-before-overwrite.dump`，约 413KB

## 18084 边界

本次对 18084 只做了只读 dump 和只读查询：

- 未停止 `sub2api-candidate`。
- 未停止 `sub2api-candidate-postgres`。
- 未停止 `sub2api-candidate-redis`。
- 未写入 18084 数据库或 Redis。
- 未修改 nginx、Cloudflare Tunnel 或 `8080 -> 18084` 链路。

`sub2api-candidate` 启动时间仍为 `2026-06-27T13:25:13Z`，镜像仍为 `sub2api-candidate:20260627-221441-traffic-card-fix`。

## 恢复后 18080 状态

```text
sub2api-main-preview            sub2api-main-preview:20260628-173235-eb95da02c       Up (healthy)   127.0.0.1:18080->8080/tcp
sub2api-main-preview-postgres   postgres:18-alpine                                   Up             5432/tcp
sub2api-main-preview-redis      redis:8-alpine                                       Up             6379/tcp
```

18080 preview DB：

```text
preview_users|49
preview_active_users|46
preview_password_hash_nonempty|46
preview_auth_identities|47
preview_api_keys|41
preview_migrations|192
```

最新迁移：

```text
156_seed_codex_79_subscription_plan.sql
155_seed_codex_subscription_plans_baseline.sql
154_seed_codex_99_subscription_plan.sql
153_scheduler_outbox_pending_dedup_key_index_notx.sql
152_scheduler_outbox_dedup_key.sql
```

公网候选账号已出现在 18080：

```text
1038686518@qq.com active has_password=true
2799523972@qq.com active has_password=true
admin@sub2api.local active has_password=true
cnfoxian@gmail.com active has_password=true
xiaobianfuai@gmail.com active has_password=true
```

其中 `xiaobianfuai@gmail.com` 仍存在一个软删除历史重复账号；active 且 not_deleted 的账号为可用账号。

## 验证

- `curl -fsS http://127.0.0.1:18080/health` 返回 `{"status":"ok"}`。
- `curl -fsS http://127.0.0.1:18084/health` 返回 `{"status":"ok"}`。
- `curl -fsS http://127.0.0.1:8080/health` 返回 `{"status":"ok"}`。
- `http://127.0.0.1:18080/api/v1/settings/public` 返回 200。
- 18080 公开 settings 非敏感状态：
  - `registration_enabled=true`
  - `email_verify_enabled=true`
  - `password_reset_enabled=true`
  - `purchase_subscription_enabled=false`
  - `promo_code_enabled=true`
  - `site_name=天才程序员小站`

## 说明

使用旧 preview 容器环境里的 `ADMIN_PASSWORD` 对 clone 后的 `admin@sub2api.local` 做登录 smoke test 返回 401。原因是 clone 后该账号数据来自 18084 候选库，不再是旧 preview 自动创建的密码；这不影响公网真实账号使用自己的 18084 密码登录 18080。
