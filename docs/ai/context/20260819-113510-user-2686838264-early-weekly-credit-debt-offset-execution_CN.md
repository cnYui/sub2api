# 用户 2686838264@qq.com 提前刷新周额度抵扣欠费记录

## 授权与范围

按管理员要求，将用户 `2686838264@qq.com` 的下一周余额套餐额度提前结算，并优先抵扣当前欠费，使用户恢复正常使用。

本次仅处理用户 `users.id=610`、余额套餐 `user_balance_packages.id=143` 及关联订单 `payment_orders.id=639`。未修改订单金额、订单状态、退款金额、历史用量、API Key 配额、流量卡或套餐有效期。

## 执行前状态

- 普通余额：`-1.25219097 USD`。
- 余额套餐：每周额度 `76.00000000 USD`，当前窗口剩余 `0 USD`，已到账 `1/4`。
- 套餐状态：`debt_paused`；第 2 期原定到账时间为 `2026-08-21 10:17:50.751811 +08:00`，有效期至 `2026-09-11 10:17:50.751811 +08:00`。
- 关联订单：`639`，状态 `COMPLETED`，退款金额 `0 CNY`。
- 执行前最近 30 分钟有 1 条已落库用量记录（`usage_logs.id=350510`，`actual_cost=1.48598604 USD`），对应 `usage_billing_dedup` 记录存在，未发现未结算请求。

## 执行方式

在单个 `SERIALIZABLE` PostgreSQL 事务中锁定用户、套餐和关联订单，并校验邮箱、余额、套餐状态、到账期数、周额度、剩余额度、刷新间隔、有效期、订单状态与退款金额。任一前置条件不匹配即回滚。

第 2 期结算按正常周刷新语义执行：先以 `76 USD` 抵扣普通余额欠费，剩余部分写入当前套餐窗口。

## 执行结果

- 第 `2/4` 期 `76 USD` 已于 `2026-08-19 10:34:21 +08:00` 提前结算。
- `1.25219097 USD` 欠费已全额抵扣。
- 用户普通余额与当前套餐剩余均为 `74.74780903 USD`。
- 套餐从 `debt_paused` 恢复为 `active`，下次刷新为 `2026-08-28 10:17:50.751811 +08:00`；有效期保持不变。
- 新增支付审计 `payment_audit_logs.id=1945`：`BALANCE_PACKAGE_EARLY_WEEKLY_CREDIT_2`，操作者 `admin:authorized_manual_settlement`。
- 新增欠费还款账本 `balance_debt_ledger.id=28`：来源 `balance_package_early_weekly_credit`，引用 `package:143:credit:2`。

## 缓存与核验

- 已尝试清理余额缓存 `billing:balance:610`，执行时不存在（删除结果 `0`）。
- 已对该用户现有 `3` 个 API Key 删除鉴权缓存并发布 `auth:cache:invalidate` 失效通知；执行时缓存键均不存在（删除总数 `0`）。
- 数据库回读确认余额 `74.74780903 USD`、套餐 `active`、到账进度 `2/4`、剩余 `74.74780903 USD`、下次刷新时间、订单状态、支付审计和欠费还款账本均符合预期。
- 当前余额已非负，负余额硬拦截不再阻止该用户调用。

