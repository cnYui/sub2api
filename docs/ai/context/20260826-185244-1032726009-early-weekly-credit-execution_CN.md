# 用户 1032726009@qq.com 提前刷新周额度记录

## 授权与范围

按管理员要求，提前刷新用户 `1032726009@qq.com`（`users.id=505`）当前余额套餐的下一周额度。

本次仅处理当前余额套餐 `user_balance_packages.id=123` 及关联订单 `payment_orders.id=600`。未修改订单金额、订单状态、退款金额、套餐有效期、历史用量、API Key 配额、流量卡或历史套餐。

## 执行前状态

生产数据库只读快照时间为 `2026-08-26 17:48:14 +08:00`：

- 用户普通余额：`-3.84820178 USD`。
- 当前套餐：`id=123`，状态 `active`，已到账 `3/4` 期。
- 每期额度：`781 USD`；当前套餐窗口剩余：`0 USD`。
- 原定下一次刷新：`2026-08-27 19:53:21.810233 +08:00`。
- 套餐有效期：至 `2026-09-03 19:53:21.810233 +08:00`。
- 订单 `600`：`COMPLETED`，退款金额 `0`。

定时任务尚未刷新该期，因此继续执行管理员要求的提前刷新。

## 执行方式

执行时间为 `2026-08-26 17:51:09 +08:00`。使用 PostgreSQL `SERIALIZABLE` 事务，事务内重新按邮箱锁定用户、按当前有效套餐锁定套餐、锁定关联订单并校验套餐仍为未完成状态。

按余额套餐正常周刷新语义计算：

- 本期到账：`781 USD`。
- 先抵扣普通余额欠费：`3.84820178 USD`。
- 本期进入套餐窗口：`777.15179822 USD`。
- 套餐从 `3/4` 变为 `4/4`，状态从 `active` 变为 `completed`，`next_credit_at` 清空。

## 执行结果

- 用户普通余额：`-3.84820178 USD` -> `777.15179822 USD`。
- 当前套餐剩余：`0 USD` -> `777.15179822 USD`。
- 套餐 `123` 已完成 `4/4`，有效期保持不变。
- 新增支付审计 `payment_audit_logs.id=2159`：`BALANCE_PACKAGE_EARLY_WEEKLY_CREDIT_4`，操作者 `admin:authorized_manual_settlement`。
- 新增欠费还款账本 `balance_debt_ledger.id=47`，金额 `3.8482017800 USD`，来源 `balance_package_weekly_credit`，引用 `package:123:credit:4`。
- 订单 `600` 仍为 `COMPLETED`，退款金额保持 `0`。
- `users.total_recharged` 按本期额度增加 `781 USD`，从 `2343 USD` 变为 `3124 USD`。

## 缓存与核验

- 已尝试删除 Redis 余额缓存 `billing:balance:505`，删除结果为 `0`，说明执行时该缓存键不存在。
- 事务内为该用户 `5` 个未删除 API Key 写入认证缓存失效 outbox；随后回读 outbox 已为空，说明失效事件已被 worker 消费。
- Redis 回读确认 `billing:balance:505` 不存在，后续请求会从数据库读取新余额。
- 生产应用、PostgreSQL、Redis 均为 `healthy`，本地健康端点 `/health` 返回 HTTP `200`。
