# 订单 717 退款失败诊断

## 结论

订单 `717`（用户 `kangzhao6618@gmail.com`，用户 ID `484`）是余额套餐订单，当前状态为 `REFUND_FAILED`。服务端按退款页面报价计算出的退款金额为 `21.49 CNY`，不是全额退款。

失败原因是 ZPay 原支付商户返回业务错误：

```text
easypay refund failed (HTTP 200): 卖家余额不足
```

HTTP `200` 仅表示 ZPay 接口收到请求，不代表退款成功。应用在 2026-08-25 17:08:59、17:09:34、17:10:48（+08）连续收到同一响应，因此订单保持 `REFUND_FAILED`，没有标记为 `REFUNDED`，也没有执行本地退款后的权益撤销。

## 核验数据

- 原订单金额：`29.00 CNY`
- 实付金额：`29.29 CNY`
- 当前退款金额：`21.49 CNY`
- 支付方式：支付宝
- ZPay 交易号：`2026082423001470681427749855`
- 支付实例：`1`
- 退款报价接口在 17:08:49（+08）返回 HTTP `200`
- 订单最近失败原因与运行日志一致，均为 ZPay 的“卖家余额不足”
- 当前应用、PostgreSQL、Redis 容器均 healthy，ZPay 域名已配置直连

## 处理建议

需要为绑定该原订单的 ZPay 商户账户补足可退款余额，或等待 ZPay 商户余额恢复后，再从订单页面重试退款。不能手工把订单改成 `REFUNDED`，否则平台会显示已退款而 ZPay 实际未出款。

本次仅做只读排查，未修改订单、用户余额、套餐、数据库或容器。
