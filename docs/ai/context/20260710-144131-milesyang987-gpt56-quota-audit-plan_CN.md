# milesyang987@gmail.com GPT-5.6 与额度为 0 只读审计计划

## 背景

用户指出 `milesyang987@gmail.com` 当前额度仍为 0，需要核对今天早上是否使用 GPT-5.6，以及如果该用户有问题是否意味着其他用户计费也有问题。

## 审计原则

- 只读查询运行态 `sub2api-candidate-postgres`。
- 不修改 Postgres、Redis、nginx、容器或用户余额。
- 不凭 `usage_logs.total_cost` 单点判断，必须核对订阅字段、dedup 与用量窗口。
- 重点区分：
  - 用户是否存在。
  - 用户是否有 active subscription。
  - 今天早上是否有 GPT-5.6 usage。
  - 如果有 usage，是否绑定订阅、是否写 dedup、是否进入 `daily_usage_usd`。
  - 如果当前额度显示 0，原因是没请求、窗口重置、订阅选择、前端字段还是计费缺陷。

## 查询范围

- 主要窗口：北京时间 `2026-07-10 00:00:00+08` 到 `2026-07-10 12:00:00+08`。
- 当天窗口：北京时间 `2026-07-10 00:00:00+08` 到 `2026-07-11 00:00:00+08`。

## 输出

- 给出该用户是否早上使用 GPT-5.6 的明确结论。
- 如果额度为 0，说明具体字段和原因。
- 如发现真实计费异常，只给 dry-run/修复方向，不直接写库。
