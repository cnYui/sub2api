# 用户 itjiangzengwen@gmail.com 提前刷新周额度执行记录

## 授权与范围

管理员要求将用户 `itjiangzengwen@gmail.com` 的下一周余额套餐额度提前发放，并抵消欠费，使其恢复使用。

本次只处理用户 `users.id=463`、余额套餐 `user_balance_packages.id=6` 及关联订单 `payment_orders.id=536`。未修改订单金额、订单状态、退款金额、历史用量、API Key 配额、流量卡或套餐有效期。

## 执行前状态

- 普通余额：`0.00227127 USD`。
- 余额套餐：每周额度 `128.00000000 USD`，当前窗口剩余 `0 USD`，已到账 `2/4`。
- 套餐状态：`debt_paused`；下一期原定时间为 `2026-08-12 19:43:54.672744 +08:00`，有效期至 `2026-08-26 19:43:54.672744 +08:00`。
- 关联订单：`536`，状态 `COMPLETED`，退款金额 `0 CNY`。
- `balance_debt_ledger` 中没有该用户未结欠费流水；此前 `-0.15772873 USD` 的欠费已由管理员于 `2026-08-13 07:04:28 +08:00` 通过余额调整抵消，因此本次不重复写入欠费还款流水。

## 执行方式

在单个 `SERIALIZABLE` PostgreSQL 事务中锁定用户、套餐和关联订单，并校验邮箱、余额、套餐状态、到账期数、剩余额度、原定刷新时间、有效期、订单状态与退款金额。任一前置条件不匹配即回滚。

本次提前结算按周刷新语义将第 `3` 期 `128 USD` 写入用户余额和当前套餐窗口；由于执行前余额已为正，欠费抵扣金额为 `0 USD`，不伪造还款账本。

## 执行结果

- 第 `3/4` 期 `128 USD` 已于 `2026-08-13 07:15:58 +08:00` 提前结算。
- 用户普通余额：`0.00227127 -> 128.00227127 USD`。
- 当前套餐剩余：`0 -> 128.00000000 USD`。
- 套餐状态：`debt_paused -> active`。
- 下次刷新按原 7 天节奏顺延至 `2026-08-19 19:43:54.672744 +08:00`；有效期保持不变。
- 新增支付审计：`payment_audit_logs.id=1639`，动作 `BALANCE_PACKAGE_EARLY_WEEKLY_CREDIT_3`，操作者 `admin:authorized_manual_settlement`。
- 未新增 `balance_debt_ledger` 记录，因为执行前已无负余额。

## 缓存与核验

- 删除余额缓存 `billing:balance:463`，返回 `0`（键当时不存在）。
- 用户现有 `2` 个 API Key 的鉴权缓存已删除，并发布 `auth:cache:invalidate` 失效通知。
- 数据库回读确认余额 `128.00227127 USD`、套餐 `active`、到账进度 `3/4`、剩余 `128 USD`、下次刷新时间、订单状态和支付审计均与上述结果一致。

