# xinlise 真实请求余额优先扣费验证

## 背景

用户 `xinlise@gmail.com`（ID `504`）页面同时显示普通余额和 OpenAI 流量卡额度，需确认流量卡额度不会阻断普通余额充足时的请求。

## 真实验证

- 2026-08-08 14:05:34 +08，使用用户提供的 API Key 向 `https://api.aaccx.pw/v1/chat/completions` 发起最小真实请求。
- 请求模型为 `gpt-5.5`，响应为 HTTP `200` 和 `OK`；响应 token 用量为输入 `4389`、输出 `5`。
- 新增用量记录 `usage_logs.id=308604`：API Key `260`、分组 13、账号 `1132`、`actual_cost=0.0130005 USD`。
- 用户普通余额由 `258.0000000 USD` 变为 `257.9869995 USD`，差额恰为本次实际扣费；有效 OpenAI 流量卡余额保持 `30.9744953 USD` 不变。

## 结论

普通余额充足时，服务端已优先从普通余额扣费，流量卡不会参与该笔结算或造成余额拦截。该真实 Key 当前绑定的分组 13 及账号 1132 可正常请求。

此前无法调用的是旧 Key 所属分组 9：其唯一账号 1128 因上游返回 `401 API key is disabled` 而不可调度，与用户普通余额、流量卡额度或余额优先级无关。
