# AGENTS.md 压缩整理计划

## 背景

当前根目录 `AGENTS.md` 已膨胀为 266 行、约 172KB，主体是 2026-06-24 至 2026-07-17 的项目流水记忆。它已经不适合作为每次会话快速读取的入口；更适合沉淀为 `docs/ai/context/` 下的长期压缩记忆，再让根 `AGENTS.md` 保留短规则、当前红线和索引。

## 目标

- 将当前 `AGENTS.md` 的长期记忆压缩迁移到新的 `docs/ai/context/*-agents-memory-condensed_CN.md`。
- 根 `AGENTS.md` 改为短入口，保留：
  - 协作规则和代码偏好；
  - 最新压缩记忆指针；
  - 当前架构定论、运行态红线和高风险操作提醒；
  - 维护规则和 Git 远端规则。
- 不覆盖、重命名或删除任何既有 `docs/ai/context/` 文档。

## 保留标准

必须保留：

- 默认中文、代码风格、design/plan/result 文档规则、上下文落盘规则。
- Sub2API / CLIProxyAPI / yui.web 的职责边界。
- 当前公网链路、关键端口、当前应用/数据库/Redis 容器命名。
- 支付、退款、余额、流量卡、计费、用量事实、迁移和部署风险。
- 当前仍影响后续实现的业务定论：同套餐续费、跨套餐拦截、组合支付、退款状态机、生图计费、Dashboard quota、API Key 自动分组、并发、正式 `/v1/*` 路径等。
- 账号与敏感信息红线、Git 远端和提交前上下文检查规则。

可折叠：

- 已完成的逐日操作流水账。
- 重复 health check、镜像 ID、备份大小、日志快照。
- 已有历史上下文文档可追溯的详细排查过程。

## 执行步骤

1. 读取并结构化检查当前 `AGENTS.md`。
2. 新增本计划文档。
3. 新增压缩记忆文档 `20260717-093308-agents-memory-condensed_CN.md`。
4. 将根 `AGENTS.md` 精简为短入口，指向新的压缩记忆。
5. 运行验证脚本、`git diff --check` 和未跟踪上下文检查。

## 不做的事

- 不修改业务代码、schema、迁移、容器、数据库、Redis、Nginx 或公网服务。
- 不提交、不推送、不 stage。
- 不把密码、完整 API Key、HMAC secret、SMTP 密码、支付密钥写入新文档。
