# 用户 xunskyler@gmail.com 余额套餐取消记录

## 执行

- 用户 ID：`454`。
- 当前有效余额套餐：`user_balance_packages.id=4`，订单 `540`，余额套餐 ¥59，每周额度 `$154`。
- 取消前状态：`active`；本周剩余额度 `$0`；下一次刷新时间为 `2026-08-07 16:20:10 +08`；套餐有效期至 `2026-08-28 16:20:10 +08`。
- 按管理员要求仅取消套餐权益，不发起退款，不修改普通余额或流量卡额度。
- 同一事务内将套餐标记为 `cancelled`、清零 `remaining_usd`，并清除 `next_credit_at`。
- 订单 `540` 保持 `REFUND_FAILED`，原退款报价 `43.06`、退款请求原因“刷新额度”和失败审计均保留。
- 新增审计：`payment_audit_logs.id=1398`，`action=BALANCE_PACKAGE_MANUAL_CANCELLED`，操作人为 `admin`。

## 取消后核验

- 当前有效余额套餐：`0`。
- 用户普通余额保持 `-0.22649341 USD`。
- 有效 OpenAI 流量卡额度保持 `$0.00039155`。
- 历史套餐、订单、用量和流量卡流水未修改。
