# 用户 2197057077@qq.com 提前刷新周额度执行记录

## 授权与范围

按管理员要求，将用户 `2197057077@qq.com` 的下一周余额套餐额度提前结算，并优先抵扣欠费，使其恢复使用。

本次仅处理用户 `users.id=565`、余额套餐 `user_balance_packages.id=121` 及关联订单 `payment_orders.id=589`。未修改订单金额、订单状态、退款金额、历史用量、API Key 配额、流量卡或套餐有效期。

## 执行前状态

- 普通余额：`-0.07226668 USD`。
- 余额套餐：每周额度 `520.00000000 USD`，当前窗口剩余 `0 USD`，已到账 `1/4`。
- 套餐状态：`debt_paused`；第 2 期原定到账时间为 `2026-08-12 22:26:42.153829 +08:00`，有效期至 `2026-09-02 22:26:42.153829 +08:00`。
- 关联订单：`589`，状态 `COMPLETED`，退款金额 `0 CNY`。
- 最近 30 分钟无用量记录。

## 执行方式

在单个 `SERIALIZABLE` PostgreSQL 事务中锁定用户、套餐和关联订单，并校验余额、套餐状态、到账期数、周额度、剩余额度、原定刷新时间、有效期、订单状态与退款金额。任一前置条件不匹配即回滚。

第 2 期结算按周刷新语义执行：先以 `520 USD` 抵扣普通余额欠费，余额为正的部分写入当前套餐窗口。

## 执行结果

- 第 `2/4` 期 `520 USD` 已于 `2026-08-13 23:56:54 +08:00` 提前结算。
- `0.07226668 USD` 欠费已全额抵扣。
- 用户普通余额与当前套餐剩余均为 `519.92773332 USD`。
- 套餐从 `debt_paused` 恢复为 `active`，下次刷新为 `2026-08-19 22:26:42.153829 +08:00`；有效期保持不变。
- 新增支付审计 `payment_audit_logs.id=1672`：`BALANCE_PACKAGE_EARLY_WEEKLY_CREDIT_2`。
- 新增欠费还款账本 `balance_debt_ledger.id=20`：来源 `balance_package_early_weekly_credit`，引用 `package:121:credit:2`。

## 缓存与核验

- 删除余额缓存 `billing:balance:565`，返回 `0`（执行时不存在）。
- 用户唯一现有 API Key 的鉴权缓存已删除，并发布 `auth:cache:invalidate` 失效通知。
- 数据库回读确认余额、套餐状态、套餐剩余额度、到账进度、下次刷新时间、支付审计和欠费账本均符合预期。
- 应用容器状态为 `healthy`，本地健康端点返回 HTTP `200`。

