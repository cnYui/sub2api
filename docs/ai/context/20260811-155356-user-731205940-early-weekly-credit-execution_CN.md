# 用户 731205940@qq.com 提前周额度刷新执行记录

## 请求与核查结论

- 2026-08-11，管理员要求将用户 `731205940@qq.com` 的下一次余额套餐刷新提前约 3 天，使其可立即继续使用。
- 用户 ID 为 `516`，API Key 为 ID `111`。核查发现该用户本周并非没有调用：2026-08-04 至 2026-08-10 共 `310` 次请求，实际扣费 `203.55329010 USD`，最后一次调用为 2026-08-10 14:22:00（+08）。
- 执行前普通余额为 `-0.66703840 USD`；余额套餐 ID `25` 的每周额度为 `258 USD`，已记录为 `4/4` 次到账、剩余 `0`、状态 `completed`。另有 `0.22518230 USD` OpenAI 流量卡额度，未修改。

## 根因

- 用户于 2026-08-05 完成续费订单 `582`。该订单将套餐到期时间从 2026-08-06 18:17:39（+08）延长至 2026-09-03 18:17:39（+08），但沿用了已完成的 `4/4` 到账计数和 `completed` 状态。
- 当前续费实现只延长有效期，不为已经完成的套餐周期重新排入下一周期周额度，因此用户续费后不会自动得到新周期额度。此次按管理员指令进行单用户纠正；全局续费规则需要另行确认后再改动。

## 执行

- 在 PostgreSQL 单事务中锁定用户 `516` 与套餐 `25`，以原余额、套餐 ID、订单 ID、到账进度、状态及到期时间作为前置条件。
- 按续费后的新周期接续，将原计划 2026-08-13 18:17:39（+08）的第 2 次周额度提前到 2026-08-11 14:50:13（+08）发放。
- 普通余额增加 `258 USD`，先覆盖 `0.66703840 USD` 欠费，结果为 `257.33296160 USD`。
- 套餐更新为：`starts_at=2026-08-06 18:17:39+08`、`credited_count=2/4`、`remaining_usd=257.33296160`、`status=active`、下一次刷新 `2026-08-20 18:17:39+08`，到期时间保持 `2026-09-03 18:17:39+08`。最后一次自动刷新仍为 2026-08-27。
- 已写入支付审计 `payment_audit_logs.id=1557`（`BALANCE_PACKAGE_EARLY_WEEKLY_CREDIT_2`）和欠费偿还账本 `balance_debt_ledger.id=16`，未改写历史订单、用量和流量卡。

## 缓存与验证

- 删除 `billing:balance:516` 余额缓存。
- 删除该用户 API Key 的 Redis L2 鉴权快照并发布 `auth:cache:invalidate` 失效消息；整个过程不记录或输出 API Key 原文。
- 回读确认用户余额为 `257.33296160 USD`，套餐状态为 `active`，可用套餐额度为 `257.33296160 USD`，下次刷新时间正确。
- `http://127.0.0.1:18082/health` 返回 HTTP `200`。
