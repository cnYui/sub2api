# /usage-guide 图生图调用说明改造执行计划

时间：2026-06-24 16:31 JST

## 步骤

1. 复核当前 `UsageGuideView` 和测试断言，确认改动面只落在普通用户使用教程前端。
2. 把「生图方法」栏目从旧版 `images/generations` 示例切换为真实可用的 `images/edits` 图生图说明。
3. 同步更新源码字符串断言测试，确保页面仍只暴露公网入口和用户侧必要信息。
4. 运行前端测试、`typecheck`、`build`，记录验证结果。
5. 新建结果文档并补充 `AGENTS.md` 运行记录。
6. 切新分支、提交本次改动、推送到 `personal`、创建目标 `main` 的 PR。
7. 基于 diff 做一次客观 review；若发现问题，先修复再更新验证。
8. 合并 PR 到 `main`，本地切回 `main` 并同步远端最新状态。

## 风险点

- 当前工作区已有 `AGENTS.md` 修改和无关 `deploy/*` 未跟踪文件，提交时必须精确选择文件。
- 现有截图里出现 `Trae 接入`，但本地源码未实现该栏目；本次不顺手补做，避免需求漂移。
- `UsageGuideView.spec.ts` 采用源码字符串断言，文案改动后必须同步更新测试 token。

## 完成标准

- `/usage-guide` 的「生图方法」栏目已展示真实图生图调用方法。
- 页面内不出现内网端口、管理员分组 ID 或敏感凭证。
- 前端验证通过。
- GitHub PR 已创建、review、合并，且本地与远端 `main` 已同步。
