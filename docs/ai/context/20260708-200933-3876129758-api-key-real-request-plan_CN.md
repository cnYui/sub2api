# 3876129758@qq.com API Key 真实模型请求测试计划

## 背景

- 用户要求测试 `3876129758@qq.com` 的 API Key 能否正确访问模型。
- 本次只做真实请求与只读核对，不手工改库、不构建、不替换、不重启、不改 nginx/Redis。
- 完整 API Key 不写入文档、回复或日志摘要。

## 已确认状态

- 用户：`users.id=56`，`email=3876129758@qq.com`，状态 `active`。
- active Key：
  - `api_keys.id=105`，名称 `Codex`，脱敏 `sk-bba5b55...5164`，最近使用时间 `2026-07-08 17:46:42+08`。
  - `api_keys.id=106`，名称 `Codex++`，脱敏 `sk-6abb393...0cbd`，最近使用时间 `2026-07-08 19:04:51+08`。
- 当前未删除 active 订阅：`user_subscriptions.id=90`，`group_id=4/codex-pool-49-usd`，有效期到 `2026-08-07 15:53:48+08`。
- 旧订阅 `user_subscriptions.id=76` 已为 `expired + deleted_at`，不应参与本次扣费。
- 分组日限额：`49 USD`；请求前订阅今日用量为 `0.2819010000 USD`。
- OpenAI 流量卡可用余额：`0 USD`。
- 请求前最新 `usage_logs.id=67399`。

## 测试方案

1. 使用最近使用过的 active Key `api_keys.id=106/Codex++`，避免两把 Key 都测试导致额外扣费。
2. 请求公网正式入口 `https://api.aaccx.pw/v1/responses`。
3. 模型使用 `gpt-5.5`，输入使用最小 ping 文本。
4. 核对 HTTP 状态码、响应状态、usage log 是否新增、扣费是否归属到 `subscription_id=90`。

## 预期

- 请求应返回 200。
- 用量应扣在当前 49 元订阅 `subscription_id=90`。
- 不应扣流量卡。
