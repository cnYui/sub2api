# 2026-06-28 18085 SMTP 运行态配置同步到 18084 结果

## 背景

公网新用户注册邮箱验证码接口 `/api/v1/auth/send-verify-code` 此前返回 200，但 `sub2api-candidate` 后台 EmailQueue worker 报 `EMAIL_NOT_CONFIGURED`，根因是 18084 公网候选库 `sub2api-candidate-postgres` 的 SMTP settings 为空；18085 测试库 `sub2api-smtp-test-postgres` 已配置 Gmail SMTP 和 App Password。

本次按确认方案执行：只同步 18085 的 SMTP 运行态配置到 18084 的 `settings` 表，不重建镜像，不停止或重建 Postgres/Redis，不修改 nginx 指向，不整库覆盖。

## 执行内容

- 备份 18084 同步前 SMTP settings 到 `deploy/backups/20260628-153633-18084-smtp-settings-before-sync.tsv`，文件按敏感文件处理，不输出内容。
- 从 18085 读取并同步以下 7 个 SMTP key 到 18084：
  - `smtp_host`
  - `smtp_port`
  - `smtp_username`
  - `smtp_password`
  - `smtp_from`
  - `smtp_from_name`
  - `smtp_use_tls`
- 按用户要求将 `smtp_from_name` 覆盖为 `天才程序员小站`，没有沿用 18085 原值。
- 使用 `settings.key` 唯一约束执行 `insert ... on conflict (key) do update`。
- 导入过程中未打印 `smtp_password` 明文；本地和容器内临时导出文件已删除。

## 18084 脱敏确认

18084 当前 SMTP 配置脱敏状态：

```text
smtp_from=[CONFIGURED]
smtp_from_name=天才程序员小站
smtp_host=smtp.gmail.com
smtp_password=[CONFIGURED]
smtp_port=587
smtp_use_tls=false
smtp_username=[CONFIGURED]
```

注册、邮箱验证和密码重置开关仍为：

```text
registration_enabled=true
email_verify_enabled=true
password_reset_enabled=true
```

## 真实验证

通过公网入口真实请求：

```text
POST https://api.aaccx.pw/api/v1/auth/send-verify-code
HTTP 200
```

响应：

```json
{"code":0,"message":"success","data":{"message":"Verification code sent successfully","countdown":60}}
```

`sub2api-candidate` 日志确认：

```text
[EmailQueue] Worker 1 sent verify code to xiaobianfuai+codex-smtp-20260628153814@gmail.com
```

同步后未再出现 `EMAIL_NOT_CONFIGURED`。

## 健康检查

以下健康检查均返回 `{"status":"ok"}`：

- `http://127.0.0.1:18084/health`
- `http://127.0.0.1:8080/health`
- `https://api.aaccx.pw/health`

## 结论

公网 18084 注册邮箱验证码无法发送的问题已确认是运行态 SMTP settings 缺失，不是镜像或代码未打包。当前已同步 SMTP 配置并完成公网真实发送验证；无需重建镜像或重启应用容器。
