# 18082 实时用量与扣费监控记录

## 核验范围

- 目标实例：`sub2api-official-18082`
- 数据库时间：2026-08-04 11:20:15+08 左右
- 应用容器环境：`BILLING_FINAL_MULTIPLIER=15`
- 核验方式：只读查询 PostgreSQL、应用日志和一段实时余额采样，未修改业务数据。

## 当前扣费结论

分组 9（GPT/Codex）的当前实时扣费正常。自 2026-08-04 10:25:42+08 以后：

- 用量记录 300 条；基础成本合计 `25.6975914800`。
- 实际扣费合计 `57.8195808300`。
- 合计比例为 `2.25 = 0.15（分组倍率）× 15（服务端最终倍率）`。
- 所有记录推导出的最终倍率均为 `15`，没有非 15x 的当前记录。
- 最近 30 分钟 238 条用量记录全部为正的 `actual_cost` 和正的 `rate_multiplier`，没有负余额用户。

## 余额实时对账

用户 `553 / jianniang00@163.com` 的实时采样窗口：

| 项目 | 值 |
| --- | ---: |
| 11:17:38.143 余额 | `116.22087560` |
| 11:18:09.034 余额 | `116.06902760` |
| 余额减少 | `0.15184800` |
| 新增用量日志 | `usage_logs.id=287055` |
| 基础成本 `total_cost` | `0.06748800` |
| 实际扣费 `actual_cost` | `0.15184800` |
| 分组倍率 `rate_multiplier` | `0.1500` |
| 推导最终倍率 | `15.0000` |

余额减少额与新增用量的 `actual_cost` 完全一致，说明当前标准余额扣费链路没有少扣或重复扣费。`billing_usage_entries` 当前为空符合现有实现，标准余额计费以 `usage_logs` 和事务内更新的 `users.balance` 为准。

## 历史记录提示

当前库中仍有切换前的历史用量记录：2026-08-03 11:19:40 至 2026-08-04 10:17:47+08 的 3903 条记录推导最终倍率为 `1`，另有一条在切换窗口内已经是 `15x`。这些记录属于恢复/切换前历史数据，不能作为当前实时链路异常的证据；本次未对历史订单或余额做追溯补差。

## 服务可用性提示

最近 30 分钟 `ops_error_logs` 有 40 条错误：路由 503 共 24 条、请求 403 共 10 条、上游 502 共 6 条。应用日志中未发现 `deduct balance failed` 或同类扣费失败信息。上述错误属于路由、认证或上游可用性问题，和当前已核验通过的余额扣费逻辑分开处理。

## 结论

当前用户请求、15 倍隐藏倍率和余额扣费均正常；建议另行排查最近的路由 503/上游 502，避免部分请求无法正常完成。
