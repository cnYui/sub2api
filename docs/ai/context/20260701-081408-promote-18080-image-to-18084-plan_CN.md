# 2026-07-01 18080 镜像发布到 18084 执行计划

## 目标

- 执行已确认的方案 1：保留公网 `sub2api-candidate-postgres` 与 `sub2api-candidate-redis`，只替换 `sub2api-candidate` 应用容器。
- 使用 18080 已验证镜像 `sub2api-main-preview:20260629-092226-ddd4fb9a9` 的同一镜像内容发布到 18084。
- 让新应用启动时自动在公网 DB 上应用 `156/157/158` migration。

## 当前基线

- 18080 应用镜像：`sub2api-main-preview:20260629-092226-ddd4fb9a9`
- 18080 镜像 digest：`sha256:1cc424ad95735188f6c707c5c1b19af42b670a2c332c863ef8e8a9cc4ddb563b`
- 18084 当前应用镜像：`sub2api-candidate:20260627-221441-traffic-card-fix`
- 18084 当前 DB migration 数量：`191`
- 18084 当前最新 migration：`155_seed_codex_subscription_plans_baseline.sql`
- 18080/18084/8080 health 当前均为 `{"status":"ok"}`

## 执行步骤

1. 重新备份 `sub2api-candidate-postgres`，输出到 `deploy/backups/20260701-081408-sub2api-candidate-postgres-before-migration-promote.dump`，权限 `600`。
2. 校验新 PostgreSQL dump 可被 `pg_restore -l` 读取。
3. 将 18080 验证过的镜像重新 tag 为公网候选发布镜像：
   - `sub2api-candidate:20260701-081408-ddd4fb9a9`
4. 记录旧 `sub2api-candidate` 的 image、started_at、network、port、healthcheck。
5. 导出旧容器 env 到临时文件，不打印 env 内容。
6. 停止并删除旧 `sub2api-candidate` 应用容器。
7. 使用新 tag 按原网络与端口重建 `sub2api-candidate`，不挂载 18080 DB/Redis，不修改 nginx。
8. 等待 `18084/health` 返回成功。
9. 验证 18084 PostgreSQL migration 从 `191` 变为 `194`，最新为 `158_enable_affiliate_default.sql`。
10. 验证 `79 元订阅池`、`codex-pool-69-usd`、`affiliate_enabled=true`。
11. 验证 `18084/health`、`8080/health`、`https://api.aaccx.pw/health` 和公网前端资源。
12. 删除临时 env 文件。

## 回滚入口

- 应用层回滚：用旧镜像 `sub2api-candidate:20260627-221441-traffic-card-fix` 按原网络端口重建 `sub2api-candidate`。
- 数据层精确回滚：使用本轮新 PostgreSQL dump 或上一份 `20260701-080310` dump，执行前必须停止应用容器。

## 明确不做

- 不用 18080 PostgreSQL 覆盖 18084 PostgreSQL。
- 不用 18080 Redis 覆盖 18084 Redis。
- 不修改 nginx。
- 不手工编辑 `schema_migrations`。
- 不提交 `deploy/backups/` 下备份文件。
