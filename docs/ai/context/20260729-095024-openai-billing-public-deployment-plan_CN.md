# OpenAI 原子计费授权公网部署计划

## 目标

将已完成本地重构与验证的 `main` 外层服务，通过本机公网 Nginx 入口恢复至公网链路，并完成重建、重启和验证。

## 前置事实

- `main` 已包含 OpenAI 原子计费授权合并提交 `fe1b4b513`，本地验证记录提交为 `8508f5e20`。
- 外层 `127.0.0.1:18080` 与内层 `127.0.0.1:18086` 已完成健康检查、迁移检查和无认证 401 检查。
- PostgreSQL 外层与内层完整备份位于 `deploy/backups/public-billing-rollout-20260729-093220`，两份 dump 均已验证可读；数据库与 Redis 容器和 volume 不重建、不清空。
- 外层旧镜像保留为 `sub2api-localdev-sub2api:rollback-20260729-093220`。

## 执行步骤

1. 读取 Nginx 容器标签、挂载、端口和配置，确认上游仍指向 `host.docker.internal:18080`，并确认 Cloudflare Tunnel 链路的本地入口端口。
2. 再次确认 `main` 提交、外层与内层健康状态及当前外层镜像。
3. 按 Nginx 原 Compose 配置无缓存构建并强制重建 Nginx 容器；仅触及 Nginx 容器，不重建外层、内层、数据库、Redis 或 Tunnel。
4. 确认本机 `127.0.0.1:8080/health` 返回 200，且经 Nginx 的无认证 `/v1/models` 返回 401。
5. 从公网域名仅进行 `/health` 和无认证 `/v1/models` 验证；不携带 API Key、不调用模型，避免计费。
6. 若 Nginx 构建、启动或任一健康检查失败，立即停止 Nginx，使公网恢复下线状态；外层旧镜像和数据库备份保留以供进一步回滚。

## 验收条件

- 18080、18086、8080 健康检查均为 200。
- Nginx 代理的无认证模型列表为 401。
- 公网健康检查为 200，且无认证模型列表为 401。
- 不产生模型上游请求，不记录或输出任何密钥、令牌或连接串。
