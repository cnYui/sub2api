# 首页旧套餐展示移除结果

## 已完成

- 删除 Hero 下方三枚功能标签。
- 删除截图中的 29、39、59、99 元旧套餐卡。
- 删除仅被该区域使用的中英文翻译项。
- 更新首页回归测试，确保旧标签和套餐卡不会再次渲染。

## 验证

- `pnpm vitest run src/views/__tests__/HomeView.spec.ts`：6/6 通过。
- `pnpm build`：通过；包含 TypeScript 检查。

## 保留范围

GPT-5.6 模型区、13 张完整套餐与流量卡价格清单，以及原有购买跳转均未修改。
