# 2026-08-20 当日扣费与流量卡账务只读调查

## 调查范围

- 调查时间：2026-08-20 15:36:52（东京时间）；数据库运行时区为 `Asia/Shanghai`，查询窗口使用 `2026-08-20 00:00:00+08` 至调查快照时间。
- 目标实例：`sub2api-official-18082`，应用容器与 PostgreSQL 均 healthy。
- 本次仅执行只读 SQL、容器日志和源码核对，没有修改业务数据。

## 结果摘要

截至快照（数据库时间 2026-08-20 14:36:52+08）：

| 核对项 | 结果 |
| --- | ---: |
| `usage_logs` 总记录 | 695 |
| 实际扣费记录（`actual_cost > 0`） | 683 |
| 实际扣费合计 | `572.5704413600 USD` |
| 统一扣费幂等键 `usage_billing_dedup` | 695 |
| 流量卡扣费流水 | 9 笔，`1.8595609600 USD` |
| 流量卡欠费流水（当天） | 0 笔 |
| `billing_usage_entries` | 0（当前生产实现不依赖此旧对账表） |

## 对账结论

逐项反查未发现以下异常：

- 正成本用量没有统一扣费幂等键：0 笔，0 USD。
- 有幂等扣费键但找不到用量日志：0 笔。
- 同一 `(request_id, api_key_id)` 重复用量记录：0 组。
- 流量卡流水找不到对应的同用户请求：0 笔。
- 流量卡流水金额与对应 `usage_logs.actual_cost` 不一致：0 笔；9 笔合计完全一致。
- 流量卡余额为负、超过初始额度，或最新流水余额与 `user_traffic_credits.remaining_usd` 不一致：均为 0。
- 当天 `traffic_credit_debt_ledger` 欠费记录：0 笔。
- `usage_logs` 中负费用、正成本但原始成本为 0 的记录：0 笔。

当天运行日志未检出 `Create usage log failed`、同步兜底失败、统一扣费失败或流量卡扣费失败关键字。

## 需要持续观察的边界记录

当天有 12 条 `usage_logs` 同时满足：`total_cost=0`、`actual_cost=0`、token/image 计数为 0。它们不是漏扣证据，因为没有可计费成本，也没有对应错误日志；更像是空响应或探测类请求占位记录。若业务上不允许这类请求成功后落用量日志，建议后续单独确认请求类型 `2` 的预期语义，避免运营报表把它们误读成“已调用但未收费”。

## 倍率核验

当天正成本使用记录均可由 `actual_cost = total_cost × rate_multiplier × final_multiplier` 解释；没有既不符合 `16x` 也不符合历史 `18x` 的正成本记录。应用当前运行环境为 `BILLING_FINAL_MULTIPLIER=16`。

## 结论

截至快照，项目当天没有证据表明存在丢失扣费、重复扣费或流量卡扣费记录错误。流量卡自动扣费链路已产生 9 笔流水，逐笔与用量及卡余额一致。唯一值得跟踪的是 12 条零 token/零成本占位记录，它属于记录语义问题候选，不属于已确认的账务异常。
