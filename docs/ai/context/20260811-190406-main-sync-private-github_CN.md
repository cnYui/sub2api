# 主分支同步私有 GitHub 仓库

## 同步范围

- 当前本地 `main` 已包含所有本地 `codex/*` 分支的历史提交；这些分支均为 `main` 的祖先，不需要额外合并。
- 工作区已审计的源码、部署配置和上下文文档共 28 个文件，提交为 `01180abc5`（`feat: 增加管理员取消余额套餐入口`）。
- 未将公共 `upstream/main` 的新提交混入私有仓库；本次目标是同步用户自己的 `fork` 私有仓库。

## 推送结果

- 远程：`fork`，地址为 `https://github.com/cnYui/sub2api.git`。
- 推送方式：`git push fork main`，从 `c7b32febc` 快进到 `01180abc5`，未使用强制推送。
- 推送后本地 `main` 与远程 `fork/main` 指向同一提交，工作区无未提交改动。

## 验证

- 远程 `refs/heads/main` 回读为 `01180abc5f39ff017eea50198f93103c790a9301`。
- 与本次业务改动相关的 Go 服务测试、管理员接口测试和前端类型检查均已通过；生产镜像构建及公网健康检查也已在此前发布记录中完成。
