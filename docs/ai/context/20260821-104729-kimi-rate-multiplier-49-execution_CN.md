# Kimi 分组倍率调整为 4.9x

## 变更内容

- 执行时间：2026-08-21 10:47（Asia/Tokyo）。
- 目标生产分组：`groups.id=7`，当前名称为 `Kimi0.7倍率`。
- 将 `rate_multiplier` 从 `0.7000` 更新为 `4.9000`。
- Kimi 校准基础价保持不变：
  - `kimi-k3`：`$3.00 / $15.00 / $0.30`。
  - `kimi-k2.6`：`$0.95 / $4.00 / $0.16`。
  - `kimi-k2.5`：`$0.60 / $3.00 / $0.10`。
- 按 `4.9x` 计算的模型广场实付价：
  - `kimi-k3`：`$14.70 / $73.50 / $1.47`。
  - `kimi-k2.6`：`$4.655 / $19.60 / $0.784`。
  - `kimi-k2.5`：`$2.94 / $14.70 / $0.49`。
- 分组名称暂未改动，因此名称中的 `0.7` 与实际倍率 `4.9x` 不一致；本次只按用户明确要求修改数值倍率。
- 未修改历史用量、订单、余额、用户专属倍率、账号统计倍率或最终计费倍率。

## 验证结果

- PostgreSQL 事务更新成功，既有 `trg_groups_auth_cache_invalidation` 触发器正常触发。
- `user_group_rate_multipliers` 中 `group_id=7` 的用户专属覆盖数为 `0`。
- 后端 Kimi 模型广场定向测试通过。
- 前端模型广场测试通过：`PlazaModelPricingTable.spec.ts`，14 项全部通过。
- 本地与公网模型广场接口均返回倍率 `4.9` 及上述实付价格。
- 应用容器保持 healthy，本地、Nginx 和三个公网健康端点均返回 HTTP 200。
