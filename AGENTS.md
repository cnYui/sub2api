# AI 协作入口

> 最新压缩记忆见 `docs/ai/context/20260624-195608-agents-memory-compressed_CN.md`。
> 早期压缩记忆见 `docs/ai/context/20260619-151920-sub2api-compressed-memory_CN.md`。
> 后续长期上下文统一新建到 `docs/ai/context/YYYYMMDD-HHMMSS-*.md`，不要覆写、重命名或删除历史文档。

## 最高优先级定论

- 2026-07-08 已在本地 `main` 完成后端测试 stub 卫生优化且未影响 18084 公网服务：`backend/Makefile` 新增 `test-server-unit`，固定 `go test -count=1 -tags=unit ./internal/server`；`api_contract_test.go` 新增 stub 误调用测试，并把 `CalibrateActiveDailyUsageWindows`/`CountStaleActiveDailyWindows` 从空成功改为 `errors.New("not implemented")`。已按 TDD 验证红绿，`make -C backend test-server-unit`、目标 unit、`go test -count=1 -tags=unit ./...`、`go test -count=1 ./...`、`git diff --check` 均通过；未构建镜像、未重启容器、未改 nginx/DB/Redis、未部署、未提交 git。结果见 `docs/ai/context/20260708-172141-test-stub-hygiene-optimization-result_CN.md`。
- 2026-07-08 已写后端测试 stub 卫生优化设计：推荐在 `backend/Makefile` 增加 `test-server-unit`，固定使用 `go test -count=1 -tags=unit ./internal/server`；同时把 `api_contract_test.go` 中 `CalibrateActiveDailyUsageWindows` 与 `CountStaleActiveDailyWindows` stub 从空成功改为 `errors.New("not implemented")`，让误调用失败优先。设计见 `docs/ai/context/20260708-170920-test-stub-hygiene-optimization-design_CN.md`。

- 2026-07-08 本地分支 `codex/east8-subscription-auto-refresh` 已完成东八区订阅日用量自动刷新重构：删除 `CheckBillingEligibility` API 入口 DB 写入式惰性刷新，改为只读窗口归一化准入；新增完成时记账窗口自愈，跨零点按 `usage_logs.created_at` / `CompletedAt` 计入当天；新增 active 订阅后台 daily window 校准 scheduler，按今天 usage_logs 聚合分批推进 stale active 订阅、失效 billing cache，并在单轮结束仍有 stale remaining 时输出 `ALERT` 日志；`/v1/usage` 支持自动 Key 并做订阅窗口展示归一化。已通过 Wire 生成、后端 unit、repository integration、`go test ./cmd/server`、前端 typecheck、`git diff --check`；未提交 git、未构建镜像、未部署公网。结果见 `docs/ai/context/20260708-112027-east8-subscription-auto-refresh-result_CN.md`。
- 2026-07-08 已写东八区订阅日用量自动刷新实施计划：按 TDD 拆为完成时间字段、完成时订阅扣费窗口自愈、active 订阅后台校准仓储、删除 API 入口写库刷新并改只读归一化准入、后台 scheduler 服务、`/v1/usage` 自动 Key 与展示归一化、整体回归和上线验收；实施时默认不提交 git，除非用户明确要求。计划见 `docs/ai/context/20260708-092157-east8-subscription-auto-refresh-implementation-plan_CN.md`。
- 2026-07-08 已定东八区订阅日用量自动刷新重构设计：删除 `CheckBillingEligibility` 的 API 入口 DB 写入式惰性刷新，新增东八区 00:00 后台任务主动推进所有 active 订阅窗口；后台任务需多实例锁、重试、stale 监控和告警；保留完成时记账窗口自愈，跨零点请求按完成并写 `usage_logs` 的时间计入当天额度；同时修 `/v1/usage` 自动 Key 和窗口归一化，确保前端只展示今天真实窗口。设计见 `docs/ai/context/20260708-101716-east8-subscription-auto-refresh-design_CN.md`。
- 2026-07-08 已只读核查当前公网 18084 东八区订阅日窗口刷新：代码确实实现 API 计费入口惰性刷新，但不是 00:00 全量后台任务；当前 52 个未过期 active 订阅中 38 个 `daily_window_start` 仍早于 `2026-07-08 00:00:00+08`，25 个保留昨天非零 `daily_usage_usd`，所以“所有用户真实写库刷新”不成立。今天真正发生订阅扣费的 13 个 active 订阅均已刷新到今天 0 点，且 `user_subscriptions.daily_usage_usd` 与今天 `usage_logs.total_cost` 聚合一致，未发现今天扣费但订阅表不展示的错账；`/subscriptions` 与管理订阅列表会返回前归一化旧窗口，不会把昨天加进今天展示。独立风险：当前 68 个 active API Key 全是自动 Key（`group_id=NULL`），`/v1/usage` 不在自动 Key endpoint policy 中，Key Usage 页可能被拒或拿不到订阅额度块；固定分组路径也不会触发窗口刷新，需单独修。结果见 `docs/ai/context/20260708-095354-east8-subscription-daily-window-refresh-audit_CN.md`。
- 2026-07-08 已修正公网 18084 的 2026-07-07 旧邀请返利：写库前备份 `deploy/backups/20260708-100436-sub2api-candidate-before-affiliate-recalc.dump`；删除旧 20% 返利流水 `15.80/19.80` 和已转余额 `19.80`，按当前 8%、单被邀请用户上限 100 元重算为 `6.32/7.92`，按原订单返利时间冻结 24 小时，两个邀请人当前 `aff_frozen_quota=6.32/7.92`、`balance=0`；旧金额残留数为 0，18084/8080 health 正常。同轮本地修复实质漏返 bug：支付宝 `traffic_pack` 履约原先未调用 `applyAffiliateRebateForOrder()`，会导致被邀请用户买 GPT 流量包不返利；已按 TDD 补测试并修复，后端 service 单测和前端 aff_code 传递目标测试通过。代码修复尚未部署公网。结果见 `docs/ai/context/20260708-101706-affiliate-rebate-recalculation-and-traffic-pack-bugfix-result_CN.md`。
- 2026-07-08 已修复用户 `/affiliate` 页返利金额币种展示：可转返利额度、历史返利额度，以及同页冻结额度、邀请明细返利和转入成功提示均使用人民币窄符号 `¥`；仅改前端展示，不改后端返利计算和 API 字段。计划见 `docs/ai/context/20260708-101332-affiliate-rebate-rmb-symbol-ui-plan_CN.md`。
- 2026-07-08 本地分支 `codex/rmb-balance-payment-affiliate-rebate` 已完成人民币余额支付与邀请返利重构：余额充值支付宝-only 且 1:1 人民币入账；用户购买页只展示支付宝和余额；余额支付套餐/流量包走 `payment_type=balance` 完成订单并事务内扣 `pay_amount`，不产生返利；支付宝完成订单按 `amount` 返利，默认 8%、冻结 24 小时、有效期 365 天、单被邀请用户累计上限 ¥100；订单展示与余额文案改为人民币，后台订单筛选补 `balance`/`traffic_pack`。已通过后端 unit/migrations、前端 typecheck/目标 vitest/build、`go test ./cmd/server`；未部署公网、未执行本地浏览器手工验收。结果见 `docs/ai/context/20260708-090140-rmb-balance-payment-affiliate-rebate-result_CN.md`。
- 2026-07-07 已写人民币余额支付与邀请返利重构实施计划：计划按 TDD 拆为订单类型/金额口径、外部支付支付宝-only、余额支付事务、邀请返利默认值、前端充值/余额支付、订单展示、人民币文案和全量验收；执行时必须先修正 `users.balance` 透支扣减风险，余额购买只能条件扣款成功后发货。计划见 `docs/ai/context/20260707-230444-rmb-balance-payment-affiliate-rebate-implementation-plan_CN.md`。
- 2026-07-07 已定人民币余额支付与邀请返利重构方案：`users.balance` 统一视为人民币余额；用户侧 `/purchase` 只展示支付宝与余额，余额充值卡固定为商品列表第一张，充值金额 1-100 整数、默认 1 元、只走支付宝且无手续费；商品购买保留支付宝直付和余额支付，余额不足直接进入充值确认页，不做混合支付。余额支付购买套餐/流量包要生成 `payment_type=balance` 完成订单并扣含手续费 `pay_amount`，但不产生返利；支付宝余额充值、支付宝直购套餐和流量包产生邀请返利，返利比例 8%、冻结 24 小时、有效期 365 天、单个被邀请用户累计上限 ¥100，返利基数为 `amount` 而非含手续费 `pay_amount`。同轮还要求修复 `order_type` 非法值白名单和后台订单筛选缺少 `traffic_pack`。设计见 `docs/ai/context/20260707-225836-rmb-balance-payment-affiliate-rebate-design_CN.md`。
- Sub2API 是唯一公网 API 入口，也是唯一用户 Key、计费和用量事实源。
- CLIProxyAPI 只作为内网账号池、OAuth、协议转换和轮询上游；yui.web/shop 只保留展示、说明和跳转。
- 当前主链路：`Cloudflare Tunnel -> nginx 127.0.0.1:8080 -> sub2api-candidate 127.0.0.1:18084 -> CLIProxyAPI 127.0.0.1:8317`。
- 不要让 Sub2API、yui.web、CLIProxyAPI 同时对同一个用户 Key 做状态判定或扣费。
- 不要在文档、提交或日志中记录完整 API Key、内部 token、HMAC secret、SMTP 密码。

## 当前运行态提醒

- `aaccx.pw/shop` 归 yui.web；`aaccx.pw/v1/*`、`/api/*` 和 Sub2API 控制台路由归 Sub2API；`api.aaccx.pw` 也是 Sub2API 入口。
- 2026-06-27 当前公网链路已由 18084 候选环境承接：`Cloudflare Tunnel -> nginx 127.0.0.1:8080 -> sub2api-candidate 127.0.0.1:18084 -> CLIProxyAPI 127.0.0.1:8317`，容器内访问上游聚合入口为 `host.docker.internal:8317`；`sub2api-candidate` healthy，DB 为 `sub2api-candidate-postgres`，候选最新库为 47 users / 40 keys / 191 migrations；旧 `sub2api` 18080 已 `Exited (0)`，`weishaw/sub2api:latest` 镜像暂留；Nginx 反代全部指向 18084，`/purchase`、`/health`、`/v1/responses`、`/v1/chat/completions` 均为 200，LLM 真实回复已证明 `sub2api -> CLIProxyAPI -> 上游 OpenAI` 全栈打通。结果见 `docs/ai/context/20260627-102157-18084-public-candidate-chain-agents-update-result_CN.md`。
- 2026-06-27 已按用户要求停止旧链路残留容器：`sub2api-postgres`、`sub2api-redis`、`sub2api-preview`（原 18081）、`sub2api-main-existingdb-preview`（原 18083）；当前公网 `sub2api-candidate`、`sub2api-candidate-postgres`、`sub2api-candidate-redis` 仍 healthy，`18084/health` 与 `8080/health` 均为 200。结果见 `docs/ai/context/20260627-103515-stop-old-18080-db-redis-preview-result_CN.md`。
- 2026-06-27 蓝绿测试端口与邮箱验证码排查：`8080` 是 nginx 公网入口并反代 `18084`，`18084` 是当前公网候选，`18082` 是旧 main-preview；本地 main 前端建议用 `5174 -> 18082`，数据库/Redis 仅容器内使用 `5432/6379`，如需宿主机调试用 `15432/16379`。18084 验证码接口返回 200 但 worker 报 `EMAIL_NOT_CONFIGURED`，原因是候选库 SMTP 配置为空；18082 则是 `REGISTRATION_DISABLED`。结果见 `docs/ai/context/20260627-110649-blue-green-email-verify-port-plan_CN.md`。
- 2026-06-27 已另起 SMTP 测试实例 `sub2api-smtp-test`：`127.0.0.1:18085 -> 8080`，独立 DB `sub2api-smtp-test-postgres`、独立 Redis `sub2api-smtp-test-redis`；测试库已打开 `registration_enabled/email_verify_enabled/password_reset_enabled`，SMTP 真实账号仍为空。`20260619-220357-gmail-smtp-enabled-result_CN.md` 是本地 ignored 文档，未被 git 跟踪；Gmail SMTP 配置曾是运行态 DB settings，不是源码/迁移。结果见 `docs/ai/context/20260627-111200-smtp-test-port-18085-result_CN.md`。
- 2026-06-27 已在 18085 测试库写入 Gmail SMTP 非敏感字段：`smtp_host=smtp.gmail.com`、`smtp_port=587`、`smtp_username/smtp_from=xiaobianfuai@gmail.com`、`smtp_from_name=AACCX`、`smtp_use_tls=false`；`smtp_password` 仍需用户通过 Google App Password 获取后手动写入运行态，绝不能进 git。结果见 `docs/ai/context/20260627-113243-smtp-18085-gmail-config-result_CN.md`。
- 2026-06-27 已在 18085 测试库写入 Gmail App Password，脱敏状态为 `smtp_password=[CONFIGURED]`；`POST /api/v1/auth/send-verify-code` 返回 200，`sub2api-smtp-test` 日志确认 worker 已成功发送验证码邮件，不再报 `EMAIL_NOT_CONFIGURED`。密码明文不得进入文档、提交或日志摘要。结果见 `docs/ai/context/20260627-113800-smtp-18085-gmail-password-configured-result_CN.md`。
- 2026-06-27 已将本地蓝绿测试环境 `sub2api-smtp-test` 重新构建并替换为本地 `main` 最新版本 `e4704061d`，新镜像为 `sub2api-smtp-test:20260627-214036`，仍使用 `127.0.0.1:18085->8080`，保留原 18085 测试库与 Redis；`18085/health` 与公网候选 `18084/health` 均为 200，公网 `sub2api-candidate*` 未被替换或重启。结果见 `docs/ai/context/20260627-214649-18085-local-latest-restart-result_CN.md`。
- 2026-06-27 已按确认方案只替换公网链路中的 `sub2api-candidate` 应用容器镜像，保留 `sub2api-candidate-postgres`、`sub2api-candidate-redis`、nginx 指向和 18085 测试栈；当前 `sub2api-candidate` 镜像为 `sub2api-candidate:20260627-214036-e4704061d`，digest `sha256:00f926480c5f01e59a06796a1f8e9bfe569598d40e2a236a266880702d7bb4bc`，端口仍为 `127.0.0.1:18084->8080`。替换前已备份候选库到 `deploy/backups/20260627-215617-sub2api-candidate-before-app-image-replace.dump`；替换前后 `schema_migrations` 均为 191；`18084/health`、`8080/health`、`api.aaccx.pw/health`、`aaccx.pw/dashboard` 和 `/purchase` 均验证正常。结果见 `docs/ai/context/20260627-215859-18084-app-image-replace-result_CN.md`。
- 2026-06-27 对 `2799523972@qq.com` 真实公网 API Key 实测：用户余额为 0，订阅已软删除，仅有 OpenAI 流量卡 15 USD；使用其 active Key 请求 `https://api.aaccx.pw/v1/chat/completions` 返回 `403 SUBSCRIPTION_NOT_FOUND`，流量卡余额仍为 15 USD，`traffic_credit_ledger` 无 deduction，`usage_logs`/`billing_usage_entries` 仍为 0。根因是认证中间件仍在 `api_key_auth.go:153-162` 对订阅型 group 无 active subscription 直接硬拒绝，未进入 `BillingCacheService.CheckBillingEligibility()`；Google 中间件 `api_key_auth_google.go:81-90` 也有同类硬拒绝。结果见 `docs/ai/context/20260627-220815-2799523972-traffic-card-public-api-test-result_CN.md`。
- 2026-06-27 已修复“订阅已取消但有 OpenAI/GPT 流量卡仍不能用”的路径：`api_key_auth.go` 和 `api_key_auth_google.go` 在订阅型 group 找不到 active subscription 时不再直接 403，而是让请求进入 handler 中的 `BillingCacheService.CheckBillingEligibility()`；非 `ErrSubscriptionNotFound` 的订阅加载错误仍返回失败。新增中间件回归测试，`go test -count=1 -tags=unit ./internal/server/middleware` 与 `go test -count=1 -tags=unit ./internal/service` 均通过。已构建 `sub2api-traffic-card-fix:20260627-221441`，digest `sha256:299560875687ba0fc7c9b9703a5bece639a832c35720fb6ce47f8dd222483e22`，并替换 18085 与 18084 应用容器；DB/Redis/nginx 未改。使用 `2799523972@qq.com` 的真实公网 Key 请求 `/v1/responses` + `gpt-5.5` 返回 200，流量卡余额从 15.0000000000 降至 14.9959290000，新增 deduction `0.0040710000`。结果见 `docs/ai/context/20260627-222742-traffic-card-without-subscription-fix-result_CN.md`。
- 2026-06-27 对 `cnfoxian@gmail.com` 做额度耗尽后公网实测：公网 18084 当前运行 `sub2api-candidate:20260627-221441-traffic-card-fix`，该用户 active subscription 今日 `daily_usage_usd=19.0282566500` 已超过 `daily_limit_usd=19.00000000`，余额为 0，OpenAI 流量卡请求前剩余 `9.8976433500`。使用其真实公网 Key 请求 `/v1/responses` + `gpt-5.5` 返回 200；请求后流量卡剩余 `9.8934673500`，新增 `traffic_credit_ledger` deduction `0.0041760000`，新增 `usage_logs.id=25156` 且 `subscription_id=NULL`，订阅 `daily_usage_usd` 未继续增加。结果见 `docs/ai/context/20260627-223338-cnfoxian-traffic-card-quota-exhausted-public-test-result_CN.md`。
- 2026-06-28 公网注册邮箱验证码无法发送排查：18084 `sub2api-candidate` 已运行最新 `traffic-card-fix` 镜像，但公网库 `sub2api-candidate-postgres` 中 SMTP 配置为空：`smtp_host/smtp_username/smtp_from/smtp_password` 均为空；日志显示验证码任务入队成功后 EmailQueue worker 报 `EMAIL_NOT_CONFIGURED`。18085 测试库 `sub2api-smtp-test-postgres` 仍有 Gmail SMTP 配置和 App Password。根因是 SMTP 是运行态 DB settings，不随镜像/源码替换进入 18084；之前修复发生在 18085 测试库，18084 保留数据层时未同步该配置。结果见 `docs/ai/context/20260628-152717-public-18084-email-verify-smtp-not-configured-diagnosis_CN.md`。
- 2026-06-28 已将 18085 测试库的 SMTP 运行态配置同步到 18084 公网候选库，只更新 `settings` 表 7 个 SMTP key，不重建镜像、不重启容器、不改 DB/Redis/nginx；`smtp_from_name` 按用户要求设为 `天才程序员小站`，`smtp_password` 仅以脱敏状态确认。公网 `POST https://api.aaccx.pw/api/v1/auth/send-verify-code` 返回 200，`sub2api-candidate` 日志确认 EmailQueue worker 已成功发送验证码，不再报 `EMAIL_NOT_CONFIGURED`；`18084/health`、`8080/health`、`api.aaccx.pw/health` 均为 200。结果见 `docs/ai/context/20260628-153845-sync-18085-smtp-config-to-18084-result_CN.md`。
- 2026-06-28 已按用户要求关闭第三套本地残留 `main-preview` 栈：`sub2api-main-preview`、`sub2api-main-preview-postgres`、`sub2api-main-preview-redis` 均为 `Exited (0)`；只执行 `docker stop`，未删除容器或 volume。当前仍运行 18084 公网候选栈和 18085 SMTP 测试栈，`18084/health`、`18085/health`、`8080/health` 均为 200。结果见 `docs/ai/context/20260628-154934-stop-main-preview-stack-result_CN.md`。
- 2026-06-28 已按用户要求关闭 18085 SMTP 测试栈：`sub2api-smtp-test`、`sub2api-smtp-test-postgres`、`sub2api-smtp-test-redis` 均为 `Exited (0)`；只执行 `docker stop`，未删除容器或 volume。当前仅保留 18084 公网候选栈运行，`18084/health`、`8080/health`、`api.aaccx.pw/health` 均为 200。结果见 `docs/ai/context/20260628-155809-stop-smtp-test-stack-result_CN.md`。
- 2026-06-28 已启动本地 main-preview 蓝绿测试栈：用当前本机 `main` HEAD `eb95da02c` 构建镜像 `sub2api-main-preview:20260628-173235-eb95da02c`，应用容器 `127.0.0.1:18080->8080`，连接独立 `sub2api-main-preview-postgres` 与 `sub2api-main-preview-redis`；旧停止中的 main-preview 应用容器被删除重建以改端口，数据卷保留。公网 `sub2api-candidate*` 未停止/重建/重命名，18084 与 nginx 8080 health 均为 200。结果见 `docs/ai/context/20260628-174205-main-preview-18080-bluegreen-result_CN.md`。
- 2026-06-28 已按确认方案只读 dump 18084 候选库并覆盖导入到 18080 main-preview：源 `sub2api-candidate-postgres` 只读导出，目标 `sub2api-main-preview-postgres` drop/recreate 后 restore，`sub2api-main-preview-redis` 已清空，`sub2api-main-preview` 重启后自动应用本地 main 新迁移，18080 当前为 49 users / 41 keys / 192 migrations，最新迁移 `156_seed_codex_79_subscription_plan.sql`。18084 `sub2api-candidate*` 未停止/重建/写入，18080/18084/8080 health 均为 200。敏感 dump 文件在 `deploy/backups/`，不要提交。结果见 `docs/ai/context/20260628-183052-clone-18084-db-to-18080-preview-result_CN.md`。
- 2026-06-28 已将 18080 main-preview 更新到当前本机 `main` HEAD `befa8f138`，最终镜像为 `sub2api-main-preview:20260628-195911-befa8f138-migrationfix`，端口仍为 `127.0.0.1:18080->8080`，保留 `sub2api-main-preview-postgres` 与 `sub2api-main-preview-redis`。启动中发现 `156_seed_codex_79_subscription_plan.sql` 被提交 `2f0ac55c7` 改动导致 checksum mismatch；已恢复 156 原始 seed 价 `79.79`、保留 157 修正基础价到 `79.00`，并修正 156 checksum 兼容规则。18080 当前为 49 users / 41 keys / 194 migrations，`157` 与 `158` 已应用；18080/18084/8080 health 均为 200，18084 `sub2api-candidate*` 未停止/重建/写入。结果见 `docs/ai/context/20260628-201022-main-preview-restart-latest-main-result_CN.md`。
- 2026-06-28 已将 `codex/enable-affiliate-hide-register-promo` 合并进本地 `main`：注册页手填优惠码入口固定隐藏，`aff_code` 仍通过链接/OAuth/session 传递；邀请返利默认开启并新增 `158_enable_affiliate_default.sql`。同批本地 main 已分批提交 79 元套餐基础价修复和上下文文档；合并后 `git diff --check`、后端 service/repository/migrations 测试、前端注册页/settings/store/购买相关测试均通过。结果见 `docs/ai/context/20260628-193500-merge-affiliate-worktree-and-main-batches-result_CN.md`。
- 2026-06-28 注册重复邮箱预检拦截已实现：新增 `POST /api/v1/auth/precheck-register` 只读校验注册开放、保留邮箱、邮箱后缀和邮箱唯一性，不生成验证码、不写验证码缓存、不入队邮件；前端注册页在邮箱验证开启时预检通过后才写 `register_data` 并跳转 `/email-verify`，重复邮箱停留在注册页报错。结果见 `docs/ai/context/20260628-203901-register-duplicate-email-precheck-result_CN.md`。
- 2026-06-28 已将 18080 main-preview 更新到当前本机 `main` HEAD `399f278f3`，最终镜像为 `sub2api-main-preview:20260628-205543-399f278f3`，digest `sha256:46b73dddfcd4f916b54dfbcb584f5f665e084ffb3759d9b4d607b3d73e5002f9`，端口仍为 `127.0.0.1:18080->8080`，保留 `sub2api-main-preview-postgres` 与 `sub2api-main-preview-redis`；18080 health 为 200，前端资源为 `index-BVRJ38ir.js` / `index-nffSQZgD.css`，preview 库为 194 migrations，最新 `158_enable_affiliate_default.sql`。18084 `sub2api-candidate*` 未停止/重建/写入，候选应用仍为 `sub2api-candidate:20260627-221441-traffic-card-fix` 且 DB 仍为 191 migrations；18084/8080 health 均为 200。本轮还补了 Docker/pnpm 构建兼容：`.dockerignore` 排除 `deploy/backups/`，Dockerfile 前端依赖安装层复制 `.npmrc`，新增 `frontend/pnpm-workspace.yaml` 给本地 pnpm v11 批准 `esbuild/vue-demi` 构建脚本。结果见 `docs/ai/context/20260628-212254-main-preview-restart-399f278f-result_CN.md`。
- 2026-06-29 已将本地未提交/未跟踪内容与所有未合并本地分支收敛进 `main`：包括 deploy runtime scripts、GPT 流量包购买修复、候选预演脚本、订阅用量页迁移和 usage-guide 99/图生图分支；合并后 `git branch --no-merged main` 为空。冲突取舍为保留当前统一商品卡片、支付直达流程、usage-guide Base URL/相对路径/完整 URL 拆分说明和新版 AGENTS 压缩入口；未触碰 18084 公网运行链路。结果见 `docs/ai/context/20260629-091206-merge-local-work-and-branches-result_CN.md`。
- 2026-06-29 已将 18080 main-preview 更新到合并后本地 `main` HEAD `ddd4fb9a9`，最终镜像为 `sub2api-main-preview:20260629-092226-ddd4fb9a9`，digest `sha256:1cc424ad95735188f6c707c5c1b19af42b670a2c332c863ef8e8a9cc4ddb563b`，端口仍为 `127.0.0.1:18080->8080`，保留 `sub2api-main-preview-postgres` 与 `sub2api-main-preview-redis`；18080 health 为 200，前端资源为 `index-DHHTg5y4.js` / `index-B--asykV.css`，preview 库为 194 migrations，最新 `158_enable_affiliate_default.sql`。18084 `sub2api-candidate*` 未停止/重建/写入，候选应用启动时间未变化；18084/8080 health 均为 200。结果见 `docs/ai/context/20260629-093900-main-preview-restart-merged-main-result_CN.md`。
- 2026-07-01 已备份当前公网候选数据层：PostgreSQL dump 为 `deploy/backups/20260701-080310-sub2api-candidate-postgres-before-app-promote.dump`，Redis RDB 为 `deploy/backups/20260701-080310-sub2api-candidate-redis-before-app-promote.rdb`，二者权限均为 `600` 且已校验可读；备份后 `18084/health` 与 `8080/health` 均为 200，未停止/重建 `sub2api-candidate*` 或 nginx。结果见 `docs/ai/context/20260701-080310-public-candidate-db-backup-result_CN.md`。
- 2026-07-01 已写 18080 preview 发布到公网 18084 的设计：推荐只替换 `sub2api-candidate` 应用容器为 18080 已验证镜像，保留 `sub2api-candidate-postgres` 与 `sub2api-candidate-redis`，让新应用启动时在公网 DB 上自动应用 `156/157/158` migration；当前差异主要是新增/修正 `79 元订阅池`、`codex-pool-69-usd` 和 `affiliate_enabled=true`，不要用 18080 DB 覆盖公网 DB，也不要改 nginx 指向 18080。设计见 `docs/ai/context/20260701-080725-promote-18080-preview-to-public-18084-design_CN.md`。
- 2026-07-02 当前公网入口实际为 `api.aaccx.pw -> nginx 127.0.0.1:8080 -> sub2api 127.0.0.1:18080`，运行容器为 `sub2api`、`sub2api-postgres`、`sub2api-redis`；已备份 `sub2api-postgres` 到 `deploy/backups/20260702-125423-sub2api-before-user-expiry-plus-10usd-traffic-card.dump`，并按批次 `grant-20260702-10usd-day30` 给 52 个未删除用户各发放 1 张 10 USD OpenAI/GPT 流量卡，新增 52 条完成态系统订单、52 条 purchase 流水，总额 520 USD；同时将 36 条未删除 active 订阅 `expires_at` 增加 1 天。`18080/health`、`8080/health`、`api.aaccx.pw/health` 均为 200。结果见 `docs/ai/context/20260702-132616-user-expiry-plus-10usd-traffic-card-result_CN.md`。
- 2026-07-02 已按用户要求使用当前本地 `main` HEAD `6ef887a8d` 重新构建 `weishaw/sub2api:latest` 并只替换公网 `sub2api` 应用容器，保留 `sub2api-postgres` 与 `sub2api-redis`；新镜像 ID 为 `sha256:b195fcb03a925a0adda2026d75f6be42111162d2f7a023a74f4d44b80512a287`，公网资源已从 `assets/app-index-DKtOnsbF.js` / `assets/index-BMta9z_W.css` 更新为 `assets/app-index-DBKABD7p.js` / `assets/index-B--asykV.css`，CSS 包含 `z-index:35`；`18080/health`、`8080/health`、`api.aaccx.pw/health` 均为 200。结果见 `docs/ai/context/20260702-134828-main-rebuild-public-redeploy-result_CN.md`。
- 2026-07-02 公网随机用户 API Key 真实请求 `/v1/responses` 均为 200；99 元用户 `2864533153@qq.com` 初始返回 503，日志为 `openai.account_select_failed: no available accounts`，根因是唯一上游账号 `cliproxy-local-openai` 未绑定新套餐组 `codex-pool-89-usd` 与 `codex-pool-69-usd`。已备份公网库到 `deploy/backups/20260702-140054-sub2api-before-79-99-account-binding.dump`，并幂等新增 `account_groups` 绑定 `account_id=1 -> group_id=8/9`；修复后 99 元用户 Key 请求 `gpt-5.5` 返回 200，`usage_logs` 记录 `user_id=54/group_id=8/account_id=1`。当前无 active 79 元订阅用户可做用户 Key 真实请求，但绑定层已补齐。结果见 `docs/ai/context/20260702-140327-public-random-and-99-api-key-request-result_CN.md`。
- 2026-07-03 流量卡“我的订阅”显示排查：当前 nginx 实际将 `api.aaccx.pw` 和 `aaccx.pw` 的 Sub2API API 路由反代到 `127.0.0.1:18084`，对应 `sub2api-candidate-postgres`；不要把 `sub2api-postgres/18080` 的 2026-07-02 批量发卡结果误认为当前公网数据。`2799523972@qq.com` 真实登录后 `/subscriptions` 会显示 `GPT 流量包` 汇总 `$15.00`，但当前 18084 只有旧 10 USD + 5 USD 两张卡，缺少 2026-07-02 新 10 USD 卡，且用户侧 Key 列表和可绑定分组均为空；`1038686518@qq.com` 在 18084 无可用流量卡。公网 18084 流量卡计费链路本身已用无 active 订阅但有 active Key 的用户请求 `/v1/responses` 验证可扣费。结果见 `docs/ai/context/20260703-112900-traffic-card-subscriptions-page-diagnosis-result_CN.md`。
- 2026-07-03 已将 `2799523972@qq.com` 修复为正常可用的 79 元套餐用户：执行前备份 18084 库到 `deploy/backups/20260703-113109-sub2api-candidate-before-fix-2799-79-subscription.dump`；新增 active 订阅 `user_subscriptions.id=71 -> group_id=9/codex-pool-69-usd`，到期 `2026-08-02T10:35:29+08:00`；旧 `Codex_used` Key 的 key 字段已是 `__deleted__...` 占位值，不能恢复使用，已保持删除态；通过用户 API 新建 active Key `api_keys.id=65 -> group_id=9`。保留 GPT 流量包 `14.995929 USD` 未变；真实 `/v1/responses` 请求 200，走 `subscription_id=71/billing_type=1`，订阅日用量增加 `0.004096`，流量卡未扣；18084/8080/api.aaccx.pw health 均为 200。结果见 `docs/ai/context/20260703-113743-fix-2799-79-subscription-user-result_CN.md`。
- 2026-07-03 已给当前公网 18084 的 52 个未删除用户补发一张 10 USD OpenAI/GPT 流量卡：执行前备份 `sub2api-candidate-postgres` 到 `deploy/backups/20260703-114705-sub2api-candidate-before-18084-10usd-grant.dump`；批次号 `grant-20260703-10usd-18084-current-users`，最终 52 条 `payment_orders`、52 张 `user_traffic_credits`、52 条 `traffic_credit_ledger` purchase 流水，合计 520 USD。因 PostgreSQL 数据修改 CTE 快照规则，订单、卡、流水分三步用同一批次号幂等补齐。补发后 `1038686518@qq.com` 有 10 USD 卡，`2799523972@qq.com` 流量卡余额为 `24.995929 USD`，浏览器 `/subscriptions` 显示 `GPT 流量包 $25.00` 与 `codex-pool-69-usd`；18084/8080/api.aaccx.pw health 均为 200。结果见 `docs/ai/context/20260703-115114-18084-all-users-10usd-traffic-card-grant-result_CN.md`。
- 2026-07-04 `441565547@qq.com` 连续三次 `gpt-image-2` 生图失败排查：请求体只有约 3.3KB，不是提示词过长；三次 `/v1/images/generations` 均在约 125 秒后返回 502，Sub2API 转发 `host.docker.internal:8317` 报 `context canceled`，CLIProxyAPI 对应错误日志也为 `context canceled`，更像调用方或中间链路超时取消。结果见 `docs/ai/context/20260704-163929-gpt-image-2-user-441565547-timeout-diagnosis-result_CN.md`。
- 2026-07-04 本次 `gpt-image-2` 约 120 秒超时若要放大，优先改调用方 `ylcraft.openai_sdk_image` / provider `aaccx-gpt-image-2` 的 OpenAI Python SDK `AsyncOpenAI(timeout=...)` 或请求级 timeout；Sub2API 的 `GATEWAY_IMAGE_STREAM_DATA_INTERVAL_TIMEOUT=900` 是图片流数据间隔，CLIProxyAPI 生图路径 HTTP client 当前 `timeout=0`，都不像该 120 秒来源。结果见 `docs/ai/context/20260704-165832-gpt-image-2-120s-timeout-modification-point_CN.md`。
- 2026-07-07 已补 `gpt-image-2` 生图 timeout 改为 300 秒的具体操作说明：应在部署 `aaccx-gpt-image-2` provider 的 Python 服务中修改 `ylcraft/openai_sdk_image.py` 的 `AsyncOpenAI(timeout=300.0)` 或 provider 配置 `timeout: 300`，不在当前 Sub2API 仓库修改。结果见 `docs/ai/context/20260707-102912-gpt-image-2-timeout-300-change-guide_CN.md`。
- 2026-07-06 已将本地 `main` HEAD `9966c6f5b`（feat: finish automatic api key effective group）发布到公网：只替换 `sub2api-candidate` 应用容器，保留 `sub2api-candidate-postgres` 与 `sub2api-candidate-redis`；新镜像 `sub2api-candidate:20260706-080023-9966c6f5b-local-main`，digest `sha256:3273f560b6854d1a4887704c46c4131da4082f923bf1b45e0443562037d4315e`；发布前备份 Postgres dump（29MB）和 Redis RDB（69KB）到 `deploy/backups/`。发布后 DB 从 194 migrations 升至 195（`159_auto_api_key_effective_group.sql`），新增 `traffic-pack-openai` 分组（id=10, openai, active, exclusive, allow_image_generation=true），1 个 OpenAI 上游账号已绑定该分组，52 个用户 API Keys 全部迁移为 `group_id=NULL`（自动 Key）；`18084/health`、`8080/health`、`api.aaccx.pw/health` 均为 200。未修改 nginx，未重建 Postgres/Redis，未执行回滚。结果见 `docs/ai/context/20260706-081100-public-main-redeploy-result_CN.md`。
- 2026-07-06 `3876129758@qq.com` 套餐额度排查：当前 18084 公网库中用户 `id=56` 为 `codex-pool-19-usd` active 订阅，日窗口已刷新到 `2026-07-06 00:00:00+08`，不是昨天残留未清零；今天 `00:23:00-00:48:35+08` 之间 64 次成功请求累计消耗 `12.4792935000` USD，当前剩余约 `6.5207065` USD，无流量卡、无结构化限额拒绝记录。结果见 `docs/ai/context/20260706-100145-3876129758-quota-usage-diagnosis-result_CN.md`。
- 2026-07-06 已处理公网 18084 订阅 stale 日窗口运行态数据：先手动清零 3 位 stale 超限用户，后按“没有今天成功用量则清零、有今天成功用量则写回今天 usage_logs 聚合值”的规则全量校准 34 条 active 订阅，最终 stale 窗口数为 0，并删除所有 `billing:sub:*` 订阅缓存让后续请求回源重建；执行前分别备份候选库。结果见 `docs/ai/context/20260706-105241-reset-three-stale-daily-windows-result_CN.md` 与 `docs/ai/context/20260706-105804-reset-all-stale-daily-windows-preserve-today-result_CN.md`。
- 2026-07-06 本地 `main` 已实现订阅 API 入口窗口刷新：`BillingCacheService` 在套餐限额判断前按 `timezone.StartOfDay/StartOfWeek` 和 30 天滚动月窗口刷新 DB，Redis `billing:sub:*` 缓存新增 daily/weekly/monthly window 字段，旧缓存缺字段时回源 DB；新增条件式 `RefreshExpiredUsageWindows` 防止并发重复清零。已通过 `go test -count=1 -tags=unit ./internal/service ./internal/repository`；未部署公网。结果见 `docs/ai/context/20260706-101719-subscription-window-refresh-result_CN.md`。
- 2026-07-07 已写订阅窗口刷新修复的公网重启计划：继续采用只替换 `sub2api-candidate` 应用容器、保留 `sub2api-candidate-postgres` 与 `sub2api-candidate-redis`、不改 nginx 的方式；计划包含本地验证、Docker 构建、Postgres/Redis 备份、发布前后 DB 快照、旧容器 env 临时保存、应用层回滚和谨慎数据层回滚步骤。尚未执行构建、备份、替换或公网迁移。计划见 `docs/ai/context/20260707-082618-public-subscription-window-refresh-redeploy-plan_CN.md`。
- 2026-07-07 已将本地 `main` HEAD `74e5a4bb0`（包含 `d6e337238` 订阅窗口刷新修复）发布到公网：只替换 `sub2api-candidate` 应用容器，保留 `sub2api-candidate-postgres` 与 `sub2api-candidate-redis`，未修改 nginx 或 Cloudflare Tunnel；新镜像 `sub2api-candidate:20260707-084458-74e5a4bb0-subscription-window-refresh`，image id `sha256:82406485ee568f033aca7cc6ed3291a262be37329de19fbecab151792e879e5a`。发布前备份候选 Postgres 与 Redis 到 `deploy/backups/`，发布后 `18084/health`、`8080/health`、`api.aaccx.pw/health`、`aaccx.pw/dashboard`、`aaccx.pw/purchase` 均为 200，migration 保持 195。结果见 `docs/ai/context/20260707-084700-public-subscription-window-refresh-redeploy-result_CN.md`。
- 2026-07-07 使用测试账号 `2799523972@qq.com` 对当前公网 18084 做真实验收：购买页 29/39/59/79/99 套餐与 5/10/20 刀 GPT 流量卡均可见，10 刀流量卡可进入支付宝确认页但未付款；前端成功创建自动分组临时 Key（`group_id=NULL`），请求 `https://api.aaccx.pw/v1/responses` + `gpt-5.5` 返回 200/`OK`，`usage_logs.id=55398` 解析到 `group_id=9/subscription_id=71/billing_type=1`，扣 `codex-pool-69-usd` 套餐 `0.004031`，流量卡余额 `24.995929` 与 deduction 数不变；临时 Key 已前端删除并 DB 复核为软删除。结果见 `docs/ai/context/20260707-085709-2799-public-account-smoke-test-result_CN.md`。
- 2026-07-07 对截图中 4 个 active 订阅用户做只读排查：问题不是东八区 0 点计算错，而是老订阅 `daily_window_start IS NULL` 时当前 `RefreshExpiredUsageWindows` 只补窗口、不清零旧 `daily_usage_usd`；管理端列表也不会主动触发窗口刷新。`897858381@qq.com` 与 `qixiaocheng777@gmail.com` 页面用量含 2026-07-06 残留，`3056163754@qq.com` 窗口已是 2026-07-07 0 点但用量仍为昨日+今日，`daleselaji@gmail.com` 正常等于今日用量。结果见 `docs/ai/context/20260707-100704-four-users-usage-window-diagnosis-result_CN.md`。
- 2026-07-07 已按用户要求将当前公网 18084 所有 active 订阅的 `daily_usage_usd` 校准为今天 0 点后的 `usage_logs.total_cost` 聚合值：执行前备份 `sub2api-candidate-postgres` 到 `deploy/backups/20260707-101105-sub2api-candidate-before-daily-usage-calibration.dump`，修复前 38 条 active 订阅存在日窗口/日用量差异，事务内更新 38 条，删除当时已有 `billing:sub:*` 订阅缓存；修复后全量复查 `remaining_mismatch_count=0`，截图 4 位用户均只保留今天用量，18084/8080/api.aaccx.pw health 均为 200。结果见 `docs/ai/context/20260707-101401-active-subscriptions-daily-usage-calibration-result_CN.md`。
- 2026-07-07 已将订阅 daily 窗口 NULL 根因单独成文：当前 SQL 对 `daily_window_start IS NULL` 只补窗口、不清 `daily_usage_usd`，weekly/monthly 有同类模式；管理端列表不会触发 API 计费入口刷新，所以更容易暴露旧值。代码尚未修复，后续需覆盖 NULL 窗口初始化回归测试。说明见 `docs/ai/context/20260707-101847-subscription-daily-window-null-root-cause_CN.md`。
- 2026-07-07 已写订阅 NULL 窗口用量残留彻底修复设计：推荐同时修 `RefreshExpiredUsageWindows` 写路径、订阅列表/progress 展示归一化和上线前 `usage_logs` 校准检查；daily/weekly/monthly 的 `NULL + usage > 0` 都必须覆盖回归测试。设计见 `docs/ai/context/20260707-104040-subscription-null-window-refresh-fix-design_CN.md`。
- 2026-07-07 已写订阅 NULL 窗口用量残留修复实施计划：按 TDD 拆为仓储 integration 失败测试、service 读路径失败测试、SQL 与展示归一化最小实现、相关 unit/integration 验证和结果归档。计划见 `docs/ai/context/20260707-104609-subscription-null-window-refresh-fix-implementation-plan_CN.md`。
- 2026-07-07 已修复订阅 NULL 窗口用量残留：`RefreshExpiredUsageWindows` 对 daily/weekly/monthly 的 `NULL` 窗口初始化会同步清零 usage，订阅列表和 progress 读路径也会隐藏 `NULL + usage > 0` 的历史值；相关 unit 与新增仓储 integration 测试通过。结果见 `docs/ai/context/20260707-110703-subscription-null-window-refresh-fix-result_CN.md`。
- 2026-07-07 已做当前代码冗余与优化静态审查：优先问题是自动 API Key 语义和路由覆盖、API Key 认证中间件重复、订阅额度窗口规则分散；同时记录 handler 计费准入、OpenAI-only 路由、账号弹窗副本、SettingsView、gateway service、settings schema、部署脚本和套餐 seed 的后续收口建议。结果见 `docs/ai/context/20260707-090748-code-redundancy-review_CN.md`。
- 2026-07-07 已基于代码冗余审查写 P0/P1/P2/P3 治理设计：P0 收口自动 API Key 语义与路由策略；P1 收口 API Key 认证核心、订阅 usage window policy，并将部署脚本旧拓扑硬编码提升为生产操作风险；P2 处理 handler 计费准入、OpenAI-only route helper 和账号弹窗副本；P3 处理 SettingsView、gateway service 工具层、settings schema 与套餐 seed 模板化。设计见 `docs/ai/context/20260707-091540-code-redundancy-design_CN.md`。
- 2026-07-07 已在分支 `codex/code-redundancy-p0-auto-key` 完成 P0 自动 API Key 路由语义收口：新增 supported-endpoint policy wrapper，不支持的 `/v1beta` 与 `/antigravity/*` 自动 Key 返回明确 `AUTO_KEY_UNSUPPORTED_ENDPOINT`，fixed group Key 仍跳过 resolver；前端用户 Key 创建 API 移除 `groupId` 参数并新增 API 层测试。验证通过 middleware/routes Go unit、前端 keys spec 与 typecheck；未触碰公网运行态。结果见 `docs/ai/context/20260707-093135-code-redundancy-p0-auto-key-result_CN.md`。
- 2026-07-07 已继续完成代码冗余治理 P1/P2/P3：收口 usage window policy、部署 target、API Key 认证核心、OpenAI-only route helper、handler billing helper、账号弹窗 composable、套餐 seed 生成器、上游响应头 helper 和 Settings OpenAI fast policy payload helper；同轮修复本分支相关的 AccountUsageCell/KeysView 前端测试。后端核心包、前端 typecheck、相关前端切片、shell 与脚本测试通过；完整前端 `pnpm vitest run` 仍有 5 个未触碰文件的 10 个既有失败，需后续单独处理。结果见 `docs/ai/context/20260707-102315-code-redundancy-p1-p2-p3-result_CN.md`。
- 2026-07-05 仅购买 OpenAI/GPT 流量包、没有 active 订阅的新用户无法创建可用 OpenAI API Key 的问题真实存在：当前公网 nginx 仍指向 18084；18084 有 15 个 active 用户有 OpenAI 流量包余额但无 active subscription，按当前 `/groups/available` 规则均为 `available_total=1`、`available_openai=0`，只能看到 `anthropic/default`，不能绑定 OpenAI 订阅分组。根因是 API Key 分组授权层只把 OpenAI 分组绑定建模为订阅权益，未把流量包权益映射为可绑定 OpenAI 入口分组；计费层已有流量包兜底，但新 Key 创建卡在权限层。结果见 `docs/ai/context/20260705-174552-traffic-card-only-user-no-openai-key-group-diagnosis_CN.md`。
- 2026-07-05 已按用户要求停止 18080 本地 Sub2API 栈：`sub2api`、`sub2api-postgres`、`sub2api-redis` 均为停止态，`docker ps --filter publish=18080` 无运行容器且 18080 无监听进程；18084 公网候选 `sub2api-candidate*` 未停止/重启/重建，`18084/health`、`8080/health`、`api.aaccx.pw/health` 均为 200。结果见 `docs/ai/context/20260705-175544-stop-18080-stack-result_CN.md`。
- 2026-07-05 自动 API Key 设计已定：普通用户前端删除 API Key 分组下拉框，新建 Key 默认 `group_id=NULL`；后端新增 request-scoped `effective_group` 解析，有 active OpenAI 套餐时选择当前套餐 group，无套餐但有 GPT/OpenAI 流量包时选择内部 `traffic-pack-openai` 入口 group；旧 OpenAI API Key 上线迁移为自动 Key，非 OpenAI 固定 Key 和管理员固定分组能力保留。设计见 `docs/ai/context/20260705-175642-auto-api-key-effective-group-design_CN.md`。
- 2026-07-05 自动 API Key effective_group 实施计划已写：计划按 TDD 拆为 resolver、request-scoped group 解析中间件、用户 Key 创建/更新自动化、`traffic-pack-openai` 迁移、前端移除分组选择、后端路由验证、全量构建与候选运行态验收；计划见 `docs/ai/context/20260705-215045-auto-api-key-effective-group-implementation-plan_CN.md`。
- 2026-07-05 自动 API Key effective_group 本地实现已完成：普通用户 Key 创建/编辑不再选择或提交分组，旧 OpenAI Key 通过 `159_auto_api_key_effective_group.sql` 迁移为 `group_id=NULL`，请求时按 active OpenAI 套餐或 GPT/OpenAI 流量包解析 request-scoped effective group；本轮只改本地分支代码和文档，未连接、迁移、重启或替换公网服务。结果见 `docs/ai/context/20260705-225115-auto-api-key-effective-group-result_CN.md`。
- 2026-07-06 已写“将当前公网 Sub2API 前后端替换为本地 main 最新代码”的执行计划：当前公网应用容器是 `sub2api-candidate`，镜像 `sub2api-main-preview:20260702-local-main-6ef887a8d`，端口 `127.0.0.1:18084->8080`；计划要求先备份 `sub2api-candidate-postgres` 与 `sub2api-candidate-redis` 到 `deploy/backups/`，再只替换应用容器、保留 DB/Redis 和 nginx，并明确 159 迁移后的回滚必须恢复 Postgres dump。尚未执行构建、备份、替换或公网迁移。计划见 `docs/ai/context/20260706-075410-public-main-redeploy-plan_CN.md`。
- 2026-07-05 已按用户要求删除本地 Codex skill `/Users/wujianxiang/.codex/skills/sub2api-local-redeploy`，包括 `SKILL.md` 与 `agents/openai.yaml`；未执行其中的重部署、Docker、Compose 或公网验证流程。结果见 `docs/ai/context/20260705-215015-remove-sub2api-local-redeploy-skill-result_CN.md`。
- 当前 Nginx 公网入口请求体上限已与 Sub2API 对齐为 256MB；`aaccx.pw` 已补裸 `/responses`、`/chat/completions`、`/embeddings`、`/images/*` 代理到 Sub2API。真实 100MB+ 公网请求仍可能受 Cloudflare 套餐上传上限影响。结果见 `docs/ai/context/20260624-201352-413-payload-too-large-nginx-fix-result_CN.md`。
- 2026-06-24 413 归因：大量 413 集中在 Codex Desktop Windows 裸 `/responses`，并伴随裸 `/models?client_version=0.142.0` 404，不是 `/v1/images/*` 集中触发；`/usage-guide` 生图文案已拆分 Base URL、相对路径和完整 URL。结果见 `docs/ai/context/20260624-202759-usage-guide-image-url-413-result_CN.md`。
- 2026-06-24 分支合并排查：`codex/usage-guide-99-plan-main-20260624` 只是从 `origin/main` 创建的空内容分支，真正的 99 文案分支 `codex/usage-guide-99-plan-20260624` 未合并；公网 `/usage-guide` 已有 99/图生图/Trae 文案，但运行态 DB 只有 29/39/59 三个在售订阅，没有 99 元套餐和对应 group。结果见 `docs/ai/context/20260624-210639-main-merge-missing-branch-diagnosis_CN.md`。
- 2026-06-24 `/usage-guide` 生图方法以真实图生图为主：公网完整 URL 为 `https://api.aaccx.pw/v1/images/edits`，分离填写工具中路径填 `/images/edits`，模型 `gpt-image-2`，支持 JSON `images[].image_url` 或 multipart `image=@...`。结果见 `docs/ai/context/20260624-163548-usage-guide-image-edit-result_CN.md`。
- 2026-06-24 公网 502 归因：22:30 JST 左右不是 Cloudflare/nginx/Sub2API 全局不可用，`/v1/responses` 当前可返回 401/200；对应连续 502 主要集中在流式 `/v1/chat/completions`，Sub2API 日志显示上游流缺少终止事件 `stream usage incomplete: missing terminal event`。结果见 `docs/ai/context/20260624-223544-public-502-bad-gateway-diagnosis_CN.md`。
- 2026-06-25 `Selected model is at capacity. Please try a different model.` 归因：这是 OpenAI 官方/上游返回的模型容量不足错误，不是 Sub2API 自造拦截；Sub2API 会把它识别为临时容量错误并触发同账号重试/账号 failover。结果见 `docs/ai/context/20260625-101447-openai-model-capacity-diagnosis_CN.md`。
- 2026-06-25 并发/上游排查：当前四个套餐分组共用一个 `cliproxy-local-openai` 上游账号，Sub2API 账号并发为 3，确实会在高峰排队；但 09:00 后主要错误是上游 overloaded、CLIProxyAPI 内部账号 429 usage limit reached 和 auth unavailable，不是大面积 Sub2API 排队超时。结果见 `docs/ai/context/20260625-121913-sub2api-cliproxyapi-concurrency-upstream-diagnosis_CN.md`。
- 2026-06-25 已按用户要求将运行态 `cliproxy-local-openai` 的 Sub2API 账号并发从 3 调整为 10；未修改用户并发、分组绑定或 CLIProxyAPI 配置。结果见 `docs/ai/context/20260625-192607-sub2api-account-concurrency-10-result_CN.md`。
- CLIProxyAPI 是聚合上游，不是单个静态 OpenAI Key；Sub2API 上游账号需启用 `credentials.pool_mode=true`，并让 401/403/429 在同账号内重试。
- 订阅、余额充值和 GPT 流量包购买全部走 Sub2API 支付订单与 ZPay/EasyPay；当前 ZPay 支付宝实例应使用 `popup/submit.php` 托管收银台，不要把 ZPay `mapi.php` 返回的 `qr.alipay.com` 原始码直接渲染给用户扫码。自动履约必须以签名正确、订单号匹配、provider/merchant metadata 匹配、回调或查单金额等于本地 `pay_amount` 为准。
- 2026-06-26 本地 `127.0.0.1:5174/purchase` 显示“充值功能暂未开放”的直接原因不是 ZPay 代码未合并，而是运行态 DB 没有任何 `payment_provider_instances`，且支付宝/微信可见支付方式均未启用；结果见 `docs/ai/context/20260626-124242-purchase-zpay-merge-runtime-diagnosis_CN.md`。
- 2026-06-26 ZPay 购买支付与自动履约需求已沉淀：目标是用户在当前 `/purchase` 完成套餐购买，ZPay/EasyPay 动态支付成功后按订单 `pay_amount` 校验并自动添加/续期订阅；仍需用户提供 ZPay 创建订单、签名、回调、查单文档与脱敏样例。见 `docs/ai/context/20260626-132547-zpay-purchase-payment-fulfillment-requirements_CN.md`。
- 2026-06-26 已写 ZPay/EasyPay 支付宝-only 运行态配置计划：仅启用支付宝、关闭微信和退款，商户配置只在执行时通过隐藏输入写入运行态，不写入源码或长期文档；计划见 `docs/ai/context/20260626-133635-zpay-alipay-runtime-config-plan_CN.md`。
- 2026-06-26 分支 `codex/zpay-alipay-runtime-config` 已新增运行态配置脚本并写入 ZPay/EasyPay 支付宝-only provider；13:50 左右发现 `qrcode/mapi.php` 模式返回 `qr.alipay.com` 原始码，扫码会报“交易买家不匹配”，已改为 `payment_mode=popup` 走 ZPay `submit.php` 托管收银台。真实支付端到端验收仍需新建订单验证。结果见 `docs/ai/context/20260626-135223-zpay-alipay-runtime-config-result_CN.md`。
- 2026-06-26 ZPay 1% 手续费已通过运行态 `RECHARGE_FEE_RATE=1` 处理；不要修改套餐/流量包基础价格，后端订单用 `pay_amount` 承载实付金额，前端 `/purchase` 列表卡片、确认区和按钮展示含手续费实付价。结果见 `docs/ai/context/20260626-151923-zpay-1-percent-fee-runtime-result_CN.md`。
- 2026-06-26 `/purchase` 用户页已下架余额充值 UI，只保留订阅套餐和 GPT 流量包购买；`?tab=recharge` 不再进入充值页。后端余额订单能力保留用于历史订单和支付恢复。结果见 `docs/ai/context/20260626-152551-purchase-remove-balance-recharge-result_CN.md`。
- 2026-06-26 `liyutong2883@gmail.com` 支付宝购买 3 元 GPT 流量包已自动履约；误新增的手工 10 USD 流量卡已撤回，当前保留两张 10 USD 卡，合计 20 USD。结果见 `docs/ai/context/20260626-154442-liyutong-3yuan-traffic-card-result_CN.md`。
- 2026-06-29 `liyutong2883@gmail.com` 公网两张 10 USD GPT 流量卡只读核对：`credit_id=7` 已扣满 10 USD 归零，`credit_id=46` 已扣 `9.9768260000`、剩余 `0.0231740000`，两张卡合计剩余 `0.0231740000`；按 request 聚合后 ledger 与 `usage_logs` 均为 `19.9768260000`，是真实用量扣费。结果见 `docs/ai/context/20260629-204438-liyutong-public-traffic-cards-balance-result_CN.md`。
- 2026-06-27 已修复认证中间件提前拦截订阅超限/零余额导致流量卡无法兜底的问题：中间件只保留身份、Key 状态、订阅存在/过期/暂停等校验，订阅额度、余额、流量卡兜底和限流统一由 `BillingCacheService.CheckBillingEligibility()` 判定；本地代码已测，运行态仍需重新构建部署后生效。结果见 `docs/ai/context/20260627-212100-unify-billing-eligibility-middleware-result_CN.md`。
- 2026-06-26 候选预演第一次真实执行因 Docker Compose project 未隔离，误停并重建公网 `sub2api`、`sub2api-postgres`、`sub2api-redis`；公网 Postgres 数据未见批量损坏，但 Redis refresh token 会话态丢失，导致旧登录态失效。恢复候选预演前必须先读 `docs/ai/context/20260626-205933-sub2api-candidate-rehearsal-incident-diagnosis_CN.md`，并确认候选 project 不可能等于公网 project。
- 2026-06-26 候选预演纠偏确认：最新生产 DB 数据保留在 Docker named volume `deploy_postgres_data`（47 users、40 keys、191 migrations，`xiaobianfuai@gmail.com` 是 `id=13` admin）；当前 `deploy/postgres_data` bind 目录是旧库（32 users、188 migrations），不能作为最新生产 DB 克隆源。候选/恢复必须从 `deploy_postgres_data` 或其 dump 取数，见 `docs/ai/context/20260626-214600-latest-db-volume-candidate-rehearsal-result_CN.md`。
- 2026-06-26 18084 候选环境购买入口隐藏归因：候选 sanitize 不能把 `payment_enabled=false` 写入克隆库，否则 HTML 注入的 `window.__APP_CONFIG__` 和前端路由守卫会隐藏 `/purchase`；候选预演应保留 `payment_enabled=true`，只禁用具体 provider、可见支付方式、SMTP 和监控副作用。结果见 `docs/ai/context/20260626-220130-candidate-purchase-nav-hidden-fix-result_CN.md`。
- 2026-06-26 `/purchase` 主内容空白归因：`PaymentView.vue` 购买选择态多包了一层无指令裸 `<template>`，构建后套餐卡片进入原生 template content，DOM 有文字但视觉不渲染；已删除 wrapper 并新增回归测试。结果见 `docs/ai/context/20260626-221115-payment-view-native-template-blank-fix-result_CN.md`。
- 2026-06-26 18084 候选环境“支付功能未开放”归因：ZPay 链路代码已打通，但候选 sanitize 误把克隆来的 `ZPay Alipay` provider 和支付宝可见方式关掉，导致 checkout `methods={}`；已恢复为支付宝-only `easypay_alipay`、`payment_mode=popup`，并修正候选 sanitize 只关闭其他 provider/退款/通知副作用。结果见 `docs/ai/context/20260626-221915-candidate-zpay-provider-restore-result_CN.md`。
- 2026-06-26 已在 18084 候选环境对应数据库 `sub2api-candidate-postgres` 创建普通用户 `1038686518@qq.com`，用户 id=48，email 登录接口返回 200；只写候选库，未改生产库、API Key、套餐、余额或支付配置。结果见 `docs/ai/context/20260626-234732-candidate-18084-create-user-result_CN.md`。
- 套餐分组名 `codex-pool-19-usd`、`codex-pool-29-usd`、`codex-pool-49-usd`、`codex-pool-69-usd` 分别对应每日 19/29/49/69 USD。
- 当前售卖套餐里 `29 元订阅池` 对应 `subscription_plans.id=1 -> group_id=2 -> codex-pool-19-usd`；不要误绑到 `codex-pool-29-usd`。
- `79 元订阅池` 基础价为 79 元，30 天有效期，对应 `codex-pool-69-usd`，每日 69 USD；用户 `/purchase` 在 1% 手续费下展示和支付 79.79 元，通过 `subscription_plans` 上架并复用现有支付和订阅履约链路。
- `/purchase` 只负责充值、订阅和 GPT 流量包购买，不再展示当前订阅详情；当前订阅状态统一在“我的订阅”页查看。
- 2026-06-28 本地 main 已移除 `/purchase` 登录后页面标题下方说明“通过内嵌页面完成订阅购买”，仅改前端路由 meta 与未引用 locale 文案，未改支付流程；结果见 `docs/ai/context/20260628-165245-purchase-remove-description-result_CN.md`。
- 2026-06-28 `/usage-guide` Codex 接入第 4 步已从“支付后发兑换码”改为“完成支付”，只保留一张支付确认截图；第 5 步同步改为“支付完成后，去 API Key 页面生成密钥”；旧两张兑换码流程配图已删除，因当前支付成功后自动履约、用户可直接使用。结果见 `docs/ai/context/20260628-171325-usage-guide-step4-payment-direct-result_CN.md`。
- `xiaobianfuai@gmail.com` 是管理员和本机 Codex Local Key 所属账号，不要按普通用户删除。

## 维护规则

- 需要新增长期上下文时，只在 `docs/ai/context/` 创建新文件。
- 进入实现前先写 design/plan 上下文；完成后写结果上下文。
- 每次合并功能分支、提交 main 或做收尾前，必须运行 `git ls-files --others --exclude-standard docs/ai/context` 检查未跟踪上下文文档；确认无敏感信息后纳入同一次功能提交，或单独做 `docs: archive ai context` 提交。
- 不要把 `docs/ai/context/` 加回 `.gitignore`；未跟踪状态表示尚未提交，不表示被忽略。
- 如果某个上下文文档暂不提交，必须在回复里说明原因和后续处理方式。
- 若上下文过长，先沉淀到新的压缩文档，再继续精简本文件。
- Git 远端：`origin=https://github.com/Wei-Shaw/sub2api.git` 是上游；`personal=https://github.com/cnYui/sub2api.git` 是用户个人 fork。后续保存当前工作分支优先推送到 `personal`，不要误推到上游 `origin`。
