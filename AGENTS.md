# AI 协作入口

> 最新压缩记忆见 `docs/ai/context/20260624-195608-agents-memory-compressed_CN.md`。
> 早期压缩记忆见 `docs/ai/context/20260619-151920-sub2api-compressed-memory_CN.md`。
> 后续长期上下文统一新建到 `docs/ai/context/YYYYMMDD-HHMMSS-*.md`，不要覆写、重命名或删除历史文档。

## 最高优先级定论

- 2026-07-13 已确认流量卡进度条修复设计：保持历史“仅汇总 `remaining_usd > 0` 且未过期批次、单批次用满即移除、最后一张用满后整卡消失”语义；后端 `TrafficCreditSummary` 新增 `total_initial_usd`，与 `total_remaining_usd` 使用相同可用批次过滤条件，前端以两者差值展示 `已用/总额` 和正向进度。不得从在售商品、订单或流水猜测总额，不改真实扣费、优先级、有效期或履约。设计见 `docs/ai/context/20260713-124740-traffic-credit-progress-fix-design_CN.md`。
- 2026-07-12 全站品牌 Logo 替换已提交、快进合并到本地 `main` 并同步到 `personal/main`：功能提交为 `807160c4f feat: replace site branding logo`，合并前已确认本地 `main` 与 `personal/main` 基线同为 `2a5835641`，无远端分叉；在 `main` 上复验前端目标测试 14/14 与 `go test -count=1 -tags=embed ./internal/web` 通过。独立审查无 Critical/Important，唯一旧图尺寸文档误差已从 580×558 修正为实际 580×560。推送后本地与远端哈希一致且 ahead/behind 为 0/0；`origin` 未触碰，已有 `backend/resources/certs/tls.crt` 用户改动保持未提交。同步结果见 `docs/ai/context/20260712-095448-site-logo-main-personal-sync-result_CN.md`。
- 2026-07-12 本地分支 `codex/replace-site-logo` 已把全站默认品牌图标从蓝色 Sub2API 图标替换为用户提供的 `/Users/wujianxiang/Downloads/2273.JPG` 头像：保持原始正方形构图，使用 Lanczos 缩放、元数据剥离和 256 色调色板压缩生成 `frontend/public/logo.png`，规格从旧 580×560/149,928 字节变为 256×256/37,570 字节，体积减少约 74.9%。侧栏、认证页、首页、Key 用量页、法律页和 favicon 均继续统一使用 `/logo.png`，运行态 18084 `site_logo` 当前为空，不会覆盖默认资源；后台自定义 Logo 能力保留。TDD 资源契约测试先因旧尺寸/体积 2 项失败，替换后通过；相邻前端测试 14/14、`pnpm typecheck`、`pnpm build`、`go test -tags=embed ./internal/web` 和首页/登录页浏览器视觉检查通过。未部署 18084，未改 DB/Redis/nginx/容器或运行态配置，未触碰已有 `backend/resources/certs/tls.crt` 用户改动。结果见 `docs/ai/context/20260712-092701-site-logo-replacement-result_CN.md`。
- 2026-07-11 本地分支 `codex/fix-openai-priority-pricing` 已按 OpenAI 官方 Priority 价格完成计费修复：GPT-5.4 及日期快照/既有后缀为 `2x`，GPT-5.5 及日期快照/既有后缀为 `2.5x`，GPT-5.6 sol/terra/luna 为 `2x`；其他模型继续 `1.5x`，明确避免裸 `gpt-5/gpt-5.1` 被宽泛 fallback 误套 GPT-5.4 倍率，`gpt-5.4-mini/nano/pro` 与 `gpt-5.5-pro` 仍为 `1.5x`。`fast -> priority`、Standard、Flex `0.5x`、长上下文、渠道 flat/interval、分组倍率和 reasoning token 口径不变；内置 pricing JSON 已同步 GPT-5.5 日期快照与三款 GPT-5.6 Priority 字段。验证通过 `internal/service` 全量 unit、`internal/pkg/openai ./internal/handler` unit、`cmd/server`、JSON 解析和 `git diff --check`；未构建镜像、未部署 18084、未改 DB/Redis/nginx/容器、未补扣历史 usage。结果见 `docs/ai/context/20260711-213759-openai-official-priority-pricing-fix-result_CN.md`。
- 2026-07-11 已确认从本地 `main@937c0498c` 分支修复 OpenAI 官方 Priority 计费，分支为 `codex/fix-openai-priority-pricing`。采用窄范围模型级倍率方案：GPT-5.4 Priority `2x`、GPT-5.5 `2.5x`、GPT-5.6 sol/terra/luna `2x`，其他模型继续现有 Priority 规则；Standard、Flex、Fast->Priority、长上下文、分组倍率、reasoning token 口径均不变。同步修正内置 pricing JSON，但运行时计费由模型级倍率保障，不依赖当前 GPT-5.5 Priority 仍过时的远程镜像。明确不处理上游 Priority 降级后按响应实际 tier 计费、不改运行态数据、不补扣历史 usage。设计见 `docs/ai/context/20260711-122334-openai-official-priority-pricing-fix-design_CN.md`，实施计划见 `docs/ai/context/20260711-205801-openai-official-priority-pricing-fix-implementation-plan_CN.md`。
- 2026-07-11 已按方案一把运行态 Sub2API 唯一上游账号 `accounts.id=1/cliproxy-local-openai` 并发从 10 调整为 100，使普通请求账号级闸门与 CLIProxyAPI 100/100 对齐；修改前备份 PostgreSQL 到 `deploy/backups/20260711-215209-sub2api-candidate-before-account-concurrency-100.dump`，文件约 53MB、权限 600，`pg_restore -l` 可读。更新通过正式管理员登录和账号管理接口完成，只提交 `concurrency=100`；最终 PostgreSQL 与 Redis `sched:acc:1` 均为 100，账号仍 active/schedulable，95 个用户仍全部 `users.concurrency=5`，Redis 保持 `concurrency:api_key:*` 且无 `concurrency:user:*`，现有账号活跃槽未清理。CLIProxyAPI 普通 100/100、图片 10/10 未变，18084、8080、公网和 8317 health 均 200，近 10 分钟无关键 scheduler/Redis/DB/panic/account-select 错误。计划中的 outbox 查询误用了不存在的 `processed_at`，实际事件已被 worker 消费删除，Redis 快照证明同步成功。本轮未改代码/schema/CLIProxyAPI/用户数据，未重启、未清 Redis、未做真实 100 并发压测、未提交推送。结果见 `docs/ai/context/20260711-220953-sub2api-upstream-account-concurrency-100-result_CN.md`。
- 2026-07-11 已确认方案一并写 Sub2API 上游账号并发调整为 100 的实施计划：执行时先确认唯一目标 `accounts.id=1/cliproxy-local-openai` 与 CLIProxyAPI 普通 100/100、图片 10/10，再备份并验证 PostgreSQL；通过项目 `sub2api-admin` 管理 CLI 单字段更新 `concurrency=100`，随后核对 DB、Redis `sched:acc:1`、scheduler outbox、API Key 槽、用户并发、三层 health 和日志。全程不重启、不清 Redis、不改 CLIProxyAPI、不做 100 路真实压测；异常时通过同一管理接口回滚到 10，整库恢复需另行授权。当前只写计划，未修改运行态。计划见 `docs/ai/context/20260711-214428-sub2api-upstream-account-concurrency-100-implementation-plan_CN.md`。
- 2026-07-11 本地分支 `codex/fix-ccswitch-v1-endpoint` 已修复 `/keys` 导入 CCSwitch 时 OpenAI/Codex endpoint 缺少 `/v1`：正式 `/v1/*` 路由约束上线后，`frontend/src/utils/ccswitchImport.ts` 仍原样写入 `baseUrl`，而公开 `api_base_url` 为空时 `/keys` 回退 `window.location.origin`，导致导入裸域名并命中 `400 INVALID_BASE_URL`；现仅在 OpenAI/Codex 分支清理空白、尾斜杠和连续末尾 `/v1`，生成恰好一个 `/v1` endpoint，并通过根地址 `usageBaseUrl` 保持现有 `{{baseUrl}}/v1/usage` 正确，`app=codex`、`model=gpt-5.5`、其他平台和自动 Key `group=null` fallback 不变。TDD 最终目标测试 `15/15`、含 UseKeyModal 邻近回归 `19/19`、`pnpm typecheck`、`git diff --check`、`git show --check` 均通过；规范审查 9 项全部通过，质量复审无 Critical/Important/Minor 且 Ready to merge。未改 KeysView 生产代码、后端、Nginx、DB、Redis 或运行态配置，未部署。结果见 `docs/ai/context/20260711-210311-ccswitch-v1-endpoint-fix-result_CN.md`。
- 2026-07-11 已写 Sub2API 上游账号并发从 10 调整为 100 的设计：推荐只通过管理接口更新 `accounts.id=1/cliproxy-local-openai.concurrency=100`，与 CLIProxyAPI 普通请求 `global=100/per-key=100` 对齐；保留 Sub2API API Key 级并发和账号级背压，图片仍由 CLIProxyAPI endpoint override 限制为 10。管理接口会写 PostgreSQL、scheduler outbox 并立即刷新 `sched:acc:1`，不需要重启或清 Redis；实施前必须备份 Postgres，更新后验证 DB/调度快照/API Key 槽和三层 health。当前仅写设计，未修改业务代码、DB、Redis、容器、nginx 或 CLIProxyAPI。设计见 `docs/ai/context/20260711-213214-sub2api-upstream-account-concurrency-100-design_CN.md`。
- 2026-07-11 已只读排查“购买 29 元套餐后 API Key 返回 401”：今天 nginx 有 77 次 OpenAI 模型入口 401，其中 67 次为 54 字节 `INVALID_API_KEY`、10 次为 143 字节 `API_KEY_REQUIRED`；401 在 Key 查找阶段早于套餐/额度判断，不是购买套餐未生效。近期 29 元用户中最符合反馈的是 `405045701@qq.com/user_id=96`：订单 `156` 已完成，订阅 `100/group_id=2` active 到 2026-08-09，用户状态正常；第一把 Key `124/Codex-used` 创建约 5 分钟后被删除，旧 Key 实测 `/v1/models` 为 401，后续当前 Key `125/126` 均 active 且实测 `/v1/models` 200，无 usage/费用，说明套餐识别和鉴权正常。该用户历史模型 usage 为 0，最可能是客户端仍配置已删除 Key或复制了掩码/不完整 Key；应替换为后台当前完整 Key并使用 `Authorization: Bearer`。`8.219.150.152` 今天持续产生 50 次模型/Gemini 401且历史成功 usage 为 0，但因 `ops_monitoring_enabled=false`、`ops_error_logs` 无鉴权记录，不能从日志严格证明该 IP 提交的就是 Key 124。诊断请求仅更新当前 Key 的 `last_used_at`，未改订单、订阅、Key、缓存或运行配置。结果见 `docs/ai/context/20260711-213023-29-subscription-api-key-401-diagnosis_CN.md`。
- 2026-07-11 已核对 Sub2API 与 CLIProxyAPI 的 API Key 并发设计：按 2026-07-10 三份文档，主要实现与运行态一致，公网 18084 已包含 `176605740`，模型入口按 `api_key_id` 使用 `concurrency:api_key:*`，CLIProxyAPI 普通入站限制为 100/100、图片 10/10，没有按共用上游 Key 错限全站 5；目标测试均通过。但最新口头目标仍有两处未严格满足：每 Key 上限实际复用可修改的 `users.concurrency`，当前 95 人都是 5 只是运行数据现状，不是代码硬上限；Sub2API 仍保留唯一上游账号 `cliproxy-local-openai` 的共享并发 10，因此全站进入 CLIProxyAPI 前仍会先被 10 限制。若要求无条件每 Key 最高 5，应固定/封顶该值并处理后台并发控制面；若要求 Sub2API 只鉴权计费后直通，还需另行决定移除或提高账号级 10。结果见 `docs/ai/context/20260711-211142-sub2api-cliproxyapi-api-key-concurrency-design-audit_CN.md`。
- 2026-07-11 已只读审计 OpenAI GPT-5.4/5.5/5.6 定价：运行态 Standard 短上下文基础价与 OpenAI 官方现行价格一致，但 Fast/Priority 不正确。`fast` 会归一为 `service_tier=priority`，计费核心把所有 Priority 统一乘 `1.5`；官方现行 GPT-5.4 Priority 为 2x、GPT-5.5 为 2.5x、GPT-5.6 sol/terra/luna 为 2x。当前 GPT-5.4/5.6 Priority 只收官方价的 75%，少收 25%；GPT-5.5 只收官方价的 60%，少收 40%。运行态 GPT-5.5 与 GPT-5.6 Priority usage 已验证按 1.5x 落库；无 channel pricing，相关分组倍率均为 1。另有两项风险：GPT-5.5 动态价格文件的 Priority 仍是过时 2x；计费记录请求 tier 而非上游响应实际 tier，Priority 被降级为 default 时可能仍按 Priority 扣费。目标单测 PASS 并明确固化 1.5x；本轮未改业务代码、DB、Redis、nginx、容器或运行态配置。结果见 `docs/ai/context/20260711-120137-openai-gpt54-gpt55-gpt56-priority-pricing-audit_CN.md`。
- 2026-07-11 本地分支 `codex/fix-balance-traffic-pack-transaction` 已修复余额购买流量包的 `user_traffic_credits_order_id_fkey` 外键失败：`trafficPackRepository.CreditPurchase()` 在 `context` 已有 Ent 事务时复用 `tx.Client()` 写入流量额度和购买流水，没有外层事务时仍自行开启本地 SQL 事务；余额扣减、未提交订单、流量额度和流水恢复同一事务原子性，未移除/延迟外键，未提前提交订单。新增 PostgreSQL 集成测试真实创建未提交父订单，修复前稳定 RED 为外键违反，修复后事务内额度/流水可见且外层回滚后三者均不存在。验证通过目标 integration、流量包仓储 unit、余额支付 service unit、`./internal/repository ./internal/service` 完整 unit 和 `git diff --check`；未构建镜像、未部署 18084、未改运行态数据。结果见 `docs/ai/context/20260711-115115-balance-traffic-pack-transaction-fix-result_CN.md`。
- 2026-07-11 已只读定位 `1038686518@qq.com` 使用已解冻返利余额购买流量包失败的根因：运行态用户 `users.id=48/status=active`，三笔各 `7.92` 元返利已全部从 `aff_quota/aff_frozen_quota` 转入 `users.balance=23.76`，当前 `aff_quota=0/aff_frozen_quota=0/aff_history_quota=23.76`，余额足够购买任一在售流量包。2026-07-10 23:01:50、23:01:56、23:03:04 JST 三次 `POST /api/v1/payment/orders/balance-pay` 均 HTTP 500，应用和 Postgres 明确报 `user_traffic_credits_order_id_fkey`；临时订单 `158/159/160` 随事务回滚，未留下订单、流量额度、流水、审计或余额扣减。代码根因是 `BalancePayOrder()` 在 Ent 外层事务内创建未提交订单后，流量包仓储通过全局 `*sql.DB` 另开事务写 `user_traffic_credits`，独立事务看不到父订单；月度套餐仓储会复用外层事务，所以昨日余额购买套餐测试可成功。正确修复必须让流量包发放加入同一事务并补 PostgreSQL 外键集成测试，不能移除外键或提前提交。结果见 `docs/ai/context/20260711-094343-1038686518-balance-traffic-pack-failure-diagnosis_CN.md`。
- 2026-07-10 已确认 `2799523972@qq.com` 可用于余额购买/退款流程测试并加测试资金：运行态用户 `users.id=31/status=active`，当前无未删除 active 套餐，无软删但仍 active 的套餐残留，`user_allowed_groups=0`，API Key `65/Codex_79` 为 active 且 `group_id=NULL`；历史订阅仅 `id=71/group_id=9` 与 `id=49/group_id=2`，均 `status=expired` 且有 `deleted_at`。写库前备份 Postgres `deploy/backups/20260710-220900-sub2api-candidate-before-2799523972-balance-test.dump`，容器内 `pg_restore -l` 可读；已给该用户余额从 `0` 加到 `200.00000000`，`total_recharged` 保持 `0`，新增审计 `redeem_codes.id=23/type=admin_balance/value=200/status=used/used_by=31`，备注为购买/退款流程测试资金；已清 `billing:balance:31` 并发布 API Key auth cache 失效，复核 `billing:sub:31:*` 不存在，18084 `/health` 200。结果见 `docs/ai/context/20260710-221100-2799523972-balance-test-result_CN.md`。
- 2026-07-10 误将 `xinlise@gmail.com` 当作测试账号后已立即恢复：误操作前已备份 Postgres `deploy/backups/20260710-220013-sub2api-candidate-before-xinlise-balance-test-prep.dump` 与 Redis `deploy/backups/20260710-220013-sub2api-candidate-redis-before-xinlise-balance-test-prep.rdb`；误写入包括将 `user_subscriptions.id=98/user_id=69/group_id=12` 从 active 改为 expired+deleted_at、给用户余额加 200、插入 `redeem_codes.id=22/code=ADM-XIN-20260710210227/type=admin_balance/value=200`。用户指出账号搞错后已立即事务恢复：`subscription_id=98` 恢复 `status=active/deleted_at=NULL`，余额减回 `0`，误审计 `redeem_codes.id=22` 已删除，Redis 清 `billing:balance:69`、`billing:sub:69:12`、`billing:sub:69:8`；复核 `xinlise@gmail.com` 当前 `users.id=69/status=active/balance=0`，唯一未删除 active 套餐为 `id=98/group_id=12/codex-pool-179-usd/expires_at=2026-08-09 16:43:25+08`，旧订阅 `id=88` 仍 expired+deleted_at，API Key `99/102` 均 active 且 `group_id=NULL`，分组 12 绑定 `account_id=1/cliproxy-local-openai active/schedulable`；使用 `api_key_id=102` 调 `/v1/models` 返回 HTTP 200、不产生 usage 扣费。后续给测试账号加余额或撤套餐前，必须先让用户明确邮箱或用户 ID，不得从上一段上下文推断账号。记录见 `docs/ai/context/20260710-220450-xinlise-mistaken-test-prep-rollback-result_CN.md`。
- 2026-07-10 已修复并发布“管理员在 Subscriptions 页面撤销套餐后，用户仍被单 active 套餐保护拦截、无法二次购买”的根因：`DELETE /api/v1/admin/subscriptions/:id` 走 `SubscriptionService.RevokeSubscription()`，旧代码只软删 `user_subscriptions.deleted_at` 不改 `status`，会留下 `deleted_at IS NOT NULL AND status='active' AND expires_at > now()`；前端购买页也可能用 60 秒 active subscription 缓存先行本地拦截。后端已改为撤销时先 `UpdateStatus(expired)` 再软删，前端订阅套餐选择/确认前强制 `fetchActiveSubscriptions(true)`；新增后端撤销顺序测试与前端 stale cache 回归测试。验证通过 `GOMAXPROCS=2 go test -p=1 -count=1 -tags=unit ./internal/service`、`pnpm vitest run src/views/user/__tests__/PaymentView.spec.ts`、`pnpm typecheck`、`git diff --check`。发布前备份 Postgres `deploy/backups/20260710-213756-sub2api-candidate-before-admin-subscription-revoke-repurchase-deploy.dump`（容器内 `pg_restore -l` 可读）和 Redis `deploy/backups/20260710-213756-sub2api-candidate-redis-before-admin-subscription-revoke-repurchase-deploy.rdb`；已构建并发布 `sub2api-candidate:20260710-214000-a575f28c1-admin-sub-revoke-repurchase` 到 18084，旧容器为 `sub2api-candidate-before-promote-20260710-214730`，Postgres/Redis/nginx/Cloudflare Tunnel 未重建，18084 与公网 `/health` 均 200。发布后清理 8 条历史软删但 active 的脏订阅为 `expired`，对应 Redis `billing:sub:*` 删除返回 0，复核脏状态计数为 0；`xinlise@gmail.com` 旧订阅 `id=88` 为 `expired+deleted_at`，新 199 元订阅 `id=98/group_id=12` 保持 active。结果见 `docs/ai/context/20260710-215037-admin-subscription-revoke-repurchase-fix-result_CN.md`。
- 2026-07-10 已只读排查 `bohuyeshan@163.com` 日志问题：运行态用户 `users.id=65/status=active`，仅一把 active Key `api_keys.id=84/name=oc/group_id=NULL`，active 订阅 `user_subscriptions.id=83/group_id=8/codex-pool-89-usd` 到 `2026-08-06 13:41:44+08`，未删除未过期；当前不能用的直接原因是今天日额度已满，`daily_usage_usd=89.1596930000/89 USD`，最后成功 usage 在 `2026-07-10 16:07:37+08`，随后 `16:07:47+08` 请求 `/v1/responses gpt-5.6-sol` 被本地计费准入拦截，容器 stdout 记录 `openai.billing_eligibility_check_failed`，`DAILY_LIMIT_EXCEEDED daily usage limit exceeded`，HTTP 403；`ops_error_logs` 无记录，Redis `billing:sub:65:8` 不存在。今天成功 usage 450 条，`gpt-5.6-sol` 335 条/`63.9893470000 USD`，`gpt-5.4` 46 条/`19.2823680000 USD`，`gpt-5.6-terra` 69 条/`5.8879780000 USD`。同日还出现过 `gpt-5.6-luna` 9 次 `no available accounts`（上游 404 问题）、一次 `gpt-5.6-terra` 24h extended prompt caching 错误和一次 `gpt-5.6-sol` stream usage incomplete；当前建议等 `2026-07-11 00:00:00+08` 日窗口刷新，并避免使用 `gpt-5.6-luna`。本轮未改 DB/Redis/nginx/容器/代码/Key。结果见 `docs/ai/context/20260710-214427-bohuyeshan-log-audit-result_CN.md`。
- 2026-07-10 已修复 `xinlise@gmail.com` 新买 199 元套餐后使用 503：用户 `users.id=69` 在 `2026-07-10 16:43:25+08` 支付完成订单 `payment_orders.id=153/plan_id=9/subscription_group_id=12`，生成 active 订阅 `user_subscriptions.id=98/group_id=12/codex-pool-179-usd`；但运行态 `account_groups` 里 149/199 两个新套餐分组 `group_id=11/codex-pool-135-usd` 与 `group_id=12/codex-pool-179-usd` 均未绑定上游账号，导致该用户两把自动 Key 请求进入 `group_id=12` 后在账号选择阶段报 `openai.account_select_failed: no available accounts` 并返回 503。写库前备份 `deploy/backups/20260710-175341-sub2api-candidate-before-149-199-account-binding.dump` 并用 `pg_restore -l` 验证可读；已幂等插入 `account_id=1/cliproxy-local-openai -> group_id=11,12`，`priority=50`。复核两个分组均绑定 active/schedulable 上游；用 `api_key_id=102` 真实小请求 `/v1/responses gpt-5.5` 返回 HTTP 200，新增 `usage_logs.id=82131`，扣到 `subscription_id=98/group_id=12`，费用 `0.0039810000 USD`；18084 health 200。本次未改订单、退款、订阅周期、API Key、上游凭据、Redis/nginx/容器。结果见 `docs/ai/context/20260710-175958-xinlise-199-plan-account-binding-result_CN.md`。
- 2026-07-10 已处理 `xinlise@gmail.com` 退款失败后仍无法购买的问题：用户 `users.id=69` 手动取消订阅时只写入了 `user_subscriptions.id=88.deleted_at=2026-07-10 16:27:50.204049+08`，但 `status` 仍为 `active`；当前购买保护/活跃订阅查询按 `status='active' AND expires_at > now()` 识别，未统一显式排除软删除记录，因此这条已软删除订阅仍可能被识别为 active 并拦截购买。写库前备份 `deploy/backups/20260710-172913-sub2api-candidate-before-xinlise-subscription-cancel-cleanup.dump` 并用 `pg_restore -l` 验证可读；已将 `subscription_id=88/group_id=8/codex-pool-89-usd` 更新为 `status=expired`，保留 deleted_at；复核购买保护命中数为 0，`user_allowed_groups` 为 0，API Key `99/codex` 与 `102/佳一老师` 均为自动分组 `group_id=NULL`，无 pending 订单，`DEL billing:sub:69:8` 返回 0，18084 health 200。订单 `payment_orders.id=140` 仍为 `REFUND_FAILED`，失败原因为 ZPay/EasyPay `卖家余额不足`，本轮未改退款状态、未执行退款。长期代码建议：活跃订阅查询显式过滤 `deleted_at IS NULL`。结果见 `docs/ai/context/20260710-173213-xinlise-purchase-block-cleanup-result_CN.md`。
- 2026-07-10 已只读核算 `xinlise@gmail.com` 退款天数与失败原因：用户 `users.id=69/status=active`，当前订阅 `user_subscriptions.id=88/group_id=8/codex-pool-89-usd` 仍 active，周期 `2026-07-08 03:57:35+08` 到 `2026-08-07 03:57:35+08`；截至 `2026-07-10 16:20:36+08` 实际时钟已过约 2.516 天，但按自动退款规则剩余完整天数 `27`，等价已消耗/不退款 `3` 天；支付宝订单 `payment_orders.id=140` 基础价 `99.00`、实付 `99.99`，退款不退 1% 手续费，系统试算/建议退款额 `89.10` 元。今天额度已满：`daily_usage_usd=89.1284430000/89 USD`，usage_logs 今天 849 次、`89.1284430000 USD`；成功扣费自然日为 2026-07-08/09/10 三天，合计 2165 次、`245.2471827500 USD`，全部扣到 `subscription_id=88`。用户已在 `2026-07-10 16:16:44+08` 发起自动退款，订单状态 `REFUND_FAILED`，原因 `easypay refund failed (HTTP 200): 卖家余额不足`；订阅未撤销。用户自助入口不能直接重试 `REFUND_FAILED`，管理员退款接口可处理该状态；补足 ZPay/EasyPay 商户余额后可按 `89.10` 元管理员退款，或线下退款后同步撤销订阅/记录订单。结果见 `docs/ai/context/20260710-172036-xinlise-refund-days-audit-result_CN.md`。
- 2026-07-10 当前多用户“报错、用不了”的主要根因已只读定位：不是 Sub2API 整站不可用，`sub2api-candidate`/Postgres/Redis healthy，`/api/*` 与大量 `/v1/responses` 仍 200；高频失败集中在 `gpt-5.6-luna`，最近两小时约 221 次 `/v1/responses gpt-5.6-luna` 返回 503 `no available accounts`，最近 6 小时 319 次 503、4 次 502。CLIProxyAPI `/v1/models` 仍列出 `gpt-5.6-luna`，但直接调用 CLIProxyAPI `/v1/responses` 时 `gpt-5.6-sol`/`terra` 200，`gpt-5.6-luna` 404 `Model not found gpt-5.6-luna`；Sub2API 唯一上游账号 `account_id=1/cliproxy-local-openai` 已记录模型级冷却 `gpt-5.6-luna reason=upstream_404_model_not_found`。`liyutong2883@gmail.com` 是 `user_id=12`，账号/Key/订阅未坏：订阅 `id=7/group_id=2/codex-pool-19-usd` 到 `2026-07-17`，今日已用 `6.2934636500/19 USD`，未超额；其 `api_key_id=117/name=5.6` 在 `2026-07-10 14:31:11+08` 到 `15:05:53+08` 连续 181 次请求 `gpt-5.6-luna` 命中 `no available accounts`。短期应让用户改用 `gpt-5.6-sol` 或 `gpt-5.6-terra`，并尽快从展示/模型列表隐藏 `gpt-5.6-luna`，直到上游真实调用可 200。结果见 `docs/ai/context/20260710-171245-current-log-error-audit-result_CN.md`。
- 2026-07-10 已确认 `caogang@sdufe.edu.cn` 存在于运行态候选库且已按用户要求绕过支付直加 99 元订阅池：用户 `users.id=89/status=active`，直加前无未删除 active 订阅；后台 `/admin/subscriptions` 查不到的根因是该页查订阅记录而不是纯用户记录，直加前没有 `user_subscriptions` 行。写库前备份 `deploy/backups/20260710-152449-sub2api-candidate-before-caogang-99-subscription.dump` 并用容器内 `pg_restore -l` 验证可读；新增 `user_subscriptions.id=95/group_id=8/codex-pool-89-usd`，每日 `89 USD`，有效期 `2026-07-10 14:26:32+08` 到 `2026-08-09 14:26:32+08`，`assigned_by=13/xiaobianfuai@gmail.com`，初始用量为 0；Redis `billing:sub:89:8` 不存在且删除返回 0。内置浏览器已打开 `https://aaccx.pw/admin/subscriptions`，小写 `w` 密码登录失败，改用 `Wu15951875192` 登录成功；订阅页第一行已显示该用户 `codex-pool-89-usd` 生效订阅。结果见 `docs/ai/context/20260710-152711-caogang-99-subscription-direct-add-result_CN.md`。
- 2026-07-10 已只读确认 `milesyang987@gmail.com` 当前每日额度为 0 不是 GPT-5.6 计费漏扣：运行态用户为 `user_id=3`，active 订阅 `subscription_id=2/codex-pool-19-usd`，`daily_window_start=2026-07-10 00:00:00+08`、`daily_usage_usd=0`；今天 `2026-07-10` 全天截至审计无 usage、无 API Key `id=2` usage、无 error/system log，早上 GPT-5.6 rows 为 0，全历史 GPT-5.6 rows 也为 0；该用户最近使用是 `2026-07-09 12:11:26+08` 的 `gpt-5.4`，昨日 131 条均 `billing_type=1`、绑定 `subscription_id=2`、有 `usage_billing_dedup`。结果见 `docs/ai/context/20260710-144458-milesyang987-gpt56-quota-audit-result_CN.md`。
- 2026-07-10 已只读确认今天早上 GPT-5.6 费用真实进入每日额度：北京时间 `2026-07-10 00:00:00+08` 到 `12:00:00+08`，运行态 `sub2api-candidate-postgres` 有 317 条 GPT-5.6 usage，`gpt-5.6-sol` 304 条、`gpt-5.6-terra` 13 条、合计 `48.7976285000 USD`，全部 `billing_type=1`、全部有 `subscription_id`、全部有 `usage_billing_dedup`，6 个相关订阅的 `user_subscriptions.daily_usage_usd` 与从 `daily_window_start` 起的 usage 总和完全一致，当前 GPT-5.6 规则复算最大差值为 0；截至北京时间 `13:39:20+08`，今天全天快照 1012 条、`128.7151040000 USD` 也全部进入订阅每日额度。结果见 `docs/ai/context/20260710-143933-gpt56-morning-daily-quota-audit-result_CN.md`。
- 2026-07-10 已补充 GPT-5.6 历史用量补计费计划：在平台 GPT-5.6 计费规则正式生效前产生的 `gpt-5.6-sol/terra/luna` 调用，当时不进入平台实时扣费，但不是永久免单；规则颁布后必须按新规则加载历史 5.6 用量并立即计入用户账户额度/扣费。当前只读审计 `sub2api-candidate-postgres.usage_logs` 已有 285 条 GPT-5.6 记录、6 位用户、`usage_logs.total_cost` 合计约 46.2364195000 USD，其中 `gpt-5.6-sol` 272 条、`gpt-5.6-terra` 13 条；执行前必须重新审计、备份 Postgres/Redis、明确 `GPT56_BILLING_RULE_EFFECTIVE_AT`、先出 dry-run 清单并经用户确认，补扣必须基于真实 token usage 且幂等防重复，不能只凭 `usage_logs.total_cost` 判断已扣费。补充计划见 `docs/ai/context/20260710-124347-gpt56-historical-usage-backfill-plan-supplement_CN.md`。
- 2026-07-10 已写本地 Sub2API 与 CLIProxyAPI 启动前备份设计：本次“Subql API”按当前 Sub2API 理解；当前 18084 运行态容器为 `sub2api-candidate*`，但真实数据目录挂在旧 worktree `/Users/wujianxiang/CodeSpace/sub2api/.worktrees/codex-sub2api-candidate-rehearsal-20260626/deploy/candidate/...`，不能按当前 `main/deploy/candidate` 猜；启动/重启前必须先备份 `sub2api-candidate-postgres` 与内存态 `sub2api-candidate-redis`，Redis 当前 `--save "" --appendonly no`，需主动导出 RDB；CLIProxyAPI 当前是宿主机进程监听 `8317`，cwd 为 `/Users/wujianxiang/CodeSpace/CLIProxyAPI`，不是 Docker 容器。设计见 `docs/ai/context/20260710-123901-local-sub2api-cliproxyapi-startup-backup-design_CN.md`。
- 2026-07-10 本地 `main` 已为完整测试清理既有 golangci-lint 问题：安装项目文档要求的 `golangci-lint v2.7` 后，根目录 `make test` 在 lint 阶段暴露既有 `errcheck/ineffassign/unused` 问题；已最小修复 `Rows.Close`/`Tx.Rollback` defer 错误忽略、`tierMultiplier` 无效赋值和若干未使用旧 helper，并删除只覆盖死代码的陈旧退款测试段。记录见 `docs/ai/context/20260710-121544-full-test-lint-cleanup-result_CN.md`。
- 2026-07-10 本地已实现“每个 API Key 最多 5 并发，用户创建多把 Key 可以扩大并发”：Sub2API 模型请求入口第一层调用方并发槽已从 `user_id` 替换为 `api_key_id`，每把 Key 复用现有默认并发 5；Claude Messages、OpenAI Responses/Chat/Embeddings/Images/WebSocket、Gemini native 入口均已切换，Redis 新增 `concurrency:api_key:{apiKeyID}` 与 `concurrency:wait:api_key:{apiKeyID}`，上游账号级并发保留。验证通过 `GOMAXPROCS=2 go test -p=1 -count=1 -tags=unit ./internal/service ./internal/handler`、`./internal/repository`、`./internal/...`、`go test ./cmd/server` 和 `git diff --check`；未构建镜像、未部署 18084、未改 DB/nginx/Redis/CLIProxyAPI。结果见 `docs/ai/context/20260710-114846-api-key-concurrency-replacement-result_CN.md`。
- 2026-07-10 已确认“每个 API Key 最多 5 并发，用户创建多把 Key 可以扩大并发”的设计方向：替换当前用户级入口并发限制，不再让同一用户所有 Key 共享 `users.concurrency=5`；Sub2API 模型请求入口应按 `api_key_id` 抢 Redis 槽，每把 Key 复用现有默认并发 5，上游账号级并发继续保留。不得再用 CLIProxyAPI `inbound-limits per-api-key=5` 实现此目标，因为那会按 Sub2API 共用上游 Key 聚合误伤全站。设计与计划见 `docs/ai/context/20260710-112016-api-key-concurrency-replacement-design_CN.md` 和 `docs/ai/context/20260710-112016-api-key-concurrency-replacement-implementation-plan_CN.md`。
- 2026-07-10 本地分支 `codex/fix-chat-completions-raw-routing` 已完成 `961109198@qq.com` `/v1/chat/completions` 流式 `stream usage incomplete: missing terminal event` 502 的代码根治：OpenAI APIKey 账号自动模式下，标准 Chat Completions body（有 `messages`）优先 raw 直转上游 `/v1/chat/completions`，继续强制 `stream_options.include_usage=true`；Responses shape、OAuth、`force_responses` 保持原 Responses 路径。TDD 验证通过 `ResolveResponsesSupportMode` 目标红绿、Chat shape 分流目标红绿、`GOMAXPROCS=2 go test -p=1 -count=1 -tags=unit ./internal/pkg/openai_compat ./internal/service` 和 `git diff --check`；未改用户 Key/订阅/额度/DB/nginx/容器，未构建或部署 18084。结果见 `docs/ai/context/20260710-111508-961109198-chat-stream-502-root-fix-result_CN.md`。
- 2026-07-10 已修正 CLIProxyAPI `inbound-limits` 的 per-key 误伤：上一版 `per-api-key-concurrency=5` 会按 Sub2API 共用的 CLIProxyAPI 上游 Key 聚合计数，实际把整条 Sub2API -> CLIProxyAPI 链路卡成 5 并发；图片 `per-api-key=1` 同理会误伤全站图片并发。现已把 `/Users/wujianxiang/CodeSpace/CLIProxyAPI/config.yaml` 改为普通 `global=100/per-api-key=100`，图片 `/v1/images/generations` 与 `/v1/images/edits` 为 `global=10/per-api-key=10`；用户级 5 并发仍由 Sub2API `users.concurrency=5` 控制。记录见 `docs/ai/context/20260710-111144-cliproxyapi-inbound-limits-per-key-correction-result_CN.md`。

- 2026-07-10 已按用户要求修改运行态 `/Users/wujianxiang/CodeSpace/CLIProxyAPI/config.yaml`，启用 `inbound-limits`：全局并发 `100`，单 API Key 并发 `5`，`/v1/images/generations` 与 `/v1/images/edits` 全局图片并发 `10`、单 Key 图片并发 `1`，超限返回 `429 too many concurrent requests`；当前 8317 进程 cwd 为 CLIProxyAPI 且启动参数 `--config config.yaml`，代码支持 watcher 热更新并会调用 `inboundLimiter.SetConfig`，理论上不必重启，低峰重启可作为兜底。记录见 `docs/ai/context/20260710-110539-cliproxyapi-inbound-limits-config-result_CN.md`。

- 2026-07-10 本地已完成 GPT-5.6 完整模型名与 Priority 1.5x 计费实现：`openai.DefaultModels` 和 `/v1/models` 默认 fallback 现在包含 `gpt-5.6-sol`、`gpt-5.6-terra`、`gpt-5.6-luna`，不包含裸 `gpt-5.6`；模型名归一支持紧凑写法但阻止裸/未知 GPT-5.6 回退到 GPT-5.4；内置 pricing JSON 与 BillingService fallback 均补三款模型官方 token 定价、long context 策略和 priority 1.5x；`reasoning_tokens` 不重复叠加到 output 扣费。已通过 `GOMAXPROCS=2 go test -p=1 -count=1 -tags=unit ./internal/pkg/openai ./internal/handler ./internal/service`、`GOMAXPROCS=2 go test -p=1 -count=1 ./cmd/server`、pricing JSON 解析与 `git diff --check`；未构建镜像、未部署 18084、未改 DB/nginx/Redis。结果见 `docs/ai/context/20260710-104141-gpt56-priority15-billing-implementation-result_CN.md`。

- 2026-07-10 已写 GPT-5.6 完整模型名与 Priority 1.5x 计费实施计划：按 TDD 修改默认模型列表、模型名归一、内置 pricing JSON、PricingService long context 解析、安全禁止 `gpt-5.6-*` 缺价回退到裸 `gpt-5.6`、BillingService fallback 与 usage/reasoning 回归；最终约束为只展示/调用 `gpt-5.6-sol`、`gpt-5.6-terra`、`gpt-5.6-luna`，不展示裸 `gpt-5.6`，只有 `service_tier=priority` 按 1.5x，Advanced/Faster/Smarter 等 reasoning 文案均按 1x 和官方 token 单价。计划见 `docs/ai/context/20260710-095810-gpt56-models-priority15-billing-implementation-plan_CN.md`。

- 2026-07-10 已确认 dashboard 删除“按平台拆分”设计：仅改前端，不新增后端逻辑；`/dashboard` 原平台拆分/平台 quota/计费展示区域改为“可用模型”，最终只硬编码展示 6 个模型：`gpt-5.6-sol`、`gpt-5.6-terra`、`gpt-5.6-luna`、`gpt-5.5`、`gpt-5.4`、`gpt-image-2`；用户明确要求不展示 `gpt-image-1`、`gpt-image-1.5`、`gpt-5.4-mini`、`gpt-5.3-codex`、`gpt-5.3-codex-spark`、`codex-auto-review`、`gpt-5.2`；`DashboardView` 应停止拉取 `/user/platform-quotas`，真实后端计费、模型路由和 `/v1/models` 行为不变。设计计划见 `docs/ai/context/20260710-095239-dashboard-available-models-design-plan_CN.md`。
- 2026-07-10 已核对 GPT-5.6 “思考程度”计费：`Advanced`、`Faster`、`Consumes Usage Limits Faster`、`Smarter` 更像产品 UI 文案，不是 OpenAI API 独立价格档位；API 正式控制面是 `reasoning.effort`（GPT-5.6 支持 `none/low/medium/high/xhigh/max`）、`reasoning.mode`（`standard/pro`）和 `service_tier`。永久计费应按上游 usage 事实扣费：input、cached input、cache writes、output tokens 乘对应官方价格；reasoning tokens 已包含在 output tokens 内，只做展示/审计，不能重复扣费。`Faster` 只有在明确映射到 API `service_tier=priority` 时才按 Priority 价格，否则仍按实际 token；Codex/ChatGPT Fast mode credits 不适用于 API key 链路。设计见 `docs/ai/context/20260710-095230-gpt56-reasoning-effort-billing-design_CN.md`。
- 2026-07-10 已写 GPT-5.6 永久支持设计：按用户要求只展示并调用完整模型名 `gpt-5.6-sol`、`gpt-5.6-terra`、`gpt-5.6-luna`，不展示 `gpt-5.6` alias；推荐方案是同时更新 `openai.DefaultModels`、内置 `backend/resources/model-pricing/model_prices_and_context_window.json`、`BillingService` fallback/归一化/长上下文策略与测试，确保展示、调用、官方价格计费和离线 fallback 一致。数据库不应“每模型一列”，现有 `usage_logs.*`、`channel_model_pricing.models` 与 `channel_pricing_intervals` 足够承载；本次不创建默认 channel，不改变当前默认计费来源。设计见 `docs/ai/context/20260710-094223-gpt56-full-model-names-pricing-design_CN.md`。
- 2026-07-10 已只读排查 `sk-LOCAL-454...e28804` 请求 `/v1/models` 没有 GPT-5.6：上游 CLIProxyAPI `8317/v1/models` 已有 `gpt-5.6-sol`、`gpt-5.6-terra`、`gpt-5.6-luna`，且 Sub2API 公网 `/v1/responses` 使用 `gpt-5.6-sol` 已 HTTP 200；但该 Key 通过 active 订阅落到 `group_id=5/codex-pool-local-unlimited`，分组 `models_list_config={"enabled": false}`，绑定账号 `account_id=1/cliproxy-local-openai` 无 `model_mapping`，所以 `/v1/models` 回落到代码静态 `openai.DefaultModels`。单纯重启不会生成 5.6；永久方案是修改 `openai.DefaultModels` 后构建部署，运行态不发版方案是给账号配置完整 identity `model_mapping`（包含旧默认模型与 `gpt-5.6-sol/terra/luna`），必要时再用分组 `models_list_config` 排序/隐藏；单独改 `models_list_config` 会被默认列表过滤，不能凭空显示 5.6。`gpt-5.6` alias 当前真实请求 502，未做 alias 映射前不要展示。结果见 `docs/ai/context/20260710-093251-gpt56-models-list-refresh-diagnosis_CN.md`。
- 2026-07-09 已为 `961109198@qq.com` `/v1/chat/completions` 流式 `stream usage incomplete: missing terminal event` 502 写根治设计与实施计划：不改 Key/订阅/额度，不删除 usage 保护；推荐代码层改为 OpenAI APIKey 账号在自动模式下对标准 Chat Completions body（有 `messages`）优先 raw 直转上游 `/v1/chat/completions` 并保留 `stream_options.include_usage=true`，`/v1/responses` 与 Responses shape 兼容、OAuth、`force_responses` 继续走 Responses 路径。计划按 TDD 修改 `openai_compat` 模式解析与 `ForwardAsChatCompletions()` 入口分流，验证 service/unit 与 `git diff --check`；本轮仅写设计/计划，未改业务代码、DB、nginx、容器。设计见 `docs/ai/context/20260709-220635-961109198-chat-stream-502-root-fix-design_CN.md`，计划见 `docs/ai/context/20260709-220635-961109198-chat-stream-502-root-fix-implementation-plan_CN.md`。
- 2026-07-09 已只读排查 `961109198@qq.com` 截图附近 502：不是 Key、订阅、额度、余额或 Cloudflare 源站整体不可用；用户 `id=71`，active 订阅 `user_subscriptions.id=86/group_id=8/codex-pool-89-usd` 到 `2026-08-06 20:50:54+08`，日用量 `65.094204/89 USD`。截图 `20:41` 对应北京时间 `20:40:31/41/52` 三次 `/v1/chat/completions` 流式重试，`api_key_id=96/mac_hermes`、`model=gpt-5.5`、`User-Agent=OpenAI/Python 2.24.0`、`account_id=1/cliproxy-local-openai`，应用日志均为 `openai_chat_completions.forward_failed: stream usage incomplete: missing terminal event`，nginx access 为 502 但 error.log 同窗口无 upstream 源站错误；当天同用户 `20:29:28/38/50` 也有同因三连失败，`20:29:22` 有一次成功落库。结果见 `docs/ai/context/20260709-215017-961109198-chat-stream-502-diagnosis-result_CN.md`。
- 2026-07-09 已将自动套餐退款、`/v1/responses` 混入 `messages` 本地 400 防御、以及 Docker 前端构建 pnpm 固定发布到公网 18084：新镜像 `sub2api-candidate:20260709-102735-d4fae0839-auto-refund`（image id `sha256:2636c361...`），旧应用容器重命名为 `sub2api-candidate-before-promote-20260709-103251`，只替换应用容器，未重建 Postgres/Redis/nginx/Cloudflare Tunnel；发布后 `18084/health` 通过。构建前曾因 `pnpm@latest` 不再读取 `package.json` 内 `pnpm.overrides` 导致 frozen install 失败，已固定 Dockerfile 与 `frontend/package.json` 到 `pnpm@10.34.4`。未执行真实退款，未推送远端。结果见 `docs/ai/context/20260709-105128-auto-refund-public-deploy-result_CN.md`。
- 2026-07-09 已只读排查用户贴出的 `API call failed after 3 retries: HTTP 502` 且 Cloudflare `origin_bad_gateway` 的问题：这不是 API Key、订阅、额度或模型协议问题；截图响应体是 Cloudflare 源站错误格式。nginx 日志显示 `2026-07-09 08:17-08:36 JST` 期间大量 `connect() failed (61: Connection refused) while connecting to upstream`，upstream 为 `127.0.0.1:18084`，同段 `/v1/chat/completions`、`/v1/models`、控制台 API 均有 502；结合当天 18084 发布前 Docker/Postgres 恢复过程，根因是 Cloudflare Tunnel 后面的 nginx 无法连接 Sub2API 应用容器。`08:36:39 JST` 后源站恢复 200；当前 18084 和公网 `/health` 均为 200。后续应用层 502（如 `no available accounts`、`thinking_signature_invalid`）是另一类问题，不要和 Cloudflare `origin_bad_gateway` 混判。结果见 `docs/ai/context/20260709-101829-cloudflare-origin-502-diagnosis_CN.md`。
- 2026-07-09 已实现用户侧自动套餐退款并打开运行态 ZPay 退款按钮：`/orders` 支付宝月度套餐仍按可退款 provider 显示按钮，余额支付月度套餐即使无 `provider_instance_id` 也显示按钮，流量卡不退款；`POST /api/v1/payment/orders/:id/refund-request` 改为自动退款，支付宝只退 `amount` 基础价剩余完整天数、不退 1% 手续费，余额退款用 `pay_amount` 基数退手续费，剩余天数 `floor((expires_at-now)/24h)`，金额四舍五入到 1 位小数，退款成功后撤销 active subscription。写库前备份 `deploy/backups/20260709-100455-sub2api-candidate-before-zpay-auto-refund.dump` 并验证可读；公网候选库 `ZPay Alipay` 已设置 `refund_enabled=true/allow_user_refund=true/apiBase=https://zpayz.cn`，PID/KEY 只写入运行态 config 不进文档。验证通过后端 service unit、前端退款目标 vitest、`pnpm typecheck`、`git diff --check`；本条为发布前实现记录，后续已发布到 18084，未执行真实退款。结果见 `docs/ai/context/20260709-101745-auto-subscription-refund-result_CN.md`。
- 2026-07-09 已只读排查购买页弹出“需要先和管理员联系来进行退款”：截图当前登录用户为 `2799523972@qq.com`，公网候选库仍有一个未删除、未过期 active 订阅 `user_subscriptions.id=71`，`group_id=9/codex-pool-69-usd`，对应 `79 元订阅池`，到期 `2026-08-02 10:35:29+08`；因此命中 2026-07-08 新增的单 active 订阅购买保护。前端点击任意订阅卡会本地弹 `ACTIVE_SUBSCRIPTION_EXISTS`，后端创建订阅订单也会拒绝；不是支付通道异常。若要支持续费/升级/补差，需另设业务流而不是直接移除此保护。本轮未改代码/DB/容器。结果见 `docs/ai/context/20260709-100908-purchase-active-subscription-block-diagnosis_CN.md`。
- 2026-07-09 本地已完成 `/v1/responses` 混入 Chat Completions `messages` 的服务端防御修复：`OpenAIGatewayHandler.Responses()` 在 model/stream 基础校验后检测“有 `messages` 且无 `input`”，直接返回 `400 invalid_request_error`，message 为 `/v1/responses expects input; use /v1/chat/completions for messages`；不自动转换协议，`/v1/chat/completions` 既有 Chat -> Responses 转换链路不变。TDD 目标测试和 `GOMAXPROCS=2 go test -p=1 -count=1 -tags=unit ./internal/handler`、`git diff --check` 均通过；本条为发布前实现记录，后续已随自动退款镜像发布到 18084。结果见 `docs/ai/context/20260709-100135-responses-messages-defense-result_CN.md`。
- 2026-07-09 已写 `/v1/responses` 混入 Chat Completions `messages` 的服务端防御修复设计问答：Codex Desktop 正常应走 `/v1/responses` 并发送 Responses `input`；若客户端把 `messages` 发到 `/v1/responses`，Sub2API 应在 OpenAI gateway 本地返回 400 `invalid_request_error`，提示使用 `input` 或改走 `/v1/chat/completions`，不建议在 `/v1/responses` 自动把 `messages` 转 `input`，以免掩盖客户端协议错误。后续实施需先补 handler 级失败测试，再做最小 shape guard；不改 DB/nginx/容器。设计见 `docs/ai/context/20260709-095803-responses-messages-defense-design-qa_CN.md`。
- 2026-07-09 分支 `codex/add-formal-api-usage-guide` 已在 `/usage-guide` 新增“规范使用”栏目：左侧导航/移动端 tab 增加正式 `/v1/*` API 使用说明，用表格列出 Responses、Chat Completions、Embeddings、Images、Models、Usage、Claude Messages 等规范 URL，并列出裸 `/responses`、`/models`、`/chat/completions`、`/backend-api/codex/responses` 等旧路径会返回 `400 INVALID_BASE_URL` 及迁移目标；Codex 配置明确 `base_url="https://api.aaccx.pw/v1"`、`wire_api="responses"`。已通过目标 vitest、`pnpm typecheck`、`git diff --check`；未构建镜像、未重启容器。结果见 `docs/ai/context/20260709-094132-usage-guide-formal-api-paths-result_CN.md`。
- 2026-07-09 已按用户要求删除 4 个未被容器引用的 Docker 匿名小 volume：`1d4dce6ca...`、`875d1fb3...`、`29792943...`、`ee97006e...`；未删除当前 `sub2api-candidate-postgres` 引用的匿名 volume `97d3544b...`，也未删除 `deploy_*`、`sub2api-main-preview-*` 等 named data volumes。复核 3 个运行容器均 healthy，`18084/health` 返回 ok。结果见 `docs/ai/context/20260709-093912-docker-anonymous-volume-cleanup-result_CN.md`。
- 2026-07-09 已清理 Docker 旧应用容器、未引用旧镜像和 Build Cache：删除停止态 `sub2api-candidate-before-promote-20260709-083942`、`sub2api-candidate-before-window-refresh-20260707-084458`，执行 `docker builder prune -af` 与 `docker image prune -af`；Build Cache `20.81GB -> 0B`，Images `24.23GB -> 724.3MB`，剩余运行容器仅 `sub2api-candidate`、`sub2api-candidate-redis`、`sub2api-candidate-postgres` 且 healthy。未清理 Docker volumes；当前 12 个 local volumes 中多为旧链路/preview 残留，未确认备份前不要直接 `docker volume prune`。结果见 `docs/ai/context/20260709-093212-docker-image-cache-cleanup-result_CN.md`。
- 2026-07-09 已排查当前 `/responses` 非 200 但 `/chat/completions` 可 200：正式入口仍是 `/v1/*`，裸 `/responses` 与裸 `/chat/completions` 返回 `400 INVALID_BASE_URL` 属预期；`/v1/responses` 使用 Responses 格式 `input` 已真实返回 200，`/v1/chat/completions` 使用 Chat Completions 格式 `messages` 已真实返回 200。最新可复现失败是把 `messages` 发到 `/v1/responses`，上游返回 `400 Unsupported parameter: messages`，Sub2API 当前包装为 502；建议客户端按协议分流，后续可选在 `/v1/responses` 本地校验 `messages` 并返回 400。结果见 `docs/ai/context/20260709-092958-responses-vs-chat-completions-diagnosis-result_CN.md`。
- 2026-07-09 已只读核查当前退款设置：公网候选库唯一支付实例为 `payment_provider_instances.id=1 / easypay / ZPay Alipay`，`supported_types=alipay`、`payment_mode=popup`、`enabled=true`，但 `refund_enabled=false` 且 `allow_user_refund=false`；支付宝可见入口 `payment_visible_method_alipay_source=easypay_alipay`。代码层面官方支付宝 provider 会调用 `TradeRefund`，EasyPay/ZPay provider 会调用 `api.php?act=refund`，但当前运行态退款被 Sub2API 拦在 provider 开关处，不能承诺“支付宝原路退回”；若继续用 ZPay，只能在确认 ZPay 退款能力后表述为“通过 ZPay/EasyPay 原支付通道退款”。当前库无退款状态订单和 `REFUND_*` 审计流水；本轮未改 DB/容器/代码。结果见 `docs/ai/context/20260709-073340-payment-refund-current-state-audit_CN.md`。
- 2026-07-09 已将本地 `main` 的 149/199 元订阅套餐改动同步到远端 `personal/main`：提交 `32725ff31 feat: add 149 and 199 subscription plans`，常规执行 `git push personal main`，推送范围 `5c9a6253f..32725ff31`，未使用 force push；推送前 `personal/main...main = 0 1`，远端没有本地缺失提交。结果见 `docs/ai/context/20260709-073228-personal-main-sync-result_CN.md`。
- 2026-07-09 本地已用 149/199 两档替代上一轮未发布的 198 元套餐方案：删除未提交的 `backend/migrations/161_seed_codex_198_subscription_plan.sql`，新增 `backend/migrations/161_seed_codex_149_199_subscription_plans.sql`，幂等创建/更新 `149 元订阅池`/`codex-pool-135-usd`（基础价 `149.00`、每日 `135 USD`、`sort_order=149`）和 `199 元订阅池`/`codex-pool-179-usd`（基础价 `199.00`、每日 `179 USD`、`sort_order=199`）；前端购买页测试覆盖“阅读订阅套餐F=149/135刀”和“阅读订阅套餐G=199/179刀”，并验证 `amount=199/plan_id=7` 下单。TDD 已验证 RED/GREEN，`go test -count=1 ./migrations`、`pnpm vitest run src/views/user/__tests__/PaymentView.spec.ts`、`git diff --check` 均通过。本轮未构建镜像、未部署 18084、未改 DB/nginx/Redis；公网发布后必须单独把 `cliproxy-local-openai` 绑定到 `codex-pool-135-usd` 与 `codex-pool-179-usd` 并做真实 `/v1/responses` 验收。结果见 `docs/ai/context/20260709-072356-codex-149-199-subscription-result_CN.md`。
- 2026-07-08 已将当前本地 `main` 同步到远端 `personal/main`：先提交上下文 `5e960634c docs: record public model path verification`，再常规执行 `git push personal main`，首次推送范围 `6153d2707..5e960634c`，未使用 force push。结果见 `docs/ai/context/20260708-213614-personal-main-sync-result_CN.md`。
- 2026-07-08 当前准备将本地 `main` 同步到远端 `personal/main`：`git fetch personal main` 后确认 `personal/main...main = 0 23`，远端没有本地缺失提交；同步方式为先提交当前上下文文档与 `AGENTS.md` 记忆，再常规推送到 `personal main`，不做 force push。计划见 `docs/ai/context/20260708-213457-personal-main-sync-plan_CN.md`。
- 2026-07-08 本地已修复后端直连裸模型 API 被 embedded frontend SPA fallback 吃掉的问题：`backend/internal/web/embed_on.go` 的 `shouldBypassEmbeddedFrontend()` 已补齐 `/models`、`/chat/completions`、`/embeddings`，让这些路径继续进入已注册的 `invalidBaseURLHandler` 并返回 `400 INVALID_BASE_URL`；`backend/internal/web/embed_test.go` 已补回归测试。TDD 已验证 RED/GREEN，`GOMAXPROCS=2 go test -p=1 -count=1 -tags=embed ./internal/web`、`GOMAXPROCS=2 go test -p=1 -count=1 -tags=unit ./internal/server/routes`、`git diff --check` 均通过；本轮未构建镜像、未替换/重启 18084 容器、未改 nginx/DB/Redis，直连 18084 运行态需发布新镜像后才生效。结果见 `docs/ai/context/20260708-205941-backend-direct-bare-api-path-fix-result_CN.md`。
- 2026-07-08 本地已修复并审计 `3876129758@qq.com` 用自动 Key 请求 `https://api.aaccx.pw/v1/models` 返回 403 的问题：根因是 Sub2API 自动 Key 白名单漏放行 `/v1/models`，不是 CLIProxyAPI 上游、订阅、额度或 Key 无效；除中间件 `automaticKeySupportsOpenAIEndpoint()` 外，service 层 `isFormalOpenAIRequestPath()` 也有重复白名单漏项，均已补 `/v1/models` 并补回归测试。已核对 `/v1` 当前实际注册网关路由，未发现其它已注册正式 OpenAI 兼容路径漏放行；`/v1/audio/*`、`/v1/files`、`/v1/batches`、`/v1/moderations` 等当前未注册，不属于漏白名单。验证通过 `go test -count=1 -tags=unit ./internal/service ./internal/server/middleware ./internal/server/routes`；本轮未构建镜像、未替换/重启 18084 容器、未改 DB/nginx，公网需发布新镜像后才生效。结果见 `docs/ai/context/20260708-210025-auto-key-whitelist-audit-result_CN.md`。
- 2026-07-08 已只读核查最新 18084 镜像后的 Nginx 正式 `/v1` API 约束：`sub2api-candidate:20260708-203429-cc845e468-formal-v1-api` healthy，`nginx -t` 通过；`api.aaccx.pw` 与 `aaccx.pw` 经 8080 访问裸 `/models`、`/responses`、`/responses/*`、`/chat/completions`、`/embeddings`、`/images/generations`、`/images/edits`、`/backend-api/codex/responses`、`/backend-api/codex/responses/*` 均返回 `400 INVALID_BASE_URL`，`/v1/responses` 与 `/v1/chat/completions` 返回 `401 API_KEY_REQUIRED` 而不是裸路径错误。注意不能说“所有请求都往 `/v1` 发送”，因为 `/api/*`、控制台页面、静态资源和 health 是正常非模型 API/前端流量；直连 18084 仍有 `/models`、`/chat/completions`、`/embeddings` 返回前端 HTML 200 的后端遗漏，需要后续修真实 router 的 SPA fallback 顺序。结果见 `docs/ai/context/20260708-205014-nginx-formal-v1-api-audit-result_CN.md`。

- 2026-07-08 已复现 `3876129758@qq.com` 用当前 active Key（`api_keys.id=106/Codex++`，脱敏 `sk-6abb393...0cbd`）请求 `https://api.aaccx.pw/v1/models` 返回 403：错误为 `AUTO_KEY_UNSUPPORTED_ENDPOINT: Automatic API Key is not supported for this endpoint.`；这是 Sub2API 自动 Key endpoint policy 漏放行 `/v1/models`，不是 CLIProxyAPI 上游、订阅、额度或 Key 无效问题。同一 Key 请求 `/v1/responses` + `gpt-5.5` 已 200；`/v1/models` 请求后 `last_used_at` 更新，说明认证通过但中间件拒绝。修复点：`backend/internal/server/middleware/effective_group.go` 的 `automaticKeySupportsOpenAIEndpoint()` 增加 `/v1/models`，并补 middleware 测试；本轮未改代码/DB/容器/nginx。结果见 `docs/ai/context/20260708-205013-3876129758-v1-models-auto-key-403-diagnosis_CN.md`。
- 2026-07-09 已修复公网 18084 TLS 链路：生成 cli-proxy TLS 证书（10年），启用 TLS，Dockerfile 嵌入 CLIProxy CA；`/v1/responses` 和 `/v1/chat/completions` 均 200。CA 位置 `backend/resources/certs/tls.crt`。结果见 `docs/ai/context/20260709-105010-public-18084-tls-fix-result_CN.md`.
- 2026-07-08 已只读排查 `xinlise@gmail.com` 与 `3876129758@qq.com` 的日志问题：两者当前 Key/订阅均可用，真实请求均 200；日志问题主要不是 Key/订阅，而是上游账号池单点与流式终止事件。`3876129758@qq.com` 今日有 111 条 `/v1/chat/completions` 流式 `stream usage incomplete: missing terminal event`（`api_key_id=75` 100 条、`104` 11 条），另有 `api_key_id=106` 请求 `gpt-4o-mini` 时唯一上游返回 502 后 `no available accounts`；旧订阅 `id=76` 的扣费只发生在删除前，删除后均扣 `subscription_id=90/group_id=4`。`xinlise@gmail.com` 今日 `api_key_id=99` 成功请求 499 次、494 次流式、总费用 `61.8332090000`，并在 16:50-16:56 有 61 次上游 503 后 `no available accounts`，09:02-09:05 有 30 次上游 400 `encrypted content could not be decrypted or parsed`；`group_id=4` 与 `8` 当前都只绑定 `account_id=1/cliproxy-local-openai`。结果见 `docs/ai/context/20260708-201626-two-users-log-issue-diagnosis_CN.md`。
- 2026-07-08 已用 `3876129758@qq.com` 最近使用的 active Key（`api_keys.id=106/Codex++`，脱敏 `sk-6abb393...0cbd`）真实请求公网 `https://api.aaccx.pw/v1/responses` + `gpt-5.5`，HTTP 200、状态 `completed`、无 error；新增 `usage_logs.id=67410`，`subscription_id=90`、`group_id=4/codex-pool-49-usd`、`total_cost=0.0039960000`，未扣流量卡；订阅今日用量从 `0.2819010000` 增至 `0.2858970000`，旧订阅 `id=76` 已 `expired + deleted_at` 未参与。本轮未手工改 DB、未构建/替换/重启容器、未改 nginx/Redis。结果见 `docs/ai/context/20260708-201120-3876129758-api-key-real-request-result_CN.md`。
- 2026-07-08 已用 `xinlise@gmail.com` 最近使用的 active Key（`api_keys.id=99/codex`，脱敏 `sk-f1acac8...9374`）真实请求公网 `https://api.aaccx.pw/v1/responses` + `gpt-5.5`，HTTP 200、状态 `completed`、无 error；新增 `usage_logs.id=67361`，`subscription_id=88`、`group_id=8/codex-pool-89-usd`、`total_cost=0.0039960000`，未扣流量卡。该用户还有第二把 active Key `api_keys.id=102/佳一老师` 未测试。测试附近另有用户自己的 Codex Desktop 流式请求 `usage_logs.id=67362`，不要误算到本次 curl；本轮未手工改 DB、未构建/替换/重启容器、未改 nginx/Redis。结果见 `docs/ai/context/20260708-200701-xinlise-api-key-real-request-result_CN.md`。
- 2026-07-08 已诊断 `3876129758@qq.com` 截图中的 502：不是旧 29 元订阅/59 元订阅/Key 绑定问题；当前用户只剩 `user_subscriptions.id=90` 一个未删除 active 订阅，旧 `id=76` 已 `expired + deleted_at`。截图对应失败为 `2026-07-08 17:07:56+08` 的 `/v1/chat/completions` 流式请求，已选中 `api_keys.id=75`、`group_id=4/codex-pool-49-usd`、`model=gpt-5.5`，但 Sub2API 报 `stream usage incomplete: missing terminal event` 后返回 502。随后同 Key 非流式 chat 返回 200 并新增 `usage_logs.id=66675`，同 Key 流式 chat 返回 200 且 SSE 含 usage + `[DONE]`，新增 `usage_logs.id=66690`，均扣在 `subscription_id=90`；判断为偶发流式终止 usage 事件缺失，不是订阅问题。本轮未改 DB、未构建/替换/重启 18084 容器。结果见 `docs/ai/context/20260708-181100-3876129758-chat-stream-502-diagnosis-result_CN.md`。
- 2026-07-08 已按用户要求删除 `3876129758@qq.com` 的旧 29 元 active 订阅且不发布本地代码到公网：写库前备份 `deploy/backups/20260708-175300-sub2api-candidate-before-3876129758-delete-old-29-subscription.dump` 并用 `pg_restore -l` 验证可读；将旧订阅 `user_subscriptions.id=76` 从 `active/deleted_at=NULL` 改为 `expired/deleted_at=2026-07-08 16:58:50.722897+08`，保留当前 59 元订阅 `id=90` 为唯一未删除 active 订阅；随后用该用户 active 自动 Key 真实请求 `https://api.aaccx.pw/v1/responses` + `gpt-5.5` 返回 200，新增 `usage_logs.id=66563`、`subscription_id=90`、`group_id=4/codex-pool-49-usd`、`total_cost=0.0044060000`，未扣流量卡；18084/8080 health 均为 200，未构建镜像、未替换容器、未重启服务。结果见 `docs/ai/context/20260708-175300-3876129758-delete-old-29-subscription-result_CN.md`。
- 2026-07-08 已优先处理 `3876129758@qq.com` 当前 59 元套餐可用性：写库前备份公网候选库到 `deploy/backups/20260708-174022-sub2api-candidate-before-3876129758-sub90-usage-reset.dump`，并用 `pg_restore -l` 验证备份可读；已清零当前 59 元套餐 `user_subscriptions.id=90` 的 daily/weekly/monthly usage，随后用该用户 active 自动 Key（`api_keys.id=75`，`group_id=NULL`，掩码 `sk-c7b2c6c...5607`）真实请求 `https://api.aaccx.pw/v1/responses` + `gpt-5.5` 返回 200，新增 `usage_logs.id=66379`、`subscription_id=90`、`group_id=4/codex-pool-49-usd`、`total_cost=0.0049760000`，未扣流量卡；当时旧 29 元套餐 `user_subscriptions.id=76` 仍 active，后续已按上一条删除。结果见 `docs/ai/context/20260708-174022-3876129758-reset-current-subscription-usage-and-real-request-result_CN.md`。
- 2026-07-08 已修复“同一用户可购买多个生效订阅”的 bug：后端订阅下单校验新增 `ACTIVE_SUBSCRIPTION_EXISTS`，已有任意 active 未过期订阅时拒绝新订阅购买，message 固定为 `需要先和管理员联系来进行退款`；余额支付订阅路径已补传 `UserID`，不会绕过拦截；前端购买页点击订阅卡片/弹窗选套餐/确认支付前也会弹同一句提示，并补中文/英文 i18n。验证已通过后端目标红绿测试、`go test -count=1 -tags=unit ./internal/service`、前端目标 vitest、`pnpm typecheck`、`go test -count=1 ./cmd/server`、`git diff --check`。结果见 `docs/ai/context/20260708-173725-single-active-subscription-purchase-guard-result_CN.md`。
- 2026-07-08 已在本地 `main` 完成后端测试 stub 卫生优化且未影响 18084 公网服务：`backend/Makefile` 新增 `test-server-unit`，固定 `go test -count=1 -tags=unit ./internal/server`；`api_contract_test.go` 新增 stub 误调用测试，并把 `CalibrateActiveDailyUsageWindows`/`CountStaleActiveDailyWindows` 从空成功改为 `errors.New("not implemented")`。已按 TDD 验证红绿，`make -C backend test-server-unit`、目标 unit、`go test -count=1 -tags=unit ./...`、`go test -count=1 ./...`、`git diff --check` 均通过；未构建镜像、未重启容器、未改 nginx/DB/Redis、未部署、未提交 git。结果见 `docs/ai/context/20260708-172141-test-stub-hygiene-optimization-result_CN.md`。
- 2026-07-09 已成功将本地 `main` HEAD `f2cb7e705`（含 feat: add 149 and 199 subscription plans）发布到公网 18084：新镜像 `sub2api-candidate:20260709-083942-f2cb7e705-new-subscription-plans`（image id `sha256:f1b17fce...`），只替换应用容器，保留 Postgres/Redis/nginx；18084/api/dashboard/purchase 均为 200。未执行人工浏览器验收，未修改 nginx/Cloudflare Tunnel。发布前清理了 Xcode 缓存（释放 30GB+），修复了 Docker Hub 凭证损坏、Postgres 18 PGDATA 路径和 pg_hba.conf trust 认证问题。结果见 `docs/ai/context/20260709-084617-public-18084-new-subscription-plans-redeploy-result_CN.md`.
- 2026-07-08 已写后端测试 stub 卫生优化设计：推荐在 `backend/Makefile` 增加 `test-server-unit`，固定使用 `go test -count=1 -tags=unit ./internal/server`；同时把 `api_contract_test.go` 中 `CalibrateActiveDailyUsageWindows` 与 `CountStaleActiveDailyWindows` stub 从空成功改为 `errors.New("not implemented")`，让误调用失败优先。设计见 `docs/ai/context/20260708-170920-test-stub-hygiene-optimization-design_CN.md`。
- 2026-07-08 已写“只保留 `/v1/*` 为正式模型 API”的实施计划：按 TDD 先让后端裸路径测试失败，再新增后端 `400 INVALID_BASE_URL` handler 并移除裸兼容转发，同步收敛 `stream_error_event` 对裸 `/responses` 的历史判断；随后修改 Nginx `aaccx-root.conf`/`cliproxy.conf` 让裸 `/models`、`/responses`、`/chat/completions`、`/embeddings`、`/images/*`、`/backend-api/codex/responses` 返回 400，并验证 8080/18084/health/控制台。计划见 `docs/ai/context/20260708-170543-formal-v1-only-api-implementation-plan_CN.md`。
- 2026-07-08 已确认 API 规范化目标：只保留 `/v1/*` 为正式模型 API；裸 `/responses`、`/responses/*`、`/chat/completions`、`/embeddings`、`/images/generations`、`/images/edits`、`/models` 以及 `/backend-api/codex/responses`、`/backend-api/codex/responses/*` 要在 Nginx 和后端同时返回 `400 INVALID_BASE_URL`，提示使用 `base_url=https://api.aaccx.pw/v1`；不做 301/302 redirect。设计见 `docs/ai/context/20260708-164114-formal-v1-only-api-design_CN.md`。
- 2026-07-08 已核查当前 Nginx 生效配置：`nginx -T` 通过，实际加载 `/opt/homebrew/etc/nginx/nginx.conf`、`mime.types`、`servers/aaccx-root.conf`、`servers/cliproxy.conf`；`backups/*`、顶层 `.bak`、`*.default` 未生效。不规范/建议收敛点包括保留 Homebrew 默认 `localhost` server、无条件 `Connection: upgrade`、`proxy_read_timeout 86400` 和 `proxy_request_buffering off` 覆盖过宽、`sub_filter + Accept-Encoding "" + no-store` 静态资源改写 hack、`Clear-Site-Data` 长期挂根页面、`aaccx.pw /assets/*` 被 Sub2API 抢占、公开 `/SKILL.md`、裸 `/responses` 只能作为兼容别名不应宣传。结果见 `docs/ai/context/20260708-162554-nginx-active-config-audit_CN.md`。
- 2026-07-08 已成功将本地 `main` HEAD `4bf902234`（fix: 补齐模型路径白名单与裸路径拦截）发布到公网 18084：新镜像 `sub2api-candidate:20260708-211028-4bf902234-model-path-whitelist`（image id `sha256:c437fe24...`），只替换应用容器，保留 Postgres/Redis/nginx；18084/8080/api/dashboard/purchase 均为 200，日志无关键错误。未执行人工浏览器验收，未修改 nginx/Cloudflare Tunnel。上次备份（Postgres 36MB + Redis 130KB）仍有效。结果见 `docs/ai/context/20260708-211958-public-18084-model-path-whitelist-redeploy-result_CN.md`.
- 2026-07-08 已深度核查 `docs/ai/context/20260708-104500-test-stub-missing-methods-fix_CN.md`：`UserSubscriptionRepository` 新增方法导致的 server unit stub 编译阻塞已解决，unit/default/integration 编译门/e2e 编译门均通过；未发现其它测试 stub 缺方法。残留不完美点：原文档命令应写 `go test -tags=unit ./internal/server`，且 `api_contract_test.go` 新增 stub 返回空结果和 nil，建议改为 `not implemented` 以免未来误调用被伪装成成功。结果见 `docs/ai/context/20260708-121254-test-stub-missing-methods-deep-audit_CN.md`。
- 2026-07-08 本地分支 `codex/east8-subscription-auto-refresh` 已完成东八区订阅日用量自动刷新重构：删除 `CheckBillingEligibility` API 入口 DB 写入式惰性刷新，改为只读窗口归一化准入；新增完成时记账窗口自愈，跨零点按 `usage_logs.created_at` / `CompletedAt` 计入当天；新增 active 订阅后台 daily window 校准 scheduler，按今天 usage_logs 聚合分批推进 stale active 订阅、失效 billing cache，并在单轮结束仍有 stale remaining 时输出 `ALERT` 日志；`/v1/usage` 支持自动 Key 并做订阅窗口展示归一化。已通过 Wire 生成、后端 unit、repository integration、`go test ./cmd/server`、前端 typecheck、`git diff --check`；未提交 git、未构建镜像、未部署公网。结果见 `docs/ai/context/20260708-112027-east8-subscription-auto-refresh-result_CN.md`。
- 2026-07-08 已写东八区订阅日用量自动刷新实施计划：按 TDD 拆为完成时间字段、完成时订阅扣费窗口自愈、active 订阅后台校准仓储、删除 API 入口写库刷新并改只读归一化准入、后台 scheduler 服务、`/v1/usage` 自动 Key 与展示归一化、整体回归和上线验收；实施时默认不提交 git，除非用户明确要求。计划见 `docs/ai/context/20260708-092157-east8-subscription-auto-refresh-implementation-plan_CN.md`。
- 2026-07-08 已核对 `cnfoxian@gmail.com` 提供的完整 Windows Codex `config.toml`：完整 TOML 语法解析通过，`notify`、marketplace、node_repl MCP、projects 路径写法语法上可用；请求关键字段仍是 `model_provider=custom`、`model=gpt-5.5`、`wire_api=responses`、`requires_openai_auth=true`、`base_url=https://api.aaccx.pw/v1`，与此前真实 `codex exec` 成功落库的配置一致。插件/desktop/MCP/Windows sandbox 不改变模型请求链路；仍建议删除 `disable_response_storage=true`，因为 Codex 0.142.5 strict-config 会报 unknown field。结果见 `docs/ai/context/20260708-102942-cnfoxian-full-codex-config-parse-check_CN.md`。
- 2026-07-08 已成功将本地 `main` HEAD `cc845e468`（含 merge: enforce formal v1 api paths）发布到公网 18084：新镜像 `sub2api-candidate:20260708-203429-cc845e468-formal-v1-api`（image id `sha256:c8f24c939...`），只替换应用容器，保留 Postgres/Redis/nginx；18084/8080/api/dashboard/purchase 均为 200，日志无关键错误。未执行人工浏览器验收，未修改 nginx/Cloudflare Tunnel。上次备份（Postgres 36MB + Redis 130KB）仍有效。结果见 `docs/ai/context/20260708-204335-public-18084-formal-v1-api-redeploy-result_CN.md`.
- 2026-07-08 已修正公网 18084 的 2026-07-07 旧邀请返利：写库前备份 `deploy/backups/20260708-100436-sub2api-candidate-before-affiliate-recalc.dump`；删除旧 20% 返利流水 `15.80/19.80` 和已转余额 `19.80`，按当前 8%、单被邀请用户上限 100 元重算为 `6.32/7.92`，按原订单返利时间冻结 24 小时，两个邀请人当前 `aff_frozen_quota=6.32/7.92`、`balance=0`；旧金额残留数为 0，18084/8080 health 正常。同轮本地修复实质漏返 bug：支付宝 `traffic_pack` 履约原先未调用 `applyAffiliateRebateForOrder()`，会导致被邀请用户买 GPT 流量包不返利；已按 TDD 补测试并修复，后端 service 单测和前端 aff_code 传递目标测试通过。代码修复尚未部署公网。结果见 `docs/ai/context/20260708-101706-affiliate-rebate-recalculation-and-traffic-pack-bugfix-result_CN.md`。
- 2026-07-08 已修复用户 `/affiliate` 页返利金额币种展示：可转返利额度、历史返利额度，以及同页冻结额度、邀请明细返利和转入成功提示均使用人民币窄符号 `¥`；仅改前端展示，不改后端返利计算和 API 字段。计划见 `docs/ai/context/20260708-101332-affiliate-rebate-rmb-symbol-ui-plan_CN.md`。
- 2026-07-08 已只读核查公网 18084 的 2026-07-07 邀请返利记录：昨天东八区共有 3 条邀请绑定，`1510623550@qq.com -> zhudi189@gmail.com`、`1038686518@qq.com -> 1032726009@qq.com`、`1038686518@qq.com -> 961109198@qq.com`；其中 `zhudi189@gmail.com` 的 79 元订阅产生返利 `15.80`，`961109198@qq.com` 的 99 元订阅产生返利 `19.80` 且邀请人已转入余额，`1032726009@qq.com` 仅绑定未支付所以无返利流水。昨天流水按旧 20% 规则生成，今天 160 迁移后的 8% 不 retroactively 改写。结果见 `docs/ai/context/20260708-095632-public-affiliate-records-20260707-audit_CN.md`。
- 2026-07-08 已只读核查当前公网 18084 东八区订阅日窗口刷新：代码确实实现 API 计费入口惰性刷新，但不是 00:00 全量后台任务；当前 52 个未过期 active 订阅中 38 个 `daily_window_start` 仍早于 `2026-07-08 00:00:00+08`，25 个保留昨天非零 `daily_usage_usd`，所以“所有用户真实写库刷新”不成立。今天真正发生订阅扣费的 13 个 active 订阅均已刷新到今天 0 点，且 `user_subscriptions.daily_usage_usd` 与今天 `usage_logs.total_cost` 聚合一致，未发现今天扣费但订阅表不展示的错账；`/subscriptions` 与管理订阅列表会返回前归一化旧窗口，不会把昨天加进今天展示。独立风险：当前 68 个 active API Key 全是自动 Key（`group_id=NULL`），`/v1/usage` 不在自动 Key endpoint policy 中，Key Usage 页可能被拒或拿不到订阅额度块；固定分组路径也不会触发窗口刷新，需单独修。结果见 `docs/ai/context/20260708-095354-east8-subscription-daily-window-refresh-audit_CN.md`。
- 2026-07-08 已验证 `cnfoxian@gmail.com` 截图中的 Codex custom provider 配置：`model_provider/custom`、`model=gpt-5.5`、`wire_api=responses`、`requires_openai_auth=true`、`base_url=https://api.aaccx.pw/v1` 思路正确，使用该用户 active Key 和临时 `CODEX_HOME` 真实 `codex exec` 可返回 `OK`，并在公网 DB 写入 `usage_logs.id=63383/63390`；但 `disable_response_storage=true` 在 Codex 0.142.5 的 `--strict-config` 下是 unknown field，普通运行会忽略，推荐删除；截图 `notify` 行若未闭合也会导致 TOML 解析失败。结果见 `docs/ai/context/20260708-095400-cnfoxian-codex-custom-provider-config-test-result_CN.md`。
- 2026-07-08 已复现并定位 `cnfoxian@gmail.com` 请求 `step-3.7-flash` 返回 502：同一 active Key 请求 `step-3.7-flash` 稳定返回 502，日志为唯一 OpenAI 上游 `cliproxy-local-openai/account_id=1` 返回 upstream 502 后无第二账号可 failover，最终 `no available accounts`；同一 Key 请求 `gpt-5.5` 返回 200，并刷新订阅日窗口到 `2026-07-08 00:00:00+08`，新增 `usage_logs.id=63256`、扣订阅 `0.004586 USD`。不是额度/Key/余额支付新 bug；订阅额度不足仍可请求是设计，因 OpenAI 流量卡兜底；失败 502 不写 `usage_logs`、不扣费，只在 `ops_system_logs` 记录。结果见 `docs/ai/context/20260708-094536-cnfoxian-step-502-quota-usage-diagnosis-result_CN.md`。
- 2026-07-08 已将本地 `main` HEAD `a93548db1`（fix: hide balance payment fee label）发布到公网 18084：新镜像 `sub2api-candidate:20260708-094943-a93548db1-frontend-fee-label-fix`（image id `sha256:fd481ca9...`），只替换应用容器，保留 Postgres/Redis/nginx；DB/返利 settings 未变；18084/8080/api/dashboard/purchase 均为 200，前端资源更新为 `app-index-CXRS0hdS.js`。未执行人工浏览器验收，未修改 nginx/Cloudflare Tunnel。上次备份（Postgres 36MB + Redis 130KB）仍有效。结果见 `docs/ai/context/20260708-100610-public-18084-frontend-fee-label-fix-redeploy-result_CN.md`.
- 2026-07-08 本地分支 `codex/rmb-balance-payment-affiliate-rebate` 已完成人民币余额支付与邀请返利重构：余额充值支付宝-only 且 1:1 人民币入账；用户购买页只展示支付宝和余额；余额支付套餐/流量包走 `payment_type=balance` 完成订单并事务内扣 `pay_amount`，不产生返利；支付宝完成订单按 `amount` 返利，默认 8%、冻结 24 小时、有效期 365 天、单被邀请用户累计上限 ¥100；订单展示与余额文案改为人民币，后台订单筛选补 `balance`/`traffic_pack`。已通过后端 unit/migrations、前端 typecheck/目标 vitest/build、`go test ./cmd/server`；未部署公网、未执行本地浏览器手工验收。结果见 `docs/ai/context/20260708-090140-rmb-balance-payment-affiliate-rebate-result_CN.md`。
- 2026-07-07 已写人民币余额支付与邀请返利重构实施计划：计划按 TDD 拆为订单类型/金额口径、外部支付支付宝-only、余额支付事务、邀请返利默认值、前端充值/余额支付、订单展示、人民币文案和全量验收；执行时必须先修正 `users.balance` 透支扣减风险，余额购买只能条件扣款成功后发货。计划见 `docs/ai/context/20260707-230444-rmb-balance-payment-affiliate-rebate-implementation-plan_CN.md`。
- 2026-07-07 已定人民币余额支付与邀请返利重构方案：`users.balance` 统一视为人民币余额；用户侧 `/purchase` 只展示支付宝与余额，余额充值卡固定为商品列表第一张，充值金额 1-100 整数、默认 1 元、只走支付宝且无手续费；商品购买保留支付宝直付和余额支付，余额不足直接进入充值确认页，不做混合支付。余额支付购买套餐/流量包要生成 `payment_type=balance` 完成订单并扣含手续费 `pay_amount`，但不产生返利；支付宝余额充值、支付宝直购套餐和流量包产生邀请返利，返利比例 8%、冻结 24 小时、有效期 365 天、单个被邀请用户累计上限 ¥100，返利基数为 `amount` 而非含手续费 `pay_amount`。同轮还要求修复 `order_type` 非法值白名单和后台订单筛选缺少 `traffic_pack`。设计见 `docs/ai/context/20260707-225836-rmb-balance-payment-affiliate-rebate-design_CN.md`。
- 2026-07-07 本地 `main` 站内余额是真实现：后端支持 `order_type=balance` 余额充值订单，ZPay 对应 EasyPay provider，`/api/v1/payment/webhook/easypay` 验签和金额校验后会把订单置为 paid 并履约，最终通过内部余额兑换码自动调用 `userRepo.UpdateBalance()` 增加 `users.balance`；API 用量也会扣 `users.balance`。但当前没有“用站内余额购买套餐或流量卡”的支付分支，前端购买页只选择外部支付方式，后端也没有 `payment_type=balance` 或 `pay_from_balance` 入口。结果见 `docs/ai/context/20260707-110848-main-balance-payment-chain-inspection-result_CN.md`。
- 2026-07-07 本地 `main` 已实现邀请返利闭环：返利码为 `aff_code`，区别于注册准入 `invitation_code`；用户页 `/affiliate` 可复制 `/register?aff=...` 邀请链接，余额充值/订阅购买完成和正数余额兑换码会按比例给邀请人产生返利，默认比例 20%、默认开启，可配置冻结期/有效期/单被邀请人上限和用户专属比例。用户“提现”当前是 `POST /api/v1/user/aff/transfer` 将可用返利全部转入站内余额，并写 `user_affiliate_ledger(action='transfer')`；没有外部支付宝/银行卡/USDT 打款提现流程。结果见 `docs/ai/context/20260707-103748-main-affiliate-invite-rebate-withdraw-inspection-result_CN.md`。
- 2026-07-07 已在当前公网 `sub2api-candidate-postgres` 为 `chunpo.wu@uni-konstanz.de` 手动新增 99 元订阅：`user_subscriptions.id=87`，`user_id=72`，`group_id=8/codex-pool-89-usd`，有效期 `2026-07-07 21:44:49+08` 到 `2026-08-06 21:44:49+08`，未创建支付订单，保留其自动分组 Key `api_keys.id=95`；写库前备份见 `deploy/backups/20260707-224343-sub2api-candidate-before-chunpo-99-subscription.dump`。结果见 `docs/ai/context/20260707-224900-chunpo-99-subscription-public-db-result_CN.md`。
- Sub2API 是唯一公网 API 入口，也是唯一用户 Key、计费和用量事实源。
- CLIProxyAPI 只作为内网账号池、OAuth、协议转换和轮询上游；yui.web/shop 只保留展示、说明和跳转。
- 当前主链路：`Cloudflare Tunnel -> nginx 127.0.0.1:8080 -> sub2api-candidate 127.0.0.1:18084 -> CLIProxyAPI 127.0.0.1:8317`。
- 不要让 Sub2API、yui.web、CLIProxyAPI 同时对同一个用户 Key 做状态判定或扣费。
- 不要在文档、提交或日志中记录完整 API Key、内部 token、HMAC secret、SMTP 密码。

## 当前运行态提醒

- `aaccx.pw/shop` 归 yui.web；`aaccx.pw/v1/*`、`/api/*` 和 Sub2API 控制台路由归 Sub2API；`api.aaccx.pw` 也是 Sub2API 入口。
- 2026-06-27 当前公网链路已由 18084 候选环境承接：`Cloudflare Tunnel -> nginx 127.0.0.1:8080 -> sub2api-candidate 127.0.0.1:18084 -> CLIProxyAPI 127.0.0.1:8317`，容器内访问上游聚合入口为 `host.docker.internal:8317`；`sub2api-candidate` healthy，DB 为 `sub2api-candidate-postgres`，候选最新库为 47 users / 40 keys / 191 migrations；旧 `sub2api` 18080 已 `Exited (0)`，`weishaw/sub2api:latest` 镜像暂留；Nginx 反代全部指向 18084，`/purchase`、`/health`、`/v1/responses`、`/v1/chat/completions` 均为 200，LLM 真实回复已证明 `sub2api -> CLIProxyAPI -> 上游 OpenAI` 全栈打通。结果见 `docs/ai/context/20260627-102157-18084-public-candidate-chain-agents-update-result_CN.md`。
- 2026-06-27 已按用户要求停止旧链路残留容器：`sub2api-postgres`、`sub2api-redis`、`sub2api-preview`（原 18081）、`sub2api-main-existingdb-preview`（原 18083）；当前公网 `sub2api-candidate`、`sub2api-candidate-postgres`、`sub2api-candidate-redis` 仍 healthy，`18084/health` 与 `8080/health` 均为 200。结果见 `docs/ai/context/20260627-103515-stop-old-18080-db-redis-preview-result_CN.md`。
- 2026-06-27 蓝绿测试端口与邮箱验证码排查：`8080` 是 nginx 公网入口并反代 `18084`，`18084` 是当前公网候选，`18082` 是旧 main-preview；本地 main 前端建议用 `5174 -> 18082`，数据库/Redis 仅容器内使用 `5432/6379`，如需宿主机调试用 `15432/16379`。18084 验证码接口返回 200 但 worker 报 `EMAIL_NOT_CONFIGURED`，原因是候选库 SMTP 配置为空；18082 则是 `REGISTRATION_DISABLED`。结果见 `docs/ai/context/20260627-110649-blue-green-email-verify-port-plan_CN.md`。
- 2026-06-27 已另起 SMTP 测试实例 `sub2api-smtp-test`：`127.0.0.1:18085 -> 8080`，独立 DB `sub2api-smtp-test-postgres`、独立 Redis `sub2api-smtp-test-redis`；测试库已打开 `registration_enabled/email_verify_enabled/password_reset_enabled`，SMTP 真实账号仍为空。`20260619-220357-gmail-smtp-enabled-result_CN.md` 是本地 ignored 文档，未被 git 跟踪；Gmail SMTP 配置曾是运行态 DB settings，不是源码/迁移。结果见 `docs/ai/context/20260627-111200-smtp-test-port-18085-result_CN.md`。
- 2026-06-27 已在 18085 测试库写入 Gmail SMTP 非敏感字段：`smtp_host=smtp.gmail.com`、`smtp_port=587`、`smtp_username/smtp_from=xiaobianfuai@gmail.com`、`smtp_from_name=AACCX`、`smtp_use_tls=false`；`smtp_password` 仍需用户通过 Google App Password 获取后手动写入运行态，绝不能进 git。结果见 `docs/ai/context/20260627-113243-smtp-18085-gmail-config-result_CN.md`。
- 2026-06-27 已在 18085 测试库写入 Gmail App Password，脱敏状态为 `smtp_password=[CONFIGURED]`；`POST /api/v1/auth/send-verify-code` 返回 200，`sub2api-smtp-test` 日志确认 worker 已成功发送验证码邮件，不再报 `EMAIL_NOT_CONFIGURED`。密码明文不得进入文档、提交或日志摘要。结果见 `docs/ai/context/20260627-113800-smtp-18085-gmail-password-configured-result_CN.md`。
- 2026-06-27 已将本地蓝绿测试环境 `sub2api-smtp-test` 重新构建并替换为本地 `main` 最新版本 `e4704061d`，新镜像为 `sub2api-smtp-test:20260627-214036`，仍使用 `127.0.0.1:18085->8080`，保留原 18085 测试库与 Redis；`18085/health` 与公网候选 `18084/health` 均为 200，公网 `sub2api-candidate*` 未被替换或重启。结果见 `docs/ai/context/20260627-214649-18085-local-latest-restart-result_CN.md`。
- 2026-06-27 已按确认方案只替换公网链路中的 `sub2api-candidate` 应用容器镜像，保留 `sub2api-candidate-postgres`、`sub2api-candidate-redis`、nginx 指向和 18085 测试栈；当前 `sub2api-candidate` 镜像为 `sub2api-candidate:20260627-214036-e4704061d`，digest `sha256:00f926480c5f01e59a06796a1f8e9bfe569598d40e2a236a266880702d7bb4bc`，端口仍为 `127.0.0.1:18084->8080`。替换前已备份候选库到 `deploy/backups/20260627-215617-sub2api-candidate-before-app-image-replace.dump`；替换前后 `schema_migrations` 均为 191；`18084/health`、`8080/health`、`api.aaccx.pw/health`、`aaccx.pw/dashboard` 和 `/purchase` 均验证正常。结果见 `docs/ai/context/20260627-215859-18084-app-image-replace-result_CN.md`。
- 2026-06-27 对 `2799523972@qq.com` 真实公网 API Key 实测：用户余额为 0，订阅已软删除，仅有 OpenAI 流量卡 15 USD；使用其 active Key 请求 `https://api.aaccx.pw/v1/chat/completions` 返回 `403 SUBSCRIPTION_NOT_FOUND`，流量卡余额仍为 15 USD，`traffic_credit_ledger` 无 deduction，`usage_logs`/`billing_usage_entries` 仍为 0。根因是认证中间件仍在 `api_key_auth.go:153-162` 对订阅型 group 无 active subscription 直接硬拒绝，未进入 `BillingCacheService.CheckBillingEligibility()`；Google 中间件 `api_key_auth_google.go:81-90` 也有同类硬拒绝。结果见 `docs/ai/context/20260627-220815-2799523972-traffic-card-public-api-test-result_CN.md`。
- 2026-06-27 已修复“订阅已取消但有 OpenAI/GPT 流量卡仍不能用”的路径：`api_key_auth.go` 和 `api_key_auth_google.go` 在订阅型 group 找不到 active subscription 时不再直接 403，而是让请求进入 handler 中的 `BillingCacheService.CheckBillingEligibility()`；非 `ErrSubscriptionNotFound` 的订阅加载错误仍返回失败。新增中间件回归测试，`go test -count=1 -tags=unit ./internal/server/middleware` 与 `go test -count=1 -tags=unit ./internal/service` 均通过。已构建 `sub2api-traffic-card-fix:20260627-221441`，digest `sha256:299560875687ba0fc7c9b9703a5bece639a832c35720fb6ce47f8dd222483e22`，并替换 18085 与 18084 应用容器；DB/Redis/nginx 未改。使用 `2799523972@qq.com` 的真实公网 Key 请求 `/v1/responses` + `gpt-5.5` 返回 200，流量卡余额从 15.0000000000 降至 14.9959290000，新增 deduction `0.0040710000`。结果见 `docs/ai/context/20260627-222742-traffic-card-without-subscription-fix-result_CN.md`。
- 2026-06-27 对 `cnfoxian@gmail.com` 做额度耗尽后公网实测：公网 18084 当前运行 `sub2api-candidate:20260627-221441-traffic-card-fix`，该用户 active subscription 今日 `daily_usage_usd=19.0282566500` 已超过 `daily_limit_usd=19.00000000`，余额为 0，OpenAI 流量卡请求前剩余 `9.8976433500`。使用其真实公网 Key 请求 `/v1/responses` + `gpt-5.5` 返回 200；请求后流量卡剩余 `9.8934673500`，新增 `traffic_credit_ledger` deduction `0.0041760000`，新增 `usage_logs.id=25156` 且 `subscription_id=NULL`，订阅 `daily_usage_usd` 未继续增加。结果见 `docs/ai/context/20260627-223338-cnfoxian-traffic-card-quota-exhausted-public-test-result_CN.md`。
- 2026-06-28 公网注册邮箱验证码无法发送排查：18084 `sub2api-candidate` 已运行最新 `traffic-card-fix` 镜像，但公网库 `sub2api-candidate-postgres` 中 SMTP 配置为空：`smtp_host/smtp_username/smtp_from/smtp_password` 均为空；日志显示验证码任务入队成功后 EmailQueue worker 报 `EMAIL_NOT_CONFIGURED`。18085 测试库 `sub2api-smtp-test-postgres` 仍有 Gmail SMTP 配置和 App Password。根因是 SMTP 是运行态 DB settings，不随镜像/源码替换进入 18084；之前修复发生在 18085 测试库，18084 保留数据层时未同步该配置。结果见 `docs/ai/context/20260628-152717-public-18084-email-verify-smtp-not-configured-diagnosis_CN.md`。
- 2026-06-28 已将 18085 测试库的 SMTP 运行态配置同步到 18084 公网候选库，只更新 `settings` 表 7 个 SMTP key，不重建镜像、不重启容器、不改 DB/Redis/nginx；`smtp_from_name` 按用户要求设为 `天才程序员小站`，`smtp_password` 仅以脱敏状态确认。公网 `POST https://api.aaccx.pw/api/v1/auth/send-verify-code` 返回 200，`sub2api-candidate` 日志确认 EmailQueue worker 已成功发送验证码，不再报 `EMAIL_NOT_CONFIGURED`；`18084/health`、`8080/health`、`api.aaccx.pw/health` 均为 200。结果见 `docs/ai/context/20260628-153845-sync-18085-smtp-config-to-18084-result_CN.md`。
- 2026-06-28 已按用户要求关闭第三套本地残留 `main-preview` 栈：`sub2api-main-preview`、`sub2api-main-preview-postgres`、`sub2api-main-preview-redis` 均为 `Exited (0)`；只执行 `docker stop`，未删除容器或 volume。当前仍运行 18084 公网候选栈和 18085 SMTP 测试栈，`18084/health`、`18085/health`、`8080/health` 均为 200。结果见 `docs/ai/context/20260628-154934-stop-main-preview-stack-result_CN.md`。
- 2026-06-28 已按用户要求关闭 18085 SMTP 测试栈：`sub2api-smtp-test`、`sub2api-smtp-test-postgres`、`sub2api-smtp-test-redis` 均为 `Exited (0)`；只执行 `docker stop`，未删除容器或 volume。当前仅保留 18084 公网候选栈运行，`18084/health`、`8080/health`、`api.aaccx.pw/health` 均为 200。结果见 `docs/ai/context/20260628-155809-stop-smtp-test-stack-result_CN.md`。
- 2026-06-28 已启动本地 main-preview 蓝绿测试栈：用当前本机 `main` HEAD `eb95da02c` 构建镜像 `sub2api-main-preview:20260628-173235-eb95da02c`，应用容器 `127.0.0.1:18080->8080`，连接独立 `sub2api-main-preview-postgres` 与 `sub2api-main-preview-redis`；旧停止中的 main-preview 应用容器被删除重建以改端口，数据卷保留。公网 `sub2api-candidate*` 未停止/重建/重命名，18084 与 nginx 8080 health 均为 200。结果见 `docs/ai/context/20260628-174205-main-preview-18080-bluegreen-result_CN.md`。
- 2026-06-28 已按确认方案只读 dump 18084 候选库并覆盖导入到 18080 main-preview：源 `sub2api-candidate-postgres` 只读导出，目标 `sub2api-main-preview-postgres` drop/recreate 后 restore，`sub2api-main-preview-redis` 已清空，`sub2api-main-preview` 重启后自动应用本地 main 新迁移，18080 当前为 49 users / 41 keys / 192 migrations，最新迁移 `156_seed_codex_79_subscription_plan.sql`。18084 `sub2api-candidate*` 未停止/重建/写入，18080/18084/8080 health 均为 200。敏感 dump 文件在 `deploy/backups/`，不要提交。结果见 `docs/ai/context/20260628-183052-clone-18084-db-to-18080-preview-result_CN.md`。
- 2026-06-28 已将 18080 main-preview 更新到当前本机 `main` HEAD `befa8f138`，最终镜像为 `sub2api-main-preview:20260628-195911-befa8f138-migrationfix`，端口仍为 `127.0.0.1:18080->8080`，保留 `sub2api-main-preview-postgres` 与 `sub2api-main-preview-redis`。启动中发现 `156_seed_codex_79_subscription_plan.sql` 被提交 `2f0ac55c7` 改动导致 checksum mismatch；已恢复 156 原始 seed 价 `79.79`、保留 157 修正基础价到 `79.00`，并修正 156 checksum 兼容规则。18080 当前为 49 users / 41 keys / 194 migrations，`157` 与 `158` 已应用；18080/18084/8080 health 均为 200，18084 `sub2api-candidate*` 未停止/重建/写入。结果见 `docs/ai/context/20260628-201022-main-preview-restart-latest-main-result_CN.md`。
- 2026-06-28 已将 `codex/enable-affiliate-hide-register-promo` 合并进本地 `main`：注册页手填优惠码入口固定隐藏，`aff_code` 仍通过链接/OAuth/session 传递；邀请返利默认开启并新增 `158_enable_affiliate_default.sql`。同批本地 main 已分批提交 79 元套餐基础价修复和上下文文档；合并后 `git diff --check`、后端 service/repository/migrations 测试、前端注册页/settings/store/购买相关测试均通过。结果见 `docs/ai/context/20260628-193500-merge-affiliate-worktree-and-main-batches-result_CN.md`。
- 2026-06-28 注册重复邮箱预检拦截已实现：新增 `POST /api/v1/auth/precheck-register` 只读校验注册开放、保留邮箱、邮箱后缀和邮箱唯一性，不生成验证码、不写验证码缓存、不入队邮件；前端注册页在邮箱验证开启时预检通过后才写 `register_data` 并跳转 `/email-verify`，重复邮箱停留在注册页报错。结果见 `docs/ai/context/20260628-203901-register-duplicate-email-precheck-result_CN.md`。
- 2026-06-28 已将 18080 main-preview 更新到当前本机 `main` HEAD `399f278f3`，最终镜像为 `sub2api-main-preview:20260628-205543-399f278f3`，digest `sha256:46b73dddfcd4f916b54dfbcb584f5f665e084ffb3759d9b4d607b3d73e5002f9`，端口仍为 `127.0.0.1:18080->8080`，保留 `sub2api-main-preview-postgres` 与 `sub2api-main-preview-redis`；18080 health 为 200，前端资源为 `index-BVRJ38ir.js` / `index-nffSQZgD.css`，preview 库为 194 migrations，最新 `158_enable_affiliate_default.sql`。18084 `sub2api-candidate*` 未停止/重建/写入，候选应用仍为 `sub2api-candidate:20260627-221441-traffic-card-fix` 且 DB 仍为 191 migrations；18084/8080 health 均为 200。本轮还补了 Docker/pnpm 构建兼容：`.dockerignore` 排除 `deploy/backups/`，Dockerfile 前端依赖安装层复制 `.npmrc`，新增 `frontend/pnpm-workspace.yaml` 给本地 pnpm v11 批准 `esbuild/vue-demi` 构建脚本。结果见 `docs/ai/context/20260628-212254-main-preview-restart-399f278f-result_CN.md`。
- 2026-06-29 已将本地未提交/未跟踪内容与所有未合并本地分支收敛进 `main`：包括 deploy runtime scripts、GPT 流量包购买修复、候选预演脚本、订阅用量页迁移和 usage-guide 99/图生图分支；合并后 `git branch --no-merged main` 为空。冲突取舍为保留当前统一商品卡片、支付直达流程、usage-guide Base URL/相对路径/完整 URL 拆分说明和新版 AGENTS 压缩入口；未触碰 18084 公网运行链路。结果见 `docs/ai/context/20260629-091206-merge-local-work-and-branches-result_CN.md`。
- 2026-06-29 已将 18080 main-preview 更新到合并后本地 `main` HEAD `ddd4fb9a9`，最终镜像为 `sub2api-main-preview:20260629-092226-ddd4fb9a9`，digest `sha256:1cc424ad95735188f6c707c5c1b19af42b670a2c332c863ef8e8a9cc4ddb563b`，端口仍为 `127.0.0.1:18080->8080`，保留 `sub2api-main-preview-postgres` 与 `sub2api-main-preview-redis`；18080 health 为 200，前端资源为 `index-DHHTg5y4.js` / `index-B--asykV.css`，preview 库为 194 migrations，最新 `158_enable_affiliate_default.sql`。18084 `sub2api-candidate*` 未停止/重建/写入，候选应用启动时间未变化；18084/8080 health 均为 200。结果见 `docs/ai/context/20260629-093900-main-preview-restart-merged-main-result_CN.md`。
- 2026-07-01 已备份当前公网候选数据层：PostgreSQL dump 为 `deploy/backups/20260701-080310-sub2api-candidate-postgres-before-app-promote.dump`，Redis RDB 为 `deploy/backups/20260701-080310-sub2api-candidate-redis-before-app-promote.rdb`，二者权限均为 `600` 且已校验可读；备份后 `18084/health` 与 `8080/health` 均为 200，未停止/重建 `sub2api-candidate*` 或 nginx。结果见 `docs/ai/context/20260701-080310-public-candidate-db-backup-result_CN.md`。
- 2026-07-01 已写 18080 preview 发布到公网 18084 的设计：推荐只替换 `sub2api-candidate` 应用容器为 18080 已验证镜像，保留 `sub2api-candidate-postgres` 与 `sub2api-candidate-redis`，让新应用启动时在公网 DB 上自动应用 `156/157/158` migration；当前差异主要是新增/修正 `79 元订阅池`、`codex-pool-69-usd` 和 `affiliate_enabled=true`，不要用 18080 DB 覆盖公网 DB，也不要改 nginx 指向 18080。设计见 `docs/ai/context/20260701-080725-promote-18080-preview-to-public-18084-design_CN.md`。
- 2026-07-02 当前公网入口实际为 `api.aaccx.pw -> nginx 127.0.0.1:8080 -> sub2api 127.0.0.1:18080`，运行容器为 `sub2api`、`sub2api-postgres`、`sub2api-redis`；已备份 `sub2api-postgres` 到 `deploy/backups/20260702-125423-sub2api-before-user-expiry-plus-10usd-traffic-card.dump`，并按批次 `grant-20260702-10usd-day30` 给 52 个未删除用户各发放 1 张 10 USD OpenAI/GPT 流量卡，新增 52 条完成态系统订单、52 条 purchase 流水，总额 520 USD；同时将 36 条未删除 active 订阅 `expires_at` 增加 1 天。`18080/health`、`8080/health`、`api.aaccx.pw/health` 均为 200。结果见 `docs/ai/context/20260702-132616-user-expiry-plus-10usd-traffic-card-result_CN.md`。
- 2026-07-02 已按用户要求使用当前本地 `main` HEAD `6ef887a8d` 重新构建 `weishaw/sub2api:latest` 并只替换公网 `sub2api` 应用容器，保留 `sub2api-postgres` 与 `sub2api-redis`；新镜像 ID 为 `sha256:b195fcb03a925a0adda2026d75f6be42111162d2f7a023a74f4d44b80512a287`，公网资源已从 `assets/app-index-DKtOnsbF.js` / `assets/index-BMta9z_W.css` 更新为 `assets/app-index-DBKABD7p.js` / `assets/index-B--asykV.css`，CSS 包含 `z-index:35`；`18080/health`、`8080/health`、`api.aaccx.pw/health` 均为 200。结果见 `docs/ai/context/20260702-134828-main-rebuild-public-redeploy-result_CN.md`。
- 2026-07-02 公网随机用户 API Key 真实请求 `/v1/responses` 均为 200；99 元用户 `2864533153@qq.com` 初始返回 503，日志为 `openai.account_select_failed: no available accounts`，根因是唯一上游账号 `cliproxy-local-openai` 未绑定新套餐组 `codex-pool-89-usd` 与 `codex-pool-69-usd`。已备份公网库到 `deploy/backups/20260702-140054-sub2api-before-79-99-account-binding.dump`，并幂等新增 `account_groups` 绑定 `account_id=1 -> group_id=8/9`；修复后 99 元用户 Key 请求 `gpt-5.5` 返回 200，`usage_logs` 记录 `user_id=54/group_id=8/account_id=1`。当前无 active 79 元订阅用户可做用户 Key 真实请求，但绑定层已补齐。结果见 `docs/ai/context/20260702-140327-public-random-and-99-api-key-request-result_CN.md`。
- 2026-07-03 流量卡“我的订阅”显示排查：当前 nginx 实际将 `api.aaccx.pw` 和 `aaccx.pw` 的 Sub2API API 路由反代到 `127.0.0.1:18084`，对应 `sub2api-candidate-postgres`；不要把 `sub2api-postgres/18080` 的 2026-07-02 批量发卡结果误认为当前公网数据。`2799523972@qq.com` 真实登录后 `/subscriptions` 会显示 `GPT 流量包` 汇总 `$15.00`，但当前 18084 只有旧 10 USD + 5 USD 两张卡，缺少 2026-07-02 新 10 USD 卡，且用户侧 Key 列表和可绑定分组均为空；`1038686518@qq.com` 在 18084 无可用流量卡。公网 18084 流量卡计费链路本身已用无 active 订阅但有 active Key 的用户请求 `/v1/responses` 验证可扣费。结果见 `docs/ai/context/20260703-112900-traffic-card-subscriptions-page-diagnosis-result_CN.md`。
- 2026-07-03 已将 `2799523972@qq.com` 修复为正常可用的 79 元套餐用户：执行前备份 18084 库到 `deploy/backups/20260703-113109-sub2api-candidate-before-fix-2799-79-subscription.dump`；新增 active 订阅 `user_subscriptions.id=71 -> group_id=9/codex-pool-69-usd`，到期 `2026-08-02T10:35:29+08:00`；旧 `Codex_used` Key 的 key 字段已是 `__deleted__...` 占位值，不能恢复使用，已保持删除态；通过用户 API 新建 active Key `api_keys.id=65 -> group_id=9`。保留 GPT 流量包 `14.995929 USD` 未变；真实 `/v1/responses` 请求 200，走 `subscription_id=71/billing_type=1`，订阅日用量增加 `0.004096`，流量卡未扣；18084/8080/api.aaccx.pw health 均为 200。结果见 `docs/ai/context/20260703-113743-fix-2799-79-subscription-user-result_CN.md`。
- 2026-07-03 已给当前公网 18084 的 52 个未删除用户补发一张 10 USD OpenAI/GPT 流量卡：执行前备份 `sub2api-candidate-postgres` 到 `deploy/backups/20260703-114705-sub2api-candidate-before-18084-10usd-grant.dump`；批次号 `grant-20260703-10usd-18084-current-users`，最终 52 条 `payment_orders`、52 张 `user_traffic_credits`、52 条 `traffic_credit_ledger` purchase 流水，合计 520 USD。因 PostgreSQL 数据修改 CTE 快照规则，订单、卡、流水分三步用同一批次号幂等补齐。补发后 `1038686518@qq.com` 有 10 USD 卡，`2799523972@qq.com` 流量卡余额为 `24.995929 USD`，浏览器 `/subscriptions` 显示 `GPT 流量包 $25.00` 与 `codex-pool-69-usd`；18084/8080/api.aaccx.pw health 均为 200。结果见 `docs/ai/context/20260703-115114-18084-all-users-10usd-traffic-card-grant-result_CN.md`。
- 2026-07-04 `441565547@qq.com` 连续三次 `gpt-image-2` 生图失败排查：请求体只有约 3.3KB，不是提示词过长；三次 `/v1/images/generations` 均在约 125 秒后返回 502，Sub2API 转发 `host.docker.internal:8317` 报 `context canceled`，CLIProxyAPI 对应错误日志也为 `context canceled`，更像调用方或中间链路超时取消。结果见 `docs/ai/context/20260704-163929-gpt-image-2-user-441565547-timeout-diagnosis-result_CN.md`。
- 2026-07-04 本次 `gpt-image-2` 约 120 秒超时若要放大，优先改调用方 `ylcraft.openai_sdk_image` / provider `aaccx-gpt-image-2` 的 OpenAI Python SDK `AsyncOpenAI(timeout=...)` 或请求级 timeout；Sub2API 的 `GATEWAY_IMAGE_STREAM_DATA_INTERVAL_TIMEOUT=900` 是图片流数据间隔，CLIProxyAPI 生图路径 HTTP client 当前 `timeout=0`，都不像该 120 秒来源。结果见 `docs/ai/context/20260704-165832-gpt-image-2-120s-timeout-modification-point_CN.md`。
- 2026-07-07 已补 `gpt-image-2` 生图 timeout 改为 300 秒的具体操作说明：应在部署 `aaccx-gpt-image-2` provider 的 Python 服务中修改 `ylcraft/openai_sdk_image.py` 的 `AsyncOpenAI(timeout=300.0)` 或 provider 配置 `timeout: 300`，不在当前 Sub2API 仓库修改。结果见 `docs/ai/context/20260707-102912-gpt-image-2-timeout-300-change-guide_CN.md`。
- 2026-07-06 已将本地 `main` HEAD `9966c6f5b`（feat: finish automatic api key effective group）发布到公网：只替换 `sub2api-candidate` 应用容器，保留 `sub2api-candidate-postgres` 与 `sub2api-candidate-redis`；新镜像 `sub2api-candidate:20260706-080023-9966c6f5b-local-main`，digest `sha256:3273f560b6854d1a4887704c46c4131da4082f923bf1b45e0443562037d4315e`；发布前备份 Postgres dump（29MB）和 Redis RDB（69KB）到 `deploy/backups/`。发布后 DB 从 194 migrations 升至 195（`159_auto_api_key_effective_group.sql`），新增 `traffic-pack-openai` 分组（id=10, openai, active, exclusive, allow_image_generation=true），1 个 OpenAI 上游账号已绑定该分组，52 个用户 API Keys 全部迁移为 `group_id=NULL`（自动 Key）；`18084/health`、`8080/health`、`api.aaccx.pw/health` 均为 200。未修改 nginx，未重建 Postgres/Redis，未执行回滚。结果见 `docs/ai/context/20260706-081100-public-main-redeploy-result_CN.md`。
- 2026-07-06 `3876129758@qq.com` 套餐额度排查：当前 18084 公网库中用户 `id=56` 为 `codex-pool-19-usd` active 订阅，日窗口已刷新到 `2026-07-06 00:00:00+08`，不是昨天残留未清零；今天 `00:23:00-00:48:35+08` 之间 64 次成功请求累计消耗 `12.4792935000` USD，当前剩余约 `6.5207065` USD，无流量卡、无结构化限额拒绝记录。结果见 `docs/ai/context/20260706-100145-3876129758-quota-usage-diagnosis-result_CN.md`。
- 2026-07-06 已处理公网 18084 订阅 stale 日窗口运行态数据：先手动清零 3 位 stale 超限用户，后按“没有今天成功用量则清零、有今天成功用量则写回今天 usage_logs 聚合值”的规则全量校准 34 条 active 订阅，最终 stale 窗口数为 0，并删除所有 `billing:sub:*` 订阅缓存让后续请求回源重建；执行前分别备份候选库。结果见 `docs/ai/context/20260706-105241-reset-three-stale-daily-windows-result_CN.md` 与 `docs/ai/context/20260706-105804-reset-all-stale-daily-windows-preserve-today-result_CN.md`。
- 2026-07-06 本地 `main` 已实现订阅 API 入口窗口刷新：`BillingCacheService` 在套餐限额判断前按 `timezone.StartOfDay/StartOfWeek` 和 30 天滚动月窗口刷新 DB，Redis `billing:sub:*` 缓存新增 daily/weekly/monthly window 字段，旧缓存缺字段时回源 DB；新增条件式 `RefreshExpiredUsageWindows` 防止并发重复清零。已通过 `go test -count=1 -tags=unit ./internal/service ./internal/repository`；未部署公网。结果见 `docs/ai/context/20260706-101719-subscription-window-refresh-result_CN.md`。
- 2026-07-07 已写订阅窗口刷新修复的公网重启计划：继续采用只替换 `sub2api-candidate` 应用容器、保留 `sub2api-candidate-postgres` 与 `sub2api-candidate-redis`、不改 nginx 的方式；计划包含本地验证、Docker 构建、Postgres/Redis 备份、发布前后 DB 快照、旧容器 env 临时保存、应用层回滚和谨慎数据层回滚步骤。尚未执行构建、备份、替换或公网迁移。计划见 `docs/ai/context/20260707-082618-public-subscription-window-refresh-redeploy-plan_CN.md`。
- 2026-07-07 已将本地 `main` HEAD `74e5a4bb0`（包含 `d6e337238` 订阅窗口刷新修复）发布到公网：只替换 `sub2api-candidate` 应用容器，保留 `sub2api-candidate-postgres` 与 `sub2api-candidate-redis`，未修改 nginx 或 Cloudflare Tunnel；新镜像 `sub2api-candidate:20260707-084458-74e5a4bb0-subscription-window-refresh`，image id `sha256:82406485ee568f033aca7cc6ed3291a262be37329de19fbecab151792e879e5a`。发布前备份候选 Postgres 与 Redis 到 `deploy/backups/`，发布后 `18084/health`、`8080/health`、`api.aaccx.pw/health`、`aaccx.pw/dashboard`、`aaccx.pw/purchase` 均为 200，migration 保持 195。结果见 `docs/ai/context/20260707-084700-public-subscription-window-refresh-redeploy-result_CN.md`。
- 2026-07-07 使用测试账号 `2799523972@qq.com` 对当前公网 18084 做真实验收：购买页 29/39/59/79/99 套餐与 5/10/20 刀 GPT 流量卡均可见，10 刀流量卡可进入支付宝确认页但未付款；前端成功创建自动分组临时 Key（`group_id=NULL`），请求 `https://api.aaccx.pw/v1/responses` + `gpt-5.5` 返回 200/`OK`，`usage_logs.id=55398` 解析到 `group_id=9/subscription_id=71/billing_type=1`，扣 `codex-pool-69-usd` 套餐 `0.004031`，流量卡余额 `24.995929` 与 deduction 数不变；临时 Key 已前端删除并 DB 复核为软删除。结果见 `docs/ai/context/20260707-085709-2799-public-account-smoke-test-result_CN.md`。
- 2026-07-07 对截图中 4 个 active 订阅用户做只读排查：问题不是东八区 0 点计算错，而是老订阅 `daily_window_start IS NULL` 时当前 `RefreshExpiredUsageWindows` 只补窗口、不清零旧 `daily_usage_usd`；管理端列表也不会主动触发窗口刷新。`897858381@qq.com` 与 `qixiaocheng777@gmail.com` 页面用量含 2026-07-06 残留，`3056163754@qq.com` 窗口已是 2026-07-07 0 点但用量仍为昨日+今日，`daleselaji@gmail.com` 正常等于今日用量。结果见 `docs/ai/context/20260707-100704-four-users-usage-window-diagnosis-result_CN.md`。
- 2026-07-07 已按用户要求将当前公网 18084 所有 active 订阅的 `daily_usage_usd` 校准为今天 0 点后的 `usage_logs.total_cost` 聚合值：执行前备份 `sub2api-candidate-postgres` 到 `deploy/backups/20260707-101105-sub2api-candidate-before-daily-usage-calibration.dump`，修复前 38 条 active 订阅存在日窗口/日用量差异，事务内更新 38 条，删除当时已有 `billing:sub:*` 订阅缓存；修复后全量复查 `remaining_mismatch_count=0`，截图 4 位用户均只保留今天用量，18084/8080/api.aaccx.pw health 均为 200。结果见 `docs/ai/context/20260707-101401-active-subscriptions-daily-usage-calibration-result_CN.md`。
- 2026-07-07 已将订阅 daily 窗口 NULL 根因单独成文：当前 SQL 对 `daily_window_start IS NULL` 只补窗口、不清 `daily_usage_usd`，weekly/monthly 有同类模式；管理端列表不会触发 API 计费入口刷新，所以更容易暴露旧值。代码尚未修复，后续需覆盖 NULL 窗口初始化回归测试。说明见 `docs/ai/context/20260707-101847-subscription-daily-window-null-root-cause_CN.md`。
- 2026-07-07 已写订阅 NULL 窗口用量残留彻底修复设计：推荐同时修 `RefreshExpiredUsageWindows` 写路径、订阅列表/progress 展示归一化和上线前 `usage_logs` 校准检查；daily/weekly/monthly 的 `NULL + usage > 0` 都必须覆盖回归测试。设计见 `docs/ai/context/20260707-104040-subscription-null-window-refresh-fix-design_CN.md`。
- 2026-07-07 已写订阅 NULL 窗口用量残留修复实施计划：按 TDD 拆为仓储 integration 失败测试、service 读路径失败测试、SQL 与展示归一化最小实现、相关 unit/integration 验证和结果归档。计划见 `docs/ai/context/20260707-104609-subscription-null-window-refresh-fix-implementation-plan_CN.md`。
- 2026-07-07 已修复订阅 NULL 窗口用量残留：`RefreshExpiredUsageWindows` 对 daily/weekly/monthly 的 `NULL` 窗口初始化会同步清零 usage，订阅列表和 progress 读路径也会隐藏 `NULL + usage > 0` 的历史值；相关 unit 与新增仓储 integration 测试通过。结果见 `docs/ai/context/20260707-110703-subscription-null-window-refresh-fix-result_CN.md`。
- 2026-07-07 已做当前代码冗余与优化静态审查：优先问题是自动 API Key 语义和路由覆盖、API Key 认证中间件重复、订阅额度窗口规则分散；同时记录 handler 计费准入、OpenAI-only 路由、账号弹窗副本、SettingsView、gateway service、settings schema、部署脚本和套餐 seed 的后续收口建议。结果见 `docs/ai/context/20260707-090748-code-redundancy-review_CN.md`。
- 2026-07-07 已基于代码冗余审查写 P0/P1/P2/P3 治理设计：P0 收口自动 API Key 语义与路由策略；P1 收口 API Key 认证核心、订阅 usage window policy，并将部署脚本旧拓扑硬编码提升为生产操作风险；P2 处理 handler 计费准入、OpenAI-only route helper 和账号弹窗副本；P3 处理 SettingsView、gateway service 工具层、settings schema 与套餐 seed 模板化。设计见 `docs/ai/context/20260707-091540-code-redundancy-design_CN.md`。
- 2026-07-07 已在分支 `codex/code-redundancy-p0-auto-key` 完成 P0 自动 API Key 路由语义收口：新增 supported-endpoint policy wrapper，不支持的 `/v1beta` 与 `/antigravity/*` 自动 Key 返回明确 `AUTO_KEY_UNSUPPORTED_ENDPOINT`，fixed group Key 仍跳过 resolver；前端用户 Key 创建 API 移除 `groupId` 参数并新增 API 层测试。验证通过 middleware/routes Go unit、前端 keys spec 与 typecheck；未触碰公网运行态。结果见 `docs/ai/context/20260707-093135-code-redundancy-p0-auto-key-result_CN.md`。
- 2026-07-07 已继续完成代码冗余治理 P1/P2/P3：收口 usage window policy、部署 target、API Key 认证核心、OpenAI-only route helper、handler billing helper、账号弹窗 composable、套餐 seed 生成器、上游响应头 helper 和 Settings OpenAI fast policy payload helper；同轮修复本分支相关的 AccountUsageCell/KeysView 前端测试。后端核心包、前端 typecheck、相关前端切片、shell 与脚本测试通过；完整前端 `pnpm vitest run` 仍有 5 个未触碰文件的 10 个既有失败，需后续单独处理。结果见 `docs/ai/context/20260707-102315-code-redundancy-p1-p2-p3-result_CN.md`。
- 2026-07-05 仅购买 OpenAI/GPT 流量包、没有 active 订阅的新用户无法创建可用 OpenAI API Key 的问题真实存在：当前公网 nginx 仍指向 18084；18084 有 15 个 active 用户有 OpenAI 流量包余额但无 active subscription，按当前 `/groups/available` 规则均为 `available_total=1`、`available_openai=0`，只能看到 `anthropic/default`，不能绑定 OpenAI 订阅分组。根因是 API Key 分组授权层只把 OpenAI 分组绑定建模为订阅权益，未把流量包权益映射为可绑定 OpenAI 入口分组；计费层已有流量包兜底，但新 Key 创建卡在权限层。结果见 `docs/ai/context/20260705-174552-traffic-card-only-user-no-openai-key-group-diagnosis_CN.md`。
- 2026-07-05 已按用户要求停止 18080 本地 Sub2API 栈：`sub2api`、`sub2api-postgres`、`sub2api-redis` 均为停止态，`docker ps --filter publish=18080` 无运行容器且 18080 无监听进程；18084 公网候选 `sub2api-candidate*` 未停止/重启/重建，`18084/health`、`8080/health`、`api.aaccx.pw/health` 均为 200。结果见 `docs/ai/context/20260705-175544-stop-18080-stack-result_CN.md`。
- 2026-07-05 自动 API Key 设计已定：普通用户前端删除 API Key 分组下拉框，新建 Key 默认 `group_id=NULL`；后端新增 request-scoped `effective_group` 解析，有 active OpenAI 套餐时选择当前套餐 group，无套餐但有 GPT/OpenAI 流量包时选择内部 `traffic-pack-openai` 入口 group；旧 OpenAI API Key 上线迁移为自动 Key，非 OpenAI 固定 Key 和管理员固定分组能力保留。设计见 `docs/ai/context/20260705-175642-auto-api-key-effective-group-design_CN.md`。
- 2026-07-05 自动 API Key effective_group 实施计划已写：计划按 TDD 拆为 resolver、request-scoped group 解析中间件、用户 Key 创建/更新自动化、`traffic-pack-openai` 迁移、前端移除分组选择、后端路由验证、全量构建与候选运行态验收；计划见 `docs/ai/context/20260705-215045-auto-api-key-effective-group-implementation-plan_CN.md`。
- 2026-07-05 自动 API Key effective_group 本地实现已完成：普通用户 Key 创建/编辑不再选择或提交分组，旧 OpenAI Key 通过 `159_auto_api_key_effective_group.sql` 迁移为 `group_id=NULL`，请求时按 active OpenAI 套餐或 GPT/OpenAI 流量包解析 request-scoped effective group；本轮只改本地分支代码和文档，未连接、迁移、重启或替换公网服务。结果见 `docs/ai/context/20260705-225115-auto-api-key-effective-group-result_CN.md`。
- 2026-07-06 已写“将当前公网 Sub2API 前后端替换为本地 main 最新代码”的执行计划：当前公网应用容器是 `sub2api-candidate`，镜像 `sub2api-main-preview:20260702-local-main-6ef887a8d`，端口 `127.0.0.1:18084->8080`；计划要求先备份 `sub2api-candidate-postgres` 与 `sub2api-candidate-redis` 到 `deploy/backups/`，再只替换应用容器、保留 DB/Redis 和 nginx，并明确 159 迁移后的回滚必须恢复 Postgres dump。尚未执行构建、备份、替换或公网迁移。计划见 `docs/ai/context/20260706-075410-public-main-redeploy-plan_CN.md`。
- 2026-07-05 已按用户要求删除本地 Codex skill `/Users/wujianxiang/.codex/skills/sub2api-local-redeploy`，包括 `SKILL.md` 与 `agents/openai.yaml`；未执行其中的重部署、Docker、Compose 或公网验证流程。结果见 `docs/ai/context/20260705-215015-remove-sub2api-local-redeploy-skill-result_CN.md`。
- 当前 Nginx 公网入口请求体上限已与 Sub2API 对齐为 256MB；`aaccx.pw` 已补裸 `/responses`、`/chat/completions`、`/embeddings`、`/images/*` 代理到 Sub2API。真实 100MB+ 公网请求仍可能受 Cloudflare 套餐上传上限影响。结果见 `docs/ai/context/20260624-201352-413-payload-too-large-nginx-fix-result_CN.md`。
- 2026-06-24 413 归因：大量 413 集中在 Codex Desktop Windows 裸 `/responses`，并伴随裸 `/models?client_version=0.142.0` 404，不是 `/v1/images/*` 集中触发；`/usage-guide` 生图文案已拆分 Base URL、相对路径和完整 URL。结果见 `docs/ai/context/20260624-202759-usage-guide-image-url-413-result_CN.md`。
- 2026-06-24 分支合并排查：`codex/usage-guide-99-plan-main-20260624` 只是从 `origin/main` 创建的空内容分支，真正的 99 文案分支 `codex/usage-guide-99-plan-20260624` 未合并；公网 `/usage-guide` 已有 99/图生图/Trae 文案，但运行态 DB 只有 29/39/59 三个在售订阅，没有 99 元套餐和对应 group。结果见 `docs/ai/context/20260624-210639-main-merge-missing-branch-diagnosis_CN.md`。
- 2026-06-24 `/usage-guide` 生图方法以真实图生图为主：公网完整 URL 为 `https://api.aaccx.pw/v1/images/edits`，分离填写工具中路径填 `/images/edits`，模型 `gpt-image-2`，支持 JSON `images[].image_url` 或 multipart `image=@...`。结果见 `docs/ai/context/20260624-163548-usage-guide-image-edit-result_CN.md`。
- 2026-06-24 公网 502 归因：22:30 JST 左右不是 Cloudflare/nginx/Sub2API 全局不可用，`/v1/responses` 当前可返回 401/200；对应连续 502 主要集中在流式 `/v1/chat/completions`，Sub2API 日志显示上游流缺少终止事件 `stream usage incomplete: missing terminal event`。结果见 `docs/ai/context/20260624-223544-public-502-bad-gateway-diagnosis_CN.md`。
- 2026-06-25 `Selected model is at capacity. Please try a different model.` 归因：这是 OpenAI 官方/上游返回的模型容量不足错误，不是 Sub2API 自造拦截；Sub2API 会把它识别为临时容量错误并触发同账号重试/账号 failover。结果见 `docs/ai/context/20260625-101447-openai-model-capacity-diagnosis_CN.md`。
- 2026-06-25 并发/上游排查：当前四个套餐分组共用一个 `cliproxy-local-openai` 上游账号，Sub2API 账号并发为 3，确实会在高峰排队；但 09:00 后主要错误是上游 overloaded、CLIProxyAPI 内部账号 429 usage limit reached 和 auth unavailable，不是大面积 Sub2API 排队超时。结果见 `docs/ai/context/20260625-121913-sub2api-cliproxyapi-concurrency-upstream-diagnosis_CN.md`。
- 2026-06-25 已按用户要求将运行态 `cliproxy-local-openai` 的 Sub2API 账号并发从 3 调整为 10；未修改用户并发、分组绑定或 CLIProxyAPI 配置。结果见 `docs/ai/context/20260625-192607-sub2api-account-concurrency-10-result_CN.md`。
- CLIProxyAPI 是聚合上游，不是单个静态 OpenAI Key；Sub2API 上游账号需启用 `credentials.pool_mode=true`，并让 401/403/429 在同账号内重试。
- 订阅、余额充值和 GPT 流量包购买全部走 Sub2API 支付订单与 ZPay/EasyPay；当前 ZPay 支付宝实例应使用 `popup/submit.php` 托管收银台，不要把 ZPay `mapi.php` 返回的 `qr.alipay.com` 原始码直接渲染给用户扫码。自动履约必须以签名正确、订单号匹配、provider/merchant metadata 匹配、回调或查单金额等于本地 `pay_amount` 为准。
- 2026-06-26 本地 `127.0.0.1:5174/purchase` 显示“充值功能暂未开放”的直接原因不是 ZPay 代码未合并，而是运行态 DB 没有任何 `payment_provider_instances`，且支付宝/微信可见支付方式均未启用；结果见 `docs/ai/context/20260626-124242-purchase-zpay-merge-runtime-diagnosis_CN.md`。
- 2026-06-26 ZPay 购买支付与自动履约需求已沉淀：目标是用户在当前 `/purchase` 完成套餐购买，ZPay/EasyPay 动态支付成功后按订单 `pay_amount` 校验并自动添加/续期订阅；仍需用户提供 ZPay 创建订单、签名、回调、查单文档与脱敏样例。见 `docs/ai/context/20260626-132547-zpay-purchase-payment-fulfillment-requirements_CN.md`。
- 2026-06-26 已写 ZPay/EasyPay 支付宝-only 运行态配置计划：仅启用支付宝、关闭微信和退款，商户配置只在执行时通过隐藏输入写入运行态，不写入源码或长期文档；计划见 `docs/ai/context/20260626-133635-zpay-alipay-runtime-config-plan_CN.md`。
- 2026-06-26 分支 `codex/zpay-alipay-runtime-config` 已新增运行态配置脚本并写入 ZPay/EasyPay 支付宝-only provider；13:50 左右发现 `qrcode/mapi.php` 模式返回 `qr.alipay.com` 原始码，扫码会报“交易买家不匹配”，已改为 `payment_mode=popup` 走 ZPay `submit.php` 托管收银台。真实支付端到端验收仍需新建订单验证。结果见 `docs/ai/context/20260626-135223-zpay-alipay-runtime-config-result_CN.md`。
- 2026-06-26 ZPay 1% 手续费已通过运行态 `RECHARGE_FEE_RATE=1` 处理；不要修改套餐/流量包基础价格，后端订单用 `pay_amount` 承载实付金额，前端 `/purchase` 列表卡片、确认区和按钮展示含手续费实付价。结果见 `docs/ai/context/20260626-151923-zpay-1-percent-fee-runtime-result_CN.md`。
- 2026-06-26 `/purchase` 用户页已下架余额充值 UI，只保留订阅套餐和 GPT 流量包购买；`?tab=recharge` 不再进入充值页。后端余额订单能力保留用于历史订单和支付恢复。结果见 `docs/ai/context/20260626-152551-purchase-remove-balance-recharge-result_CN.md`。
- 2026-06-26 `liyutong2883@gmail.com` 支付宝购买 3 元 GPT 流量包已自动履约；误新增的手工 10 USD 流量卡已撤回，当前保留两张 10 USD 卡，合计 20 USD。结果见 `docs/ai/context/20260626-154442-liyutong-3yuan-traffic-card-result_CN.md`。
- 2026-06-29 `liyutong2883@gmail.com` 公网两张 10 USD GPT 流量卡只读核对：`credit_id=7` 已扣满 10 USD 归零，`credit_id=46` 已扣 `9.9768260000`、剩余 `0.0231740000`，两张卡合计剩余 `0.0231740000`；按 request 聚合后 ledger 与 `usage_logs` 均为 `19.9768260000`，是真实用量扣费。结果见 `docs/ai/context/20260629-204438-liyutong-public-traffic-cards-balance-result_CN.md`。
- 2026-06-27 已修复认证中间件提前拦截订阅超限/零余额导致流量卡无法兜底的问题：中间件只保留身份、Key 状态、订阅存在/过期/暂停等校验，订阅额度、余额、流量卡兜底和限流统一由 `BillingCacheService.CheckBillingEligibility()` 判定；本地代码已测，运行态仍需重新构建部署后生效。结果见 `docs/ai/context/20260627-212100-unify-billing-eligibility-middleware-result_CN.md`。
- 2026-06-26 候选预演第一次真实执行因 Docker Compose project 未隔离，误停并重建公网 `sub2api`、`sub2api-postgres`、`sub2api-redis`；公网 Postgres 数据未见批量损坏，但 Redis refresh token 会话态丢失，导致旧登录态失效。恢复候选预演前必须先读 `docs/ai/context/20260626-205933-sub2api-candidate-rehearsal-incident-diagnosis_CN.md`，并确认候选 project 不可能等于公网 project。
- 2026-06-26 候选预演纠偏确认：最新生产 DB 数据保留在 Docker named volume `deploy_postgres_data`（47 users、40 keys、191 migrations，`xiaobianfuai@gmail.com` 是 `id=13` admin）；当前 `deploy/postgres_data` bind 目录是旧库（32 users、188 migrations），不能作为最新生产 DB 克隆源。候选/恢复必须从 `deploy_postgres_data` 或其 dump 取数，见 `docs/ai/context/20260626-214600-latest-db-volume-candidate-rehearsal-result_CN.md`。
- 2026-06-26 18084 候选环境购买入口隐藏归因：候选 sanitize 不能把 `payment_enabled=false` 写入克隆库，否则 HTML 注入的 `window.__APP_CONFIG__` 和前端路由守卫会隐藏 `/purchase`；候选预演应保留 `payment_enabled=true`，只禁用具体 provider、可见支付方式、SMTP 和监控副作用。结果见 `docs/ai/context/20260626-220130-candidate-purchase-nav-hidden-fix-result_CN.md`。
- 2026-06-26 `/purchase` 主内容空白归因：`PaymentView.vue` 购买选择态多包了一层无指令裸 `<template>`，构建后套餐卡片进入原生 template content，DOM 有文字但视觉不渲染；已删除 wrapper 并新增回归测试。结果见 `docs/ai/context/20260626-221115-payment-view-native-template-blank-fix-result_CN.md`。
- 2026-06-26 18084 候选环境“支付功能未开放”归因：ZPay 链路代码已打通，但候选 sanitize 误把克隆来的 `ZPay Alipay` provider 和支付宝可见方式关掉，导致 checkout `methods={}`；已恢复为支付宝-only `easypay_alipay`、`payment_mode=popup`，并修正候选 sanitize 只关闭其他 provider/退款/通知副作用。结果见 `docs/ai/context/20260626-221915-candidate-zpay-provider-restore-result_CN.md`。
- 2026-06-26 已在 18084 候选环境对应数据库 `sub2api-candidate-postgres` 创建普通用户 `1038686518@qq.com`，用户 id=48，email 登录接口返回 200；只写候选库，未改生产库、API Key、套餐、余额或支付配置。结果见 `docs/ai/context/20260626-234732-candidate-18084-create-user-result_CN.md`。
- 套餐分组名 `codex-pool-19-usd`、`codex-pool-29-usd`、`codex-pool-49-usd`、`codex-pool-69-usd` 分别对应每日 19/29/49/69 USD。
- 当前售卖套餐里 `29 元订阅池` 对应 `subscription_plans.id=1 -> group_id=2 -> codex-pool-19-usd`；不要误绑到 `codex-pool-29-usd`。
- `79 元订阅池` 基础价为 79 元，30 天有效期，对应 `codex-pool-69-usd`，每日 69 USD；用户 `/purchase` 在 1% 手续费下展示和支付 79.79 元，通过 `subscription_plans` 上架并复用现有支付和订阅履约链路。
- `/purchase` 只负责充值、订阅和 GPT 流量包购买，不再展示当前订阅详情；当前订阅状态统一在“我的订阅”页查看。
- 2026-06-28 本地 main 已移除 `/purchase` 登录后页面标题下方说明“通过内嵌页面完成订阅购买”，仅改前端路由 meta 与未引用 locale 文案，未改支付流程；结果见 `docs/ai/context/20260628-165245-purchase-remove-description-result_CN.md`。
- 2026-06-28 `/usage-guide` Codex 接入第 4 步已从“支付后发兑换码”改为“完成支付”，只保留一张支付确认截图；第 5 步同步改为“支付完成后，去 API Key 页面生成密钥”；旧两张兑换码流程配图已删除，因当前支付成功后自动履约、用户可直接使用。结果见 `docs/ai/context/20260628-171325-usage-guide-step4-payment-direct-result_CN.md`。
- `xiaobianfuai@gmail.com` 是管理员和本机 Codex Local Key 所属账号，不要按普通用户删除。

## 维护规则

- 需要新增长期上下文时，只在 `docs/ai/context/` 创建新文件。
- 进入实现前先写 design/plan 上下文；完成后写结果上下文。
- 每次合并功能分支、提交 main 或做收尾前，必须运行 `git ls-files --others --exclude-standard docs/ai/context` 检查未跟踪上下文文档；确认无敏感信息后纳入同一次功能提交，或单独做 `docs: archive ai context` 提交。
- 不要把 `docs/ai/context/` 加回 `.gitignore`；未跟踪状态表示尚未提交，不表示被忽略。
- 如果某个上下文文档暂不提交，必须在回复里说明原因和后续处理方式。
- 若上下文过长，先沉淀到新的压缩文档，再继续精简本文件。
- Git 远端：`origin=https://github.com/Wei-Shaw/sub2api.git` 是上游；`personal=https://github.com/cnYui/sub2api.git` 是用户个人 fork。后续保存当前工作分支优先推送到 `personal`，不要误推到上游 `origin`。
